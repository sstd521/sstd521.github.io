<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>WeekNote</title>
  <subtitle>publish at least one note a week</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2016-11-07T11:03:28.859Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>sstd521</name>
    <email>sstd521@gmail.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>SQL学习笔记</title>
    <link href="http://yoursite.com/2016/11/07/SQL%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    <id>http://yoursite.com/2016/11/07/SQL学习笔记/</id>
    <published>2016-11-07T11:01:49.000Z</published>
    <updated>2016-11-07T11:03:28.859Z</updated>
    
    <content type="html"><![CDATA[<h2 id="了解数据库"><a href="#了解数据库" class="headerlink" title="了解数据库"></a>了解数据库</h2><blockquote>
<p>数据库是一个以某种有组织的方式存储的数据集合。</p>
</blockquote>
<p> <strong>数据库</strong>（database）保存有组织的数据的容器（通常是一个文件或一组文件）。确切地说，数据库软件应称为数据库管理系统（DBMS）。数据库是通过 DBMS 创建和操纵的容器。</p>
<h3 id="表"><a href="#表" class="headerlink" title="表"></a>表</h3><blockquote>
<p><strong>表</strong>是一种结构化的文件，可用来存储某种特定类型的数据。</p>
</blockquote>
<h3 id="表名"><a href="#表名" class="headerlink" title="表名"></a>表名</h3><p>数据库中的每个表都有一个名字来标识自己。这个名字是唯一的，即同一数据库中没有其他表具有相同的名字。<br> 使<strong>表名</strong>成为唯一的，实际上是数据库名和表名等的组合。虽然在相同数据库中不能两次使用相同的表名，但在不同的数据库中完全可以使用相同的表名。</p>
<h3 id="模式"><a href="#模式" class="headerlink" title="模式"></a>模式</h3><blockquote>
<p>模式是关于数据库和表的布局及特性的信息。</p>
</blockquote>
<p> 表具有一些特性，这些特性定义了数据在表中如何存储，包含存储什么样的数据，数据如何分解，各部分信息如何命名等信息。描述表的这组信息就是所谓的<u>模式（schema）</u>，模式可以用来描述数据库中特定的表，也可以用来描述整个数据库（和其中表的关系）。</p>
<h3 id="列和数据类型"><a href="#列和数据类型" class="headerlink" title="列和数据类型"></a>列和数据类型</h3><h4 id="列（column）"><a href="#列（column）" class="headerlink" title="列（column）"></a>列（column）</h4><blockquote>
<p>表中的一个字段。所有表都是由一个或多个列组成的。</p>
</blockquote>
<h3 id="行（row）"><a href="#行（row）" class="headerlink" title="行（row）"></a>行（row）</h3><blockquote>
<p>表中的一个记录<br> 如果将表想象为网格，网格中垂直的列为表列，水平行为表行。</p>
</blockquote>
<h3 id="主键（primary-key）"><a href="#主键（primary-key）" class="headerlink" title="主键（primary key）"></a>主键（primary key）</h3><blockquote>
<p>一列（或一组列），其值能够唯一标识表中每一行。</p>
</blockquote>
<p>唯一标识表中每行的这个<strong>列</strong>（或这几<strong>列</strong>）称为主键。<strong>主键用来表示一个特定的行</strong>。没有主键，更新或删除表中特定行就极为困难。 应该总是定义主键，虽然并不总是需要主键，但多数数据库设计者都会保证他们创建的每个表具有一个主键，以便于以后的数据操作和管理。</p>
<p>表中的任何列都可以作为主键，只要它满足以下条件：</p>
<p> 任意两行都不具有相同的主键值；<br> 每一行都必须具有一个主键值（主键列不允许 <code>NULL</code> 值）；<br> 主键列中的值不允许修改或更新；<br>主键值不能重用（如果某行从表中删除，它的主键不能赋给以后的新行）。</p>
<h2 id="检索数据"><a href="#检索数据" class="headerlink" title="检索数据"></a>检索数据</h2><h3 id="SELECT"><a href="#SELECT" class="headerlink" title="SELECT"></a>SELECT</h3><blockquote>
<p>为了使用 SELECT 检索表数据，必须至少给出两条信息——想选择什么，以及从什么地方选择。</p>
</blockquote>
<h3 id="检索单个列"><a href="#检索单个列" class="headerlink" title="检索单个列"></a>检索单个列</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_name</div><div class="line"><span class="keyword">FROM</span> Products;</div></pre></td></tr></table></figure>
<p>利用 <code>SELECT</code> 语句从 <code>Products</code> 表中检索一个名为<code>prod_name</code>的列。所需的列名写在 <code>SELECT</code> 关键字之后， <code>FROM</code> 关键字指出从哪个表中检索数据。如上的一条简单 <code>SELECT</code> 语句将返回表中的所有行。数据没有过滤，也没有排序。<br> 如果没有明确排序查询结果（下一课介绍），则返回的数据没有特定的顺序。返回数据的顺序可能是数据被添加到表中的顺序，也可能不是。只要返回相同数目的行，就是正常的。</p>
<h5 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h5><p> 多条 SQL语句必须以分号<code>;</code>分隔。<br>SQL语句不区分大小写，因此 <code>SELECT</code> 与 <code>select</code> 是相同的。同样，写成 <code>Select</code> 也没有关系。但是表名、列名和值可能有所不同（这有赖于具体的 DBMS及其如何配置）。<br>在处理 SQL语句时，其中所有空格都被忽略。SQL语句可以写成长长的一行，也可以分写在多行。下面这 3 种写法的作用是一样的。</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_name</div><div class="line"><span class="keyword">FROM</span> Products;</div><div class="line"></div><div class="line"><span class="keyword">SELECT</span> prod_name <span class="keyword">FROM</span> Products;</div><div class="line"></div><div class="line"><span class="keyword">SELECT</span></div><div class="line">prod_name</div><div class="line"><span class="keyword">FROM</span></div><div class="line">Products;</div></pre></td></tr></table></figure>
<p> 多数 SQL开发人员认为，将 SQL语句分成多行更容易阅读和调试。</p>
<h3 id="检索多个列"><a href="#检索多个列" class="headerlink" title="检索多个列"></a>检索多个列</h3><blockquote>
<p>要想从一个表中检索多个列，须在 <code>SELECT</code> 关键字后给出多个列名，列名之间必须以逗号分隔。<br>注意：</p>
</blockquote>
<p> 在选择多个列时，一定要在列名之间加上逗号，但最后一个列名后不加。如果在最后一个列名后加了逗号，将出现错误。</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_id, prod_name, prod_price</div><div class="line"><span class="keyword">FROM</span> Products;</div></pre></td></tr></table></figure>
<h3 id="检索所有列"><a href="#检索所有列" class="headerlink" title="检索所有列"></a>检索所有列</h3><blockquote>
<p>如果给定一个通配符（ * ），则返回表中所有列。列的顺序一般是列在表定义中出现的物理顺序，但并不总是如此。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<p> 使用通配符有一个大优点。由于不明确指定列名（因为星号检索每一列），所以能检索出名字未知的列。<br>一般而言，除非你确实需要表中的每一列，否则最好别使用 * 通配符。虽然使用通配符能让你自己省事，不用明确列出所需列，但检索不需要的列通常会降低检索和应用程序的性能。</p>
<h3 id="检索不同的值"><a href="#检索不同的值" class="headerlink" title="检索不同的值"></a>检索不同的值</h3><blockquote>
<p>使用 DISTINCT 关键字，顾名思义，它指示数据库只返回不同的值。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT DISTINCT vend_id</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<p>  <code>SELECT DISTINCT vend_id, prod_price</code> <code>DISTINCT</code> 关键字作用于所有的列，不仅仅是跟在其后的那一列。</p>
<h3 id="限制结果"><a href="#限制结果" class="headerlink" title="限制结果"></a>限制结果</h3><blockquote>
<p><code>SELECT</code> 语句返回指定表中所有匹配的行，很可能是每一行。如果你只想返回第一行或者一定数量的行在 SQL Server和 Access中使用 SELECT 时，可以使用 <code>TOP</code> 关键字来限制最多返回多少行，如只检索前 5行数据。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT TOP 5 prod_name</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<h5 id="注意事项-1"><a href="#注意事项-1" class="headerlink" title="注意事项"></a>注意事项</h5><p> 如果你使用 Oracle，需要基于 <code>ROWNUM</code> （行计数器）来计算行，像这样：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE ROWNUM &lt;=5;</div></pre></td></tr></table></figure>
<p> 如果你使用 MySQL、MariaDB、PostgreSQL或者 SQLite，需要使用 <code>LIMIT</code>子句，像这样：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">LIMIT 5;</div></pre></td></tr></table></figure>
<p>  <code>LIMIT 5</code> 指示MySQL等 DBMS返回不超过 5行的数据。</p>
<p> 为了得到后面的 5行数据，需要指定从哪儿开始以及检索的行数，像这样：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">LIMIT 5 OFFSET 5;</div></pre></td></tr></table></figure>
<p> <code>LIMIT 5 OFFSET 5</code> 指示 MySQL等 DBMS返回从第 5行起的 5行数据。第一个数字是指从哪儿开始，第二个数字是检索的行数。<u> <code>LIMIT</code> 指定返回的行数。 <code>LIMIT</code> 带的 <code>OFFSET</code> 指定从哪儿开始。</u></p>
<p> 第一个被检索的行是第 0行，而不是第 1行。因此， <code>LIMIT 1 OFFSET 1</code> 会检索第 2行，而不是第 1行。MySQL和MariaDB支持简化版的 <code>LIMIT 4 OFFSET 3</code> 语句，即 <code>LIMIT 3,4</code> ，即从第三行开始选取四行。使用这个语法，逗号之前的值对应 <code>OFFSET</code> ，逗号之后的值对应 <code>LIMIT</code> 。</p>
<h3 id="使用注释"><a href="#使用注释" class="headerlink" title="使用注释"></a>使用注释</h3><blockquote>
<p>添加一些描述性的注释，这便于今后参考，或者供项目后续参与人员参考，注释掉一些代码，还可以暂时停止要执行的 SQL代码，只测试一部分。</p>
</blockquote>
<ol>
<li>注释使用 – （两个连字符）嵌在行内。 – 之后的文本就是注释。</li>
</ol>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name -- 这是一条注释</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<ol>
<li>在一行的开始处使用 # ，这一整行都将作为注释。</li>
</ol>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"># 这是一条注释</div><div class="line">SELECT prod_name</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<ol>
<li>多行注释，注释可以在脚本的任何位置开始和停止。注释从 <code>/*</code> 开始，到 <code>*/</code> 结束， <code>/*</code> 和 <code>*/</code> 之间的任何内容都是注释。</li>
</ol>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">/* SELECT prod_name, vend_id</div><div class="line">FROM Products; */</div><div class="line">SELECT prod_name</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<h2 id="排序检索数据"><a href="#排序检索数据" class="headerlink" title="排序检索数据"></a>排序检索数据</h2><p>如果不排序，检索出的数据一般将以它在底层表中出现的顺序显示，这有可能是数据最初添加到表中的顺序。</p>
<h3 id="排序数据"><a href="#排序数据" class="headerlink" title="排序数据"></a>排序数据</h3><blockquote>
<p>使用 <code>ORDER BY</code> 子句，根据需要排序检索出的数据。</p>
</blockquote>
<p>为了明确地排序用 <code>SELECT</code> 语句检索出的数据，可使用 <code>ORDER BY</code> 子句。<code>ORDER BY</code> 子句取一个或多个列的名字，据此对输出进行排序。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_name;</div></pre></td></tr></table></figure>
<p> <code>ORDER BY</code>指示 DBMS软件对 prod_name 列以字母顺序排序数据。</p>
<h5 id="注意事项-2"><a href="#注意事项-2" class="headerlink" title="注意事项"></a>注意事项</h5><p>  在指定一条 <code>ORDER BY</code> 子句时，应该保证它是 <code>SELECT</code> 语句中最后一条子句。如果它不是最后的子句，将会出现错误消息。</p>
<h3 id="按多个列排序"><a href="#按多个列排序" class="headerlink" title="按多个列排序"></a>按多个列排序</h3><blockquote>
<p>要按多个列排序，简单指定列名，列名之间用逗号分开即可。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_price, prod_name;</div></pre></td></tr></table></figure>
<p> 仅在多个行具有相同的 <code>prod_price</code> 值时才对产品按 <code>prod_name</code> 进行排序。如果 <code>prod_price</code> 列中所有的值都是唯一的，则不会按<code>prod_name</code> 排序。</p>
<h3 id="按列位置排序"><a href="#按列位置排序" class="headerlink" title="按列位置排序"></a>按列位置排序</h3><blockquote>
<p><code>ORDER BY</code> 支持按相对列位置进行排序。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY 2, 3;</div></pre></td></tr></table></figure>
<p> <code>ORDER BY 2</code>表示按 <code>SELECT</code> 清单中的第二个列 <code>prod_name</code> 进行排序。 <code>ORDER BY 2，3</code> 表示先按 <code>prod_price</code> ，再按 <code>prod_name</code> 进行排序。<br> 如果有必要，可以混合使用实际列名和相对列位置。</p>
<h3 id="指定排序方向"><a href="#指定排序方向" class="headerlink" title="指定排序方向"></a>指定排序方向</h3><blockquote>
<p>数据排序不限于升序排序（从 A到 Z），这只是默认的排序顺序。还可以使用 <code>ORDER BY</code> 子句进行降序（从 Z 到 A）排序。为了进行降序排序，必须指定 <code>DESC</code> 关键字。如果想在多个列上进行降序排序，必须对每一列指定 <code>DESC</code> 关键字。</p>
</blockquote>
<p>以价格降序来排序产品（最贵的排在最前面）：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_price DESC;</div></pre></td></tr></table></figure>
<p>多个列排序:</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_price DESC, prod_name;</div></pre></td></tr></table></figure>
<p> <code>prod_price</code> 列以降序排序，而 <code>prod_name</code> 列（在每个价格内）仍然按标准的升序排序。</p>
]]></content>
    
    <summary type="html">
    
      数据库入门
    
    </summary>
    
      <category term="数据库" scheme="http://yoursite.com/categories/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    
    
      <category term="SQL" scheme="http://yoursite.com/tags/SQL/"/>
    
  </entry>
  
  <entry>
    <title>SQL必知必会</title>
    <link href="http://yoursite.com/2016/11/07/SQL%E5%BF%85%E7%9F%A5%E5%BF%85%E4%BC%9A/"/>
    <id>http://yoursite.com/2016/11/07/SQL必知必会/</id>
    <published>2016-11-07T10:44:53.000Z</published>
    <updated>2016-11-07T13:45:50.534Z</updated>
    
    <content type="html"><![CDATA[<h1 id="SQL学习笔记"><a href="#SQL学习笔记" class="headerlink" title="SQL学习笔记"></a>SQL学习笔记</h1><p>@(数据库)</p>
<p><img src="./azure_sql_featured_image.png" alt="Alt text"></p>
<p>[TOC]</p>
<h2 id="了解数据库"><a href="#了解数据库" class="headerlink" title="了解数据库"></a>了解数据库</h2><blockquote>
<p>数据库是一个以某种有组织的方式存储的数据集合。</p>
</blockquote>
<p> <strong>数据库</strong>（database）保存有组织的数据的容器（通常是一个文件或一组文件）。确切地说，数据库软件应称为数据库管理系统（DBMS）。数据库是通过 DBMS 创建和操纵的容器。</p>
<h3 id="表"><a href="#表" class="headerlink" title="表"></a>表</h3><blockquote>
<p><strong>表</strong>是一种结构化的文件，可用来存储某种特定类型的数据。</p>
</blockquote>
<h3 id="表名"><a href="#表名" class="headerlink" title="表名"></a>表名</h3><p>数据库中的每个表都有一个名字来标识自己。这个名字是唯一的，即同一数据库中没有其他表具有相同的名字。<br> 使<strong>表名</strong>成为唯一的，实际上是数据库名和表名等的组合。虽然在相同数据库中不能两次使用相同的表名，但在不同的数据库中完全可以使用相同的表名。</p>
<h3 id="模式"><a href="#模式" class="headerlink" title="模式"></a>模式</h3><blockquote>
<p>模式是关于数据库和表的布局及特性的信息。</p>
</blockquote>
<p> 表具有一些特性，这些特性定义了数据在表中如何存储，包含存储什么样的数据，数据如何分解，各部分信息如何命名等信息。描述表的这组信息就是所谓的<u>模式（schema）</u>，模式可以用来描述数据库中特定的表，也可以用来描述整个数据库（和其中表的关系）。</p>
<p>###列和数据类型</p>
<h4 id="列（column）"><a href="#列（column）" class="headerlink" title="列（column）"></a>列（column）</h4><blockquote>
<p>表中的一个字段。所有表都是由一个或多个列组成的。</p>
<h3 id="行（row）"><a href="#行（row）" class="headerlink" title="行（row）"></a>行（row）</h3><p>表中的一个记录<br> 如果将表想象为网格，网格中垂直的列为表列，水平行为表行。</p>
<h3 id="主键（primary-key）"><a href="#主键（primary-key）" class="headerlink" title="主键（primary key）"></a>主键（primary key）</h3><p>一列（或一组列），其值能够唯一标识表中每一行。</p>
</blockquote>
<p>唯一标识表中每行的这个<strong>列</strong>（或这几<strong>列</strong>）称为主键。<strong>主键用来表示一个特定的行</strong>。没有主键，更新或删除表中特定行就极为困难。 应该总是定义主键，虽然并不总是需要主键，但多数数据库设计者都会保证他们创建的每个表具有一个主键，以便于以后的数据操作和管理。</p>
<p>表中的任何列都可以作为主键，只要它满足以下条件：</p>
<p> 任意两行都不具有相同的主键值；<br> 每一行都必须具有一个主键值（主键列不允许 <code>NULL</code> 值）；<br> 主键列中的值不允许修改或更新；<br>主键值不能重用（如果某行从表中删除，它的主键不能赋给以后的新行）。</p>
<h2 id="检索数据"><a href="#检索数据" class="headerlink" title="检索数据"></a>检索数据</h2><h3 id="SELECT"><a href="#SELECT" class="headerlink" title="SELECT"></a>SELECT</h3><blockquote>
<p>为了使用 SELECT 检索表数据，必须至少给出两条信息——想选择什么，以及从什么地方选择。</p>
<h3 id="检索单个列"><a href="#检索单个列" class="headerlink" title="检索单个列"></a>检索单个列</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_name</div><div class="line"><span class="keyword">FROM</span> Products;</div></pre></td></tr></table></figure>
</blockquote>
<p>利用 <code>SELECT</code> 语句从 <code>Products</code> 表中检索一个名为<code>prod_name</code>的列。所需的列名写在 <code>SELECT</code> 关键字之后， <code>FROM</code> 关键字指出从哪个表中检索数据。如上的一条简单 <code>SELECT</code> 语句将返回表中的所有行。数据没有过滤，也没有排序。<br> 如果没有明确排序查询结果（下一课介绍），则返回的数据没有特定的顺序。返回数据的顺序可能是数据被添加到表中的顺序，也可能不是。只要返回相同数目的行，就是正常的。</p>
<h5 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h5><p> 多条 SQL语句必须以分号<code>;</code>分隔。<br>SQL语句不区分大小写，因此 <code>SELECT</code> 与 <code>select</code> 是相同的。同样，写成 <code>Select</code> 也没有关系。但是表名、列名和值可能有所不同（这有赖于具体的 DBMS及其如何配置）。<br>在处理 SQL语句时，其中所有空格都被忽略。SQL语句可以写成长长的一行，也可以分写在多行。下面这 3 种写法的作用是一样的。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_name</div><div class="line"><span class="keyword">FROM</span> Products;</div><div class="line"></div><div class="line"><span class="keyword">SELECT</span> prod_name <span class="keyword">FROM</span> Products;</div><div class="line"></div><div class="line"><span class="keyword">SELECT</span></div><div class="line">prod_name</div><div class="line"><span class="keyword">FROM</span></div><div class="line">Products;</div></pre></td></tr></table></figure></p>
<p> 多数 SQL开发人员认为，将 SQL语句分成多行更容易阅读和调试。</p>
<h3 id="检索多个列"><a href="#检索多个列" class="headerlink" title="检索多个列"></a>检索多个列</h3><blockquote>
<p>要想从一个表中检索多个列，须在 <code>SELECT</code> 关键字后给出多个列名，列名之间必须以逗号分隔。<br>注意：</p>
</blockquote>
<p> 在选择多个列时，一定要在列名之间加上逗号，但最后一个列名后不加。如果在最后一个列名后加了逗号，将出现错误。</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_id, prod_name, prod_price</div><div class="line"><span class="keyword">FROM</span> Products;</div></pre></td></tr></table></figure>
<h3 id="检索所有列"><a href="#检索所有列" class="headerlink" title="检索所有列"></a>检索所有列</h3><blockquote>
<p>如果给定一个通配符（ * ），则返回表中所有列。列的顺序一般是列在表定义中出现的物理顺序，但并不总是如此。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<p> 使用通配符有一个大优点。由于不明确指定列名（因为星号检索每一列），所以能检索出名字未知的列。<br>一般而言，除非你确实需要表中的每一列，否则最好别使用 * 通配符。虽然使用通配符能让你自己省事，不用明确列出所需列，但检索不需要的列通常会降低检索和应用程序的性能。</p>
<h3 id="检索不同的值"><a href="#检索不同的值" class="headerlink" title="检索不同的值"></a>检索不同的值</h3><blockquote>
<p>使用 DISTINCT 关键字，顾名思义，它指示数据库只返回不同的值。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT DISTINCT vend_id</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>  <code>SELECT DISTINCT vend_id, prod_price</code> <code>DISTINCT</code> 关键字作用于所有的列，不仅仅是跟在其后的那一列。</p>
<h3 id="限制结果"><a href="#限制结果" class="headerlink" title="限制结果"></a>限制结果</h3><blockquote>
<p><code>SELECT</code> 语句返回指定表中所有匹配的行，很可能是每一行。如果你只想返回第一行或者一定数量的行在 SQL Server和 Access中使用 SELECT 时，可以使用 <code>TOP</code> 关键字来限制最多返回多少行，如只检索前 5行数据。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT TOP 5 prod_name</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<h5 id="注意事项-1"><a href="#注意事项-1" class="headerlink" title="注意事项"></a>注意事项</h5><p> 如果你使用 Oracle，需要基于 <code>ROWNUM</code> （行计数器）来计算行，像这样：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE ROWNUM &lt;=5;</div></pre></td></tr></table></figure>
<p> 如果你使用 MySQL、MariaDB、PostgreSQL或者 SQLite，需要使用 <code>LIMIT</code>子句，像这样：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">LIMIT 5;</div></pre></td></tr></table></figure></p>
<p>  <code>LIMIT 5</code> 指示MySQL等 DBMS返回不超过 5行的数据。</p>
<p> 为了得到后面的 5行数据，需要指定从哪儿开始以及检索的行数，像这样：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">LIMIT 5 OFFSET 5;</div></pre></td></tr></table></figure></p>
<p> <code>LIMIT 5 OFFSET 5</code> 指示 MySQL等 DBMS返回从第 5行起的 5行数据。第一个数字是指从哪儿开始，第二个数字是检索的行数。<u> <code>LIMIT</code> 指定返回的行数。 <code>LIMIT</code> 带的 <code>OFFSET</code> 指定从哪儿开始。</u></p>
<p> 第一个被检索的行是第 0行，而不是第 1行。因此， <code>LIMIT 1 OFFSET 1</code> 会检索第 2行，而不是第 1行。MySQL和MariaDB支持简化版的 <code>LIMIT 4 OFFSET 3</code> 语句，即 <code>LIMIT 3,4</code> ，即从第三行开始选取四行。使用这个语法，逗号之前的值对应 <code>OFFSET</code> ，逗号之后的值对应 <code>LIMIT</code> 。</p>
<h3 id="使用注释"><a href="#使用注释" class="headerlink" title="使用注释"></a>使用注释</h3><blockquote>
<p>添加一些描述性的注释，这便于今后参考，或者供项目后续参与人员参考，注释掉一些代码，还可以暂时停止要执行的 SQL代码，只测试一部分。</p>
</blockquote>
<ol>
<li><p>注释使用 – （两个连字符）嵌在行内。 – 之后的文本就是注释。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name -- 这是一条注释</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
</li>
<li><p>在一行的开始处使用 # ，这一整行都将作为注释。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"># 这是一条注释</div><div class="line">SELECT prod_name</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
</li>
<li><p>多行注释，注释可以在脚本的任何位置开始和停止。注释从 <code>/*</code> 开始，到 <code>*/</code> 结束， <code>/*</code> 和 <code>*/</code> 之间的任何内容都是注释。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">/* SELECT prod_name, vend_id</div><div class="line">FROM Products; */</div><div class="line">SELECT prod_name</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="排序检索数据"><a href="#排序检索数据" class="headerlink" title="排序检索数据"></a>排序检索数据</h2><p>如果不排序，检索出的数据一般将以它在底层表中出现的顺序显示，这有可能是数据最初添加到表中的顺序。</p>
<h3 id="排序数据"><a href="#排序数据" class="headerlink" title="排序数据"></a>排序数据</h3><blockquote>
<p>使用 <code>ORDER BY</code> 子句，根据需要排序检索出的数据。</p>
</blockquote>
<p>为了明确地排序用 <code>SELECT</code> 语句检索出的数据，可使用 <code>ORDER BY</code> 子句。<code>ORDER BY</code> 子句取一个或多个列的名字，据此对输出进行排序。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_name;</div></pre></td></tr></table></figure></p>
<p> <code>ORDER BY</code>指示 DBMS软件对 prod_name 列以字母顺序排序数据。</p>
<h5 id="注意事项-2"><a href="#注意事项-2" class="headerlink" title="注意事项"></a>注意事项</h5><p>  在指定一条 <code>ORDER BY</code> 子句时，应该保证它是 <code>SELECT</code> 语句中最后一条子句。如果它不是最后的子句，将会出现错误消息。</p>
<h3 id="按多个列排序"><a href="#按多个列排序" class="headerlink" title="按多个列排序"></a>按多个列排序</h3><blockquote>
<p>要按多个列排序，简单指定列名，列名之间用逗号分开即可。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_price, prod_name;</div></pre></td></tr></table></figure></p>
</blockquote>
<p> 仅在多个行具有相同的 <code>prod_price</code> 值时才对产品按 <code>prod_name</code> 进行排序。如果 <code>prod_price</code> 列中所有的值都是唯一的，则不会按<code>prod_name</code> 排序。</p>
<h3 id="按列位置排序"><a href="#按列位置排序" class="headerlink" title="按列位置排序"></a>按列位置排序</h3><blockquote>
<p><code>ORDER BY</code> 支持按相对列位置进行排序。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY 2, 3;</div></pre></td></tr></table></figure>
<p> <code>ORDER BY 2</code>表示按 <code>SELECT</code> 清单中的第二个列 <code>prod_name</code> 进行排序。 <code>ORDER BY 2，3</code> 表示先按 <code>prod_price</code> ，再按 <code>prod_name</code> 进行排序。<br> 如果有必要，可以混合使用实际列名和相对列位置。</p>
<h3 id="指定排序方向"><a href="#指定排序方向" class="headerlink" title="指定排序方向"></a>指定排序方向</h3><blockquote>
<p>数据排序不限于升序排序（从 A到 Z），这只是默认的排序顺序。还可以使用 <code>ORDER BY</code> 子句进行降序（从 Z 到 A）排序。为了进行降序排序，必须指定 <code>DESC</code> 关键字。如果想在多个列上进行降序排序，必须对每一列指定 <code>DESC</code> 关键字。</p>
</blockquote>
<p>以价格降序来排序产品（最贵的排在最前面）：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_price DESC;</div></pre></td></tr></table></figure>
<p>多个列排序:</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_price DESC, prod_name;</div></pre></td></tr></table></figure>
<p> <code>prod_price</code> 列以降序排序，而 <code>prod_name</code> 列（在每个价格内）仍然按标准的升序排序。</p>
<h2 id="过滤数据"><a href="#过滤数据" class="headerlink" title="过滤数据"></a>过滤数据</h2><blockquote>
<p>只检索所需数据需要指定搜索条件（search criteria），搜索条件也称为过滤条件（filter condition）。</p>
</blockquote>
<h3 id="使用-WHERE-子句"><a href="#使用-WHERE-子句" class="headerlink" title="使用 WHERE 子句"></a>使用 WHERE 子句</h3><p>在 <code>SELECT</code> 语句中，数据根据 <code>WHERE</code> 子句中指定的搜索条件进行过滤。<code>WHERE</code> 子句在表名（ <code>FROM</code> 子句）之后给出，如下所示：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE prod_price = 3.49;</div></pre></td></tr></table></figure>
<p> 在同时使用 <code>ORDER BY</code> 和 <code>WHERE</code> 子句时，应该让 <code>ORDER BY</code> 位于<code>WHERE</code> 之后，否则将会产生错误。</p>
<h3 id="WHERE-子句操作符"><a href="#WHERE-子句操作符" class="headerlink" title="WHERE 子句操作符"></a>WHERE 子句操作符</h3><table>
<thead>
<tr>
<th style="text-align:center">操作符与说明</th>
<th style="text-align:center">操作符与说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><code>=</code> 等于</td>
<td style="text-align:center"><code>&gt;</code> 大于</td>
</tr>
<tr>
<td style="text-align:center"><code>&lt; &gt;</code> 不等于</td>
<td style="text-align:center"><code>&gt;=</code> 大于等于</td>
</tr>
<tr>
<td style="text-align:center"><code>!=</code> 不等于</td>
<td style="text-align:center"><code>!&gt;</code> 不大于</td>
</tr>
<tr>
<td style="text-align:center"><code>&lt;</code>小于</td>
<td style="text-align:center"><code>BETWEEN</code> 在指定的两个值之间</td>
</tr>
<tr>
<td style="text-align:center"><code>&lt;=</code> 小于等于</td>
<td style="text-align:center"><code>IS NULL</code> 为<code>NULL</code>值</td>
</tr>
<tr>
<td style="text-align:center"><code>!&lt;</code> 不小于</td>
</tr>
</tbody>
</table>
<h3 id="检查单个值"><a href="#检查单个值" class="headerlink" title="检查单个值"></a>检查单个值</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE prod_price &lt; 10;</div></pre></td></tr></table></figure>
<h3 id="不匹配检查"><a href="#不匹配检查" class="headerlink" title="不匹配检查"></a>不匹配检查</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_id, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE vend_id &lt;&gt; &apos;DLL01&apos;;</div></pre></td></tr></table></figure>
<p> <strong>使用引号不管使用何种形式的字符串数据类型，字符串值都必须括在单引号内</strong>。如果将值与字符串类型的列进行比较，就需要限定引号。用来与数值列进行比较的值不用引号。<br> != 和 &lt;&gt; 通常可以互换。但是，并非所有 DBMS都支持这两种不等于操作符。例如，Microsoft Access支持 &lt;&gt; 而不支持 != 。</p>
<h3 id="范围值检查"><a href="#范围值检查" class="headerlink" title="范围值检查"></a>范围值检查</h3><blockquote>
<p>要检查某个范围的值，可以使用 <code>BETWEEN</code> 操作符。它需要两个值，即范围的开始值和结束值。这两个值必须用 <code>AND</code> 关键字分隔。 <code>BETWEEN</code> 匹配范围中所有的值，<strong>包括指定的开始值和结束值。</strong></p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE prod_price BETWEEN 5 AND 10;</div></pre></td></tr></table></figure>
<h3 id="空值检查"><a href="#空值检查" class="headerlink" title="空值检查"></a>空值检查</h3><blockquote>
<p>在一个列不包含值时，称其包含空值 <code>NULL</code> 。<code>Null</code> 无值（no value），它与字段包含 0、空字符串或仅仅包含空格不同。</p>
</blockquote>
<p> <code>SELECT</code> 语句有一个特殊的 <code>WHERE</code> 子句，可用来检查具有 <code>NULL</code> 值的列。这个 <code>WHERE</code> 子句就是 <code>IS NULL</code> 子句。其语法如下：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_price IS NULL;</div></pre></td></tr></table></figure>
<p> 这条语句返回所有没有价格（空 <code>prod_price</code> 字段，不是价格为 0 ）的产品。<br> 过滤数据时，一定要验证被过滤列中含 <code>NULL</code> 的行确实出现在返回的数据中。</p>
<h2 id="高级数据过滤"><a href="#高级数据过滤" class="headerlink" title="高级数据过滤"></a>高级数据过滤</h2><h3 id="组合-WHERE-子句"><a href="#组合-WHERE-子句" class="headerlink" title="组合 WHERE 子句"></a>组合 WHERE 子句</h3><blockquote>
<p>为了进行更强的过滤控制，SQL允许给出多个 <code>WHERE</code> 子句。这些子句有两种使用方式，即以 <code>AND</code> 子句或 <code>OR</code> 子句的方式使用。</p>
</blockquote>
<h4 id="AND-操作符"><a href="#AND-操作符" class="headerlink" title="AND 操作符"></a>AND 操作符</h4><blockquote>
<p>要通过不止一个列进行过滤，可以使用 <code>AND</code> 操作符给 <code>WHERE</code> 子句附加条件。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos; AND prod_price &lt;= 4;</div></pre></td></tr></table></figure>
<p>此SQL语句检索由供应商 <code>DLL01</code> 制造且价格小于等于4美元的所有产品的名称和价格。这条 <code>SELECT</code> 语句中的 <code>WHERE</code> 子句包含两个条件，用 <code>AND</code>关键字联结在一起。</p>
<h4 id="OR-操作符"><a href="#OR-操作符" class="headerlink" title="OR 操作符"></a>OR 操作符</h4><blockquote>
<p><code>OR</code> 操作符指示 DBMS 检索匹配任一条件的行。OR 操作符告诉 DBMS匹配任一条件而不是同时匹配两个条件。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos; OR vend_id = ‘BRS01’;</div></pre></td></tr></table></figure>
<h4 id="求值顺序"><a href="#求值顺序" class="headerlink" title="求值顺序"></a>求值顺序</h4><blockquote>
<p><code>WHERE</code> 子句可以包含任意数目的 <code>AND</code> 和 <code>OR</code> 操作符。允许两者结合以进行复杂、高级的过滤。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos; OR vend_id = &apos;BRS01&apos;</div><div class="line">AND prod_price &gt;= 10;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">prod_name           prod_price</div><div class="line">------------------- ----------</div><div class="line">Fish bean bag toy   3.4900</div><div class="line">Bird bean bag toy   3.4900</div><div class="line">Rabbit bean bag toy 3.4900</div><div class="line">18 inch teddy bear  11.9900</div><div class="line">Raggedy Ann         4.9900</div></pre></td></tr></table></figure>
<p> SQL（像多数语言一样）在处理 <code>OR</code> 操作符前，优先处理 <code>AND</code> 操作符。</p>
<p>当 SQL看到上述 <code>WHERE</code> 子句时，它理解为：由供应商 <code>BRS01</code> 制造的价格为 10美元以上的所有产品，以及由供应商 <code>DLL01</code> 制造的所有产品，而不管其价格如何。换句话说，由于 <code>AND</code> 在求值过程中优先级更高，操作符被错误地组合了。此问题的解决方法是使用圆括号对操作符进行明确分组。请看下面的<code>SELECT</code> 语句及结果。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE (vend_id = &apos;DLL01&apos; OR vend_id = &apos;BRS01&apos;)</div><div class="line">AND prod_price &gt;= 10;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">prod_name           prod_price</div><div class="line">------------------- ----------</div><div class="line">18 inch teddy bear  11.9900</div></pre></td></tr></table></figure>
<p><code>SELECT</code> 语句与前一条的唯一差别是，将前两个条件用圆括号括了起来。因为圆括号具有比 <code>AND</code> 或 <code>OR</code> 操作符更高的求值顺序，所以 DBMS首先过滤圆括号内的 <code>OR</code> 条件。这时，SQL语句变成了选择由供应商 DLL01或 BRS01 制造的且价格在 10 美元及以上的所有产品，这正是我们希望的结果。<br> 在 WHERE 子句中使用圆括号<br>任何时候使用具有 <code>AND</code> 和 <code>OR</code> 操作符的 <code>WHERE</code> 子句，都应该使用圆括号明确地分组操作符。不要过分依赖默认求值顺序，即使它确实如你希望的那样。使用圆括号没有什么坏处，它能消除歧义。</p>
<h4 id="IN-操作符"><a href="#IN-操作符" class="headerlink" title="IN 操作符"></a>IN 操作符</h4><blockquote>
<p>IN 操作符用来指定条件范围，范围中的每个条件都可以进行匹配。 <code>IN</code> 取一组由逗号分隔、括在圆括号中的合法值。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id IN ( &apos;DLL01&apos;, &apos;BRS01&apos; )</div><div class="line">ORDER BY prod_name;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>此 <code>SELECT</code> 语句检索由供应商 DLL01 和 BRS01 制造的所有产品。 <code>IN</code> 操作符后跟由<strong>逗号</strong>分隔的合法值，这些值必须括在<strong>圆括号</strong>中。</p>
<h5 id="IN-操作符优点"><a href="#IN-操作符优点" class="headerlink" title="IN 操作符优点"></a>IN 操作符优点</h5><p> 在有很多合法选项时， <code>IN</code> 操作符的语法更清楚，更直观。<br>在与其他 <code>AND</code> 和 <code>OR</code> 操作符组合使用 <code>IN</code> 时，求值顺序更容易管理。<br> <code>IN</code> 操作符一般比一组 <code>OR</code> 操作符执行得更快。<br> <code>IN</code> 的最大优点是<strong>可以包含其他 <code>SELECT</code>语句</strong>，能够更动态地建立<code>WHERE</code> 子句。</p>
<h4 id="NOT-操作符"><a href="#NOT-操作符" class="headerlink" title="NOT 操作符"></a>NOT 操作符</h4><blockquote>
<p><code>WHERE</code> 子句中的 <code>NOT</code> 操作符有且只有一个功能，那就是<strong>否定其后所跟的任何条件</strong>。因为 <code>NOT</code> 从不单独使用（它总是与其他操作符一起使用），所以它的语法与其他操作符有所不同。 <code>NOT</code> 关键字可以用在要过滤的列前，而不仅是在其后。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE NOT vend_id = &apos;DLL01&apos;</div><div class="line">ORDER BY prod_name;</div></pre></td></tr></table></figure>
<p>这里的 <code>NOT</code> 否定跟在其后的条件，因此，DBMS 匹配非 <code>DLL01</code> 之外的所有东西。上面的例子也可以使用 <code>&lt;&gt;</code> 操作符来完成，如下所示。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE vend_id &lt;&gt; &apos;DLL01&apos;</div><div class="line">ORDER BY prod_name;</div></pre></td></tr></table></figure>
<h2 id="用通配符进行过滤"><a href="#用通配符进行过滤" class="headerlink" title="用通配符进行过滤"></a>用通配符进行过滤</h2><blockquote>
<p>通配符（wildcard）用来匹配值的一部分的特殊字符。<br>搜索模式（search pattern）由字面值、通配符或两者组合构成的搜索条件。 利用通配符，可以创建比较特定数据的搜索模式。</p>
<h3 id="LIKE-操作符"><a href="#LIKE-操作符" class="headerlink" title="LIKE 操作符"></a>LIKE 操作符</h3><p>为在搜索子句中使用通配符，必须使用 <code>LIKE</code> 操作符。 <code>LIKE</code>指示 DBMS，后跟的搜索模式利用通配符匹配而不是简单的相等匹配进行比较。<br>通配符搜索只能用于<strong>文本字段（字符串</strong>），非文本数据类型字段不能使用通配符搜索。</p>
<h4 id="百分号（-）通配符"><a href="#百分号（-）通配符" class="headerlink" title="百分号（%）通配符"></a>百分号（%）通配符</h4><p>最常使用的通配符是百分号（ % ）。在搜索串中， <strong>% 表示任何字符出现任意次数。</strong></p>
</blockquote>
<p>例如，为了找出所有以词 Fish 起头的产品，可发布以下 <code>SELECT</code> 语句。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_name LIKE &apos;Fish%&apos;;</div></pre></td></tr></table></figure></p>
<p>此例子使用了搜索模式 <code>&#39;Fish%&#39;</code> 。在执行这条子句时，将检索任意以<code>Fish</code> 起头的词。 % 告诉 DBMS 接受 Fish 之后的任意字符，不管它有多少字符。<br> <strong>区分大小写</strong>根据 DBMS的不同及其配置，搜索可以是区分大小写的。</p>
<blockquote>
<p>通配符可在搜索模式中的任意位置使用，并且可以使用多个通配符。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_name LIKE &apos;%bean bag%&apos;;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>搜索模式 <code>&#39;%bean bag%&#39;</code> 表示匹配任何位置上包含文本 bean bag 的值，不论它之前或之后出现什么字符。</p>
<blockquote>
<p>通配符也可以出现在搜索模式的中间，虽然这样做不太有用。</p>
</blockquote>
<p>找出以 F 起头、以 y 结尾的所有产品<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_name LIKE &apos;F%y&apos;;</div></pre></td></tr></table></figure></p>
<p> 有一种情况下把通配符放在搜索模式中间是很有用的，就是根据邮件地址的一部分来查找电子邮件 ，例如 <code>WHERE email LIKE&#39;b%@forta.com&#39;</code> 。</p>
<blockquote>
<p>除了能匹配一个或多个字符外， % 还能匹配 0 个字符。 %代表搜索模式中给定位置的 0个、1个或多个字符。</p>
<h5 id="注意事项-3"><a href="#注意事项-3" class="headerlink" title="注意事项"></a>注意事项</h5><p> 通配符 <code>%</code> 看起来像是可以匹配任何东西，但有个例外，这就是 <code>NULL</code> 。子句 <code>WHERE prod_name LIKE &#39;%&#39;</code> 不会匹配产品名称为 <code>NULL</code> 的行。</p>
<h4 id="下划线（-）通配符"><a href="#下划线（-）通配符" class="headerlink" title="下划线（_）通配符"></a>下划线（_）通配符</h4><p>下划线的用途与 % 一样，但它只匹配单个字符，而不是多个字符。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_name LIKE &apos;__ inch teddy bear&apos;;</div></pre></td></tr></table></figure>
<blockquote>
<p>与 % 能匹配 0或多个字符不同， _ 总是刚好匹配一个字符，不能多也不能少。</p>
<h4 id="方括号（-）通配符"><a href="#方括号（-）通配符" class="headerlink" title="方括号（[ ]）通配符"></a>方括号（[ ]）通配符</h4><p>方括号<code>[]</code> 通配符用来指定一个字符集，它必须匹配指定位置（通配符的位置）的一个字符。<strong>只有微软的 Access 和 SQL Server 支持集合</strong>。</p>
</blockquote>
<p>找出所有名字<strong>以 J 或 M 起头</strong>的联系人，可进行如下查询：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT cust_contact</div><div class="line">FROM Customers</div><div class="line">WHERE cust_contact LIKE &apos;[JM]%&apos;</div><div class="line">ORDER BY cust_contact;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">cust_contact</div><div class="line">-----------------</div><div class="line">Jim Jones</div><div class="line">John Smith</div><div class="line">Michelle Green</div></pre></td></tr></table></figure>
<p>此语句的 <code>WHERE</code> 子句中的模式为 <code>&#39;[JM]%&#39;</code> 。这一搜索模式使用了两个不同的通配符。 <code>[JM]</code> 匹配方括号中任意一个字符，它也<strong>只能匹配单个字符</strong>。因此，任何多于一个字符的名字都不匹配。  [JM] 之后的 % 通配符匹配第一个字符之后的任意数目的字符，返回所需结果。</p>
<blockquote>
<p>此通配符可以用前缀字符 <code>^</code> （脱字号）来否定。例如，下面的查询匹配以J 和 M 之外的任意字符起头的任意联系人名（与前一个例子相反）<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT cust_contact</div><div class="line">FROM Customers</div><div class="line">WHERE cust_contact LIKE &apos;[^JM]%&apos;</div><div class="line">ORDER BY cust_contact;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>也可以使用 NOT 操作符得出类似的结果。<code>^</code> 的唯一优点是在使用多个 <code>WHERE</code> 子句时可以简化语法：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT cust_contact</div><div class="line">FROM Customers</div><div class="line">WHERE NOT cust_contact LIKE &apos;[JM]%&apos;</div><div class="line">ORDER BY cust_contact;</div></pre></td></tr></table></figure></p>
<h5 id="使用通配符的技巧"><a href="#使用通配符的技巧" class="headerlink" title="使用通配符的技巧"></a>使用通配符的技巧</h5><p> 通配符应该细心使用，不要过度使用。如果其他操作符能达到相同的目的，应该使用其他操作符。<br>在确实需要使用通配符时，也尽量不要把它们用在搜索模式的开始处。把通配符置于开始处，搜索起来是最慢的。<br>仔细注意通配符的位置。如果放错地方，可能不会返回想要的数据。</p>
<h2 id="创建计算字段"><a href="#创建计算字段" class="headerlink" title="创建计算字段"></a>创建计算字段</h2><h3 id="计算字段"><a href="#计算字段" class="headerlink" title="计算字段"></a>计算字段</h3><blockquote>
<p>计算字段是运行时在 <code>SELECT</code> 语句内创建的。只有数据库知道 SELECT 语句中哪些列是实际的表列，哪些列是计算字段。</p>
</blockquote>
<p> 字段（field）基本上与列（column）的意思相同，经常互换使用，不过数据库列一般称为列，而术语字段通常与计算字段一起使用。</p>
<h3 id="拼接字段"><a href="#拼接字段" class="headerlink" title="拼接字段"></a>拼接字段</h3><blockquote>
<p>拼接（concatenate）<br>将值联结到一起（将一个值附加到另一个值）构成单个值。</p>
</blockquote>
<p>在 SQL中的 <code>SELECT</code> 语句中，可使用一个特殊的操作符<code>+</code>来拼接两个列。DB2、Oracle、PostgreSQL、SQLite和Open Office Base 使用 <code>||</code> 。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_name + &apos; (&apos; + vend_country + &apos;)&apos;</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p> 使用 MySQL或 MariaDB时需要使用的语句：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Concat(vend_name, &apos; (&apos;, vend_country, &apos;)&apos;)</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>结合成一个计算字段的两个列用空格填充。许多数据库（不是所有）保存填充为列宽的文本值，而实际上你要的结果不需要这些空格。为正确返回格式化的数据，必须去掉这些空格。这可以使用 SQL的 RTRIM() 函数来完成，如下所示：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">Bear Emporium (USA)</div><div class="line">Bears R Us (USA)</div><div class="line">Doll House Inc. (USA)</div><div class="line">Fun and Games (England)</div><div class="line">Furball Inc. (USA)</div><div class="line">Jouets et ours (France)</div></pre></td></tr></table></figure>
<blockquote>
<p>大多数 DBMS都支持 <code>RTRIM()</code> （去掉字符串右边的空格）、 <code>LTRIM()</code> （去掉字符串左边的空格）以及 <code>TRIM()</code> （去掉字符串左右两边的空格）。</p>
<h5 id="使用别名"><a href="#使用别名" class="headerlink" title="使用别名"></a>使用别名</h5><p>  SELECT 语句可以很好地拼接地址字段。但是，这个新计算列实际上没有名字，它只是一个值。一个未命名的列不能用于客户端应用中，因为客户端没有办法引用它。<br>为了解决这个问题，SQL 支持列别名。别名（alias）是一个字段或值的<br>替换名。别名用 <code>AS</code> 关键字赋予。请看下面的 <code>SELECT</code> 语句：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">vend_title</div><div class="line">-----------------------------------------------------------</div><div class="line">Bear Emporium (USA)</div><div class="line">Bears R Us (USA)</div><div class="line">Doll House Inc. (USA)</div><div class="line">Fun and Games (England)</div><div class="line">Furball Inc. (USA)</div><div class="line">Jouets et ours (France)</div></pre></td></tr></table></figure>
<p>SELECT 语句本身与以前使用的相同，只不过这里的计算字段之后跟了文本 <code>AS vend_title</code> 。它指示 SQL 创建一个包含指定计算结果的名为<code>vend_title</code> 的计算字段。从输出可以看到，结果与以前的相同，但现在列名为 <code>vend_title</code> ，任何客户端应用都可以按名称引用这个列，就像它是一个实际的表列一样。<br> 别名有时也称为导出列（derived column），不管怎么叫，它们所代表的是相同的东西。别名的名字既可以是一个单词，也可以是一个字符串。如果是后者，字符串应该括在引号中。虽然这种做法是合法的，但不建议这么去做。多单词的名字可读性高，不过会给客户端应用带来各种问题。因此，别名最常见的使用是将多个单词的列名重命名为一个单词的名字。</p>
<h4 id="执行算术计算"><a href="#执行算术计算" class="headerlink" title="执行算术计算"></a>执行算术计算</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id,</div><div class="line">quantity,</div><div class="line">item_price,</div><div class="line">quantity*item_price AS expanded_price</div><div class="line">FROM OrderItems</div><div class="line">WHERE order_num = 20008;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">prod_id quantity item_price expanded_price</div><div class="line">---------- ----------- ------------ -----------------</div><div class="line">RGAN0	 1 		5 4.9900 	24.9500</div><div class="line">BR03	 5 		11.9900 	59.9500</div><div class="line">BNBG01 	10 		3.4900 		34.9000</div><div class="line">BNBG02 	10 		3.4900 		34.9000</div><div class="line">BNBG03 	10 		3.4900 		34.9000</div></pre></td></tr></table></figure>
<p> SQL 支持基本算术操作符。此外，圆括号可用来区分优先顺序。<br><code>SELECT</code> 语句为测试、检验函数和计算提供了很好的方法。虽然 <code>SELECT</code>通常用于从表中检索数据，但是省略了 <code>FROM</code> 子句后就是简单地访问和处理表达式，例如 <code>SELECT 3 * 2;</code> 将返回 6， <code>SELECT Trim(&#39; abc &#39;);</code>将返回 <code>abc</code> ， <code>SELECT Now();</code> 使用 <code>Now()</code> 函数返回当前<strong>日期和时间</strong>。</p>
<p>##使用函数处理数据</p>
<h3 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h3><blockquote>
<p>函数一般是在数据上执行的，为数据的转换和处理提供了方便。</p>
</blockquote>
<p>每一个DBMS都有特定的函数，虽然所有类型的函数一般都可以在每个 DBMS中使用，但各个函数的名称和语法可能极其不同。<br>| 函数      | 语法                                       |<br>| :—— | :————————————— |<br>| 提取部分字符串 | Access使用 <code>MID()</code> ；DB2、Oracle、PostgreSQL和SQLite用<code>SUBSTR()</code> ；MySQL和SQL Server使用 <code>SUBSTRING()</code> |<br>| 数据类型转换  | Access和Oracle使用多个函数，每种类型的转换有一个函数；DB2和PostgreSQL使用 <code>CAST()</code> ；MariaDB、MySQL和SQL Server使用 <code>CONVERT()</code> |<br>| 取当前日期   | Access 使 用 <code>NOW()</code>； DB2 和 PostgreSQL 使 用 <code>CURRENT_DATE</code>；MariaDB和MySQL使用 <code>CURDATE() ；</code>Oracle使用<code>SYSDATE</code>；SQL Server使用 <code>GETDATE()</code> ；SQLite使用 <code>DATE()</code> |<br>  与 SQL语句不一样，SQL函数不是可移植的。这意味着为特定 SQL实现编写的代码在其他实现中可能不正常。使用或是不使用函数没有对错之分，如果决定使用函数，应该保证做好代码注释，以便以后你（或其他人）能确切地知道所编写的 SQL代码的含义。</p>
<h3 id="使用函数"><a href="#使用函数" class="headerlink" title="使用函数"></a>使用函数</h3><h4 id="文本处理函数"><a href="#文本处理函数" class="headerlink" title="文本处理函数"></a>文本处理函数</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_name, UPPER(vend_name) AS vend_name_upcase</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">vend_name   		vend_name_upcase</div><div class="line">------------------ ----------------------------</div><div class="line">Bear Emporium		BEAR EMPORIUM</div><div class="line">Bears R Us 			BEARS R US</div><div class="line">Doll House Inc.		DOLL HOUSE INC.</div><div class="line">Fun and Games 		FUN AND GAMES</div></pre></td></tr></table></figure>
<h5 id="常用的文本处理函数"><a href="#常用的文本处理函数" class="headerlink" title="常用的文本处理函数"></a>常用的文本处理函数</h5><table>
<thead>
<tr>
<th>函数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>LEFT()</code> （或使用子字符串函数）</td>
<td>返回字符串左边的字符</td>
</tr>
<tr>
<td><code>LENGTH()</code> （也使用 <code>DATALENGTH()</code> 或 <code>LEN()</code> ）</td>
<td>返回字符串的长度</td>
</tr>
<tr>
<td><code>LOWER()</code> （Access使用 <code>LCASE()</code> ）</td>
<td>将字符串转换为小写</td>
</tr>
<tr>
<td><code>LTRIM()</code></td>
<td>去掉字符串左边的空格</td>
</tr>
<tr>
<td><code>RIGHT()</code> （或使用子字符串函数）</td>
<td>返回字符串右边的字符</td>
</tr>
<tr>
<td><code>RTRIM()</code></td>
<td>去掉字符串右边的空格</td>
</tr>
<tr>
<td><code>SOUNDEX()</code></td>
<td>返回字符串的<code>SOUNDEX</code>值</td>
</tr>
<tr>
<td><code>UPPER()</code> （Access使用 <code>UCASE()</code> ）</td>
<td>将字符串转换为大写</td>
</tr>
</tbody>
</table>
<p> <code>SOUNDEX</code> 是一个将任何文本串转换为描述其语音表示的字母数字模式的算法。 <code>SOUNDEX</code> 考虑了类似的发音字符和音节，使得能对字符串进行发音比较而不是字母比较。虽然 <code>SOUNDEX</code> 不是 SQL 概念，但多数 DBMS 都提供对 <code>SOUNDEX</code>的支持。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM Customers</div><div class="line">WHERE SOUNDEX(cust_contact) = SOUNDEX(&apos;Michael Green&apos;);</div></pre></td></tr></table></figure></p>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">cust_contact</div><div class="line">--------------------------</div><div class="line">Michelle Green</div></pre></td></tr></table></figure>
<p>  <code>WHERE</code> 子句使用 <code>SOUNDEX()</code> 函数把 <code>cust_contact</code> 列值和搜索字符串转换为它们的 <code>SOUNDEX</code> 值。因为 <code>Michael Green</code> 和<code>Michelle Green</code> 发音相似，所以它们的 <code>SOUNDEX</code> 值匹配，因此 <code>WHERE</code>子句正确地过滤出了所需的数据。</p>
<h4 id="日期和时间处理函数"><a href="#日期和时间处理函数" class="headerlink" title="日期和时间处理函数"></a>日期和时间处理函数</h4><blockquote>
<p>应用程序一般不使用日期和时间的存储格式，因此日期和时间函数总是用来读取、统计和处理这些值。由于这个原因，日期和时间函数在 SQL中具有重要的作用。遗憾的是，它们很不一致，可移植性最差。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"> SELECT order_num</div><div class="line">FROM Orders</div><div class="line">WHERE DATEPART(yy, order_date) = 2012;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>使用了 <code>DATEPART()</code>函数，顾名思义，此函数返回日期的某一部分。 <code>DATEPART()</code> 函数有两个参数，它们<strong>分别是返回的成分和从中返回成分的日期</strong>。在此例子中，<code>DATEPART()</code> 只从 <code>order_date</code> 列中返回年份。通过与 2012 比较， <code>WHERE</code>子句只过滤出此年份的订单。</p>
<h4 id="数值处理函数"><a href="#数值处理函数" class="headerlink" title="数值处理函数"></a>数值处理函数</h4><blockquote>
<p>在主要 DBMS的函数中，数值函数是最一致、最统一的函数。<br>|函 数 |说 明<br>|—-|—————–|<br>|<code>ABS()</code> |返回一个数的绝对值<br>|<code>COS()</code> |返回一个角度的余弦<br>|<code>EXP()</code> |返回一个数的指数值<br>|<code>PI()</code> |返回圆周率<br>|<code>SIN()</code> |返回一个角度的正弦<br>|<code>SQRT()</code>| 返回一个数的平方根<br>|<code>TAN()</code> |返回一个角度的正切</p>
<h2 id="汇总数据"><a href="#汇总数据" class="headerlink" title="汇总数据"></a>汇总数据</h2><p>汇总数据用聚集函数实现。SQL 支持 5 个聚集函数，可以用多种方法使用它们，返回所需的结果。</p>
<h3 id="聚集函数"><a href="#聚集函数" class="headerlink" title="聚集函数"></a>聚集函数</h3><p>对某些行运行的函数，计算并返回一个值 ,SQL给出了 5 个聚集函数。<br>|函 数 |说 明<br>|—|—-<br>|<code>AVG()</code> |返回某列的平均值<br>|<code>COUNT()</code> |返回某列的行数<br>|<code>MAX()</code> |返回某列的最大值<br>|<code>MIN()</code> |返回某列的最小值<br>|<code>SUM()</code> |返回某列值之和</p>
<h4 id="AVG-函数"><a href="#AVG-函数" class="headerlink" title="AVG() 函数"></a>AVG() 函数</h4><p><code>AVG()</code> 通过对表中行数计数并计算其列值之和，求得该列的平均值。 <code>AVG()</code>可用来返回所有列的平均值，也可以用来返回特定列或行的平均值。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT AVG(prod_price) AS avg_price</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>此 <code>SELECT</code> 语句返回值 <code>avg_price</code> ，它包含 Products 表中所有产品的平均价格。<br><strong><code>AVG()</code> 也可以用来确定特定列或行的平均值。</strong>下面的例子返回特定供应商所提供产品的平均价格：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT AVG(prod_price) AS avg_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos;;</div></pre></td></tr></table></figure></p>
<p>这条 <code>SELECT</code> 语句与前一条的不同之处在于，它包含了 <code>WHERE</code> 子句。此<code>WHERE</code> 子句仅过滤出 <code>vend_id</code> 为 <code>DLL01</code> 的产品，因此 <code>avg_price</code> 中返回的值只是该供应商产品的平均值。<br> 只用于单个列<br><code>AVG()</code> 只能用来确定特定数值列的平均值，而且列名必须作为函数参数给出。为了获得多个列的平均值，必须使用多个 <code>AVG()</code> 函数。<code>AVG()</code> 函数忽略列<strong>值为 <code>NULL</code></strong> 的行。</p>
<h4 id="COUNT-函数"><a href="#COUNT-函数" class="headerlink" title="COUNT() 函数"></a>COUNT() 函数</h4><blockquote>
<p>COUNT() 函数进行计数。可利用 COUNT() 确定表中行的数目或符合特定条件的行的数目。</p>
</blockquote>
<p><code>COUNT()</code> 函数有两种使用方式：</p>
<ol>
<li>使用 <code>COUNT(*)</code> 对表中行的数目进行计数，不管表列中包含的是空值<code>NULL</code>还是非空值。</li>
<li>使用 <code>COUNT(column)</code> 对特定列中具有值的行进行计数，忽略 <code>NULL</code> 值。</li>
</ol>
<p>下面的例子返回 Customers 表中顾客的总数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT COUNT(*) AS num_cust</div><div class="line">FROM Customers;</div></pre></td></tr></table></figure></p>
<p> 在此例子中，利用 COUNT(*) 对所有行计数，不管行中各列有什么值。计数值在 <code>num_cust</code> 中返回。</p>
<p>下面的例子只对具有电子邮件地址的客户计数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT COUNT(cust_email) AS num_cust</div><div class="line">FROM Customers;</div></pre></td></tr></table></figure></p>
<p> 这条 SELECT 语句使用 COUNT(cust_email) 对 cust_email 列中有值的行进行计数。</p>
<h5 id="注意事项-4"><a href="#注意事项-4" class="headerlink" title="注意事项"></a>注意事项</h5><p>  <code>NULL</code> 值<br>如果指定列名，则 <code>COUNT()</code> 函数会忽略指定列的值为空的行，但如果<code>COUNT()</code> 函数中用的是星号<code>（ * ）</code>，则不忽略。</p>
<h4 id="MAX-函数"><a href="#MAX-函数" class="headerlink" title="MAX() 函数"></a>MAX() 函数</h4><blockquote>
<p><code>MAX()</code> 返回指定列中的最大值。 <code>MAX()</code> 要求指定列名，如下所示：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT MAX(prod_price) AS max_price</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<h4 id="MIN-函数"><a href="#MIN-函数" class="headerlink" title="MIN() 函数"></a>MIN() 函数</h4><blockquote>
<p><code>MIN()</code> 的功能正好与 <code>MAX()</code> 功能相反，它返回指定列的最小值。与 <code>MAX()</code>一样， <code>MIN()</code> 要求指定列名，如下所示：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT MIN(prod_price) AS min_price</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="SUM-函数"><a href="#SUM-函数" class="headerlink" title="SUM() 函数"></a>SUM() 函数</h4><blockquote>
<p><code>SUM()</code> 用来返回指定列值的和（总计）。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT SUM(quantity) AS sum_quantity</div><div class="line">FROM OrderItems</div><div class="line">WHERE order_num = 20005;</div></pre></td></tr></table></figure></p>
</blockquote>
<p><code>SUM()</code> 也可以用来合计计算值。在下面的例子中，合计每项物品的<code>item_price*quantity</code> ，得出总的订单金额：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT SUM(item_price*quantity) AS total_price</div><div class="line">FROM OrderItems</div><div class="line">WHERE order_num = 20005;</div></pre></td></tr></table></figure></p>
<p>函数 <code>SUM(item_price*quantity)</code> 返回订单中所有物品价钱之和，<code>WHERE</code> 子句同样保证只统计某个物品订单中的物品。</p>
<h3 id="聚集不同值"><a href="#聚集不同值" class="headerlink" title="聚集不同值"></a>聚集不同值</h3><p> 对所有行执行计算，指定 <code>ALL</code> 参数或不指定参数（因为 <code>ALL</code> 是默认行为）。<br>只包含不同的值，指定 <code>DISTINCT</code> 参数。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT AVG(DISTINCT prod_price) AS avg_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos;;</div></pre></td></tr></table></figure>
<p> 可以看到，在使用了 <code>DISTINCT</code> 后，此例子中的 <code>avg_price</code> 比较高，因为有多个物品具有相同的较低价格。排除它们提升了平均价格。<br>  <code>DISTINCT</code> 不能用于 <code>COUNT(*)</code>如果指定列名，则 <code>DISTINCT</code> 只能用于 <code>COUNT()</code> 。<br> <code>DISTINCT</code> 不能用于 <code>COUNT(*)</code> 。<br> 类似地， <code>DISTINCT</code> 必须使用列名，不能用于计算或表达式。</p>
<h3 id="组合聚集函数"><a href="#组合聚集函数" class="headerlink" title="组合聚集函数"></a>组合聚集函数</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT COUNT(*) AS num_items,</div><div class="line">MIN(prod_price) AS price_min,</div><div class="line">MAX(prod_price) AS price_max,</div><div class="line">AVG(prod_price) AS price_avg</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<p> 取别名在指定别名以包含某个聚集函数的结果时，不应该使用表中实际的列名。虽然这样做也算合法，但许多 SQL实现不支持，可能会产生模糊的错误消息。</p>
<h2 id="分组数据"><a href="#分组数据" class="headerlink" title="分组数据"></a>分组数据</h2><blockquote>
<p>分组数据涉及两个新<code>SELECT</code> 语句子句： <code>GROUP BY</code> 子句和 <code>HAVING</code> 子句。</p>
<h3 id="数据分组"><a href="#数据分组" class="headerlink" title="数据分组"></a>数据分组</h3><p>使用分组可以将数据分为多个逻辑组，对每个组进行聚集计算。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_id, COUNT(*) AS num_prods</div><div class="line">FROM Products</div><div class="line">GROUP BY vend_id;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>结果：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">vend_id num_prods</div><div class="line">------- ---------</div><div class="line">BRS01 	  3</div><div class="line">DLL01 	  4</div><div class="line">FNG01 	  2</div></pre></td></tr></table></figure></p>
<p>上面的 <code>SELECT</code> 语句指定了两个列： <code>vend_id</code> 包含产品供应商的 ID，<code>num_prods</code> 为计算字段（用 <code>COUNT(*)</code> 函数建立）。 <code>GROUP BY</code> 子句指示DBMS按 <code>vend_id</code> 排序并分组数据。这就会对每个 <code>vend_id</code> 而不是整个表计算 <code>num_prods</code> 一次。<br>在使用 <code>GROUP BY</code> 子句前，需要知道一些重要的规定。</p>
<ol>
<li><code>GROUP BY</code> 子句必须出现在 <code>WHERE</code> 子句之后， <code>ORDER BY</code> 子句之前。</li>
<li><code>GROUP BY</code> 子句可以包含任意数目的列，因而可以对分组进行嵌套，更细致地进行数据分组。</li>
<li>如果分组列中包含具有 <code>NULL</code> 值的行，则 <code>NULL</code> 将作为一个分组返回。<br>如果列中有多行 <code>NULL</code> 值，它们将分为一组。</li>
<li><code>GROUP BY</code> 子句中列出的每一列都必须是检索列或有效的表达式（但不能是聚集函数）。如果在 <code>SELECT</code> 中使用表达式，则必须在 <code>GROUP BY</code>子句中指定相同的表达式。不能使用别名<h4 id="过滤分组"><a href="#过滤分组" class="headerlink" title="过滤分组"></a>过滤分组</h4>SQL 允许过滤分组，规定包括哪些分组，排除哪些分组。<blockquote>
<p> <code>HAVING</code> 非常类似于 <code>WHERE</code> ，事实上，目前为止所学过的所有类型的 <code>WHERE</code> 子句都可以用 <code>HAVING</code> 来替代，唯一的差别是， <code>WHERE</code>过滤行，而 <code>HAVING</code> 过滤分组。</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_id, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> orders</div><div class="line"><span class="keyword">FROM</span> Orders</div><div class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> cust_id</div><div class="line"><span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(*) &gt;= <span class="number">2</span>;</div></pre></td></tr></table></figure>
</blockquote>
</li>
</ol>
<p>结果：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">cust_id 	orders</div><div class="line">---------- -----------</div><div class="line">1000000001  2</div></pre></td></tr></table></figure></p>
<p>最后一行增加了 <code>HAVING</code>子句，它过滤 <code>COUNT(*) &gt;= 2</code> （两个以上订单）的那些分组。<code>WHERE</code> 子句在这里不起作用，因为过滤是基于<strong>分组</strong>聚集值，而不是特定行的值。</p>
<h5 id="注意事项-5"><a href="#注意事项-5" class="headerlink" title="注意事项"></a>注意事项</h5><p>  HAVING 和 WHERE 的差别<br> <code>WHERE</code> 在数据分组前进行过滤，<code>HAVING</code> 在数据分组后进行过滤。<br>  <code>WHERE</code> 排除的行不包括在分组中。这可能会改变计算值，从而影响 <code>HAVING</code> 子句中基于这些值过滤掉的分组。<br>  <code>HAVING</code> 与 <code>WHERE</code> 非常类似，如果不指定 <code>GROUP BY</code> ，则大多数 DBMS会同等对待它们。使用 <code>HAVING</code> 时应该结合 <code>GROUP BY</code> 子句，而 <code>WHERE</code> 子句用于标准的行级过滤。</p>
<p>  同时使用 <code>WHERE</code> 和 <code>HAVING</code> 子句，返回过去 12个月内具有两个以上订单的顾客。为此，可增加一条 <code>WHERE</code> 子句，过滤出过去 12个月内下过的订单，然后再增加<code>HAVING</code> 子句过滤出具有两个以上订单的分组。</p>
<p>  列出具有两个以上产品且其价格大于等于 4 的供应商：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> vend_id, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> num_prods</div><div class="line"><span class="keyword">FROM</span> Products</div><div class="line"><span class="keyword">WHERE</span> prod_price &gt;= <span class="number">4</span></div><div class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> vend_id</div><div class="line"><span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(*) &gt;= <span class="number">2</span>;</div></pre></td></tr></table></figure>
<p>  <code>WHERE</code> 子句过滤所有 <code>prod_price</code> 至少为 4 的行，然后按 <code>vend_id</code>分组数据，<code>HAVING</code> 子句过滤计数为 2 或 2 以上的分组。</p>
<h4 id="分组和排序"><a href="#分组和排序" class="headerlink" title="分组和排序"></a>分组和排序</h4><blockquote>
<p>GROUP BY 和 ORDER BY 经常完成相同的工作，但它们非常不同，理解这一点很重要。<br>|<code>ORDER BY</code>| <code>GROUP BY</code>|<br>|:—|:—|<br>|对产生的输出排序| 对行分组，但输出可能不是分组的顺序<br>|任意列都可以使用（甚至非选择的列也可以使用）|只可能使用选择列或表达式列，而且<code>必须使用每个选择列表达式</code><br>|不一定需要 |如果与聚集函数一起使用列（或表达式），则必须使用<br> 一般在使用 <code>GROUP BY</code> 子句时，应该也给出 <code>ORDER BY</code> 子句。这是保证数据正确排序的唯一方法。千万不要仅依赖 <code>GROUP BY</code> 排序数据。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> order_num, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> items</div><div class="line"><span class="keyword">FROM</span> OrderItems</div><div class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> order_num</div><div class="line"><span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(*) &gt;= <span class="number">3</span>;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">order_num items</div><div class="line">--------- -----</div><div class="line"> 20006 		3</div><div class="line"> 20007 		5</div><div class="line"> 20008 		5</div><div class="line"> 20009 		3</div></pre></td></tr></table></figure>
<p>要按订购物品的数目排序输出，需要添加 <code>ORDER BY</code> 子句，如下所示：<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> order_num, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> items</div><div class="line"><span class="keyword">FROM</span> OrderItems</div><div class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> order_num</div><div class="line"><span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(*) &gt;= <span class="number">3</span></div><div class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> items, order_num;</div></pre></td></tr></table></figure></p>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">order_num items</div><div class="line">--------- -----</div><div class="line"> 20006 		3</div><div class="line"> 20009 		3</div><div class="line"> 20007 		5</div><div class="line"> 20008 		5</div></pre></td></tr></table></figure>
<p>使用 <code>GROUP BY</code> 子句按订单号（ <code>order_num</code> 列）分组数据，以便 <code>COUNT(*)</code> 函数能够返回每个订单中的物品数目。 <code>HAVING</code> 子句过滤数据，使得只返回包含三个或更多物品的订单。最后，用 <code>ORDER BY</code>子句排序输出。</p>
<h4 id="SELECT-子句顺序"><a href="#SELECT-子句顺序" class="headerlink" title="SELECT 子句顺序"></a>SELECT 子句顺序</h4><table>
<thead>
<tr>
<th style="text-align:center">子 句</th>
<th style="text-align:center">说 明</th>
<th style="text-align:center">是否必须使用</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><code>SELECT</code></td>
<td style="text-align:center">要返回的列或表达式</td>
<td style="text-align:center">是</td>
</tr>
<tr>
<td style="text-align:center"><code>FROM</code></td>
<td style="text-align:center">从中检索数据的表</td>
<td style="text-align:center">仅在从表选择数据时使用</td>
</tr>
<tr>
<td style="text-align:center"><code>WHERE</code></td>
<td style="text-align:center">行级过滤</td>
<td style="text-align:center">否</td>
</tr>
<tr>
<td style="text-align:center"><code>GROUP BY</code></td>
<td style="text-align:center">分组说明</td>
<td style="text-align:center">仅在按组计算聚集时使用</td>
</tr>
<tr>
<td style="text-align:center"><code>HAVING</code></td>
<td style="text-align:center">组级过滤</td>
<td style="text-align:center">否</td>
</tr>
<tr>
<td style="text-align:center"><code>ORDER BY</code></td>
<td style="text-align:center">输出排序顺序</td>
<td style="text-align:center">否</td>
</tr>
</tbody>
</table>
<h2 id="使用子查询"><a href="#使用子查询" class="headerlink" title="使用子查询"></a>使用子查询</h2><blockquote>
<p>子查询常用于 <code>WHERE</code> 子句的 <code>IN</code> 操作符中，以及用来填充计算列。</p>
<h3 id="子查询"><a href="#子查询" class="headerlink" title="子查询"></a>子查询</h3><p>查询（query）<br>任何 SQL语句都是查询。但此术语一般指 <code>SELECT</code> 语句。<br>SQL还允许创建子查询（subquery），即嵌套在其他查询中的查询。</p>
<h3 id="利用子查询进行过滤"><a href="#利用子查询进行过滤" class="headerlink" title="利用子查询进行过滤"></a>利用子查询进行过滤</h3><p>对 <code>prod_id</code> 为 <code>RGAN01</code> 的所有订单物品，检索其 <code>order_num</code> 列。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">输入</div><div class="line"><span class="keyword">SELECT</span> order_num</div><div class="line"><span class="keyword">FROM</span> OrderItems</div><div class="line"><span class="keyword">WHERE</span> prod_id = <span class="string">'RGAN01'</span>;</div><div class="line">输出</div><div class="line">order_num</div><div class="line"><span class="comment">-----------</span></div><div class="line">20007</div><div class="line">20008</div></pre></td></tr></table></figure></p>
</blockquote>
<p>下一步查询与订单 20007和 20008 相关的顾客ID。</p>
<p>输入<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_id</div><div class="line"><span class="keyword">FROM</span> Orders</div><div class="line"><span class="keyword">WHERE</span> order_num <span class="keyword">IN</span> (<span class="number">20007</span>,<span class="number">20008</span>);</div></pre></td></tr></table></figure></p>
<p>输出<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">cust_id</div><div class="line">----------</div><div class="line">1000000004</div><div class="line">1000000005</div></pre></td></tr></table></figure></p>
<p>结合这两个查询，把第一个查询（返回订单号的那一个）变为子查询。</p>
<p>输入<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_id</div><div class="line"><span class="keyword">FROM</span> Orders</div><div class="line"><span class="keyword">WHERE</span> order_num <span class="keyword">IN</span> (<span class="keyword">SELECT</span> order_num</div><div class="line">	<span class="keyword">FROM</span> OrderItems</div><div class="line">	<span class="keyword">WHERE</span> prod_id = <span class="string">'RGAN01'</span>);</div></pre></td></tr></table></figure></p>
<p>输出<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">cust_id</div><div class="line">----------</div><div class="line">1000000004</div><div class="line">1000000005</div></pre></td></tr></table></figure></p>
<p>在 SELECT 语句中，子查询总是从内向外处理。首先，它执行下面的查询：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SELECT order_num FROM orderitems WHERE prod_id=&apos;RGAN01&apos;</div></pre></td></tr></table></figure>
<p>此查询返回两个订单号： 20007 和 20008 。然后，这两个值以 <code>IN</code> 操作符要求的逗号分隔的格式传递给外部查询的 <code>WHERE</code> 子句。外部查询变成：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SELECT cust_id FROM orders WHERE order_num IN (20007,20008)</div></pre></td></tr></table></figure>
<p>输出是正确的，与前面硬编码 <code>WHERE</code> 子句所返回的值相同。</p>
<p>现在得到了订购物品 <code>RGAN01</code> 的所有顾客的 ID。下一步是检索这些顾客<br>ID的顾客信息。检索两列的 SQL语句为：<br>输入</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_name, cust_contact</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">WHERE</span> cust_id <span class="keyword">IN</span> (<span class="string">'1000000004'</span>,<span class="string">'1000000005'</span>);</div></pre></td></tr></table></figure>
<p>可以把其中的 <code>WHERE</code> 子句转换为子查询，而不是硬编码这些顾客 ID:<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_name, cust_contact</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">WHERE</span> cust_id <span class="keyword">IN</span> (<span class="keyword">SELECT</span> cust_id</div><div class="line">	<span class="keyword">FROM</span> Orders</div><div class="line">	<span class="keyword">WHERE</span> order_num <span class="keyword">IN</span> (<span class="keyword">SELECT</span> order_num</div><div class="line">		<span class="keyword">FROM</span> OrderItems</div><div class="line">		<span class="keyword">WHERE</span> prod_id = <span class="string">'RGAN01'</span>));</div></pre></td></tr></table></figure></p>
<p>输出</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">cust_name 	cust_contact</div><div class="line">---------- ------------------</div><div class="line">Fun4All 	Denise L. Stephens</div><div class="line">The Toy 	Store Kim Howard</div></pre></td></tr></table></figure>
<p>为了执行上述 <code>SELECT</code> 语句，DBMS实际上必须执行三条 <code>SELECT</code> 语句。最里边的子查询返回订单号列表，此列表用于其外面的子查询的 <code>WHERE</code>子句。外面的子查询返回顾客 <code>ID</code> 列表，此顾客 <code>ID</code> 列表用于最外层查询的 <code>WHERE</code> 子句。最外层查询返回所需的数据。<br> 只能是单列作为子查询的 SELECT 语句只能查询单个列。企图检索多个列将返回错误。</p>
<h3 id="作为计算字段使用子查询"><a href="#作为计算字段使用子查询" class="headerlink" title="作为计算字段使用子查询"></a>作为计算字段使用子查询</h3><p>输入<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_name,</div><div class="line">	   cust_state,</div><div class="line">	   (<span class="keyword">SELECT</span> <span class="keyword">COUNT</span>(*)</div><div class="line">		<span class="keyword">FROM</span> Orders</div><div class="line">		<span class="keyword">WHERE</span> Orders.cust_id = Customers.cust_id) <span class="keyword">AS</span> orders</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> cust_name;</div></pre></td></tr></table></figure></p>
<p>输出</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">cust_name 	cust_state 	orders</div><div class="line">--------- 	----------- ------</div><div class="line">Fun4All 	IN 			  1</div><div class="line">Fun4All 	AZ 			  1</div><div class="line">Kids 		Place OH 	  0</div><div class="line">The 		Toy Store IL  1</div><div class="line">Village 	Toys MI 	  2</div></pre></td></tr></table></figure>
<p> <code>orders</code> 是一个计算字段，它是由圆括号中的子查询建立的。该子查询对检索出的每个顾客执行一次。<br> 子查询中的 <code>WHERE</code> 子句与前面使用的 <code>WHERE</code> 子句稍有不同，因为它使用了完全限定列名，指定表名和列名（ <code>Orders.cust_id</code> 和 <code>Customers.cust_id</code> ）。<br> 在这个例子中，有两个 <code>cust_id</code> 列：一个在 <code>Customers</code> 中，另一个在<code>Orders</code> 中。如果不采用完全限定列名，DBMS会认为要对 <code>Orders</code> 表中的 <code>cust_id</code> 自身进行比较。</p>
<h2 id="联结表"><a href="#联结表" class="headerlink" title="联结表"></a>联结表</h2><p>联结是 SQL中一个最重要、最强大的特性。</p>
<h3 id="联结"><a href="#联结" class="headerlink" title="联结"></a>联结</h3><blockquote>
<p>联结是利用 SQL的 <code>SELECT</code> 能执行的最重要的操作，很好地理解联结及其语法是学习 SQL的极为重要的部分。</p>
<h4 id="关系表"><a href="#关系表" class="headerlink" title="关系表"></a>关系表</h4><p>关系数据可以有效地存储，方便地处理，避免信息重复，关系数据库的可伸缩性远比非关系数据库要好，这是关系数据库设计的基础。关系表的设计就是要把信息分解成多个表，一类数据一表。各表通过某些共同的值互相关联（所以才叫关系数据库）。</p>
</blockquote>
<p> 比如，一个存储供应商信息，另一个存储产品信息。 <code>Vendors</code> 表包含所有供应商信息，每个供应商占一行，具有唯一的标识。此标识称为主键（<code>primary key</code>），可以是供应商 ID 或任何其他唯一值。<br><code>Products</code> 表只存储产品信息，除了存储供应商 ID（ <code>Vendors</code> 表的主键）外，它不存储其他有关供应商的信息。 <code>Vendors</code> 表的主键将 <code>Vendors</code> 表与 <code>Products</code> 表关联，利用供应商 ID 能从 <code>Vendors</code> 表中找出相应供应商的详细信息。</p>
<h4 id="为什么使用联结"><a href="#为什么使用联结" class="headerlink" title="为什么使用联结"></a>为什么使用联结</h4><blockquote>
<p>联结是一种机制，数据存储在多个表中，用来在一条 <code>SELECT</code> 语句中关联表，因此称为联结。使用特殊的语法，可以联结多个表返回一组输出，联结在运行时关联表中正确的行。</p>
<h3 id="创建联结"><a href="#创建联结" class="headerlink" title="创建联结"></a>创建联结</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_name, prod_name, prod_price</div><div class="line">FROM Vendors, Products</div><div class="line">WHERE Vendors.vend_id = Products.vend_id;</div></pre></td></tr></table></figure>
</blockquote>
<p>这条语句的 <code>FROM</code>子句列出了两个表： <code>Vendors</code> 和 <code>Products</code> 。它们就是这条 <code>SELECT</code> 语句联结的两个表的名字。这两个表用 <code>WHERE</code> 子句正确地联结， <code>WHERE</code> 子句指示 DBMS将 <code>Vendors</code> 表中的 <code>vend_id</code> 与 <code>Products</code> 表中的 <code>vend_id</code> 匹配起来。</p>
<h4 id="WHERE-子句的重要性"><a href="#WHERE-子句的重要性" class="headerlink" title="WHERE 子句的重要性"></a>WHERE 子句的重要性</h4><p>在联结两个表时，实际要做的是将第一个表中的每一行与第二个表中的每一行配对。 <code>WHERE</code> 子句作为过滤条件，只包含那些匹配给定条件（这里是联结条件）的行。<br> 要保证所有联结都有 <code>WHERE</code> 子句，否则 DBMS将返回比想要的数据多得多的数据。同理，要保证 <code>WHERE</code> 子句的正确性。</p>
<h4 id="内联结"><a href="#内联结" class="headerlink" title="内联结"></a>内联结</h4><p>目前为止使用的联结称为等值联结（equijoin），它基于两个表之间的相等测试。这种联结也称为内联结（inner join）。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_name, prod_name, prod_price</div><div class="line">FROM Vendors INNER JOIN Products</div><div class="line">ON Vendors.vend_id = Products.vend_id;</div></pre></td></tr></table></figure>
<p>这里，两个表之间的关系是以 <code>INNER JOIN</code> 指定的部分 <code>FROM</code> 子句。在使用这种语法时，联结条件用特定的 <code>ON</code> 子句而不是 <code>WHERE</code> 子句给出。传递给 <code>ON</code> 的实际条件与传递给 <code>WHERE</code> 的相同。</p>
<h4 id="联结多个表"><a href="#联结多个表" class="headerlink" title="联结多个表"></a>联结多个表</h4><p>SQL 不限制一条 <code>SELECT</code> 语句中可以联结的表的数目。创建联结的基本规则也相同。首先列出所有表，然后定义表之间的关系。例如：<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_name, vend_name, prod_price, quantity</div><div class="line"><span class="keyword">FROM</span>  OrderItems, Products, Vendors</div><div class="line"><span class="keyword">WHERE</span> Products.vend_id = Vendors.vend_id</div><div class="line"><span class="keyword">AND</span>   Products.prod_id = OrderItems.prod_id</div><div class="line"><span class="keyword">AND</span>   order_num = <span class="number">20007</span>;</div></pre></td></tr></table></figure></p>
<p> 这个例子显示订单 20007 中的物品。订单物品存储在 <code>OrderItems</code> 表中。每个产品按其产品 ID存储，它引用 <code>Products</code> 表中的产品。这些产品通过供应商 ID联结到 <code>Vendors</code> 表中相应的供应商，供应商 ID存储在每个产品的记录中。这里的 <code>FROM</code> 子句列出三个表， <code>WHERE</code> 子句定义这两个联结条件，而第三个联结条件用来过滤出订单 20007 中的物品。</p>
<p> 注意，不要联结不必要的表。联结的表越多，性能下降越厉害。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_name, cust_contact</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">WHERE</span> cust_id <span class="keyword">IN</span> (<span class="keyword">SELECT</span> cust_id</div><div class="line">	<span class="keyword">FROM</span> Orders</div><div class="line">	<span class="keyword">WHERE</span> order_num <span class="keyword">IN</span> (<span class="keyword">SELECT</span> order_num</div><div class="line">		<span class="keyword">FROM</span> OrderItems</div><div class="line">		<span class="keyword">WHERE</span> prod_id = <span class="string">'RGAN01'</span>));</div></pre></td></tr></table></figure></p>
<p>子查询并不总是执行复杂 <code>SELECT</code> 操作的最有效方法，下面是使用联结的相同查询：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM Customers, Orders, OrderItems</div><div class="line">WHERE Customers.cust_id = Orders.cust_id</div><div class="line">AND OrderItems.order_num = Orders.order_num</div><div class="line">AND prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure></p>
<h2 id="创建高级联结"><a href="#创建高级联结" class="headerlink" title="创建高级联结"></a>创建高级联结</h2><h3 id="使用表别名"><a href="#使用表别名" class="headerlink" title="使用表别名"></a>使用表别名</h3><p>给列起别名的语法如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure></p>
<p>SQL 除了可以对列名和计算字段使用别名，还允许给表名起别名。这样做有两个主要理由：</p>
<ol>
<li>缩短 SQL语句；</li>
<li>允许在一条 SELECT 语句中多次使用相同的表。<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM  Customers AS C, Orders AS O, OrderItems AS OI</div><div class="line">WHERE C.cust_id = O.cust_id</div><div class="line">AND   OI.order_num = O.order_num</div><div class="line">AND   prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure>
</li>
</ol>
<p>在这个例子中，表别名只用于 WHERE 子句。其实它不仅能用于 WHERE 子句，还可以用于 SELECT 的列表、 ORDER BY 子句及其他语句部分。<br> 表别名只在查询执行中使用。与列别名不一样，表别名不返回到客户端。</p>
<h3 id="使用不同类型的联结"><a href="#使用不同类型的联结" class="headerlink" title="使用不同类型的联结"></a>使用不同类型的联结</h3><p>除了内联结或等值联结的简单联结外，还有其他联结：自联结（self-join）、自然联结（natural join）和外联结（outer join）。</p>
<h4 id="自联结"><a href="#自联结" class="headerlink" title="自联结"></a>自联结</h4><p>使用表别名的一个主要原因是能在一条 <code>SELECT</code> 语句中不止一次引用相同的表。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT c1.cust_id, c1.cust_name, c1.cust_contact</div><div class="line">FROM Customers AS c1, Customers AS c2</div><div class="line">WHERE c1.cust_name = c2.cust_name</div><div class="line">AND c2.cust_contact = &apos;Jim Jones&apos;;</div></pre></td></tr></table></figure></p>
<p> Oracle 中没有 <code>AS</code>，Oracle用户应该记住去掉 <code>AS</code> 。</p>
<p>此查询中需要的两个表实际上是相同的表，因此 <code>Customers</code> 表在 <code>FROM</code>子句中出现了两次。虽然这是完全合法的，但对 <code>Customers</code> 的引用具有歧义性，因为 DBMS不知道你引用的是哪个 <code>Customers</code> 表。<br>解决此问题，需要使用表别名。 <code>Customers</code> 第一次出现用了别名 <code>C1</code> ，第二次出现用了别名 <code>C2</code> 。现在可以将这些别名用作表名。例如， <code>SELECT</code> 语句使用 <code>C1</code> 前缀明确给出所需列的全名。<br> 用自联结而不用子查询<br>自联结通常作为外部语句，用来替代从相同表中检索数据的使用子查询语句。虽然最终的结果是相同的，但许多 DBMS处理联结远比处理子查询快得多。</p>
<h4 id="自然联结"><a href="#自然联结" class="headerlink" title="自然联结"></a>自然联结</h4><blockquote>
<p>无论何时对表进行联结，应该至少有一列不止出现在一个表中。标准的联结返回所有数据，相同的列甚至多次出现。自然联结排除多次出现，使每一列只返回一次。</p>
</blockquote>
<p>自然联结要求只能选择那些唯一的列，一般通过对一个表使用通配符（ <code>SELECT *</code> ），而对其他表的列使用明确的子集来完成。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">SELECT C.*, O.order_num, O.order_date,</div><div class="line">OI.prod_id, OI.quantity, OI.item_price</div><div class="line">FROM Customers AS C, Orders AS O, OrderItems AS OI</div><div class="line">WHERE C.cust_id = O.cust_id</div><div class="line">AND OI.order_num = O.order_num</div><div class="line">AND prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure></p>
<p>在这个例子中，通配符只对第一个表使用。所有其他列明确列出，所以没有重复的列被检索出来。</p>
<h4 id="外联结"><a href="#外联结" class="headerlink" title="外联结"></a>外联结</h4><p>许多联结将一个表中的行与另一个表中的行相关联，但有时候需要包含没有关联行的那些行。<br>下面的 <code>SELECT</code> 语句给出了一个简单的内联结。它检索所有顾客及其订单：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id, Orders.order_num</div><div class="line">FROM Customers INNER JOIN Orders</div><div class="line">ON Customers.cust_id = Orders.cust_id;</div></pre></td></tr></table></figure></p>
<p>外联结语法类似。要检索<strong>包括没有订单顾客在内的所有顾客</strong>，可如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id, Orders.order_num</div><div class="line">FROM Customers LEFT OUTER JOIN Orders</div><div class="line">ON Customers.cust_id = Orders.cust_id;</div></pre></td></tr></table></figure></p>
<p>这条 <code>SELECT</code> 语句使用了关键字 <code>OUTER JOIN</code>来指定联结类型（而不是在 <code>WHERE</code> 子句中指定）。但是，与内联结关联两个表中的行不同的是，<strong>外联结还包括没有关联行的行。</strong><mark>在使用 <code>OUTERJOIN</code> 语法时，必须使用 <code>RIGHT</code> 或 <code>LEFT</code> 关键字指定包括其所有行的表</mark>（ <code>RIGHT</code> 指出的是 <code>OUTER JOIN</code> 右边的表，而 <code>LEFT</code> 指出的是 <code>OUTER JOIN</code>左边的表）。上面的例子使用 <code>LEFT OUTER JOIN</code> 从 <code>FROM</code> 子句左边的表（ <code>Customers</code> 表）中选择所有行。为了从右边的表中选择所有行，需要使用 <code>RIGHT OUTER JOIN</code> ，如下例所示：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id, Orders.order_num</div><div class="line">FROM Customers RIGHT OUTER JOIN Orders</div><div class="line">ON Orders.cust_id = Customers.cust_id;</div></pre></td></tr></table></figure>
<p> 总是有两种基本的外联结形式：左外联结和右外联结。它们之间的唯一差别是所关联的表的顺序。换句话说，调整 <code>FROM</code> 或 <code>WHERE</code>子句中<strong>表的顺序</strong>，左外联结可以转换为右外联结。因此，这两种外联结可以互换使用，哪个方便就用哪个。</p>
<p>还存在另一种外联结，就是<strong>全外联结</strong>（full outer join），<strong>它检索两个表中的所有行并关联那些可以关联的行。</strong>与左外联结或右外联结包含一个表的不关联的行不同，<strong>全外联结包含两个表的不关联的行</strong>。全外联结的语法如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id, Orders.order_num</div><div class="line">FROM Orders FULL OUTER JOIN Customers</div><div class="line">ON Orders.cust_id = Customers.cust_id;</div></pre></td></tr></table></figure></p>
<h3 id="使用带聚集函数的联结"><a href="#使用带聚集函数的联结" class="headerlink" title="使用带聚集函数的联结"></a>使用带聚集函数的联结</h3><blockquote>
<p>聚集函数可以从一个表中汇总数据，但这些函数也可以与联结一起使用。</p>
</blockquote>
<p>检索所有顾客及每个顾客所下的订单数，下面的代码使用 <code>COUNT()</code> 函数完成此工作：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id,</div><div class="line">COUNT(Orders.order_num) AS num_ord</div><div class="line">FROM Customers INNER JOIN Orders</div><div class="line">ON Customers.cust_id = Orders.cust_id</div><div class="line">GROUP BY Customers.cust_id;</div></pre></td></tr></table></figure></p>
<p>这条 <code>SELECT</code> 语句使用 <code>INNER JOIN</code> 将 <code>Customers</code> 和 <code>Orders</code> 表互相关联。<code>GROUP BY</code> 子句按顾客分组数据，因此，函数调用<code>COUNT(Orders.order_num)</code>对每个顾客的订单计数，将它作为 <code>num_ord</code> 返回。</p>
<p>聚集函数也可以方便地与其他联结一起使用。请看下面的例子：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id,</div><div class="line">COUNT(Orders.order_num) AS num_ord</div><div class="line">FROM Customers LEFT OUTER JOIN Orders</div><div class="line">ON Customers.cust_id = Orders.cust_id</div><div class="line">GROUP BY Customers.cust_id;</div></pre></td></tr></table></figure></p>
<p>这个例子使用左外部联结来包含所有顾客，甚至包含那些没有任何订单的顾客。</p>
<h3 id="使用联结和联结条件"><a href="#使用联结和联结条件" class="headerlink" title="使用联结和联结条件"></a>使用联结和联结条件</h3><p>联结及其使用的要点</p>
<ol>
<li>注意所使用的联结类型。一般我们使用内联结，但使用外联结也有效。</li>
<li>保证使用正确的联结条件（不管采用哪种语法），否则会返回不正确的数据。</li>
<li>应该总是提供联结条件，否则会得出笛卡儿积</li>
<li>在一个联结中可以包含多个表，甚至可以对每个联结采用不同的联结类型。虽然这样做是合法的，一般也很有用，但应该在一起测试它们前分别测试每个联结。这会使故障排除更为简单。<h2 id="组合查询"><a href="#组合查询" class="headerlink" title="组合查询"></a>组合查询</h2><blockquote>
<p>利用 UNION 操作符将多条 <code>SELECT</code> 语句组合成一个结果集。用 <code>UNION</code> 可极大地简化复杂的 <code>WHERE</code> 子句，简化从多个表中检索数据的工作。</p>
<h3 id="组合查询-1"><a href="#组合查询-1" class="headerlink" title="组合查询"></a>组合查询</h3><p>SQL允许执行多个查询（多条 <code>SELECT</code> 语句），并将结果作为一个查询结果集返回。这些组合查询通常称为并（<code>union</code>）或复合查询（compound query）。</p>
</blockquote>
</li>
</ol>
<p>主要有两种情况需要使用组合查询：</p>
<ol>
<li>在一个查询中从不同的表返回结构数据；</li>
<li><p>对一个表执行多个查询，按一个查询返回数据。</p>
<p>任何具有多个<code>WHERE</code> 子句的 <code>SELECT</code> 语句都可以作为一个组合查询。</p>
</li>
</ol>
<h3 id="使用UNION"><a href="#使用UNION" class="headerlink" title="使用UNION"></a>使用UNION</h3><p>可用 <code>UNION</code> 操作符来组合数条 SQL 查询。利用 <code>UNION</code> ，可给出多条<code>SELECT</code> 语句，将它们的结果组合成一个结果集。使用 <code>UNION</code> 很简单，所要做的只是给出每条 <code>SELECT</code> 语句，在各条语句之间放上关键字 <code>UNION</code> 。</p>
<p>创建 <code>UNION</code> 涉及编写多条 <code>SELECT</code> 语句。首先来看单条语句：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_state IN (&apos;IL&apos;,&apos;IN&apos;,&apos;MI&apos;);</div></pre></td></tr></table></figure></p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_name = &apos;Fun4All&apos;;</div></pre></td></tr></table></figure>
<p>第一条 <code>SELECT</code> 把 <code>Illinois</code>、<code>Indiana</code>、<code>Michigan</code>等州的缩写传递给 <code>IN</code> 子句，检索出这些州的所有行。第二条 <code>SELECT</code> 利用简单的相等测试找出所有<code>Fun4All</code> 。<br>组合这两条语句，可以如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_state IN (&apos;IL&apos;,&apos;IN&apos;,&apos;MI&apos;)</div><div class="line">UNION</div><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_name = &apos;Fun4All&apos;;</div></pre></td></tr></table></figure></p>
<p>这条语句由前面的两条 <code>SELECT</code> 语句组成，之间用 <code>UNION</code> 关键字分隔。<code>UNION</code> 指示 DBMS执行这两条 <code>SELECT</code> 语句，并把输出组合成一个查询结果并集。</p>
<p>为了便于参考，这里给出使用多条 <code>WHERE</code> 子句而不是 <code>UNION</code> 的相同查询：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_state IN (&apos;IL&apos;,&apos;IN&apos;,&apos;MI&apos;)</div><div class="line">OR cust_name = &apos;Fun4All&apos;;</div></pre></td></tr></table></figure></p>
<p>#### </p>
<ol>
<li><code>UNION</code> 必须由两条或两条以上的 <code>SELECT</code> 语句组成，语句之间用关键字 <code>UNION</code> 分隔（因此，如果组合四条 <code>SELECT</code> 语句，将要使用三个 <code>UNION</code>关键字）。</li>
<li><code>UNION</code> 中的每个查询必须包含相同的列、表达式或聚集函数（不过，各个列不需要以相同的次序列出）。</li>
<li>列数据类型必须兼容：类型不必完全相同，但必须是 DBMS可以隐含转换的类型（例如，不同的数值类型或不同的日期类型）。<h4 id="包含或取消重复的行"><a href="#包含或取消重复的行" class="headerlink" title="包含或取消重复的行"></a>包含或取消重复的行</h4><code>UNION</code> 从查询结果集中自动去除了重复的行，这是 <code>UNION</code> 的默认行为；换句话说，它的行为与一条 <code>SELECT</code> 语句中使用多个 <code>WHERE</code> 子句条件一样。如果想返回所有的匹配行，可使用 <code>UNION ALL</code> 而不是 <code>UNION</code> 。<br><code>UNION</code> 几乎总是完成与多个 <code>WHERE</code> 条件相同的工作。 <code>UNION ALL</code> 为 <code>UNION</code> 的一种形式，它完成 <code>WHERE</code> 子句完成不了的工作。如果确实需要每个条件的匹配行全部出现（包括重复行），就必须使用 <code>UNION ALL</code> ，而不是 <code>WHERE</code> 。<h4 id="对组合查询结果排序"><a href="#对组合查询结果排序" class="headerlink" title="对组合查询结果排序"></a>对组合查询结果排序</h4><blockquote>
<p><code>SELECT</code> 语句的输出用 <code>ORDER BY</code> 子句排序。在用 <code>UNION</code> 组合查询时，只能使用一条 <code>ORDER BY</code> 子句，它必须位于最后一条 <code>SELECT</code> 语句之后，不允许使用多条 <code>ORDER BY</code> 子句。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_state IN (&apos;IL&apos;,&apos;IN&apos;,&apos;MI&apos;)</div><div class="line">UNION</div><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_name = &apos;Fun4All&apos;</div><div class="line">ORDER BY cust_name, cust_contact;</div></pre></td></tr></table></figure>
</blockquote>
</li>
</ol>
<h2 id="插入数据"><a href="#插入数据" class="headerlink" title="插入数据"></a>插入数据</h2><h3 id="数据插入"><a href="#数据插入" class="headerlink" title="数据插入"></a>数据插入</h3><p>INSERT 用来将行插入（或添加）到数据库表。插入有几种方式：</p>
<ol>
<li>插入完整的行；</li>
<li>插入行的一部分；</li>
<li>插入某些查询的结果。<h4 id="插入完整的行"><a href="#插入完整的行" class="headerlink" title="插入完整的行"></a>插入完整的行</h4>要求指定表名和插入到新行中的值。</li>
</ol>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> Customers</div><div class="line"><span class="keyword">VALUES</span>(<span class="string">'1000000006'</span>,</div><div class="line">	<span class="string">'Toy Land'</span>,</div><div class="line">	<span class="string">'123 Any Street'</span>,</div><div class="line">	<span class="string">'New York'</span>,</div><div class="line">	<span class="string">'NY'</span>,</div><div class="line">	<span class="string">'11111'</span>,</div><div class="line">	<span class="string">'USA'</span>,</div><div class="line">	<span class="literal">NULL</span>,</div><div class="line">	<span class="literal">NULL</span>);</div></pre></td></tr></table></figure>
<p>这个例子将一个新顾客插入到 <code>Customers</code> 表中。存储到表中每一列的数据在 <code>VALUES</code> 子句中给出，必须给每一列提供一个值。如果某列没有值，如上面的 <code>cust_contact</code> 和 <code>cust_email</code> 列，则应该使用 <code>NULL</code> 值（假定表允许对该列指定空值）。各列必须以它们在表定义中出现的次序填充。</p>
<p>上面的 SQL语句高度依赖于表中列的定义次序，还依赖于其容易获得的次序信息。即使可以得到这种次序信息，也不能保证各列在下一次表结构变动后保持完全相同的次序。<br>编写 INSERT 语句的更安全（不过更烦琐）的方法如下：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line">INSERT INTO Customers(cust_id,</div><div class="line">	cust_name,</div><div class="line">	cust_address,</div><div class="line">	cust_city,</div><div class="line">	cust_state,</div><div class="line">	cust_zip,</div><div class="line">	cust_country,</div><div class="line">	cust_contact,</div><div class="line">	cust_email)</div><div class="line">VALUES(&apos;1000000006&apos;,</div><div class="line">	   &apos;Toy Land&apos;,</div><div class="line">	   &apos;123 Any Street&apos;,</div><div class="line">	   &apos;New York&apos;,</div><div class="line">	   &apos;NY&apos;,</div><div class="line">	   &apos;11111&apos;,</div><div class="line">	   &apos;USA&apos;,</div><div class="line">	   NULL,</div><div class="line">	   NULL);</div></pre></td></tr></table></figure>
<p>在表名后的括号里明确给出了列名。在插入行时，DBMS 将用 <code>VALUES</code> 列表中的相应值填入列表中的对应项。 <code>VALUES</code> 中的第一个值对应于第一个指定列名，第二个值对应于第二个列名，如此等等。<br>因为提供了列名， <code>VALUES</code> 必须以其指定的次序匹配指定的列名，不一定按各列出现在表中的实际次序。其优点是，即使表的结构改变，这条<code>INSERT</code> 语句仍然能正确工作。</p>
<h5 id="注意事项-6"><a href="#注意事项-6" class="headerlink" title="注意事项"></a>注意事项</h5><p> 推荐使用列的列表，不推荐使用没有明确给出列的 <code>INSERT</code> 语句。给出列能使 SQL代码继续发挥作用，即使表结构发生了变化。<br>不管使用哪种 <code>INSERT</code> 语法， <code>VALUES</code> 的数目都必须正确。如果不提供列名，则必须给每个表列提供一个值；如果提供列名，则必须给列出的每个列一个值。</p>
<h4 id="插入部分行"><a href="#插入部分行" class="headerlink" title="插入部分行"></a>插入部分行</h4><p>使用 <code>INSERT</code> 推荐给出表的列名的方式，还可以省略列，这表示可以只给某些列提供值，给其他列不提供值。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">INSERT INTO Customers(cust_id,</div><div class="line">	cust_name,</div><div class="line">	cust_address,</div><div class="line">	cust_city,</div><div class="line">	cust_state,</div><div class="line">	cust_zip,</div><div class="line">	cust_country)</div><div class="line">VALUES(&apos;1000000006&apos;,</div><div class="line">	   &apos;Toy Land&apos;,</div><div class="line">	   &apos;123 Any Street&apos;,</div><div class="line">	   &apos;New York&apos;,</div><div class="line">	   &apos;NY&apos;,</div><div class="line">	   &apos;11111&apos;,</div><div class="line">	   &apos;USA&apos;);</div></pre></td></tr></table></figure></p>
<p>没有给 <code>cust_contact</code> 和 <code>cust_email</code> 这两列提供值。这表示没必要在 <code>INSERT</code> 语句中包含它们。因此，这里的 <code>INSERT</code>语句省略了这两列及其对应的值。<br> 省略的列必须满足以下某个条件。<br>该列定义为允许 <code>NULL</code> 值（无值或空值）。<br>在表定义中给出默认值。这表示如果不给出值，将使用默认值。<br>如果表中不允许有 <code>NULL</code> 值或者默认值，这时却省略了表中的值，DBMS就会产生错误消息，相应的行不能成功插入。</p>
<h4 id="插入检索出的数据"><a href="#插入检索出的数据" class="headerlink" title="插入检索出的数据"></a>插入检索出的数据</h4><p><code>INSERT</code> 一般用来给表插入具有指定列值的行。 <code>INSERT</code> 还存在另一种形式，可以利用它将 <code>SELECT</code> 语句的结果插入表中，这就是所谓的<code>INSERT SELECT</code> 。</p>
<p>假如想把另一表中的顾客列合并到 <code>Customers</code> 表中。不需要每次读取一行再将它用 <code>INSERT</code> 插入，可以如下进行：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line">INSERT INTO Customers(cust_id,</div><div class="line">	cust_contact,</div><div class="line">	cust_email,</div><div class="line">	cust_name,</div><div class="line">	cust_address,</div><div class="line">	cust_city,</div><div class="line">	cust_state,</div><div class="line">	cust_zip,</div><div class="line">	cust_country)</div><div class="line">SELECT cust_id,</div><div class="line">	cust_contact,</div><div class="line">	cust_email,</div><div class="line">	cust_name,</div><div class="line">	cust_address,</div><div class="line">	cust_city,</div><div class="line">	cust_state,</div><div class="line">	cust_zip,</div><div class="line">	cust_country</div><div class="line">FROM CustNew;</div></pre></td></tr></table></figure>
<p>这个例子使用 <code>INSERT SELECT</code> 从 <code>CustNew</code> 中将所有数据导入<code>Customers</code> 。 <code>SELECT</code> 语句从 <code>CustNew</code> 检索出要插入的值，而不是列出它们。 <code>SELECT</code> 中列出的每一列对应于 <code>Customers</code> 表名后所跟的每一列。<br> <code>INSERT SELECT</code> 中 <code>SELECT</code> 语句可以包含 <code>WHERE</code> 子句，以过滤插入的数据。</p>
<h3 id="从一个表复制到另一个表"><a href="#从一个表复制到另一个表" class="headerlink" title="从一个表复制到另一个表"></a>从一个表复制到另一个表</h3><blockquote>
<p>要将一个表的内容复制到一个全新的表（运行中创建的表），可以使用 <code>SELECT INTO</code> 语句。</p>
</blockquote>
<p>  <code>INSERT SELECT</code> 与 <code>SELECT INTO</code>它们之间的一个重要差别是前者导入数据，而后者导出数据。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">INTO CustCopy</div><div class="line">FROM Customers;</div></pre></td></tr></table></figure></p>
<p>这条 <code>SELECT</code> 语句创建一个名为 <code>CustCopy</code> 的新表，并把 <code>Customers</code> 表的整个内容复制到新表中。因为这里使用的是 <code>SELECT *</code>，所以将在<code>CustCopy</code> 表中创建（并填充）与 <code>Customers</code> 表的每一列相同的列。要想只复制部分的列，可以明确给出列名，而不是使用 <code>*</code> 通配符。</p>
<h5 id="注意事项-7"><a href="#注意事项-7" class="headerlink" title="注意事项"></a>注意事项</h5><p> 任何 <code>SELECT</code> 选项和子句都可以使用，包括 <code>WHERE</code> 和 <code>GROUP BY</code>；<br>可利用联结从多个表插入数据；<br>不管从多少个表中检索数据，数据都只能插入到一个表中。</p>
<p> <code>SELECT INTO</code> 是试验新 SQL语句前进行<strong>表复制</strong>的很好工具。先进行复制，可在复制的数据上测试 SQL代码，而不会影响实际的数据。</p>
<h2 id="更新和删除数据"><a href="#更新和删除数据" class="headerlink" title="更新和删除数据"></a>更新和删除数据</h2><h3 id="更新数据"><a href="#更新数据" class="headerlink" title="更新数据"></a>更新数据</h3><p>更新（修改）表中的数据，可以使用 <code>UPDATE</code> 语句。有两种使用 <code>UPDATE</code>的方式：</p>
<ol>
<li>更新表中的特定行。</li>
<li>更新表中的所有行。</li>
</ol>
<p>基本的 <code>UPDATE</code> 语句由三部分组成，分别是：</p>
<ol>
<li>要更新的表；</li>
<li>列名和它们的新值；</li>
<li>确定要更新哪些行的过滤条件。<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">UPDATE Customers</div><div class="line">SET cust_email = &apos;kim@thetoystore.com&apos;</div><div class="line">WHERE cust_id = &apos;1000000005&apos;;</div></pre></td></tr></table></figure>
</li>
</ol>
<p><code>UPDATE</code> 语句总是以要更新的表名开始。在这个例子中，要更新的表名为<code>Customers</code> 。 <code>SET</code> 命令用来将新值赋给被更新的列。在这里， <code>SET</code> 子句设置 <code>cust_email</code> 列为指定的值：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SET cust_email = &apos;kim@thetoystore.com&apos;</div></pre></td></tr></table></figure>
<p><code>UPDATE</code> 语句以 <code>WHERE</code> 子句结束，它告诉 DBMS更新哪一行。没有 <code>WHERE</code>子句，DBMS将会用这个电子邮件地址更新 <code>Customers</code> 表中的所有行，这不是我们希望的。</p>
<p>更新多个列：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">UPDATE Customers</div><div class="line">SET cust_contact = &apos;Sam Roberts&apos;,</div><div class="line">cust_email = &apos;sam@toyland.com&apos;</div><div class="line">WHERE cust_id = &apos;1000000006&apos;;</div></pre></td></tr></table></figure></p>
<p>在更新多个列时，只需要使用一条 <code>SET</code> 命令，每个“列=值”对之间用逗号分隔（最后一列之后不用逗号）。</p>
<p>要删除某个列的值，可设置它为 NULL （假如表定义允许 NULL 值）。如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">UPDATE Customers</div><div class="line">SET cust_email = NULL</div><div class="line">WHERE cust_id = &apos;1000000005&apos;;</div></pre></td></tr></table></figure></p>
<h3 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h3><p>从一个表中删除（去掉）数据，使用 <code>DELETE</code> 语句。有两种使用 <code>DELETE</code><br>的方式：</p>
<ol>
<li>从表中删除特定的行；</li>
<li>从表中删除所有行。</li>
</ol>
<p>下面的语句从 <code>Customers</code> 表中删除一行：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">DELETE FROM Customers</div><div class="line">WHERE cust_id = &apos;1000000006&apos;;</div></pre></td></tr></table></figure>
<p><code>DELETE FROM</code> 要求指定从中删除数据的表名，<code>WHERE</code> 子句过滤要删除的行。如果省略 <code>WHERE</code> 子句，它将删除表中每个行。</p>
<p>简单联结两个表只需要这两个表中的常用字段。存在外键时，DBMS 使用它们实施引用完整性。<mark>使用外键确保引用完整性的一个好处是，DBMS 通常可以防止删除某个关系需要用到的行。</mark>例如，要从<code>Products</code> 表中删除一个产品，而这个产品用在 <code>OrderItems</code> 的已有订单中，那么 <code>DELETE</code> 语句将抛出错误并中止。这是总要定义外键的另一个理由。</p>
<p> 在某些 SQL实现中，跟在 <code>DELETE</code> 后的关键字 <code>FROM</code> 是可选的。但是即使不需要，也最好提供这个关键字。这样做将保证SQL代码在DBMS之间可移植。</p>
<p> 删除表的内容而不是表，<code>DELETE</code> 语句从表中删除行，甚至是删除表中所有行。但是， <code>DELETE</code>不删除表本身。<br>如果想从表中删除所有行，不要使用 <code>DELETE</code> 。可使用 <code>TRUNCATE TABLE</code>语句，它完成相同的工作，而速度更快（因为不记录数据的变动）。</p>
<h3 id="更新和删除的指导原则"><a href="#更新和删除的指导原则" class="headerlink" title="更新和删除的指导原则"></a>更新和删除的指导原则</h3><p>使用的 <code>UPDATE</code> 和 <code>DELETE</code> 语句都有 <code>WHERE</code> 子句，这样做的理由很充分。如果省略了 <code>WHERE</code> 子句，则 <code>UPDATE</code> 或 <code>DELETE</code> 将被应用到表中所有的行。</p>
<p>下面是许多 SQL程序员使用 <code>UPDATE</code> 或 <code>DELETE</code> 时所遵循的重要原则。</p>
<ol>
<li>除非确实打算更新和删除每一行，否则绝对不要使用不带 <code>WHERE</code> 子句<br>的 <code>UPDATE</code> 或 <code>DELETE</code> 语句。</li>
<li>保证每个表都有主键，尽可能像 <code>WHERE</code> 子句那样使用它（可以指定各主键、多个值或值的范围）。</li>
<li>在 <code>UPDATE</code> 或 <code>DELETE</code> 语句使用 <code>WHERE</code> 子句前，应该先用 <code>SELECT</code> 进行测试，保证它过滤的是正确的记录，以防编写的 <code>WHERE</code> 子句不正确。</li>
<li>使用强制实施引用完整性的数据库（外键联结），这样 DBMS将不允许删除其数据与其他表相关联的行。</li>
<li>有的 DBMS 允许数据库管理员施加约束，防止执行不带 <code>WHERE</code> 子句的 <code>UPDATE</code> 或 <code>DELETE</code> 语句。如果所采用的 DBMS支持这个特性，应该使用它。</li>
</ol>
<h2 id="创建和操纵表"><a href="#创建和操纵表" class="headerlink" title="创建和操纵表"></a>创建和操纵表</h2><p>###创建表<br>一般有两种创建表的方法：</p>
<ol>
<li>多数 DBMS都具有交互式创建和管理数据库表的工具；</li>
<li>表也可以直接用 SQL语句操纵。</li>
</ol>
<p>用程序创建表，可以使用 SQL的 <code>CREATE TABLE</code> 语句。需要注意的是，使用交互式工具时实际上就是使用 SQL语句。这些语句不是用户编写的，界面工具会自动生成并执行相应的 SQL语句。</p>
<h4 id="表创建基础"><a href="#表创建基础" class="headerlink" title="表创建基础"></a>表创建基础</h4><p>利用 <code>CREATE TABLE</code> 创建表，必须给出下列信息：</p>
<ol>
<li>新表的名字，在关键字 <code>CREATE TABLE</code> 之后给出；</li>
<li>表列的名字和定义，用逗号分隔；</li>
<li>有的 DBMS还要求指定表的位置</li>
</ol>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE Products</div><div class="line">(</div><div class="line">prod_id CHAR(10) NOT NULL,</div><div class="line">vend_id CHAR(10) NOT NULL,</div><div class="line">prod_name CHAR(254) NOT NULL,</div><div class="line">prod_price DECIMAL(8,2) NOT NULL,</div><div class="line">prod_desc VARCHAR(1000) NULL</div><div class="line">);</div></pre></td></tr></table></figure>
<p>表名紧跟 <code>CREATE TABLE</code> 关键字。实际的表定义（所有列）括在圆括号之中，各列之间用<strong>逗号</strong>分隔。这个表由 5 列组成，每列的定义以列名（它在表中必须是唯一的）开始，后跟列的数据类型。</p>
<p> 在创建新的表时，指定的表名必须不存在，否则会出错。防止意外覆盖已有的表，SQL 要求首先手工删除该表（请参阅后面的内容），然后再重建它，而不是简单地用创建表语句覆盖它。</p>
<h4 id="使用-NULL-值"><a href="#使用-NULL-值" class="headerlink" title="使用 NULL 值"></a>使用 NULL 值</h4><blockquote>
<p><code>NULL</code> 值就是没有值或缺值。允许 <code>NULL</code> 值的列也允许在插入行时不给出该列的值。不允许 <code>NULL</code> 值的列不接受没有列值的行，换句话说，在插入或更新行时，该列必须有值。</p>
</blockquote>
<p>每个表列要么是 <code>NULL</code> 列，要么是 <code>NOT NULL</code> 列<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"> CREATE TABLE Orders</div><div class="line">(</div><div class="line">order_num INTEGER NOT NULL,</div><div class="line">order_date DATETIME NOT NULL,</div><div class="line">cust_id CHAR(10) NOT NULL</div><div class="line">);</div></pre></td></tr></table></figure></p>
<p>每一列的定义都含有关键字 <code>NOT NULL</code> 。这就会阻止插入没有值的列。如果插入没有值的列，将返回错误，且插入失败。</p>
<p>创建混合了 <code>NULL</code> 和 <code>NOT NULL</code> 列的表：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE Vendors</div><div class="line">(</div><div class="line">vend_id CHAR(10) NOT NULL,</div><div class="line">vend_name CHAR(50) NOT NULL,</div><div class="line">vend_address CHAR(50) ,</div><div class="line">vend_city CHAR(50) ,</div><div class="line">vend_state CHAR(5) ,</div><div class="line">vend_zip CHAR(10) ,</div><div class="line">vend_country CHAR(50)</div><div class="line">);</div></pre></td></tr></table></figure></p>
<p>供应商 ID 和供应商名字列是必需的，因此指定为 <code>NOT NULL</code> 。其余五列全都允许 <code>NULL</code> 值，所以不指定 <code>NOT NULL</code> 。 <code>NULL</code> 为默认设置，如果不指定 <code>NOT NULL</code> ，就认为指定的是 <code>NULL</code> 。</p>
<h5 id="注意事项-8"><a href="#注意事项-8" class="headerlink" title="注意事项"></a>注意事项</h5><p> 主键和 <code>NULL</code> 值，主键是其值唯一标识表中每一行的列。只有不允许 <code>NULL</code>值的列可作为主键，允许 <code>NULL</code> 值的列不能作为唯一标识。<br>在不指定 <code>NOT NULL</code> 时，多数 DBMS认为指定的是 <code>NULL</code> ，但不是所有的 DBMS都这样。某些 DBMS要求指定关键字 <code>NULL</code> ，如果不指定将出错。</p>
<h4 id="指定默认值"><a href="#指定默认值" class="headerlink" title="指定默认值"></a>指定默认值</h4><blockquote>
<p>SQL 允许指定默认值，在插入行时如果不给出值，DBMS 将自动采用默认值。默认值在 <code>CREATE TABLE</code> 语句的列定义中用关键字 <code>DEFAULT</code> 指定。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE OrderItems</div><div class="line">(</div><div class="line">order_num INTEGER NOT NULL,</div><div class="line">order_item INTEGER NOT NULL,</div><div class="line">prod_id CHAR(10) NOT NULL,</div><div class="line">quantity INTEGER NOT NULL DEFAULT 1,</div><div class="line">item_price DECIMAL(8,2) NOT NULL</div><div class="line">);</div></pre></td></tr></table></figure></p>
</blockquote>
<p><code>quantity</code> 列为订单中每个物品的数量，这一列的描述增加了 <code>DEFAULT 1</code> ，指示 DBMS，如果不给出数量则使用数量 1 。</p>
<p> 默认值经常用于日期或时间戳列。MySQL 用户指定 <code>DEFAULT CURRENT_DATE()</code> ，Oracle 用户指定 <code>DEFAULT SYSDATE</code> ，而 SQL Server用户指定 <code>DEFAULT GETDATE()</code> 。</p>
<p>获得系统日期<br>|DBMS| 函数/变量|<br>|:—:|:—:|<br>|Access |<code>NOW()</code><br>|DB2 |<code>CURRENT_DATE</code><br>|MySQL| <code>CURRENT_DATE()</code><br>|Oracle| <code>SYSDATE</code><br>|PostgreSQL| <code>CURRENT_DATE</code><br>|SQL Server |<code>GETDATE()</code><br>|SQLite |<code>date(&#39;now&#39;)</code></p>
<h3 id="更新表"><a href="#更新表" class="headerlink" title="更新表"></a>更新表</h3><blockquote>
<p>更新表定义，可以使用 <code>ALTER TABLE</code> 语句。</p>
</blockquote>
<p>使用 <code>ALTER TABLE</code> 更改表结构，必须给出下面的信息：</p>
<ol>
<li>在 <code>ALTER TABLE</code> 之后给出要更改的表名（该表必须存在，否则将出错）；</li>
<li>列出要做哪些更改。<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ALTER TABLE Vendors</div><div class="line">ADD vend_phone CHAR(20);</div></pre></td></tr></table></figure>
</li>
</ol>
<p>这条语句给 <code>Vendors</code> 表增加一个名为 <code>vend_phone</code> 的列，其数据类型为 <code>CHAR</code> 。</p>
<p>删除列：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ALTER TABLE Vendors</div><div class="line">DROP COLUMN vend_phone;</div></pre></td></tr></table></figure></p>
<p> 使用 <code>ALTER TABLE</code> 要极为小心，应该在进行改动前做完整的备份（表结构和数据的备份）。数据库表的更改不能撤销，如果增加了不需要的列，也许无法删除它们。类似地，如果删除了不应该删除的列，可能会丢失该列中的所有数据。</p>
<h3 id="删除表"><a href="#删除表" class="headerlink" title="删除表"></a>删除表</h3><p>删除表（删除整个表而不是其内容）非常简单，使用 <code>DROP TABLE</code> 语句即可：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">DROP TABLE CustCopy;</div></pre></td></tr></table></figure></p>
<p>这条语句删除 <code>CustCopy</code> 表。删除表没有确认，也不能撤销，执行这条语句将永久删除该表。<br> 使用关系规则防止意外删除<br>许多 DBMS允许强制实施有关规则，防止删除与其他表相关联的表。在实施这些规则时，如果对某个表发布一条 <code>DROP TABLE</code> 语句，且该表是某个关系的组成部分，则 DBMS将阻止这条语句执行，直到该关系被删除为止。如果允许，应该启用这些选项，它能防止意外删除有用的表。</p>
<h3 id="重命名表"><a href="#重命名表" class="headerlink" title="重命名表"></a>重命名表</h3><p>SQL Server用户使用 <code>sp_rename</code> 语句重命名，要求指定旧表名和新表名。</p>
<h2 id="使用视图"><a href="#使用视图" class="headerlink" title="使用视图"></a>使用视图</h2><blockquote>
<p>视图是虚拟的表 ，包含的不是数据而是根据需要检索数据的查询。视图提供了一种封装 <code>SELECT</code> 语句的层次，可用来简化数据处理，重新格式化或保护基础数据。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM Customers, Orders, OrderItems</div><div class="line">WHERE Customers.cust_id = Orders.cust_id</div><div class="line">AND OrderItems.order_num = Orders.order_num</div><div class="line">AND prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure>
<p>假如可以把整个查询包装成一个名为 <code>ProductCustomers</code> 的虚拟表，则可以如下轻松地检索出相同的数据：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM ProductCustomers</div><div class="line">WHERE prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure>
<p>这就是视图的作用。 <code>ProductCustomers</code> 是一个视图，作为视图，它不包<br>含任何列或数据，包含的是一个查询。</p>
<h4 id="为什么使用视图"><a href="#为什么使用视图" class="headerlink" title="为什么使用视图"></a>为什么使用视图</h4><ol>
<li>重用 SQL语句。</li>
<li>简化复杂的 SQL操作。在编写查询后，可以方便地重用它而不必知道其基本查询细节。</li>
<li>使用表的一部分而不是整个表。</li>
<li>保护数据。可以授予用户访问表的特定部分的权限，而不是整个表的访问权限。</li>
<li>更改数据格式和表示。视图可返回与底层表的表示和格式不同的数据。</li>
</ol>
<p>创建视图之后，可以用与表基本相同的方式使用它们。可以对视图执行<code>SELECT</code> 操作，过滤和排序数据，将视图联结到其他视图或表，甚至添加和更新数据。</p>
<p> 视图仅仅是用来查看存储在别处数据，视图本身不包含数据，每次使用视图时，都必须处理查询执行时需要的所有检索，因此返回的数据是从其他表中检索出来的。在添加或更改这些表中的数据时，视图将返回改变过的数据。</p>
<h4 id="视图的规则和限制"><a href="#视图的规则和限制" class="headerlink" title="视图的规则和限制"></a>视图的规则和限制</h4><ol>
<li>与表一样，视图必须唯一命名。</li>
<li>对于可以创建的视图数目没有限制。</li>
<li>创建视图，必须具有足够的访问权限。这些权限通常由数据库管理人员授予。</li>
<li>视图可以嵌套，即可以利用从其他视图中检索数据的查询来构造视图。</li>
<li>许多 DBMS禁止在视图查询中使用 <code>ORDER BY</code> 子句。</li>
<li>有些 DBMS要求对返回的所有列进行命名，如果列是计算字段，则需要使用别名。</li>
<li>视图不能索引，也不能有关联的触发器或默认值。</li>
<li>有些 DBMS把视图作为只读的查询，这表示可以从视图检索数据，但不能将数据写回底层表。</li>
<li>有些 DBMS 允许创建这样的视图，它不能进行导致行不再属于视图的插入或更新。例如有一个视图，只检索带有电子邮件地址的顾客。如果更新某个顾客，删除他的电子邮件地址，将使该顾客不再属于视图。<h3 id="创建视图"><a href="#创建视图" class="headerlink" title="创建视图"></a>创建视图</h3><blockquote>
<p>各种 DBMS中用来创建视图的语法相当一致。</p>
</blockquote>
</li>
</ol>
<p>视图用 <code>CREATE VIEW</code> 语句来创建。与 <code>CREATE TABLE</code> 一样， <code>CREATE VIEW</code>只能用于创建不存在的视图。<br>删除视图，可以使用 <code>DROP</code> 语句，其语法为 <code>DROP VIEW viewname;</code> 。<br>覆盖（或更新）视图，必须先删除它，然后再重新创建。</p>
<h3 id="利用视图简化复杂的联结"><a href="#利用视图简化复杂的联结" class="headerlink" title="利用视图简化复杂的联结"></a>利用视图简化复杂的联结</h3><p>一个最常见的视图应用是隐藏复杂的 SQL，这通常涉及联结。请看下面的例子：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">CREATE VIEW ProductCustomers AS</div><div class="line">SELECT cust_name, cust_contact, prod_id</div><div class="line">FROM Customers, Orders, OrderItems</div><div class="line">WHERE Customers.cust_id = Orders.cust_id</div><div class="line">AND OrderItems.order_num = Orders.order_num;</div></pre></td></tr></table></figure>
<p>这条语句创建一个名为 <code>ProductCustomers</code> 的视图，它联结三个表，返<br>回已订购了任意产品的所有顾客的列表。如果执行</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"> SELECT * FROM</div><div class="line">ProductCustomers ;</div></pre></td></tr></table></figure>
<p>将列出订购了任意产品的顾客。</p>
<p>检索订购了产品 <code>RGAN01</code> 的顾客，可如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM ProductCustomers</div><div class="line">WHERE prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure></p>
<p> 视图极大地简化了复杂 SQL语句的使用。利用视图，可一次性编写基础的 SQL，然后根据需要多次使用。</p>
<h4 id="用视图重新格式化检索出的数据"><a href="#用视图重新格式化检索出的数据" class="headerlink" title="用视图重新格式化检索出的数据"></a>用视图重新格式化检索出的数据</h4><p>视图的另一常见用途是重新格式化检索出的数据。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure></p>
<p>或</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) || &apos; (&apos; || RTRIM(vend_country) || &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>假设经常需要这个格式的结果。我们不必在每次需要时执行这种拼接，而是创建一个视图，使用它即可。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">CREATE VIEW VendorLocations AS</div><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors;</div></pre></td></tr></table></figure></p>
<p>要检索数据，创建所有的邮件标签，可如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">FROM VendorLocations;</div></pre></td></tr></table></figure></p>
<h4 id="用视图过滤不想要的数据"><a href="#用视图过滤不想要的数据" class="headerlink" title="用视图过滤不想要的数据"></a>用视图过滤不想要的数据</h4><p>视图对于应用普通的 <code>WHERE</code> 子句也很有用。例如，可以义<code>CustomerEMailList</code> 视图，过滤没有电子邮件地址的顾客。为此，可使用下面的语句：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">CREATE VIEW CustomerEMailList AS</div><div class="line">SELECT cust_id, cust_name, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_email IS NOT NULL;</div></pre></td></tr></table></figure></p>
<p>这里的 <code>WHERE</code> 子句过滤了 <code>cust_email</code> 列中具有 <code>NULL</code> 值的那些行，使它们不被检索出来。<br>现在，可以像使用其他表一样使用视图 <code>CustomerEMailList</code> 。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">FROM CustomerEMailList;</div></pre></td></tr></table></figure></p>
<h4 id="使用视图与计算字段"><a href="#使用视图与计算字段" class="headerlink" title="使用视图与计算字段"></a>使用视图与计算字段</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">CREATE VIEW OrderItemsExpanded AS</div><div class="line">SELECT order_num,</div><div class="line">prod_id,</div><div class="line">quantity,</div><div class="line">item_price,</div><div class="line">quantity*item_price AS expanded_price</div><div class="line">FROM OrderItems;</div></pre></td></tr></table></figure>
<p>检索订单 20008 的详细内容（上面的输出），如下进行：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">FROM OrderItemsExpanded</div><div class="line">WHERE order_num = 20008;</div></pre></td></tr></table></figure>
<p>正确使用视图，可极大地简化复杂数据的处理。</p>
<p>##使用存储过程</p>
<h3 id="存储过程"><a href="#存储过程" class="headerlink" title="存储过程"></a>存储过程</h3><blockquote>
<p>存储过程就是为以后使用而保存的一条或多条 SQL语句。可将其视为批文件，虽然它们的作用不仅限于批处理。 </p>
<h3 id="为什么要使用存储过程"><a href="#为什么要使用存储过程" class="headerlink" title="为什么要使用存储过程"></a>为什么要使用存储过程</h3><p>存储过程有三个主要的好处，即简单、安全、高性能。</p>
<ol>
<li>通过把处理封装在一个易用的单元中，可以简化复杂的操作。</li>
<li>由于不要求反复建立一系列处理步骤，因而保证了数据的一致性。如果所有开发人员和应用程序都使用同一存储过程，则所使用的代码都是相同的。</li>
<li>简化对变动的管理。如果表名、列名或业务逻辑（或别的内容）有变化，那么只需要更改存储过程的代码。这一点的延伸就是安全性。通过存储过程限制对基础数据的访问，减少了数据讹误的机会。</li>
<li>因为存储过程通常以编译过的形式存储，所以 DBMS处理命令所需的工作量少，提高了性能。</li>
</ol>
</blockquote>
<p>缺陷：</p>
<ol>
<li>不同 DBMS中的存储过程语法有所不同。事实上，编写真正的可移植存储过程几乎是不可能的。</li>
<li>一般来说，编写存储过程比编写基本 SQL语句复杂，需要更高的技能，更丰富的经验。<h3 id="执行存储过程"><a href="#执行存储过程" class="headerlink" title="执行存储过程"></a>执行存储过程</h3>存储过程的执行远比编写要使用得更多，执行存储过程的 SQL语句很简单，即 <code>EXECUTE</code> 。 <code>EXECUTE</code> 接受存储过程名和需要传递给它的任何参数。</li>
</ol>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">EXECUTE</span> AddNewProduct( <span class="string">'JTS01'</span>,</div><div class="line"><span class="string">'Stuffed Eiffel Tower'</span>,</div><div class="line"><span class="number">6.49</span>,</div><div class="line"><span class="string">'Plush stuffed toy with the text La</span></div><div class="line">Tour Eiffel in red white and blue' );</div></pre></td></tr></table></figure>
<p>这里执行一个名为 <code>AddNewProduct</code> 的存储过程，将一个新产品添加到<code>Products</code> 表中。 <code>AddNewProduct</code> 有四个参数，分别是：供应商 <code>ID</code>（ <code>Vendors</code> 表的主键）、产品名、价格和描述。这 4 个参数匹配存储过程中 4个预期变量（定义为存储过程自身的组成部分）。此存储过程将新行添加到 <code>Products</code> 表，并将传入的属性赋给相应的列。</p>
<h3 id="创建存储过程"><a href="#创建存储过程" class="headerlink" title="创建存储过程"></a>创建存储过程</h3><p>下面是该过程的 Oracle版本：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">CREATE PROCEDURE MailingListCount (</div><div class="line">ListCount OUT INTEGER</div><div class="line">)</div><div class="line">IS</div><div class="line">v_rows INTEGER;</div><div class="line">BEGIN</div><div class="line">SELECT COUNT(*) INTO v_rows</div><div class="line">FROM Customers</div><div class="line">WHERE NOT cust_email IS NULL;</div><div class="line">ListCount := v_rows;</div><div class="line">END;</div></pre></td></tr></table></figure></p>
<p>这个存储过程有一个名为 <code>ListCount</code> 的参数。此参数从存储过程返回一个值，关键字 <code>OUT</code> 用来指示这种行为。<code>Oracle</code>支持 <code>IN</code> （传递值给存储过程）、 <code>OUT</code> （从存储过程返回值，如这里）、<code>INOUT</code> （既传递值给存储过程也从存储过程传回值）类型的参数。存储过程的代码括在 <code>BEGIN</code> 和 <code>END</code> 语句中，这里执行一条简单的 <code>SELECT</code> 语句，它检索具有邮件地址的顾客。然后检索出行数<code>ListCount</code>（要传递的输出参数）。<br>调用 Oracle例子可以像下面这样：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">var ReturnValue NUMBER</div><div class="line">EXEC MailingListCount(:ReturnValue);</div><div class="line">SELECT ReturnValue;</div></pre></td></tr></table></figure>
<p>这段代码声明了一个变量来保存存储过程返回的任何值，然后执行存储过程，再使用 <code>SELECT</code> 语句显示返回的值。<br>下面是该过程的 SQL Server 版本<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> MailingListCount</div><div class="line"><span class="keyword">AS</span></div><div class="line"><span class="keyword">DECLARE</span> @cnt <span class="built_in">INTEGER</span></div><div class="line"><span class="keyword">SELECT</span> @cnt = <span class="keyword">COUNT</span>(*)</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">WHERE</span> <span class="keyword">NOT</span> cust_email <span class="keyword">IS</span> <span class="literal">NULL</span>;</div><div class="line">RETURN @cnt;</div></pre></td></tr></table></figure></p>
<p>此存储过程没有参数。调用程序检索 <code>SQL Server</code>的返回代码提供的值。其中用 <code>DECLARE</code> 语句声明了一个名为 <code>@cnt</code> 的局部变量（<code>SQL Server</code>中所有局部变量名都以 <code>@</code> 起头）；然后在 <code>SELECT</code> 语句中使用这个变量，让它包含 <code>COUNT()</code> 函数返回的值；最后，用 <code>RETURN @cnt</code> 语句将计数返回给调用程序。<br>调用 SQL Server例子可以像下面这样：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">DECLARE</span> @ReturnValue <span class="built_in">INT</span></div><div class="line"><span class="keyword">EXECUTE</span> @ReturnValue=MailingListCount;</div><div class="line"><span class="keyword">SELECT</span>  @ReturnValue;</div></pre></td></tr></table></figure>
<p>这段代码声明了一个变量来保存存储过程返回的任何值，然后执行存储过程，再使用 <code>SELECT</code> 语句显示返回的值。</p>
<p>下面是SQL存储过程的另一个例子，这次在 Orders 表中插入一个新订单:</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">CREATE PROCEDURE NewOrder @cust_id CHAR(10)</div><div class="line">AS</div><div class="line">-- Declare variable for order number</div><div class="line">DECLARE @order_num INTEGER</div><div class="line">-- Get current highest order number</div><div class="line">SELECT @order_num=MAX(order_num)</div><div class="line">FROM Orders</div><div class="line">-- Determine next order number</div><div class="line">SELECT @order_num=@order_num+1</div><div class="line">-- Insert new order</div><div class="line">INSERT INTO Orders(order_num, order_date, cust_id)</div><div class="line">VALUES(@order_num, GETDATE(), @cust_id)</div><div class="line">-- Return order number</div><div class="line">RETURN @order_num;</div></pre></td></tr></table></figure>
<p>此存储过程在 <code>Orders</code> 表中创建一个新订单。它只有一个参数，即下订单顾客的 <code>ID</code>。订单号和订单日期这两列在存储过程中自动生成。代码首先<strong>用<code>@</code>声明一个局部变量</strong>来存储订单号。接着，检索当前最大订单号（使用<code>MAX()</code> 函数）并增加 1（使用 <code>SELECT</code> 语句）。然后用 <code>INSERT</code> 语句插入由新生成的订单号、当前系统日期（用 <code>GETDATE()</code> 函数检索）和传递的顾客 <code>ID</code>组成的订单。最后，<code>RETURN @order_num</code> 返回订单号（处理订单物品需要它）。<br> 应该注释所有代码，存储过程也不例外。增加注释不影响性能，因此不存在缺陷（除了增加编写时间外）。<br>注释代码的好处很多，包括使别人（以及你自己）更容易地理解和更安全地修改代码。<br>对代码进行注释的标准方式是在之前放置 <code>--</code> （两个连字符）。<br>有的DBMS还支持其他的注释语法，不过所有 DBMS都支持 <code>--</code> ，因此在注释代码时最好都使用这种语法。</p>
<p>下面是相同 SQL Server代码的一个很不同的版本：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> NewOrder @cust_id <span class="built_in">CHAR</span>(<span class="number">10</span>)</div><div class="line"><span class="keyword">AS</span></div><div class="line"><span class="comment">-- Insert new order</span></div><div class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> Orders(cust_id)</div><div class="line"><span class="keyword">VALUES</span>(@cust_id)</div><div class="line"><span class="comment">-- Return order number</span></div><div class="line"><span class="keyword">SELECT</span> order_num = @@<span class="keyword">IDENTITY</span>;</div></pre></td></tr></table></figure>
<p>此存储过程也在 <code>Orders</code> 表中创建一个新订单。这次由 DBMS生成订单号，大多数 DBMS都支持这种功能；SQL Server中称这些自动增量的列为标识字段（<code>identity field</code>），而其他 DBMS称之为自动编号（<code>auto number</code>）或序列（<code>sequence</code>）。传递给此过程的参数也是一个，即下订单的顾客 <code>ID</code>。订单号和订单日期没有给出，DBMS对日期使用默认值（ <code>GETDATE()</code> 函数），订单号自动生成。怎样才能得到这个自动生成的 <code>ID</code>？在 SQL Server上可在全局变量 <code>@@IDENTITY</code> 中得到，它返回到调用程序（这里使用 <code>SELECT</code> 语句）。</p>
<h2 id="管理事务处理"><a href="#管理事务处理" class="headerlink" title="管理事务处理"></a>管理事务处理</h2><blockquote>
<p>事务是必须<strong>完整执行</strong>的 SQL语句块，利用事务处理，可以保证一组操作不会中途停止，它们要么完全执行，要么完全不执行（除非明确指示）。如果没有错误发生，整组语句提交给（写到）数据库表；如果发生错误，则进行回退（撤销），将数据库恢复到某个已知且安全的状态。</p>
<h3 id="事务处理"><a href="#事务处理" class="headerlink" title="事务处理"></a>事务处理</h3><p>使用事务处理（``），通过确保成批的 SQL 操作要么完全执行，要么完全不执行，来维护数据库的完整性。</p>
</blockquote>
<p>在某种程度上说，设计良好的数据库模式都是关联的。订单存储在 <code>Orders</code> 和<code>OrderItems</code> 两个表中： <code>Orders</code> 存储实际的订单， <code>OrderItems</code> 存储订购的各项物品。这两个表使用称为主键的唯一 <code>ID</code>互相关联，又与包含客户和产品信息的其他表相关联。</p>
<p>给系统添加订单的过程如下：<br>(1) 检查数据库中是否存在相应的顾客，如果不存在，添加他；<br>(2) 提交顾客信息；<br>(3) 检索顾客的 <code>ID</code>；<br>(4) 在 <code>Orders</code> 表添加一行，与顾客 <code>ID</code>相关联；<br>(5) 如果向 <code>Orders</code> 表添加行时出现故障，回退；<br>(6) 检索 <code>Orders</code> 表中赋予的新订单 <code>ID</code>；<br>(7) 对于订购的每项物品，添加新行到 <code>OrderItems</code> 表；通过检索出来的 <code>ID</code>把它与 <code>Orders</code> 表关联（并且通过产品 <code>ID</code>与 <code>Products</code> 表关联）。<br>(8) 如果向 <code>OrderItems</code> 添加行时出现故障，回退所有添加的 <code>OrderItems</code><br>行和 <code>Orders</code> 行。</p>
<p>在使用事务处理时，有几个反复出现的关键词。下面是关于事务处理需要知道的几个术语：</p>
<ol>
<li>事务（<code>transaction</code>）指一组 SQL语句；</li>
<li>回退（<code>rollback</code>）指撤销指定 SQL语句的过程；</li>
<li>提交（<code>commit</code>）指将未存储的 SQL语句结果写入数据库表；</li>
<li><p>保留点（<code>savepoint</code>）指事务处理中设置的临时占位符（<code>placeholder</code>），可以对它发布回退（与回退整个事务处理不同）。</p>
<p>可以回退哪些语句？<br>事务处理用来管理 <code>INSERT</code> 、 <code>UPDATE</code> 和 <code>DELETE</code> 语句。不能回退 <code>SELECT</code>语句（回退 <code>SELECT</code> 语句也没有必要），也<strong>不能回退 <code>CREATE</code> 或 <code>DROP</code> 操作</strong>。事务处理中可以使用这些语句块，但进行的回退操作不能撤销。</p>
<h3 id="控制事务处理"><a href="#控制事务处理" class="headerlink" title="控制事务处理"></a>控制事务处理</h3><blockquote>
<p>管理事务的关键在于将 SQL语句组分解为逻辑块，并明确规定数据何时应该回退，何时不应该回退。</p>
</blockquote>
</li>
</ol>
<p>有的DBMS要求明确标识事务处理块的开始和结束。如在SQL Server中，标识如下：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">BEGIN</span> <span class="keyword">TRANSACTION</span></div><div class="line">...</div><div class="line"><span class="keyword">COMMIT</span> <span class="keyword">TRANSACTION</span></div></pre></td></tr></table></figure>
<p>MariaDB和 MySQL中等同的代码为：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">START TRANSACTION</div></pre></td></tr></table></figure>
<p>Oracle使用的语法：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SET TRANSACTION</div></pre></td></tr></table></figure>
<p>多数实现没有明确标识事务处理在何处结束。事务一直存在，直到被中断。通常， <code>COMMITT</code> 用于保存更改， <code>ROLLBACK</code> 用于撤销 。</p>
<h4 id="使用-ROLLBACK"><a href="#使用-ROLLBACK" class="headerlink" title="使用 ROLLBACK"></a>使用 ROLLBACK</h4><p>SQL的 <code>ROLLBACK</code> 命令用来回退（撤销）SQL语句，请看下面的语句：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">DELETE FROM Orders;</div><div class="line">ROLLBACK;</div></pre></td></tr></table></figure></p>
<p>在此例子中，执行 <code>DELETE</code> 操作，然后用 <code>ROLLBACK</code> 语句撤销。</p>
<h4 id="使用-COMMIT"><a href="#使用-COMMIT" class="headerlink" title="使用 COMMIT"></a>使用 COMMIT</h4><p>一般的 SQL语句都是针对数据库表直接执行和编写的。这就是所谓的隐式提交（<code>implicit commit</code>），即提交（写或保存）操作是自动进行的。<br><strong>在事务处理块中，提交不会隐式进行。</strong>进行明确的提交，使用 <code>COMMIT</code> 语句。下面是一个 SQL Server的例子：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">BEGIN TRANSACTION</div><div class="line">DELETE OrderItems WHERE order_num = 12345</div><div class="line">DELETE Orders WHERE order_num = 12345</div><div class="line">COMMIT TRANSACTION</div></pre></td></tr></table></figure></p>
<p>在这个 SQL Server例子中，从系统中完全删除订单 12345 。因为涉及更新两个数据库表 <code>Orders</code> 和 <code>OrderItems</code> ，所以使用事务处理块来保证订单不被部分删除。最后的 <code>COMMIT</code> 语句仅在不出错时写出更改。如果第一条 <code>DELETE</code> 起作用，但第二条失败，则 <code>DELETE</code> 不会提交。</p>
<p>Oracle中完成相同的工作，可如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SET TRANSACTION</div><div class="line">DELETE OrderItems WHERE order_num = 12345;</div><div class="line">DELETE Orders WHERE order_num = 12345;</div><div class="line">COMMIT;</div></pre></td></tr></table></figure></p>
<h4 id="使用保留点"><a href="#使用保留点" class="headerlink" title="使用保留点"></a>使用保留点</h4><p>使用简单的 <code>ROLLBACK</code> 和 <code>COMMIT</code> 语句，就可以写入或撤销整个事务。但是，只对简单的事务才能这样做，复杂的事务可能需要部分提交或回退。要支持回退部分事务，必须在事务处理块中的合适位置放置占位符。这样，如果需要回退，可以回退到某个占位符。在 SQL 中，这些占位符称为保留点。<br>在 SQL Server 中，如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SAVE TRANSACTION delete1;</div></pre></td></tr></table></figure></p>
<p>每个保留点都要取能够标识它的唯一名字，以便在回退时，DBMS 知道回退到何处。<br>要回退到本例给出的保留点，在 SQL Server 中可如下进行。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ROLLBACK TRANSACTION delete1;</div></pre></td></tr></table></figure></p>
<p>在 MariaDB、MySQL 和 Oracle 中创建占位符，可使用 <code>SAVEPOINT</code> 语句。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SAVEPOINT delete1;</div></pre></td></tr></table></figure></p>
<p>回退到保留点<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ROLLBACK TO delete1;</div></pre></td></tr></table></figure></p>
<p>下面是一个完整的 SQL Server例子：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line">BEGIN TRANSACTION</div><div class="line">INSERT INTO Customers(cust_id, cust_name)</div><div class="line">VALUES('1000000010', 'Toys Emporium');</div><div class="line">SAVE TRANSACTION StartOrder;</div><div class="line">INSERT INTO Orders(order_num, order_date, cust_id)</div><div class="line">VALUES(20100,'2001/12/1','1000000010');</div><div class="line">IF @@ERROR &lt;&gt; 0 </div><div class="line">ROLLBACK TRANSACTION StartOrder;</div><div class="line">INSERT INTO OrderItems(order_num, order_item, prod_id, quantity,item_price)</div><div class="line">VALUES(20100, 1, 'BR01', 100, 5.49);</div><div class="line">IF @@ERROR &lt;&gt; 0 </div><div class="line">ROLLBACK TRANSACTION StartOrder;</div><div class="line">INSERT INTO OrderItems(order_num, order_item, prod_id, quantity,item_price)</div><div class="line">VALUES(20100, 2, 'BR03', 100, 10.99);</div><div class="line">IF @@ERROR &lt;&gt; 0 </div><div class="line">ROLLBACK TRANSACTION StartOrder;</div><div class="line">COMMIT TRANSACTION</div></pre></td></tr></table></figure></p>
<p>这里的事务处理块中包含了 4条 <code>INSERT</code> 语句。在第一条 <code>INSERT</code> 语句之后定义了一个保留点，因此，如果后面的任何一个 <code>INSERT</code> 操作失败，事务处理能够回退到这里。在 SQL Server 中，可检查一个名为 <code>@@ERROR</code>的变量，看操作是否成功。如果 <code>@@ERROR</code> 返回一个非 0 的值，表示有错误发生，事务处理回退到保留点。如果整个事务处理成功，发布 <code>COMMIT</code> 以保留数据。</p>
<p> 保留点越多越好<br>可以在 SQL代码中设置任意多的保留点，越多越好。因为保留点越多，越能灵活地进行回退。</p>
<h2 id="使用游标"><a href="#使用游标" class="headerlink" title="使用游标"></a>使用游标</h2><h3 id="游标"><a href="#游标" class="headerlink" title="游标"></a>游标</h3><p>SQL检索操作返回一组称为结果集的行，这组返回的行都是与 SQL语句相匹配的行（零行或多行）。简单地使用 <code>SELECT</code> 语句，没有办法得到第一行、下一行或前 10行。<br>有时，需要在检索出来的行中前进或后退一行或多行，这就是游标的用途所在。游标（<code>cursor</code>）是一个存储在 DBMS 服务器上的数据库查询，它不是一条 <code>SELECT</code> 语句，而<strong>是被该语句检索出来的结果集</strong>。<br> 结果集（result set）是SQL查询所检索出的结果。</p>
<p>不同的 DBMS 支持不同的游标选项和特性。常见的一些选项和特性如下。</p>
<ol>
<li>能够标记游标为只读，使数据能读取，但不能更新和删除。</li>
<li>能控制可以执行的定向操作（向前、向后、第一、最后、绝对位置、相对位置等）。</li>
<li>能标记某些列为可编辑的，某些列为不可编辑的。</li>
<li>规定范围，使游标对创建它的特定请求（如存储过程）或对所有请求可访问。</li>
<li>指示 DBMS对检索出的数据（而不是指出表中活动数据）进行复制，使数据在游标打开和访问期间不变化。</li>
</ol>
<p>游标主要用于交互式应用，其中用户需要滚动屏幕上的数据，并对数据进行浏览或做出更改。</p>
<p>###使用游标<br>使用游标涉及几个明确的步骤。</p>
<ol>
<li>在使用游标前，必须声明（定义）它。这个过程实际上没有检索数据，它只是定义要使用的 <code>SELECT</code> 语句和游标选项。</li>
<li>一旦声明，就必须打开游标以供使用。这个过程用前面定义的 <code>SELECT</code>语句把数据实际检索出来。</li>
<li>对于填有数据的游标，根据需要取出（检索）各行。</li>
<li>在结束游标使用时，必须关闭游标，可能的话，释放游标（有赖于具体的 DBMS）。<h4 id="创建游标"><a href="#创建游标" class="headerlink" title="创建游标"></a>创建游标</h4>使用 <code>DECLARE</code> 语句创建游标，<code>DECLARE</code> 命名游标，并定义相应的 <code>SELECT</code> 语句，根据需要带 <code>WHERE</code> 和其他子句。<br>创建此游标的 DB2、MariaDB、MySQL和 SQL Server版本:<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">DECLARE CustCursor CURSOR</div><div class="line">FOR</div><div class="line">SELECT * FROM Customers</div><div class="line">WHERE cust_email IS NULL</div></pre></td></tr></table></figure>
</li>
</ol>
<p>Oracle和 PostgreSQL版本：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">DECLARE CURSOR CustCursor</div><div class="line">IS</div><div class="line">SELECT * FROM Customers</div><div class="line">WHERE cust_email IS NULL</div></pre></td></tr></table></figure>
<p>在上面两个版本中， <code>DECLARE</code> 语句用来定义和命名游标，这里为<code>CustCursor</code> 。 <code>SELECT</code> 语句定义一个包含没有电子邮件地址（ <code>NULL</code> 值）的所有顾客的游标。定义游标之后，就可以打开它了。</p>
<h4 id="使用游标-1"><a href="#使用游标-1" class="headerlink" title="使用游标"></a>使用游标</h4><p>使用 <code>OPEN CURSOR</code> 语句打开游标，这条语句很简单，在大多数 DBMS<br>中的语法相同：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">OPEN CURSOR CustCursor</div></pre></td></tr></table></figure></p>
<p>在处理 <code>OPEN CURSOR</code> 语句时，执行查询，存储检索出的数据以供浏览和滚动。<br>现在可以用 <code>FETCH</code> 语句访问游标数据了。 <code>FETCH</code> 指出要检索哪些行，从何处检索它们以及将它们放于何处（如变量名）。</p>
<p>使用 Oracle语法：<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">DECLARE</span> <span class="keyword">TYPE</span> CustCursor <span class="keyword">IS</span> <span class="keyword">REF</span> <span class="keyword">CURSOR</span> <span class="comment">--声明变量</span></div><div class="line"><span class="keyword">RETURN</span> Customers%ROWTYPE;</div><div class="line"><span class="keyword">DECLARE</span> CustRecord Customers%ROWTYPE</div><div class="line"><span class="keyword">BEGIN</span></div><div class="line"><span class="keyword">OPEN</span> CustCursor;</div><div class="line">FETCH CustCursor INTO CustRecord;</div><div class="line">CLOSE CustCursor;</div><div class="line"><span class="keyword">END</span>;</div></pre></td></tr></table></figure></p>
<p> <code>FETCH</code> 用来检索当前行（自动从第一行开始），放到声明的变量<code>CustRecord</code> 中。对于检索出来的数据不做任何处理。</p>
<p>从第一行到最后一行，对检索出来的数据进行循环：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">DECLARE</span> <span class="keyword">TYPE</span> CustCursor <span class="keyword">IS</span> <span class="keyword">REF</span> <span class="keyword">CURSOR</span></div><div class="line"><span class="keyword">RETURN</span> Customers%ROWTYPE;</div><div class="line"><span class="keyword">DECLARE</span> CustRecord Customers%ROWTYPE</div><div class="line"><span class="keyword">BEGIN</span></div><div class="line"><span class="keyword">OPEN</span> CustCursor;</div><div class="line">LOOP</div><div class="line">FETCH CustCursor INTO CustRecord;</div><div class="line">EXIT WHEN CustCursor%NOTFOUND;</div><div class="line">...</div><div class="line"><span class="keyword">END</span> <span class="keyword">LOOP</span>;</div><div class="line">CLOSE CustCursor;</div><div class="line"><span class="keyword">END</span>;</div></pre></td></tr></table></figure>
<p>使用 <code>FETCH</code> 检索当前行，放到一个名为<code>CustRecord</code> 的变量中。但不一样的是，这里的 <code>FETCH</code> 位于 <code>LOOP</code> 内，因此它反复执行。代码 <code>EXIT WHEN CustCursor%NOTFOUND</code> 在取不出更多的行时终止处理（退出循环）。</p>
<p>使用 Microsoft SQL Server语法：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">DECLARE</span> @cust_id <span class="built_in">CHAR</span>(<span class="number">10</span>),</div><div class="line">@cust_name <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_address <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_city <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_state <span class="built_in">CHAR</span>(<span class="number">5</span>),</div><div class="line">@cust_zip <span class="built_in">CHAR</span>(<span class="number">10</span>),</div><div class="line">@cust_country <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_contact <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_email <span class="built_in">CHAR</span>(<span class="number">255</span>)</div><div class="line"><span class="keyword">OPEN</span> CustCursor</div><div class="line"><span class="keyword">FETCH</span> <span class="keyword">NEXT</span> <span class="keyword">FROM</span> CustCursor</div><div class="line"><span class="keyword">INTO</span> @cust_id, @cust_name, @cust_address,</div><div class="line">@cust_city, @cust_state, @cust_zip,</div><div class="line">@cust_country, @cust_contact, @cust_email</div><div class="line"><span class="keyword">WHILE</span> @@FETCH_STATUS = <span class="number">0</span></div><div class="line"><span class="keyword">BEGIN</span></div><div class="line"><span class="keyword">FETCH</span> <span class="keyword">NEXT</span> <span class="keyword">FROM</span> CustCursor</div><div class="line"><span class="keyword">INTO</span> @cust_id, @cust_name, @cust_address,</div><div class="line">@cust_city, @cust_state, @cust_zip,</div><div class="line">@cust_country, @cust_contact, @cust_email</div><div class="line"><span class="keyword">END</span></div><div class="line"><span class="keyword">CLOSE</span> CustCursor</div></pre></td></tr></table></figure>
<p>为每个检索出的列声明一个变量， <code>FETCH</code> 语句检索一行并保存值到这些变量中。使用 <code>WHILE</code> 循环处理每一行，条件 <code>WHILE @@FETCH_STATUS = 0</code> 在取不出更多的行时终止处理（退出循环）。</p>
<h4 id="关闭游标"><a href="#关闭游标" class="headerlink" title="关闭游标"></a>关闭游标</h4><p>游标在使用完毕时需要关闭，释放游标所占用的资源。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">CLOSE CustCursor <span class="comment">--游标名</span></div><div class="line"><span class="keyword">DEALLOCATE</span> <span class="keyword">CURSOR</span> CustCursor</div></pre></td></tr></table></figure></p>
<p>一旦游标关闭，如果不再次打开，将不能使用。第二次使用它时不需要再声明，只需用 <code>OPEN</code> 打开它即可。</p>
<h2 id="高级-SQL-特性"><a href="#高级-SQL-特性" class="headerlink" title="高级 SQL 特性"></a>高级 SQL 特性</h2><blockquote>
<p>约束是实施引用完整性的重要部分，索引可改善数据检索的性能，触发器可以用来执行运行前后的处理，安全选项可用来管理数据访问。</p>
<h3 id="约束"><a href="#约束" class="headerlink" title="约束"></a>约束</h3><p>关系数据库存储分解为多个表的数据，每个表存储相应的数据。利用键来建立从一个表到另一个表的引用（由此产生了术语引用完整性<code>referential integrity</code>）。<br>正确地进行关系数据库设计，需要一种方法保证只在表中插入合法数据。如果 <code>Orders</code> 表存储订单信息， <code>OrderItems</code> 表存储订单详细内容，应该保证 <code>OrderItems</code> 中引用的任何订单 <code>ID</code>都存在于 <code>Orders</code> 中。类似地，在 <code>Orders</code> 表中引用的任意顾客必须存在于 <code>Customers</code> 表中。</p>
</blockquote>
<p> 约束（<code>constraint</code>）<br>管理如何插入或处理数据库数据的规则。DBMS 通过在数据库表上施加约束来实施引用完整性。大多数约束是在表定义时定义的</p>
<p>####主键</p>
<blockquote>
<p>主键是一种特殊的约束，用来保证一列（或多列）中的值是唯一的，而且永不改动。换句话说，表中的一列（或多个列）的值唯一标识表中的每一行。这方便了直接或交互地处理表中的行。没有主键，要安全地 <code>UPDATE</code> 或 <code>DELETE</code> 特定行而不影响其他行会非常困难。</p>
</blockquote>
<p>表中任意列只要满足以下条件，都可以用于主键。</p>
<ol>
<li>任意两行的主键值都不相同。</li>
<li>每行都具有一个主键值（不允许 <code>NULL</code> 值）。</li>
<li>包含主键值的列从不修改或更新。</li>
<li>主键值不能重用。如果从表中删除某一行，其主键值不分配给新行。</li>
</ol>
<p>定义主键：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE Vendors</div><div class="line">(</div><div class="line">vend_id CHAR(10) NOT NULL PRIMARY KEY,</div><div class="line">vend_name CHAR(50) NOT NULL,</div><div class="line">vend_address CHAR(50) NULL,</div><div class="line">vend_city CHAR(50) NULL,</div><div class="line">vend_state CHAR(5) NULL,</div><div class="line">vend_zip CHAR(10) NULL,</div><div class="line">vend_country CHAR(50) NULL</div><div class="line">);</div></pre></td></tr></table></figure></p>
<p>给表的 <code>vend_id</code> 列定义添加关键字 <code>PRIMARY KEY</code> ，使其成为主键。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ALTER TABLE Vendors</div><div class="line">ADD CONSTRAINT PRIMARY KEY (vend_id);</div></pre></td></tr></table></figure>
<p>这里定义相同的列为主键，但使用的是 <code>CONSTRAINT</code> 语法。此语法也可以用于 <code>CREATE TABLE</code> 和 <code>ALTER TABLE</code> 语句。</p>
<h4 id="外键"><a href="#外键" class="headerlink" title="外键"></a>外键</h4><blockquote>
<p>外键是表中的一列，其值必须列在另一表的主键中。外键是保证引用完整性的极其重要部分。</p>
</blockquote>
<p>我们举个例子来理解外键。<br><code>Orders</code> 表将录入到系统的每个订单作为一行包含其中。顾客信息存储在<br><code>Customers</code> 表中。 <code>Orders</code> 表中的订单通过顾客 <code>ID</code>与 <code>Customers</code> 表中的特定行相关联。顾客 <code>ID</code> 为 <code>Customers</code> 表的主键，每个顾客都有唯一的<code>ID</code>。订单号为 <code>Orders</code> 表的主键，每个订单都有唯一的订单号。<code>Orders</code> 表中顾客 <code>ID</code>列的值不一定是唯一的。如果某个顾客有多个订单，则有多个行具有相同的顾客 <code>ID</code>（虽然每个订单都有不同的订单号）。同时， <code>Orders</code> 表中顾客 <code>ID</code>列的合法值为 <code>Customers</code> 表中顾客的 <code>ID</code>。<br>这就是外键的作用。在这个例子中，在 <code>Orders</code> 的顾客 <code>ID</code>列上定义了一<br>个外键，因此该列只能接受 <code>Customers</code> 表的主键值。</p>
<p>外键定义方法：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE Orders</div><div class="line">(</div><div class="line">order_num INTEGER NOT NULL PRIMARY KEY,</div><div class="line">order_date DATETIME NOT NULL,</div><div class="line">cust_id CHAR(10) NOT NULL REFERENCES Customers(cust_id)</div><div class="line">);</div></pre></td></tr></table></figure></p>
<p>其中的表定义使用了 <code>REFERENCES</code> 关键字，它表示 <code>cust_id</code> 中的任何值都必须是 <code>Customers</code> 表的 <code>cust_id</code> 中的值。<br>也可以在 <code>ALTER TABLE</code> 语句中用 <code>CONSTRAINT</code> 语法来完成：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">ALTER TABLE Orders</div><div class="line">ADD CONSTRAINT</div><div class="line">FOREIGN KEY (cust_id) REFERENCES Customers (cust_id)</div></pre></td></tr></table></figure></p>
<p> 外键有助防止意外删除<br>除帮助保证引用完整性外，外键还有另一个重要作用。在定义外后，<code>DBMS</code> 不允许删除在另一个表中具有关联行的行。例如，不能删除关联订单的顾客。删除该顾客的唯一方法是首先删除相关的订单（这表示还要删除相关的订单项），有的 DBMS支持称为级联删除（<code>cascading delete</code>）的特性。例如，如果启用级联删除并且从 <code>Customers</code> 表中删除某个顾客，则任何关联的订单行也会被自动删除。</p>
<h4 id="唯一约束"><a href="#唯一约束" class="headerlink" title="唯一约束"></a>唯一约束</h4><p>唯一约束用来保证一列（或一组列）中的数据是唯一的。它们类似于主键，但存在以下重要区别。</p>
<ol>
<li>表可包含多个唯一约束，但每个表只允许一个主键。</li>
<li>唯一约束列可包含 <code>NULL</code> 值。</li>
<li>唯一约束列可修改或更新。</li>
<li>唯一约束列的值可重复使用。</li>
<li><p>与主键不一样，唯一约束不能用来定义外键。</p>
<p>唯一约束既可以用 <code>UNIQUE</code> 关键字在表定义中定义，也可以用单独的 <code>CONSTRAINT</code> 定义。</p>
</li>
</ol>
<h4 id="检查约束"><a href="#检查约束" class="headerlink" title="检查约束"></a>检查约束</h4><blockquote>
<p>检查约束用来保证一列（或一组列）中的数据满足一组指定的条件。</p>
</blockquote>
<p>检查约束的常见用途有以下几点。</p>
<ol>
<li>检查最小或最大值。例如，防止 0个物品的订单（即使 0是合法的数）。</li>
<li>指定范围。例如，保证发货日期大于等于今天的日期，但不超过今天<br>起一年后的日期。</li>
<li>只允许特定的值。例如，在性别字段中只允许 M 或 F。</li>
</ol>
<p>检查约束在数据类型内又做了进一步的限制，这些限制极其重要，可以确保插入数据库的数据正是你想要的数据。不需要依赖于客户端应用程序或用户来保证正确获取它，DBMS本身将会拒绝任何无效的数据。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE OrderItems</div><div class="line">(</div><div class="line">order_num INTEGER NOT NULL,</div><div class="line">order_item INTEGER NOT NULL,</div><div class="line">prod_id CHAR(10) NOT NULL,</div><div class="line">quantity INTEGER NOT NULL CHECK (quantity &gt; 0),</div><div class="line">item_price MONEY NOT NULL</div><div class="line">);</div></pre></td></tr></table></figure>
<p>利用这个约束，任何插入（或更新）的行都会被检查，保证 <code>quantity</code>大于 0。<br>检查名为 <code>gender</code> 的列只包含 M 或 F ，可编写如下的 <code>ALTER TABLE</code> 语句：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ADD CONSTRAINT CHECK (gender LIKE &apos;[MF]&apos;)</div></pre></td></tr></table></figure></p>
<h3 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h3><blockquote>
<p>索引用来排序数据以加快搜索和排序操作的速度。</p>
</blockquote>
<p>主键数据总是排序的，这是 DBMS的工作。因此，按主键检索特定行总是一种快速有效的操作，但是，搜索其他列中的值通常效率不高。</p>
<p>使用索引，可以在一个或多个列上定义索引，使 DBMS保存其内容的一个排过序的列表。DBMS 搜索排过序的索引，找出匹配的位置，然后<br>检索这些行。</p>
<p>在开始创建索引前，应该记住以下内容:</p>
<ol>
<li>索引改善检索操作的性能，但降低了数据插入、修改和删除的性能。<br>在执行这些操作时，DBMS必须动态地更新索引。</li>
<li>索引数据可能要占用大量的存储空间。</li>
<li>并非所有数据都适合做索引。</li>
<li>索引用于数据过滤和数据排序。</li>
<li>可以在索引中定义多个列（例如，州加上城市）。</li>
</ol>
<p>索引用 <code>CREATE INDEX</code> 语句创建。</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">CREATE</span> <span class="keyword">INDEX</span> prod_name_ind</div><div class="line"><span class="keyword">ON</span> Products (prod_name);</div></pre></td></tr></table></figure>
<p>索引必须唯一命名。这里的索引名 <code>prod_name_ind</code> 在关键字 <code>CREATE INDEX</code> 之后定义。 <code>ON</code> 用来指定被索引的表，而索引中包含的列（此例中仅有一列）在表名后的圆括号中给出。<br> 检查索引<br>索引的效率随表数据的增加或改变而变化。最好定期检查索引，并根据需要对索引进行调整。</p>
<h3 id="触发器"><a href="#触发器" class="headerlink" title="触发器"></a>触发器</h3><blockquote>
<p>触发器是特殊的存储过程，它在特定的数据库活动发生时自动执行。触发器可以与特定表上的 <code>INSERT</code> 、 <code>UPDATE</code> 和 <code>DELETE</code> 操作（或组合）相关联。</p>
</blockquote>
<p>与存储过程不一样，触发器与单个的表相关联。例如， <code>Orders</code> 表上的 <code>INSERT</code> 操作相关联的触发器只在<code>Orders</code> 表中插入行时执行。</p>
<p>触发器内的代码具有以下数据的访问权：</p>
<ol>
<li><code>INSERT</code> 操作中的所有新数据；</li>
<li><code>UPDATE</code> 操作中的所有新数据和旧数据；</li>
<li><code>DELETE</code> 操作中删除的数据。</li>
</ol>
<p>触发器的一些常见用途：</p>
<ol>
<li>保证数据一致。例如，在 <code>INSERT</code> 或 <code>UPDATE</code> 操作中将所有州名转换<br>为大写。</li>
<li>基于某个表的变动在其他表上执行活动。例如，每当更新或删除一行<br>时将审计跟踪记录写入某个日志表。</li>
<li>进行额外的验证并根据需要回退数据。例如，保证某个顾客的可用资<br>金不超限定，如果已经超出，则阻塞插入。</li>
<li>计算计算列的值或更新时间戳。</li>
</ol>
<p>下面的例子创建一个触发器，它对所有 <code>INSERT</code> 和 <code>UPDATE</code> 操作，将<code>Customers</code> 表中的 <code>cust_state</code> 列转换为大写。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">CREATE TRIGGER customer_state</div><div class="line">ON Customers</div><div class="line">FOR INSERT, UPDATE</div><div class="line">AS</div><div class="line">UPDATE Customers</div><div class="line">SET cust_state = Upper(cust_state)</div><div class="line">WHERE Customers.cust_id = inserted.cust_id;</div></pre></td></tr></table></figure>
<p> 约束的处理比触发器快，因此在可能的时候，应该尽量使<br>用约束。</p>
<h3 id="数据库安全"><a href="#数据库安全" class="headerlink" title="数据库安全"></a>数据库安全</h3><p>任何安全系统的基础都是用户授权和身份确认。这是一种处理，通过这种处理对用户进行确认，保证他是有权用户，允许执行他要执行的操作。</p>
<p>需要保护的操作有：</p>
<ol>
<li>对数据库管理功能（创建表、更改或删除已存在的表等）的访问；</li>
<li>对特定数据库或表的访问；</li>
<li>访问的类型（只读、对特定列的访问等）；</li>
<li>仅通过视图或存储过程对表进行访问；</li>
<li>创建多层次的安全措施，从而允许多种基于登录的访问和控制；</li>
<li>限制管理用户账号的能力。</li>
</ol>
<p>安全性使用 SQL的 <code>GRANT</code> 和 <code>REVOKE</code> 语句来管理，不过，大多数 DBMS提供了交互式的管理实用程序，这些实用程序在内部使用 <code>GRANT</code> 和<code>REVOKE</code> 语句。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;SQL学习笔记&quot;&gt;&lt;a href=&quot;#SQL学习笔记&quot; class=&quot;headerlink&quot; title=&quot;SQL学习笔记&quot;&gt;&lt;/a&gt;SQL学习笔记&lt;/h1&gt;&lt;p&gt;@(数据库)&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./azure_sql_featured_image
    
    </summary>
    
    
      <category term="tas1" scheme="http://yoursite.com/tags/tas1/"/>
    
      <category term="tag2" scheme="http://yoursite.com/tags/tag2/"/>
    
  </entry>
  
  <entry>
    <title>SQL必知必会</title>
    <link href="http://yoursite.com/2016/11/07/SQL%E5%BF%85%E7%9F%A5%E5%BF%85%E4%BC%9A01/"/>
    <id>http://yoursite.com/2016/11/07/SQL必知必会01/</id>
    <published>2016-11-07T10:44:53.000Z</published>
    <updated>2016-11-07T10:54:37.231Z</updated>
    
    <content type="html"><![CDATA[<h1 id="SQL学习笔记"><a href="#SQL学习笔记" class="headerlink" title="SQL学习笔记"></a>SQL学习笔记</h1><p>@(数据库)</p>
<p><img src="./azure_sql_featured_image.png" alt="Alt text"></p>
<p>[TOC]</p>
<h2 id="了解数据库"><a href="#了解数据库" class="headerlink" title="了解数据库"></a>了解数据库</h2><blockquote>
<p>数据库是一个以某种有组织的方式存储的数据集合。</p>
</blockquote>
<p> <strong>数据库</strong>（database）保存有组织的数据的容器（通常是一个文件或一组文件）。确切地说，数据库软件应称为数据库管理系统（DBMS）。数据库是通过 DBMS 创建和操纵的容器。</p>
<h3 id="表"><a href="#表" class="headerlink" title="表"></a>表</h3><blockquote>
<p><strong>表</strong>是一种结构化的文件，可用来存储某种特定类型的数据。</p>
</blockquote>
<h3 id="表名"><a href="#表名" class="headerlink" title="表名"></a>表名</h3><p>数据库中的每个表都有一个名字来标识自己。这个名字是唯一的，即同一数据库中没有其他表具有相同的名字。<br> 使<strong>表名</strong>成为唯一的，实际上是数据库名和表名等的组合。虽然在相同数据库中不能两次使用相同的表名，但在不同的数据库中完全可以使用相同的表名。</p>
<h3 id="模式"><a href="#模式" class="headerlink" title="模式"></a>模式</h3><blockquote>
<p>模式是关于数据库和表的布局及特性的信息。</p>
</blockquote>
<p> 表具有一些特性，这些特性定义了数据在表中如何存储，包含存储什么样的数据，数据如何分解，各部分信息如何命名等信息。描述表的这组信息就是所谓的<u>模式（schema）</u>，模式可以用来描述数据库中特定的表，也可以用来描述整个数据库（和其中表的关系）。</p>
<p>###列和数据类型</p>
<h4 id="列（column）"><a href="#列（column）" class="headerlink" title="列（column）"></a>列（column）</h4><blockquote>
<p>表中的一个字段。所有表都是由一个或多个列组成的。</p>
<h3 id="行（row）"><a href="#行（row）" class="headerlink" title="行（row）"></a>行（row）</h3><p>表中的一个记录<br> 如果将表想象为网格，网格中垂直的列为表列，水平行为表行。</p>
<h3 id="主键（primary-key）"><a href="#主键（primary-key）" class="headerlink" title="主键（primary key）"></a>主键（primary key）</h3><p>一列（或一组列），其值能够唯一标识表中每一行。</p>
</blockquote>
<p>唯一标识表中每行的这个<strong>列</strong>（或这几<strong>列</strong>）称为主键。<strong>主键用来表示一个特定的行</strong>。没有主键，更新或删除表中特定行就极为困难。 应该总是定义主键，虽然并不总是需要主键，但多数数据库设计者都会保证他们创建的每个表具有一个主键，以便于以后的数据操作和管理。</p>
<p>表中的任何列都可以作为主键，只要它满足以下条件：</p>
<p> 任意两行都不具有相同的主键值；<br> 每一行都必须具有一个主键值（主键列不允许 <code>NULL</code> 值）；<br> 主键列中的值不允许修改或更新；<br>主键值不能重用（如果某行从表中删除，它的主键不能赋给以后的新行）。</p>
<h2 id="检索数据"><a href="#检索数据" class="headerlink" title="检索数据"></a>检索数据</h2><h3 id="SELECT"><a href="#SELECT" class="headerlink" title="SELECT"></a>SELECT</h3><blockquote>
<p>为了使用 SELECT 检索表数据，必须至少给出两条信息——想选择什么，以及从什么地方选择。</p>
<h3 id="检索单个列"><a href="#检索单个列" class="headerlink" title="检索单个列"></a>检索单个列</h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_name</div><div class="line"><span class="keyword">FROM</span> Products;</div></pre></td></tr></table></figure>
</blockquote>
<p>利用 <code>SELECT</code> 语句从 <code>Products</code> 表中检索一个名为<code>prod_name</code>的列。所需的列名写在 <code>SELECT</code> 关键字之后， <code>FROM</code> 关键字指出从哪个表中检索数据。如上的一条简单 <code>SELECT</code> 语句将返回表中的所有行。数据没有过滤，也没有排序。<br> 如果没有明确排序查询结果（下一课介绍），则返回的数据没有特定的顺序。返回数据的顺序可能是数据被添加到表中的顺序，也可能不是。只要返回相同数目的行，就是正常的。</p>
<h5 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h5><p> 多条 SQL语句必须以分号<code>;</code>分隔。<br>SQL语句不区分大小写，因此 <code>SELECT</code> 与 <code>select</code> 是相同的。同样，写成 <code>Select</code> 也没有关系。但是表名、列名和值可能有所不同（这有赖于具体的 DBMS及其如何配置）。<br>在处理 SQL语句时，其中所有空格都被忽略。SQL语句可以写成长长的一行，也可以分写在多行。下面这 3 种写法的作用是一样的。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_name</div><div class="line"><span class="keyword">FROM</span> Products;</div><div class="line"></div><div class="line"><span class="keyword">SELECT</span> prod_name <span class="keyword">FROM</span> Products;</div><div class="line"></div><div class="line"><span class="keyword">SELECT</span></div><div class="line">prod_name</div><div class="line"><span class="keyword">FROM</span></div><div class="line">Products;</div></pre></td></tr></table></figure></p>
<p> 多数 SQL开发人员认为，将 SQL语句分成多行更容易阅读和调试。</p>
<h3 id="检索多个列"><a href="#检索多个列" class="headerlink" title="检索多个列"></a>检索多个列</h3><blockquote>
<p>要想从一个表中检索多个列，须在 <code>SELECT</code> 关键字后给出多个列名，列名之间必须以逗号分隔。<br>注意：</p>
</blockquote>
<p> 在选择多个列时，一定要在列名之间加上逗号，但最后一个列名后不加。如果在最后一个列名后加了逗号，将出现错误。</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_id, prod_name, prod_price</div><div class="line"><span class="keyword">FROM</span> Products;</div></pre></td></tr></table></figure>
<h3 id="检索所有列"><a href="#检索所有列" class="headerlink" title="检索所有列"></a>检索所有列</h3><blockquote>
<p>如果给定一个通配符（ * ），则返回表中所有列。列的顺序一般是列在表定义中出现的物理顺序，但并不总是如此。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<p> 使用通配符有一个大优点。由于不明确指定列名（因为星号检索每一列），所以能检索出名字未知的列。<br>一般而言，除非你确实需要表中的每一列，否则最好别使用 * 通配符。虽然使用通配符能让你自己省事，不用明确列出所需列，但检索不需要的列通常会降低检索和应用程序的性能。</p>
<h3 id="检索不同的值"><a href="#检索不同的值" class="headerlink" title="检索不同的值"></a>检索不同的值</h3><blockquote>
<p>使用 DISTINCT 关键字，顾名思义，它指示数据库只返回不同的值。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT DISTINCT vend_id</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>  <code>SELECT DISTINCT vend_id, prod_price</code> <code>DISTINCT</code> 关键字作用于所有的列，不仅仅是跟在其后的那一列。</p>
<h3 id="限制结果"><a href="#限制结果" class="headerlink" title="限制结果"></a>限制结果</h3><blockquote>
<p><code>SELECT</code> 语句返回指定表中所有匹配的行，很可能是每一行。如果你只想返回第一行或者一定数量的行在 SQL Server和 Access中使用 SELECT 时，可以使用 <code>TOP</code> 关键字来限制最多返回多少行，如只检索前 5行数据。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT TOP 5 prod_name</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<h5 id="注意事项-1"><a href="#注意事项-1" class="headerlink" title="注意事项"></a>注意事项</h5><p> 如果你使用 Oracle，需要基于 <code>ROWNUM</code> （行计数器）来计算行，像这样：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE ROWNUM &lt;=5;</div></pre></td></tr></table></figure>
<p> 如果你使用 MySQL、MariaDB、PostgreSQL或者 SQLite，需要使用 <code>LIMIT</code>子句，像这样：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">LIMIT 5;</div></pre></td></tr></table></figure></p>
<p>  <code>LIMIT 5</code> 指示MySQL等 DBMS返回不超过 5行的数据。</p>
<p> 为了得到后面的 5行数据，需要指定从哪儿开始以及检索的行数，像这样：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">LIMIT 5 OFFSET 5;</div></pre></td></tr></table></figure></p>
<p> <code>LIMIT 5 OFFSET 5</code> 指示 MySQL等 DBMS返回从第 5行起的 5行数据。第一个数字是指从哪儿开始，第二个数字是检索的行数。<u> <code>LIMIT</code> 指定返回的行数。 <code>LIMIT</code> 带的 <code>OFFSET</code> 指定从哪儿开始。</u></p>
<p> 第一个被检索的行是第 0行，而不是第 1行。因此， <code>LIMIT 1 OFFSET 1</code> 会检索第 2行，而不是第 1行。MySQL和MariaDB支持简化版的 <code>LIMIT 4 OFFSET 3</code> 语句，即 <code>LIMIT 3,4</code> ，即从第三行开始选取四行。使用这个语法，逗号之前的值对应 <code>OFFSET</code> ，逗号之后的值对应 <code>LIMIT</code> 。</p>
<h3 id="使用注释"><a href="#使用注释" class="headerlink" title="使用注释"></a>使用注释</h3><blockquote>
<p>添加一些描述性的注释，这便于今后参考，或者供项目后续参与人员参考，注释掉一些代码，还可以暂时停止要执行的 SQL代码，只测试一部分。</p>
</blockquote>
<ol>
<li><p>注释使用 – （两个连字符）嵌在行内。 – 之后的文本就是注释。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name -- 这是一条注释</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
</li>
<li><p>在一行的开始处使用 # ，这一整行都将作为注释。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"># 这是一条注释</div><div class="line">SELECT prod_name</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
</li>
<li><p>多行注释，注释可以在脚本的任何位置开始和停止。注释从 <code>/*</code> 开始，到 <code>*/</code> 结束， <code>/*</code> 和 <code>*/</code> 之间的任何内容都是注释。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">/* SELECT prod_name, vend_id</div><div class="line">FROM Products; */</div><div class="line">SELECT prod_name</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="排序检索数据"><a href="#排序检索数据" class="headerlink" title="排序检索数据"></a>排序检索数据</h2><p>如果不排序，检索出的数据一般将以它在底层表中出现的顺序显示，这有可能是数据最初添加到表中的顺序。</p>
<h3 id="排序数据"><a href="#排序数据" class="headerlink" title="排序数据"></a>排序数据</h3><blockquote>
<p>使用 <code>ORDER BY</code> 子句，根据需要排序检索出的数据。</p>
</blockquote>
<p>为了明确地排序用 <code>SELECT</code> 语句检索出的数据，可使用 <code>ORDER BY</code> 子句。<code>ORDER BY</code> 子句取一个或多个列的名字，据此对输出进行排序。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_name;</div></pre></td></tr></table></figure></p>
<p> <code>ORDER BY</code>指示 DBMS软件对 prod_name 列以字母顺序排序数据。</p>
<h5 id="注意事项-2"><a href="#注意事项-2" class="headerlink" title="注意事项"></a>注意事项</h5><p>  在指定一条 <code>ORDER BY</code> 子句时，应该保证它是 <code>SELECT</code> 语句中最后一条子句。如果它不是最后的子句，将会出现错误消息。</p>
<h3 id="按多个列排序"><a href="#按多个列排序" class="headerlink" title="按多个列排序"></a>按多个列排序</h3><blockquote>
<p>要按多个列排序，简单指定列名，列名之间用逗号分开即可。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_price, prod_name;</div></pre></td></tr></table></figure></p>
</blockquote>
<p> 仅在多个行具有相同的 <code>prod_price</code> 值时才对产品按 <code>prod_name</code> 进行排序。如果 <code>prod_price</code> 列中所有的值都是唯一的，则不会按<code>prod_name</code> 排序。</p>
<h3 id="按列位置排序"><a href="#按列位置排序" class="headerlink" title="按列位置排序"></a>按列位置排序</h3><blockquote>
<p><code>ORDER BY</code> 支持按相对列位置进行排序。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY 2, 3;</div></pre></td></tr></table></figure>
<p> <code>ORDER BY 2</code>表示按 <code>SELECT</code> 清单中的第二个列 <code>prod_name</code> 进行排序。 <code>ORDER BY 2，3</code> 表示先按 <code>prod_price</code> ，再按 <code>prod_name</code> 进行排序。<br> 如果有必要，可以混合使用实际列名和相对列位置。</p>
<h3 id="指定排序方向"><a href="#指定排序方向" class="headerlink" title="指定排序方向"></a>指定排序方向</h3><blockquote>
<p>数据排序不限于升序排序（从 A到 Z），这只是默认的排序顺序。还可以使用 <code>ORDER BY</code> 子句进行降序（从 Z 到 A）排序。为了进行降序排序，必须指定 <code>DESC</code> 关键字。如果想在多个列上进行降序排序，必须对每一列指定 <code>DESC</code> 关键字。</p>
</blockquote>
<p>以价格降序来排序产品（最贵的排在最前面）：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_price DESC;</div></pre></td></tr></table></figure>
<p>多个列排序:</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">ORDER BY prod_price DESC, prod_name;</div></pre></td></tr></table></figure>
<p> <code>prod_price</code> 列以降序排序，而 <code>prod_name</code> 列（在每个价格内）仍然按标准的升序排序。</p>
<h2 id="过滤数据"><a href="#过滤数据" class="headerlink" title="过滤数据"></a>过滤数据</h2><blockquote>
<p>只检索所需数据需要指定搜索条件（search criteria），搜索条件也称为过滤条件（filter condition）。</p>
</blockquote>
<h3 id="使用-WHERE-子句"><a href="#使用-WHERE-子句" class="headerlink" title="使用 WHERE 子句"></a>使用 WHERE 子句</h3><p>在 <code>SELECT</code> 语句中，数据根据 <code>WHERE</code> 子句中指定的搜索条件进行过滤。<code>WHERE</code> 子句在表名（ <code>FROM</code> 子句）之后给出，如下所示：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE prod_price = 3.49;</div></pre></td></tr></table></figure>
<p> 在同时使用 <code>ORDER BY</code> 和 <code>WHERE</code> 子句时，应该让 <code>ORDER BY</code> 位于<code>WHERE</code> 之后，否则将会产生错误。</p>
<h3 id="WHERE-子句操作符"><a href="#WHERE-子句操作符" class="headerlink" title="WHERE 子句操作符"></a>WHERE 子句操作符</h3><table>
<thead>
<tr>
<th style="text-align:center">操作符与说明</th>
<th style="text-align:center">操作符与说明</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><code>=</code> 等于</td>
<td style="text-align:center"><code>&gt;</code> 大于</td>
</tr>
<tr>
<td style="text-align:center"><code>&lt; &gt;</code> 不等于</td>
<td style="text-align:center"><code>&gt;=</code> 大于等于</td>
</tr>
<tr>
<td style="text-align:center"><code>!=</code> 不等于</td>
<td style="text-align:center"><code>!&gt;</code> 不大于</td>
</tr>
<tr>
<td style="text-align:center"><code>&lt;</code>小于</td>
<td style="text-align:center"><code>BETWEEN</code> 在指定的两个值之间</td>
</tr>
<tr>
<td style="text-align:center"><code>&lt;=</code> 小于等于</td>
<td style="text-align:center"><code>IS NULL</code> 为<code>NULL</code>值</td>
</tr>
<tr>
<td style="text-align:center"><code>!&lt;</code> 不小于</td>
</tr>
</tbody>
</table>
<h3 id="检查单个值"><a href="#检查单个值" class="headerlink" title="检查单个值"></a>检查单个值</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE prod_price &lt; 10;</div></pre></td></tr></table></figure>
<h3 id="不匹配检查"><a href="#不匹配检查" class="headerlink" title="不匹配检查"></a>不匹配检查</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_id, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE vend_id &lt;&gt; &apos;DLL01&apos;;</div></pre></td></tr></table></figure>
<p> <strong>使用引号不管使用何种形式的字符串数据类型，字符串值都必须括在单引号内</strong>。如果将值与字符串类型的列进行比较，就需要限定引号。用来与数值列进行比较的值不用引号。<br> != 和 &lt;&gt; 通常可以互换。但是，并非所有 DBMS都支持这两种不等于操作符。例如，Microsoft Access支持 &lt;&gt; 而不支持 != 。</p>
<h3 id="范围值检查"><a href="#范围值检查" class="headerlink" title="范围值检查"></a>范围值检查</h3><blockquote>
<p>要检查某个范围的值，可以使用 <code>BETWEEN</code> 操作符。它需要两个值，即范围的开始值和结束值。这两个值必须用 <code>AND</code> 关键字分隔。 <code>BETWEEN</code> 匹配范围中所有的值，<strong>包括指定的开始值和结束值。</strong></p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE prod_price BETWEEN 5 AND 10;</div></pre></td></tr></table></figure>
<h3 id="空值检查"><a href="#空值检查" class="headerlink" title="空值检查"></a>空值检查</h3><blockquote>
<p>在一个列不包含值时，称其包含空值 <code>NULL</code> 。<code>Null</code> 无值（no value），它与字段包含 0、空字符串或仅仅包含空格不同。</p>
</blockquote>
<p> <code>SELECT</code> 语句有一个特殊的 <code>WHERE</code> 子句，可用来检查具有 <code>NULL</code> 值的列。这个 <code>WHERE</code> 子句就是 <code>IS NULL</code> 子句。其语法如下：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_price IS NULL;</div></pre></td></tr></table></figure>
<p> 这条语句返回所有没有价格（空 <code>prod_price</code> 字段，不是价格为 0 ）的产品。<br> 过滤数据时，一定要验证被过滤列中含 <code>NULL</code> 的行确实出现在返回的数据中。</p>
<h2 id="高级数据过滤"><a href="#高级数据过滤" class="headerlink" title="高级数据过滤"></a>高级数据过滤</h2><h3 id="组合-WHERE-子句"><a href="#组合-WHERE-子句" class="headerlink" title="组合 WHERE 子句"></a>组合 WHERE 子句</h3><blockquote>
<p>为了进行更强的过滤控制，SQL允许给出多个 <code>WHERE</code> 子句。这些子句有两种使用方式，即以 <code>AND</code> 子句或 <code>OR</code> 子句的方式使用。</p>
</blockquote>
<h4 id="AND-操作符"><a href="#AND-操作符" class="headerlink" title="AND 操作符"></a>AND 操作符</h4><blockquote>
<p>要通过不止一个列进行过滤，可以使用 <code>AND</code> 操作符给 <code>WHERE</code> 子句附加条件。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_price, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos; AND prod_price &lt;= 4;</div></pre></td></tr></table></figure>
<p>此SQL语句检索由供应商 <code>DLL01</code> 制造且价格小于等于4美元的所有产品的名称和价格。这条 <code>SELECT</code> 语句中的 <code>WHERE</code> 子句包含两个条件，用 <code>AND</code>关键字联结在一起。</p>
<h4 id="OR-操作符"><a href="#OR-操作符" class="headerlink" title="OR 操作符"></a>OR 操作符</h4><blockquote>
<p><code>OR</code> 操作符指示 DBMS 检索匹配任一条件的行。OR 操作符告诉 DBMS匹配任一条件而不是同时匹配两个条件。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos; OR vend_id = ‘BRS01’;</div></pre></td></tr></table></figure>
<h4 id="求值顺序"><a href="#求值顺序" class="headerlink" title="求值顺序"></a>求值顺序</h4><blockquote>
<p><code>WHERE</code> 子句可以包含任意数目的 <code>AND</code> 和 <code>OR</code> 操作符。允许两者结合以进行复杂、高级的过滤。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos; OR vend_id = &apos;BRS01&apos;</div><div class="line">AND prod_price &gt;= 10;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">prod_name           prod_price</div><div class="line">------------------- ----------</div><div class="line">Fish bean bag toy   3.4900</div><div class="line">Bird bean bag toy   3.4900</div><div class="line">Rabbit bean bag toy 3.4900</div><div class="line">18 inch teddy bear  11.9900</div><div class="line">Raggedy Ann         4.9900</div></pre></td></tr></table></figure>
<p> SQL（像多数语言一样）在处理 <code>OR</code> 操作符前，优先处理 <code>AND</code> 操作符。</p>
<p>当 SQL看到上述 <code>WHERE</code> 子句时，它理解为：由供应商 <code>BRS01</code> 制造的价格为 10美元以上的所有产品，以及由供应商 <code>DLL01</code> 制造的所有产品，而不管其价格如何。换句话说，由于 <code>AND</code> 在求值过程中优先级更高，操作符被错误地组合了。此问题的解决方法是使用圆括号对操作符进行明确分组。请看下面的<code>SELECT</code> 语句及结果。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE (vend_id = &apos;DLL01&apos; OR vend_id = &apos;BRS01&apos;)</div><div class="line">AND prod_price &gt;= 10;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">prod_name           prod_price</div><div class="line">------------------- ----------</div><div class="line">18 inch teddy bear  11.9900</div></pre></td></tr></table></figure>
<p><code>SELECT</code> 语句与前一条的唯一差别是，将前两个条件用圆括号括了起来。因为圆括号具有比 <code>AND</code> 或 <code>OR</code> 操作符更高的求值顺序，所以 DBMS首先过滤圆括号内的 <code>OR</code> 条件。这时，SQL语句变成了选择由供应商 DLL01或 BRS01 制造的且价格在 10 美元及以上的所有产品，这正是我们希望的结果。<br> 在 WHERE 子句中使用圆括号<br>任何时候使用具有 <code>AND</code> 和 <code>OR</code> 操作符的 <code>WHERE</code> 子句，都应该使用圆括号明确地分组操作符。不要过分依赖默认求值顺序，即使它确实如你希望的那样。使用圆括号没有什么坏处，它能消除歧义。</p>
<h4 id="IN-操作符"><a href="#IN-操作符" class="headerlink" title="IN 操作符"></a>IN 操作符</h4><blockquote>
<p>IN 操作符用来指定条件范围，范围中的每个条件都可以进行匹配。 <code>IN</code> 取一组由逗号分隔、括在圆括号中的合法值。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name, prod_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id IN ( &apos;DLL01&apos;, &apos;BRS01&apos; )</div><div class="line">ORDER BY prod_name;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>此 <code>SELECT</code> 语句检索由供应商 DLL01 和 BRS01 制造的所有产品。 <code>IN</code> 操作符后跟由<strong>逗号</strong>分隔的合法值，这些值必须括在<strong>圆括号</strong>中。</p>
<h5 id="IN-操作符优点"><a href="#IN-操作符优点" class="headerlink" title="IN 操作符优点"></a>IN 操作符优点</h5><p> 在有很多合法选项时， <code>IN</code> 操作符的语法更清楚，更直观。<br>在与其他 <code>AND</code> 和 <code>OR</code> 操作符组合使用 <code>IN</code> 时，求值顺序更容易管理。<br> <code>IN</code> 操作符一般比一组 <code>OR</code> 操作符执行得更快。<br> <code>IN</code> 的最大优点是<strong>可以包含其他 <code>SELECT</code>语句</strong>，能够更动态地建立<code>WHERE</code> 子句。</p>
<h4 id="NOT-操作符"><a href="#NOT-操作符" class="headerlink" title="NOT 操作符"></a>NOT 操作符</h4><blockquote>
<p><code>WHERE</code> 子句中的 <code>NOT</code> 操作符有且只有一个功能，那就是<strong>否定其后所跟的任何条件</strong>。因为 <code>NOT</code> 从不单独使用（它总是与其他操作符一起使用），所以它的语法与其他操作符有所不同。 <code>NOT</code> 关键字可以用在要过滤的列前，而不仅是在其后。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE NOT vend_id = &apos;DLL01&apos;</div><div class="line">ORDER BY prod_name;</div></pre></td></tr></table></figure>
<p>这里的 <code>NOT</code> 否定跟在其后的条件，因此，DBMS 匹配非 <code>DLL01</code> 之外的所有东西。上面的例子也可以使用 <code>&lt;&gt;</code> 操作符来完成，如下所示。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE vend_id &lt;&gt; &apos;DLL01&apos;</div><div class="line">ORDER BY prod_name;</div></pre></td></tr></table></figure>
<h2 id="用通配符进行过滤"><a href="#用通配符进行过滤" class="headerlink" title="用通配符进行过滤"></a>用通配符进行过滤</h2><blockquote>
<p>通配符（wildcard）用来匹配值的一部分的特殊字符。<br>搜索模式（search pattern）由字面值、通配符或两者组合构成的搜索条件。 利用通配符，可以创建比较特定数据的搜索模式。</p>
<h3 id="LIKE-操作符"><a href="#LIKE-操作符" class="headerlink" title="LIKE 操作符"></a>LIKE 操作符</h3><p>为在搜索子句中使用通配符，必须使用 <code>LIKE</code> 操作符。 <code>LIKE</code>指示 DBMS，后跟的搜索模式利用通配符匹配而不是简单的相等匹配进行比较。<br>通配符搜索只能用于<strong>文本字段（字符串</strong>），非文本数据类型字段不能使用通配符搜索。</p>
<h4 id="百分号（-）通配符"><a href="#百分号（-）通配符" class="headerlink" title="百分号（%）通配符"></a>百分号（%）通配符</h4><p>最常使用的通配符是百分号（ % ）。在搜索串中， <strong>% 表示任何字符出现任意次数。</strong></p>
</blockquote>
<p>例如，为了找出所有以词 Fish 起头的产品，可发布以下 <code>SELECT</code> 语句。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_name LIKE &apos;Fish%&apos;;</div></pre></td></tr></table></figure></p>
<p>此例子使用了搜索模式 <code>&#39;Fish%&#39;</code> 。在执行这条子句时，将检索任意以<code>Fish</code> 起头的词。 % 告诉 DBMS 接受 Fish 之后的任意字符，不管它有多少字符。<br> <strong>区分大小写</strong>根据 DBMS的不同及其配置，搜索可以是区分大小写的。</p>
<blockquote>
<p>通配符可在搜索模式中的任意位置使用，并且可以使用多个通配符。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_name LIKE &apos;%bean bag%&apos;;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>搜索模式 <code>&#39;%bean bag%&#39;</code> 表示匹配任何位置上包含文本 bean bag 的值，不论它之前或之后出现什么字符。</p>
<blockquote>
<p>通配符也可以出现在搜索模式的中间，虽然这样做不太有用。</p>
</blockquote>
<p>找出以 F 起头、以 y 结尾的所有产品<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_name LIKE &apos;F%y&apos;;</div></pre></td></tr></table></figure></p>
<p> 有一种情况下把通配符放在搜索模式中间是很有用的，就是根据邮件地址的一部分来查找电子邮件 ，例如 <code>WHERE email LIKE&#39;b%@forta.com&#39;</code> 。</p>
<blockquote>
<p>除了能匹配一个或多个字符外， % 还能匹配 0 个字符。 %代表搜索模式中给定位置的 0个、1个或多个字符。</p>
<h5 id="注意事项-3"><a href="#注意事项-3" class="headerlink" title="注意事项"></a>注意事项</h5><p> 通配符 <code>%</code> 看起来像是可以匹配任何东西，但有个例外，这就是 <code>NULL</code> 。子句 <code>WHERE prod_name LIKE &#39;%&#39;</code> 不会匹配产品名称为 <code>NULL</code> 的行。</p>
<h4 id="下划线（-）通配符"><a href="#下划线（-）通配符" class="headerlink" title="下划线（_）通配符"></a>下划线（_）通配符</h4><p>下划线的用途与 % 一样，但它只匹配单个字符，而不是多个字符。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id, prod_name</div><div class="line">FROM Products</div><div class="line">WHERE prod_name LIKE &apos;__ inch teddy bear&apos;;</div></pre></td></tr></table></figure>
<blockquote>
<p>与 % 能匹配 0或多个字符不同， _ 总是刚好匹配一个字符，不能多也不能少。</p>
<h4 id="方括号（-）通配符"><a href="#方括号（-）通配符" class="headerlink" title="方括号（[ ]）通配符"></a>方括号（[ ]）通配符</h4><p>方括号<code>[]</code> 通配符用来指定一个字符集，它必须匹配指定位置（通配符的位置）的一个字符。<strong>只有微软的 Access 和 SQL Server 支持集合</strong>。</p>
</blockquote>
<p>找出所有名字<strong>以 J 或 M 起头</strong>的联系人，可进行如下查询：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT cust_contact</div><div class="line">FROM Customers</div><div class="line">WHERE cust_contact LIKE &apos;[JM]%&apos;</div><div class="line">ORDER BY cust_contact;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">cust_contact</div><div class="line">-----------------</div><div class="line">Jim Jones</div><div class="line">John Smith</div><div class="line">Michelle Green</div></pre></td></tr></table></figure>
<p>此语句的 <code>WHERE</code> 子句中的模式为 <code>&#39;[JM]%&#39;</code> 。这一搜索模式使用了两个不同的通配符。 <code>[JM]</code> 匹配方括号中任意一个字符，它也<strong>只能匹配单个字符</strong>。因此，任何多于一个字符的名字都不匹配。  [JM] 之后的 % 通配符匹配第一个字符之后的任意数目的字符，返回所需结果。</p>
<blockquote>
<p>此通配符可以用前缀字符 <code>^</code> （脱字号）来否定。例如，下面的查询匹配以J 和 M 之外的任意字符起头的任意联系人名（与前一个例子相反）<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT cust_contact</div><div class="line">FROM Customers</div><div class="line">WHERE cust_contact LIKE &apos;[^JM]%&apos;</div><div class="line">ORDER BY cust_contact;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>也可以使用 NOT 操作符得出类似的结果。<code>^</code> 的唯一优点是在使用多个 <code>WHERE</code> 子句时可以简化语法：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT cust_contact</div><div class="line">FROM Customers</div><div class="line">WHERE NOT cust_contact LIKE &apos;[JM]%&apos;</div><div class="line">ORDER BY cust_contact;</div></pre></td></tr></table></figure></p>
<h5 id="使用通配符的技巧"><a href="#使用通配符的技巧" class="headerlink" title="使用通配符的技巧"></a>使用通配符的技巧</h5><p> 通配符应该细心使用，不要过度使用。如果其他操作符能达到相同的目的，应该使用其他操作符。<br>在确实需要使用通配符时，也尽量不要把它们用在搜索模式的开始处。把通配符置于开始处，搜索起来是最慢的。<br>仔细注意通配符的位置。如果放错地方，可能不会返回想要的数据。</p>
<h2 id="创建计算字段"><a href="#创建计算字段" class="headerlink" title="创建计算字段"></a>创建计算字段</h2><h3 id="计算字段"><a href="#计算字段" class="headerlink" title="计算字段"></a>计算字段</h3><blockquote>
<p>计算字段是运行时在 <code>SELECT</code> 语句内创建的。只有数据库知道 SELECT 语句中哪些列是实际的表列，哪些列是计算字段。</p>
</blockquote>
<p> 字段（field）基本上与列（column）的意思相同，经常互换使用，不过数据库列一般称为列，而术语字段通常与计算字段一起使用。</p>
<h3 id="拼接字段"><a href="#拼接字段" class="headerlink" title="拼接字段"></a>拼接字段</h3><blockquote>
<p>拼接（concatenate）<br>将值联结到一起（将一个值附加到另一个值）构成单个值。</p>
</blockquote>
<p>在 SQL中的 <code>SELECT</code> 语句中，可使用一个特殊的操作符<code>+</code>来拼接两个列。DB2、Oracle、PostgreSQL、SQLite和Open Office Base 使用 <code>||</code> 。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_name + &apos; (&apos; + vend_country + &apos;)&apos;</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p> 使用 MySQL或 MariaDB时需要使用的语句：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Concat(vend_name, &apos; (&apos;, vend_country, &apos;)&apos;)</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>结合成一个计算字段的两个列用空格填充。许多数据库（不是所有）保存填充为列宽的文本值，而实际上你要的结果不需要这些空格。为正确返回格式化的数据，必须去掉这些空格。这可以使用 SQL的 RTRIM() 函数来完成，如下所示：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">Bear Emporium (USA)</div><div class="line">Bears R Us (USA)</div><div class="line">Doll House Inc. (USA)</div><div class="line">Fun and Games (England)</div><div class="line">Furball Inc. (USA)</div><div class="line">Jouets et ours (France)</div></pre></td></tr></table></figure>
<blockquote>
<p>大多数 DBMS都支持 <code>RTRIM()</code> （去掉字符串右边的空格）、 <code>LTRIM()</code> （去掉字符串左边的空格）以及 <code>TRIM()</code> （去掉字符串左右两边的空格）。</p>
<h5 id="使用别名"><a href="#使用别名" class="headerlink" title="使用别名"></a>使用别名</h5><p>  SELECT 语句可以很好地拼接地址字段。但是，这个新计算列实际上没有名字，它只是一个值。一个未命名的列不能用于客户端应用中，因为客户端没有办法引用它。<br>为了解决这个问题，SQL 支持列别名。别名（alias）是一个字段或值的<br>替换名。别名用 <code>AS</code> 关键字赋予。请看下面的 <code>SELECT</code> 语句：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">vend_title</div><div class="line">-----------------------------------------------------------</div><div class="line">Bear Emporium (USA)</div><div class="line">Bears R Us (USA)</div><div class="line">Doll House Inc. (USA)</div><div class="line">Fun and Games (England)</div><div class="line">Furball Inc. (USA)</div><div class="line">Jouets et ours (France)</div></pre></td></tr></table></figure>
<p>SELECT 语句本身与以前使用的相同，只不过这里的计算字段之后跟了文本 <code>AS vend_title</code> 。它指示 SQL 创建一个包含指定计算结果的名为<code>vend_title</code> 的计算字段。从输出可以看到，结果与以前的相同，但现在列名为 <code>vend_title</code> ，任何客户端应用都可以按名称引用这个列，就像它是一个实际的表列一样。<br> 别名有时也称为导出列（derived column），不管怎么叫，它们所代表的是相同的东西。别名的名字既可以是一个单词，也可以是一个字符串。如果是后者，字符串应该括在引号中。虽然这种做法是合法的，但不建议这么去做。多单词的名字可读性高，不过会给客户端应用带来各种问题。因此，别名最常见的使用是将多个单词的列名重命名为一个单词的名字。</p>
<h4 id="执行算术计算"><a href="#执行算术计算" class="headerlink" title="执行算术计算"></a>执行算术计算</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">SELECT prod_id,</div><div class="line">quantity,</div><div class="line">item_price,</div><div class="line">quantity*item_price AS expanded_price</div><div class="line">FROM OrderItems</div><div class="line">WHERE order_num = 20008;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">prod_id quantity item_price expanded_price</div><div class="line">---------- ----------- ------------ -----------------</div><div class="line">RGAN0	 1 		5 4.9900 	24.9500</div><div class="line">BR03	 5 		11.9900 	59.9500</div><div class="line">BNBG01 	10 		3.4900 		34.9000</div><div class="line">BNBG02 	10 		3.4900 		34.9000</div><div class="line">BNBG03 	10 		3.4900 		34.9000</div></pre></td></tr></table></figure>
<p> SQL 支持基本算术操作符。此外，圆括号可用来区分优先顺序。<br><code>SELECT</code> 语句为测试、检验函数和计算提供了很好的方法。虽然 <code>SELECT</code>通常用于从表中检索数据，但是省略了 <code>FROM</code> 子句后就是简单地访问和处理表达式，例如 <code>SELECT 3 * 2;</code> 将返回 6， <code>SELECT Trim(&#39; abc &#39;);</code>将返回 <code>abc</code> ， <code>SELECT Now();</code> 使用 <code>Now()</code> 函数返回当前<strong>日期和时间</strong>。</p>
<p>##使用函数处理数据</p>
<h3 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h3><blockquote>
<p>函数一般是在数据上执行的，为数据的转换和处理提供了方便。</p>
</blockquote>
<p>每一个DBMS都有特定的函数，虽然所有类型的函数一般都可以在每个 DBMS中使用，但各个函数的名称和语法可能极其不同。<br>| 函数      | 语法                                       |<br>| :—— | :————————————— |<br>| 提取部分字符串 | Access使用 <code>MID()</code> ；DB2、Oracle、PostgreSQL和SQLite用<code>SUBSTR()</code> ；MySQL和SQL Server使用 <code>SUBSTRING()</code> |<br>| 数据类型转换  | Access和Oracle使用多个函数，每种类型的转换有一个函数；DB2和PostgreSQL使用 <code>CAST()</code> ；MariaDB、MySQL和SQL Server使用 <code>CONVERT()</code> |<br>| 取当前日期   | Access 使 用 <code>NOW()</code>； DB2 和 PostgreSQL 使 用 <code>CURRENT_DATE</code>；MariaDB和MySQL使用 <code>CURDATE() ；</code>Oracle使用<code>SYSDATE</code>；SQL Server使用 <code>GETDATE()</code> ；SQLite使用 <code>DATE()</code> |<br>  与 SQL语句不一样，SQL函数不是可移植的。这意味着为特定 SQL实现编写的代码在其他实现中可能不正常。使用或是不使用函数没有对错之分，如果决定使用函数，应该保证做好代码注释，以便以后你（或其他人）能确切地知道所编写的 SQL代码的含义。</p>
<h3 id="使用函数"><a href="#使用函数" class="headerlink" title="使用函数"></a>使用函数</h3><h4 id="文本处理函数"><a href="#文本处理函数" class="headerlink" title="文本处理函数"></a>文本处理函数</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_name, UPPER(vend_name) AS vend_name_upcase</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">vend_name   		vend_name_upcase</div><div class="line">------------------ ----------------------------</div><div class="line">Bear Emporium		BEAR EMPORIUM</div><div class="line">Bears R Us 			BEARS R US</div><div class="line">Doll House Inc.		DOLL HOUSE INC.</div><div class="line">Fun and Games 		FUN AND GAMES</div></pre></td></tr></table></figure>
<h5 id="常用的文本处理函数"><a href="#常用的文本处理函数" class="headerlink" title="常用的文本处理函数"></a>常用的文本处理函数</h5><table>
<thead>
<tr>
<th>函数</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>LEFT()</code> （或使用子字符串函数）</td>
<td>返回字符串左边的字符</td>
</tr>
<tr>
<td><code>LENGTH()</code> （也使用 <code>DATALENGTH()</code> 或 <code>LEN()</code> ）</td>
<td>返回字符串的长度</td>
</tr>
<tr>
<td><code>LOWER()</code> （Access使用 <code>LCASE()</code> ）</td>
<td>将字符串转换为小写</td>
</tr>
<tr>
<td><code>LTRIM()</code></td>
<td>去掉字符串左边的空格</td>
</tr>
<tr>
<td><code>RIGHT()</code> （或使用子字符串函数）</td>
<td>返回字符串右边的字符</td>
</tr>
<tr>
<td><code>RTRIM()</code></td>
<td>去掉字符串右边的空格</td>
</tr>
<tr>
<td><code>SOUNDEX()</code></td>
<td>返回字符串的<code>SOUNDEX</code>值</td>
</tr>
<tr>
<td><code>UPPER()</code> （Access使用 <code>UCASE()</code> ）</td>
<td>将字符串转换为大写</td>
</tr>
</tbody>
</table>
<p> <code>SOUNDEX</code> 是一个将任何文本串转换为描述其语音表示的字母数字模式的算法。 <code>SOUNDEX</code> 考虑了类似的发音字符和音节，使得能对字符串进行发音比较而不是字母比较。虽然 <code>SOUNDEX</code> 不是 SQL 概念，但多数 DBMS 都提供对 <code>SOUNDEX</code>的支持。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM Customers</div><div class="line">WHERE SOUNDEX(cust_contact) = SOUNDEX(&apos;Michael Green&apos;);</div></pre></td></tr></table></figure></p>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">cust_contact</div><div class="line">--------------------------</div><div class="line">Michelle Green</div></pre></td></tr></table></figure>
<p>  <code>WHERE</code> 子句使用 <code>SOUNDEX()</code> 函数把 <code>cust_contact</code> 列值和搜索字符串转换为它们的 <code>SOUNDEX</code> 值。因为 <code>Michael Green</code> 和<code>Michelle Green</code> 发音相似，所以它们的 <code>SOUNDEX</code> 值匹配，因此 <code>WHERE</code>子句正确地过滤出了所需的数据。</p>
<h4 id="日期和时间处理函数"><a href="#日期和时间处理函数" class="headerlink" title="日期和时间处理函数"></a>日期和时间处理函数</h4><blockquote>
<p>应用程序一般不使用日期和时间的存储格式，因此日期和时间函数总是用来读取、统计和处理这些值。由于这个原因，日期和时间函数在 SQL中具有重要的作用。遗憾的是，它们很不一致，可移植性最差。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"> SELECT order_num</div><div class="line">FROM Orders</div><div class="line">WHERE DATEPART(yy, order_date) = 2012;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>使用了 <code>DATEPART()</code>函数，顾名思义，此函数返回日期的某一部分。 <code>DATEPART()</code> 函数有两个参数，它们<strong>分别是返回的成分和从中返回成分的日期</strong>。在此例子中，<code>DATEPART()</code> 只从 <code>order_date</code> 列中返回年份。通过与 2012 比较， <code>WHERE</code>子句只过滤出此年份的订单。</p>
<h4 id="数值处理函数"><a href="#数值处理函数" class="headerlink" title="数值处理函数"></a>数值处理函数</h4><blockquote>
<p>在主要 DBMS的函数中，数值函数是最一致、最统一的函数。<br>|函 数 |说 明<br>|—-|—————–|<br>|<code>ABS()</code> |返回一个数的绝对值<br>|<code>COS()</code> |返回一个角度的余弦<br>|<code>EXP()</code> |返回一个数的指数值<br>|<code>PI()</code> |返回圆周率<br>|<code>SIN()</code> |返回一个角度的正弦<br>|<code>SQRT()</code>| 返回一个数的平方根<br>|<code>TAN()</code> |返回一个角度的正切</p>
<h2 id="汇总数据"><a href="#汇总数据" class="headerlink" title="汇总数据"></a>汇总数据</h2><p>汇总数据用聚集函数实现。SQL 支持 5 个聚集函数，可以用多种方法使用它们，返回所需的结果。</p>
<h3 id="聚集函数"><a href="#聚集函数" class="headerlink" title="聚集函数"></a>聚集函数</h3><p>对某些行运行的函数，计算并返回一个值 ,SQL给出了 5 个聚集函数。<br>|函 数 |说 明<br>|—|—-<br>|<code>AVG()</code> |返回某列的平均值<br>|<code>COUNT()</code> |返回某列的行数<br>|<code>MAX()</code> |返回某列的最大值<br>|<code>MIN()</code> |返回某列的最小值<br>|<code>SUM()</code> |返回某列值之和</p>
<h4 id="AVG-函数"><a href="#AVG-函数" class="headerlink" title="AVG() 函数"></a>AVG() 函数</h4><p><code>AVG()</code> 通过对表中行数计数并计算其列值之和，求得该列的平均值。 <code>AVG()</code>可用来返回所有列的平均值，也可以用来返回特定列或行的平均值。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT AVG(prod_price) AS avg_price</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>此 <code>SELECT</code> 语句返回值 <code>avg_price</code> ，它包含 Products 表中所有产品的平均价格。<br><strong><code>AVG()</code> 也可以用来确定特定列或行的平均值。</strong>下面的例子返回特定供应商所提供产品的平均价格：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT AVG(prod_price) AS avg_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos;;</div></pre></td></tr></table></figure></p>
<p>这条 <code>SELECT</code> 语句与前一条的不同之处在于，它包含了 <code>WHERE</code> 子句。此<code>WHERE</code> 子句仅过滤出 <code>vend_id</code> 为 <code>DLL01</code> 的产品，因此 <code>avg_price</code> 中返回的值只是该供应商产品的平均值。<br> 只用于单个列<br><code>AVG()</code> 只能用来确定特定数值列的平均值，而且列名必须作为函数参数给出。为了获得多个列的平均值，必须使用多个 <code>AVG()</code> 函数。<code>AVG()</code> 函数忽略列<strong>值为 <code>NULL</code></strong> 的行。</p>
<h4 id="COUNT-函数"><a href="#COUNT-函数" class="headerlink" title="COUNT() 函数"></a>COUNT() 函数</h4><blockquote>
<p>COUNT() 函数进行计数。可利用 COUNT() 确定表中行的数目或符合特定条件的行的数目。</p>
</blockquote>
<p><code>COUNT()</code> 函数有两种使用方式：</p>
<ol>
<li>使用 <code>COUNT(*)</code> 对表中行的数目进行计数，不管表列中包含的是空值<code>NULL</code>还是非空值。</li>
<li>使用 <code>COUNT(column)</code> 对特定列中具有值的行进行计数，忽略 <code>NULL</code> 值。</li>
</ol>
<p>下面的例子返回 Customers 表中顾客的总数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT COUNT(*) AS num_cust</div><div class="line">FROM Customers;</div></pre></td></tr></table></figure></p>
<p> 在此例子中，利用 COUNT(*) 对所有行计数，不管行中各列有什么值。计数值在 <code>num_cust</code> 中返回。</p>
<p>下面的例子只对具有电子邮件地址的客户计数：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT COUNT(cust_email) AS num_cust</div><div class="line">FROM Customers;</div></pre></td></tr></table></figure></p>
<p> 这条 SELECT 语句使用 COUNT(cust_email) 对 cust_email 列中有值的行进行计数。</p>
<h5 id="注意事项-4"><a href="#注意事项-4" class="headerlink" title="注意事项"></a>注意事项</h5><p>  <code>NULL</code> 值<br>如果指定列名，则 <code>COUNT()</code> 函数会忽略指定列的值为空的行，但如果<code>COUNT()</code> 函数中用的是星号<code>（ * ）</code>，则不忽略。</p>
<h4 id="MAX-函数"><a href="#MAX-函数" class="headerlink" title="MAX() 函数"></a>MAX() 函数</h4><blockquote>
<p><code>MAX()</code> 返回指定列中的最大值。 <code>MAX()</code> 要求指定列名，如下所示：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT MAX(prod_price) AS max_price</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<h4 id="MIN-函数"><a href="#MIN-函数" class="headerlink" title="MIN() 函数"></a>MIN() 函数</h4><blockquote>
<p><code>MIN()</code> 的功能正好与 <code>MAX()</code> 功能相反，它返回指定列的最小值。与 <code>MAX()</code>一样， <code>MIN()</code> 要求指定列名，如下所示：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT MIN(prod_price) AS min_price</div><div class="line">FROM Products;</div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="SUM-函数"><a href="#SUM-函数" class="headerlink" title="SUM() 函数"></a>SUM() 函数</h4><blockquote>
<p><code>SUM()</code> 用来返回指定列值的和（总计）。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT SUM(quantity) AS sum_quantity</div><div class="line">FROM OrderItems</div><div class="line">WHERE order_num = 20005;</div></pre></td></tr></table></figure></p>
</blockquote>
<p><code>SUM()</code> 也可以用来合计计算值。在下面的例子中，合计每项物品的<code>item_price*quantity</code> ，得出总的订单金额：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT SUM(item_price*quantity) AS total_price</div><div class="line">FROM OrderItems</div><div class="line">WHERE order_num = 20005;</div></pre></td></tr></table></figure></p>
<p>函数 <code>SUM(item_price*quantity)</code> 返回订单中所有物品价钱之和，<code>WHERE</code> 子句同样保证只统计某个物品订单中的物品。</p>
<h3 id="聚集不同值"><a href="#聚集不同值" class="headerlink" title="聚集不同值"></a>聚集不同值</h3><p> 对所有行执行计算，指定 <code>ALL</code> 参数或不指定参数（因为 <code>ALL</code> 是默认行为）。<br>只包含不同的值，指定 <code>DISTINCT</code> 参数。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT AVG(DISTINCT prod_price) AS avg_price</div><div class="line">FROM Products</div><div class="line">WHERE vend_id = &apos;DLL01&apos;;</div></pre></td></tr></table></figure>
<p> 可以看到，在使用了 <code>DISTINCT</code> 后，此例子中的 <code>avg_price</code> 比较高，因为有多个物品具有相同的较低价格。排除它们提升了平均价格。<br>  <code>DISTINCT</code> 不能用于 <code>COUNT(*)</code>如果指定列名，则 <code>DISTINCT</code> 只能用于 <code>COUNT()</code> 。<br> <code>DISTINCT</code> 不能用于 <code>COUNT(*)</code> 。<br> 类似地， <code>DISTINCT</code> 必须使用列名，不能用于计算或表达式。</p>
<h3 id="组合聚集函数"><a href="#组合聚集函数" class="headerlink" title="组合聚集函数"></a>组合聚集函数</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT COUNT(*) AS num_items,</div><div class="line">MIN(prod_price) AS price_min,</div><div class="line">MAX(prod_price) AS price_max,</div><div class="line">AVG(prod_price) AS price_avg</div><div class="line">FROM Products;</div></pre></td></tr></table></figure>
<p> 取别名在指定别名以包含某个聚集函数的结果时，不应该使用表中实际的列名。虽然这样做也算合法，但许多 SQL实现不支持，可能会产生模糊的错误消息。</p>
<h2 id="分组数据"><a href="#分组数据" class="headerlink" title="分组数据"></a>分组数据</h2><blockquote>
<p>分组数据涉及两个新<code>SELECT</code> 语句子句： <code>GROUP BY</code> 子句和 <code>HAVING</code> 子句。</p>
<h3 id="数据分组"><a href="#数据分组" class="headerlink" title="数据分组"></a>数据分组</h3><p>使用分组可以将数据分为多个逻辑组，对每个组进行聚集计算。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_id, COUNT(*) AS num_prods</div><div class="line">FROM Products</div><div class="line">GROUP BY vend_id;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>结果：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">vend_id num_prods</div><div class="line">------- ---------</div><div class="line">BRS01 	  3</div><div class="line">DLL01 	  4</div><div class="line">FNG01 	  2</div></pre></td></tr></table></figure></p>
<p>上面的 <code>SELECT</code> 语句指定了两个列： <code>vend_id</code> 包含产品供应商的 ID，<code>num_prods</code> 为计算字段（用 <code>COUNT(*)</code> 函数建立）。 <code>GROUP BY</code> 子句指示DBMS按 <code>vend_id</code> 排序并分组数据。这就会对每个 <code>vend_id</code> 而不是整个表计算 <code>num_prods</code> 一次。<br>在使用 <code>GROUP BY</code> 子句前，需要知道一些重要的规定。</p>
<ol>
<li><code>GROUP BY</code> 子句必须出现在 <code>WHERE</code> 子句之后， <code>ORDER BY</code> 子句之前。</li>
<li><code>GROUP BY</code> 子句可以包含任意数目的列，因而可以对分组进行嵌套，更细致地进行数据分组。</li>
<li>如果分组列中包含具有 <code>NULL</code> 值的行，则 <code>NULL</code> 将作为一个分组返回。<br>如果列中有多行 <code>NULL</code> 值，它们将分为一组。</li>
<li><code>GROUP BY</code> 子句中列出的每一列都必须是检索列或有效的表达式（但不能是聚集函数）。如果在 <code>SELECT</code> 中使用表达式，则必须在 <code>GROUP BY</code>子句中指定相同的表达式。不能使用别名<h4 id="过滤分组"><a href="#过滤分组" class="headerlink" title="过滤分组"></a>过滤分组</h4>SQL 允许过滤分组，规定包括哪些分组，排除哪些分组。<blockquote>
<p> <code>HAVING</code> 非常类似于 <code>WHERE</code> ，事实上，目前为止所学过的所有类型的 <code>WHERE</code> 子句都可以用 <code>HAVING</code> 来替代，唯一的差别是， <code>WHERE</code>过滤行，而 <code>HAVING</code> 过滤分组。</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_id, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> orders</div><div class="line"><span class="keyword">FROM</span> Orders</div><div class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> cust_id</div><div class="line"><span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(*) &gt;= <span class="number">2</span>;</div></pre></td></tr></table></figure>
</blockquote>
</li>
</ol>
<p>结果：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">cust_id 	orders</div><div class="line">---------- -----------</div><div class="line">1000000001  2</div></pre></td></tr></table></figure></p>
<p>最后一行增加了 <code>HAVING</code>子句，它过滤 <code>COUNT(*) &gt;= 2</code> （两个以上订单）的那些分组。<code>WHERE</code> 子句在这里不起作用，因为过滤是基于<strong>分组</strong>聚集值，而不是特定行的值。</p>
<h5 id="注意事项-5"><a href="#注意事项-5" class="headerlink" title="注意事项"></a>注意事项</h5><p>  HAVING 和 WHERE 的差别<br> <code>WHERE</code> 在数据分组前进行过滤，<code>HAVING</code> 在数据分组后进行过滤。<br>  <code>WHERE</code> 排除的行不包括在分组中。这可能会改变计算值，从而影响 <code>HAVING</code> 子句中基于这些值过滤掉的分组。<br>  <code>HAVING</code> 与 <code>WHERE</code> 非常类似，如果不指定 <code>GROUP BY</code> ，则大多数 DBMS会同等对待它们。使用 <code>HAVING</code> 时应该结合 <code>GROUP BY</code> 子句，而 <code>WHERE</code> 子句用于标准的行级过滤。</p>
<p>  同时使用 <code>WHERE</code> 和 <code>HAVING</code> 子句，返回过去 12个月内具有两个以上订单的顾客。为此，可增加一条 <code>WHERE</code> 子句，过滤出过去 12个月内下过的订单，然后再增加<code>HAVING</code> 子句过滤出具有两个以上订单的分组。</p>
<p>  列出具有两个以上产品且其价格大于等于 4 的供应商：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> vend_id, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> num_prods</div><div class="line"><span class="keyword">FROM</span> Products</div><div class="line"><span class="keyword">WHERE</span> prod_price &gt;= <span class="number">4</span></div><div class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> vend_id</div><div class="line"><span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(*) &gt;= <span class="number">2</span>;</div></pre></td></tr></table></figure>
<p>  <code>WHERE</code> 子句过滤所有 <code>prod_price</code> 至少为 4 的行，然后按 <code>vend_id</code>分组数据，<code>HAVING</code> 子句过滤计数为 2 或 2 以上的分组。</p>
<h4 id="分组和排序"><a href="#分组和排序" class="headerlink" title="分组和排序"></a>分组和排序</h4><blockquote>
<p>GROUP BY 和 ORDER BY 经常完成相同的工作，但它们非常不同，理解这一点很重要。<br>|<code>ORDER BY</code>| <code>GROUP BY</code>|<br>|:—|:—|<br>|对产生的输出排序| 对行分组，但输出可能不是分组的顺序<br>|任意列都可以使用（甚至非选择的列也可以使用）|只可能使用选择列或表达式列，而且<code>必须使用每个选择列表达式</code><br>|不一定需要 |如果与聚集函数一起使用列（或表达式），则必须使用<br> 一般在使用 <code>GROUP BY</code> 子句时，应该也给出 <code>ORDER BY</code> 子句。这是保证数据正确排序的唯一方法。千万不要仅依赖 <code>GROUP BY</code> 排序数据。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> order_num, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> items</div><div class="line"><span class="keyword">FROM</span> OrderItems</div><div class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> order_num</div><div class="line"><span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(*) &gt;= <span class="number">3</span>;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">order_num items</div><div class="line">--------- -----</div><div class="line"> 20006 		3</div><div class="line"> 20007 		5</div><div class="line"> 20008 		5</div><div class="line"> 20009 		3</div></pre></td></tr></table></figure>
<p>要按订购物品的数目排序输出，需要添加 <code>ORDER BY</code> 子句，如下所示：<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> order_num, <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> items</div><div class="line"><span class="keyword">FROM</span> OrderItems</div><div class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> order_num</div><div class="line"><span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(*) &gt;= <span class="number">3</span></div><div class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> items, order_num;</div></pre></td></tr></table></figure></p>
<p>结果：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">order_num items</div><div class="line">--------- -----</div><div class="line"> 20006 		3</div><div class="line"> 20009 		3</div><div class="line"> 20007 		5</div><div class="line"> 20008 		5</div></pre></td></tr></table></figure>
<p>使用 <code>GROUP BY</code> 子句按订单号（ <code>order_num</code> 列）分组数据，以便 <code>COUNT(*)</code> 函数能够返回每个订单中的物品数目。 <code>HAVING</code> 子句过滤数据，使得只返回包含三个或更多物品的订单。最后，用 <code>ORDER BY</code>子句排序输出。</p>
<h4 id="SELECT-子句顺序"><a href="#SELECT-子句顺序" class="headerlink" title="SELECT 子句顺序"></a>SELECT 子句顺序</h4><table>
<thead>
<tr>
<th style="text-align:center">子 句</th>
<th style="text-align:center">说 明</th>
<th style="text-align:center">是否必须使用</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><code>SELECT</code></td>
<td style="text-align:center">要返回的列或表达式</td>
<td style="text-align:center">是</td>
</tr>
<tr>
<td style="text-align:center"><code>FROM</code></td>
<td style="text-align:center">从中检索数据的表</td>
<td style="text-align:center">仅在从表选择数据时使用</td>
</tr>
<tr>
<td style="text-align:center"><code>WHERE</code></td>
<td style="text-align:center">行级过滤</td>
<td style="text-align:center">否</td>
</tr>
<tr>
<td style="text-align:center"><code>GROUP BY</code></td>
<td style="text-align:center">分组说明</td>
<td style="text-align:center">仅在按组计算聚集时使用</td>
</tr>
<tr>
<td style="text-align:center"><code>HAVING</code></td>
<td style="text-align:center">组级过滤</td>
<td style="text-align:center">否</td>
</tr>
<tr>
<td style="text-align:center"><code>ORDER BY</code></td>
<td style="text-align:center">输出排序顺序</td>
<td style="text-align:center">否</td>
</tr>
</tbody>
</table>
<h2 id="使用子查询"><a href="#使用子查询" class="headerlink" title="使用子查询"></a>使用子查询</h2><blockquote>
<p>子查询常用于 <code>WHERE</code> 子句的 <code>IN</code> 操作符中，以及用来填充计算列。</p>
<h3 id="子查询"><a href="#子查询" class="headerlink" title="子查询"></a>子查询</h3><p>查询（query）<br>任何 SQL语句都是查询。但此术语一般指 <code>SELECT</code> 语句。<br>SQL还允许创建子查询（subquery），即嵌套在其他查询中的查询。</p>
<h3 id="利用子查询进行过滤"><a href="#利用子查询进行过滤" class="headerlink" title="利用子查询进行过滤"></a>利用子查询进行过滤</h3><p>对 <code>prod_id</code> 为 <code>RGAN01</code> 的所有订单物品，检索其 <code>order_num</code> 列。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">输入</div><div class="line"><span class="keyword">SELECT</span> order_num</div><div class="line"><span class="keyword">FROM</span> OrderItems</div><div class="line"><span class="keyword">WHERE</span> prod_id = <span class="string">'RGAN01'</span>;</div><div class="line">输出</div><div class="line">order_num</div><div class="line"><span class="comment">-----------</span></div><div class="line">20007</div><div class="line">20008</div></pre></td></tr></table></figure></p>
</blockquote>
<p>下一步查询与订单 20007和 20008 相关的顾客ID。</p>
<p>输入<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_id</div><div class="line"><span class="keyword">FROM</span> Orders</div><div class="line"><span class="keyword">WHERE</span> order_num <span class="keyword">IN</span> (<span class="number">20007</span>,<span class="number">20008</span>);</div></pre></td></tr></table></figure></p>
<p>输出<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">cust_id</div><div class="line">----------</div><div class="line">1000000004</div><div class="line">1000000005</div></pre></td></tr></table></figure></p>
<p>结合这两个查询，把第一个查询（返回订单号的那一个）变为子查询。</p>
<p>输入<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_id</div><div class="line"><span class="keyword">FROM</span> Orders</div><div class="line"><span class="keyword">WHERE</span> order_num <span class="keyword">IN</span> (<span class="keyword">SELECT</span> order_num</div><div class="line">	<span class="keyword">FROM</span> OrderItems</div><div class="line">	<span class="keyword">WHERE</span> prod_id = <span class="string">'RGAN01'</span>);</div></pre></td></tr></table></figure></p>
<p>输出<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">cust_id</div><div class="line">----------</div><div class="line">1000000004</div><div class="line">1000000005</div></pre></td></tr></table></figure></p>
<p>在 SELECT 语句中，子查询总是从内向外处理。首先，它执行下面的查询：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SELECT order_num FROM orderitems WHERE prod_id=&apos;RGAN01&apos;</div></pre></td></tr></table></figure>
<p>此查询返回两个订单号： 20007 和 20008 。然后，这两个值以 <code>IN</code> 操作符要求的逗号分隔的格式传递给外部查询的 <code>WHERE</code> 子句。外部查询变成：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SELECT cust_id FROM orders WHERE order_num IN (20007,20008)</div></pre></td></tr></table></figure>
<p>输出是正确的，与前面硬编码 <code>WHERE</code> 子句所返回的值相同。</p>
<p>现在得到了订购物品 <code>RGAN01</code> 的所有顾客的 ID。下一步是检索这些顾客<br>ID的顾客信息。检索两列的 SQL语句为：<br>输入</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_name, cust_contact</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">WHERE</span> cust_id <span class="keyword">IN</span> (<span class="string">'1000000004'</span>,<span class="string">'1000000005'</span>);</div></pre></td></tr></table></figure>
<p>可以把其中的 <code>WHERE</code> 子句转换为子查询，而不是硬编码这些顾客 ID:<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_name, cust_contact</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">WHERE</span> cust_id <span class="keyword">IN</span> (<span class="keyword">SELECT</span> cust_id</div><div class="line">	<span class="keyword">FROM</span> Orders</div><div class="line">	<span class="keyword">WHERE</span> order_num <span class="keyword">IN</span> (<span class="keyword">SELECT</span> order_num</div><div class="line">		<span class="keyword">FROM</span> OrderItems</div><div class="line">		<span class="keyword">WHERE</span> prod_id = <span class="string">'RGAN01'</span>));</div></pre></td></tr></table></figure></p>
<p>输出</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">cust_name 	cust_contact</div><div class="line">---------- ------------------</div><div class="line">Fun4All 	Denise L. Stephens</div><div class="line">The Toy 	Store Kim Howard</div></pre></td></tr></table></figure>
<p>为了执行上述 <code>SELECT</code> 语句，DBMS实际上必须执行三条 <code>SELECT</code> 语句。最里边的子查询返回订单号列表，此列表用于其外面的子查询的 <code>WHERE</code>子句。外面的子查询返回顾客 <code>ID</code> 列表，此顾客 <code>ID</code> 列表用于最外层查询的 <code>WHERE</code> 子句。最外层查询返回所需的数据。<br> 只能是单列作为子查询的 SELECT 语句只能查询单个列。企图检索多个列将返回错误。</p>
<h3 id="作为计算字段使用子查询"><a href="#作为计算字段使用子查询" class="headerlink" title="作为计算字段使用子查询"></a>作为计算字段使用子查询</h3><p>输入<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_name,</div><div class="line">	   cust_state,</div><div class="line">	   (<span class="keyword">SELECT</span> <span class="keyword">COUNT</span>(*)</div><div class="line">		<span class="keyword">FROM</span> Orders</div><div class="line">		<span class="keyword">WHERE</span> Orders.cust_id = Customers.cust_id) <span class="keyword">AS</span> orders</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> cust_name;</div></pre></td></tr></table></figure></p>
<p>输出</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">cust_name 	cust_state 	orders</div><div class="line">--------- 	----------- ------</div><div class="line">Fun4All 	IN 			  1</div><div class="line">Fun4All 	AZ 			  1</div><div class="line">Kids 		Place OH 	  0</div><div class="line">The 		Toy Store IL  1</div><div class="line">Village 	Toys MI 	  2</div></pre></td></tr></table></figure>
<p> <code>orders</code> 是一个计算字段，它是由圆括号中的子查询建立的。该子查询对检索出的每个顾客执行一次。<br> 子查询中的 <code>WHERE</code> 子句与前面使用的 <code>WHERE</code> 子句稍有不同，因为它使用了完全限定列名，指定表名和列名（ <code>Orders.cust_id</code> 和 <code>Customers.cust_id</code> ）。<br> 在这个例子中，有两个 <code>cust_id</code> 列：一个在 <code>Customers</code> 中，另一个在<code>Orders</code> 中。如果不采用完全限定列名，DBMS会认为要对 <code>Orders</code> 表中的 <code>cust_id</code> 自身进行比较。</p>
<h2 id="联结表"><a href="#联结表" class="headerlink" title="联结表"></a>联结表</h2><p>联结是 SQL中一个最重要、最强大的特性。</p>
<h3 id="联结"><a href="#联结" class="headerlink" title="联结"></a>联结</h3><blockquote>
<p>联结是利用 SQL的 <code>SELECT</code> 能执行的最重要的操作，很好地理解联结及其语法是学习 SQL的极为重要的部分。</p>
<h4 id="关系表"><a href="#关系表" class="headerlink" title="关系表"></a>关系表</h4><p>关系数据可以有效地存储，方便地处理，避免信息重复，关系数据库的可伸缩性远比非关系数据库要好，这是关系数据库设计的基础。关系表的设计就是要把信息分解成多个表，一类数据一表。各表通过某些共同的值互相关联（所以才叫关系数据库）。</p>
</blockquote>
<p> 比如，一个存储供应商信息，另一个存储产品信息。 <code>Vendors</code> 表包含所有供应商信息，每个供应商占一行，具有唯一的标识。此标识称为主键（<code>primary key</code>），可以是供应商 ID 或任何其他唯一值。<br><code>Products</code> 表只存储产品信息，除了存储供应商 ID（ <code>Vendors</code> 表的主键）外，它不存储其他有关供应商的信息。 <code>Vendors</code> 表的主键将 <code>Vendors</code> 表与 <code>Products</code> 表关联，利用供应商 ID 能从 <code>Vendors</code> 表中找出相应供应商的详细信息。</p>
<h4 id="为什么使用联结"><a href="#为什么使用联结" class="headerlink" title="为什么使用联结"></a>为什么使用联结</h4><blockquote>
<p>联结是一种机制，数据存储在多个表中，用来在一条 <code>SELECT</code> 语句中关联表，因此称为联结。使用特殊的语法，可以联结多个表返回一组输出，联结在运行时关联表中正确的行。</p>
<h3 id="创建联结"><a href="#创建联结" class="headerlink" title="创建联结"></a>创建联结</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_name, prod_name, prod_price</div><div class="line">FROM Vendors, Products</div><div class="line">WHERE Vendors.vend_id = Products.vend_id;</div></pre></td></tr></table></figure>
</blockquote>
<p>这条语句的 <code>FROM</code>子句列出了两个表： <code>Vendors</code> 和 <code>Products</code> 。它们就是这条 <code>SELECT</code> 语句联结的两个表的名字。这两个表用 <code>WHERE</code> 子句正确地联结， <code>WHERE</code> 子句指示 DBMS将 <code>Vendors</code> 表中的 <code>vend_id</code> 与 <code>Products</code> 表中的 <code>vend_id</code> 匹配起来。</p>
<h4 id="WHERE-子句的重要性"><a href="#WHERE-子句的重要性" class="headerlink" title="WHERE 子句的重要性"></a>WHERE 子句的重要性</h4><p>在联结两个表时，实际要做的是将第一个表中的每一行与第二个表中的每一行配对。 <code>WHERE</code> 子句作为过滤条件，只包含那些匹配给定条件（这里是联结条件）的行。<br> 要保证所有联结都有 <code>WHERE</code> 子句，否则 DBMS将返回比想要的数据多得多的数据。同理，要保证 <code>WHERE</code> 子句的正确性。</p>
<h4 id="内联结"><a href="#内联结" class="headerlink" title="内联结"></a>内联结</h4><p>目前为止使用的联结称为等值联结（equijoin），它基于两个表之间的相等测试。这种联结也称为内联结（inner join）。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT vend_name, prod_name, prod_price</div><div class="line">FROM Vendors INNER JOIN Products</div><div class="line">ON Vendors.vend_id = Products.vend_id;</div></pre></td></tr></table></figure>
<p>这里，两个表之间的关系是以 <code>INNER JOIN</code> 指定的部分 <code>FROM</code> 子句。在使用这种语法时，联结条件用特定的 <code>ON</code> 子句而不是 <code>WHERE</code> 子句给出。传递给 <code>ON</code> 的实际条件与传递给 <code>WHERE</code> 的相同。</p>
<h4 id="联结多个表"><a href="#联结多个表" class="headerlink" title="联结多个表"></a>联结多个表</h4><p>SQL 不限制一条 <code>SELECT</code> 语句中可以联结的表的数目。创建联结的基本规则也相同。首先列出所有表，然后定义表之间的关系。例如：<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> prod_name, vend_name, prod_price, quantity</div><div class="line"><span class="keyword">FROM</span>  OrderItems, Products, Vendors</div><div class="line"><span class="keyword">WHERE</span> Products.vend_id = Vendors.vend_id</div><div class="line"><span class="keyword">AND</span>   Products.prod_id = OrderItems.prod_id</div><div class="line"><span class="keyword">AND</span>   order_num = <span class="number">20007</span>;</div></pre></td></tr></table></figure></p>
<p> 这个例子显示订单 20007 中的物品。订单物品存储在 <code>OrderItems</code> 表中。每个产品按其产品 ID存储，它引用 <code>Products</code> 表中的产品。这些产品通过供应商 ID联结到 <code>Vendors</code> 表中相应的供应商，供应商 ID存储在每个产品的记录中。这里的 <code>FROM</code> 子句列出三个表， <code>WHERE</code> 子句定义这两个联结条件，而第三个联结条件用来过滤出订单 20007 中的物品。</p>
<p> 注意，不要联结不必要的表。联结的表越多，性能下降越厉害。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">SELECT</span> cust_name, cust_contact</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">WHERE</span> cust_id <span class="keyword">IN</span> (<span class="keyword">SELECT</span> cust_id</div><div class="line">	<span class="keyword">FROM</span> Orders</div><div class="line">	<span class="keyword">WHERE</span> order_num <span class="keyword">IN</span> (<span class="keyword">SELECT</span> order_num</div><div class="line">		<span class="keyword">FROM</span> OrderItems</div><div class="line">		<span class="keyword">WHERE</span> prod_id = <span class="string">'RGAN01'</span>));</div></pre></td></tr></table></figure></p>
<p>子查询并不总是执行复杂 <code>SELECT</code> 操作的最有效方法，下面是使用联结的相同查询：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM Customers, Orders, OrderItems</div><div class="line">WHERE Customers.cust_id = Orders.cust_id</div><div class="line">AND OrderItems.order_num = Orders.order_num</div><div class="line">AND prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure></p>
<h2 id="创建高级联结"><a href="#创建高级联结" class="headerlink" title="创建高级联结"></a>创建高级联结</h2><h3 id="使用表别名"><a href="#使用表别名" class="headerlink" title="使用表别名"></a>使用表别名</h3><p>给列起别名的语法如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure></p>
<p>SQL 除了可以对列名和计算字段使用别名，还允许给表名起别名。这样做有两个主要理由：</p>
<ol>
<li>缩短 SQL语句；</li>
<li>允许在一条 SELECT 语句中多次使用相同的表。<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM  Customers AS C, Orders AS O, OrderItems AS OI</div><div class="line">WHERE C.cust_id = O.cust_id</div><div class="line">AND   OI.order_num = O.order_num</div><div class="line">AND   prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure>
</li>
</ol>
<p>在这个例子中，表别名只用于 WHERE 子句。其实它不仅能用于 WHERE 子句，还可以用于 SELECT 的列表、 ORDER BY 子句及其他语句部分。<br> 表别名只在查询执行中使用。与列别名不一样，表别名不返回到客户端。</p>
<h3 id="使用不同类型的联结"><a href="#使用不同类型的联结" class="headerlink" title="使用不同类型的联结"></a>使用不同类型的联结</h3><p>除了内联结或等值联结的简单联结外，还有其他联结：自联结（self-join）、自然联结（natural join）和外联结（outer join）。</p>
<h4 id="自联结"><a href="#自联结" class="headerlink" title="自联结"></a>自联结</h4><p>使用表别名的一个主要原因是能在一条 <code>SELECT</code> 语句中不止一次引用相同的表。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT c1.cust_id, c1.cust_name, c1.cust_contact</div><div class="line">FROM Customers AS c1, Customers AS c2</div><div class="line">WHERE c1.cust_name = c2.cust_name</div><div class="line">AND c2.cust_contact = &apos;Jim Jones&apos;;</div></pre></td></tr></table></figure></p>
<p> Oracle 中没有 <code>AS</code>，Oracle用户应该记住去掉 <code>AS</code> 。</p>
<p>此查询中需要的两个表实际上是相同的表，因此 <code>Customers</code> 表在 <code>FROM</code>子句中出现了两次。虽然这是完全合法的，但对 <code>Customers</code> 的引用具有歧义性，因为 DBMS不知道你引用的是哪个 <code>Customers</code> 表。<br>解决此问题，需要使用表别名。 <code>Customers</code> 第一次出现用了别名 <code>C1</code> ，第二次出现用了别名 <code>C2</code> 。现在可以将这些别名用作表名。例如， <code>SELECT</code> 语句使用 <code>C1</code> 前缀明确给出所需列的全名。<br> 用自联结而不用子查询<br>自联结通常作为外部语句，用来替代从相同表中检索数据的使用子查询语句。虽然最终的结果是相同的，但许多 DBMS处理联结远比处理子查询快得多。</p>
<h4 id="自然联结"><a href="#自然联结" class="headerlink" title="自然联结"></a>自然联结</h4><blockquote>
<p>无论何时对表进行联结，应该至少有一列不止出现在一个表中。标准的联结返回所有数据，相同的列甚至多次出现。自然联结排除多次出现，使每一列只返回一次。</p>
</blockquote>
<p>自然联结要求只能选择那些唯一的列，一般通过对一个表使用通配符（ <code>SELECT *</code> ），而对其他表的列使用明确的子集来完成。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">SELECT C.*, O.order_num, O.order_date,</div><div class="line">OI.prod_id, OI.quantity, OI.item_price</div><div class="line">FROM Customers AS C, Orders AS O, OrderItems AS OI</div><div class="line">WHERE C.cust_id = O.cust_id</div><div class="line">AND OI.order_num = O.order_num</div><div class="line">AND prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure></p>
<p>在这个例子中，通配符只对第一个表使用。所有其他列明确列出，所以没有重复的列被检索出来。</p>
<h4 id="外联结"><a href="#外联结" class="headerlink" title="外联结"></a>外联结</h4><p>许多联结将一个表中的行与另一个表中的行相关联，但有时候需要包含没有关联行的那些行。<br>下面的 <code>SELECT</code> 语句给出了一个简单的内联结。它检索所有顾客及其订单：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id, Orders.order_num</div><div class="line">FROM Customers INNER JOIN Orders</div><div class="line">ON Customers.cust_id = Orders.cust_id;</div></pre></td></tr></table></figure></p>
<p>外联结语法类似。要检索<strong>包括没有订单顾客在内的所有顾客</strong>，可如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id, Orders.order_num</div><div class="line">FROM Customers LEFT OUTER JOIN Orders</div><div class="line">ON Customers.cust_id = Orders.cust_id;</div></pre></td></tr></table></figure></p>
<p>这条 <code>SELECT</code> 语句使用了关键字 <code>OUTER JOIN</code>来指定联结类型（而不是在 <code>WHERE</code> 子句中指定）。但是，与内联结关联两个表中的行不同的是，<strong>外联结还包括没有关联行的行。</strong><mark>在使用 <code>OUTERJOIN</code> 语法时，必须使用 <code>RIGHT</code> 或 <code>LEFT</code> 关键字指定包括其所有行的表</mark>（ <code>RIGHT</code> 指出的是 <code>OUTER JOIN</code> 右边的表，而 <code>LEFT</code> 指出的是 <code>OUTER JOIN</code>左边的表）。上面的例子使用 <code>LEFT OUTER JOIN</code> 从 <code>FROM</code> 子句左边的表（ <code>Customers</code> 表）中选择所有行。为了从右边的表中选择所有行，需要使用 <code>RIGHT OUTER JOIN</code> ，如下例所示：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id, Orders.order_num</div><div class="line">FROM Customers RIGHT OUTER JOIN Orders</div><div class="line">ON Orders.cust_id = Customers.cust_id;</div></pre></td></tr></table></figure>
<p> 总是有两种基本的外联结形式：左外联结和右外联结。它们之间的唯一差别是所关联的表的顺序。换句话说，调整 <code>FROM</code> 或 <code>WHERE</code>子句中<strong>表的顺序</strong>，左外联结可以转换为右外联结。因此，这两种外联结可以互换使用，哪个方便就用哪个。</p>
<p>还存在另一种外联结，就是<strong>全外联结</strong>（full outer join），<strong>它检索两个表中的所有行并关联那些可以关联的行。</strong>与左外联结或右外联结包含一个表的不关联的行不同，<strong>全外联结包含两个表的不关联的行</strong>。全外联结的语法如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id, Orders.order_num</div><div class="line">FROM Orders FULL OUTER JOIN Customers</div><div class="line">ON Orders.cust_id = Customers.cust_id;</div></pre></td></tr></table></figure></p>
<h3 id="使用带聚集函数的联结"><a href="#使用带聚集函数的联结" class="headerlink" title="使用带聚集函数的联结"></a>使用带聚集函数的联结</h3><blockquote>
<p>聚集函数可以从一个表中汇总数据，但这些函数也可以与联结一起使用。</p>
</blockquote>
<p>检索所有顾客及每个顾客所下的订单数，下面的代码使用 <code>COUNT()</code> 函数完成此工作：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id,</div><div class="line">COUNT(Orders.order_num) AS num_ord</div><div class="line">FROM Customers INNER JOIN Orders</div><div class="line">ON Customers.cust_id = Orders.cust_id</div><div class="line">GROUP BY Customers.cust_id;</div></pre></td></tr></table></figure></p>
<p>这条 <code>SELECT</code> 语句使用 <code>INNER JOIN</code> 将 <code>Customers</code> 和 <code>Orders</code> 表互相关联。<code>GROUP BY</code> 子句按顾客分组数据，因此，函数调用<code>COUNT(Orders.order_num)</code>对每个顾客的订单计数，将它作为 <code>num_ord</code> 返回。</p>
<p>聚集函数也可以方便地与其他联结一起使用。请看下面的例子：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT Customers.cust_id,</div><div class="line">COUNT(Orders.order_num) AS num_ord</div><div class="line">FROM Customers LEFT OUTER JOIN Orders</div><div class="line">ON Customers.cust_id = Orders.cust_id</div><div class="line">GROUP BY Customers.cust_id;</div></pre></td></tr></table></figure></p>
<p>这个例子使用左外部联结来包含所有顾客，甚至包含那些没有任何订单的顾客。</p>
<h3 id="使用联结和联结条件"><a href="#使用联结和联结条件" class="headerlink" title="使用联结和联结条件"></a>使用联结和联结条件</h3><p>联结及其使用的要点</p>
<ol>
<li>注意所使用的联结类型。一般我们使用内联结，但使用外联结也有效。</li>
<li>保证使用正确的联结条件（不管采用哪种语法），否则会返回不正确的数据。</li>
<li>应该总是提供联结条件，否则会得出笛卡儿积</li>
<li>在一个联结中可以包含多个表，甚至可以对每个联结采用不同的联结类型。虽然这样做是合法的，一般也很有用，但应该在一起测试它们前分别测试每个联结。这会使故障排除更为简单。<h2 id="组合查询"><a href="#组合查询" class="headerlink" title="组合查询"></a>组合查询</h2><blockquote>
<p>利用 UNION 操作符将多条 <code>SELECT</code> 语句组合成一个结果集。用 <code>UNION</code> 可极大地简化复杂的 <code>WHERE</code> 子句，简化从多个表中检索数据的工作。</p>
<h3 id="组合查询-1"><a href="#组合查询-1" class="headerlink" title="组合查询"></a>组合查询</h3><p>SQL允许执行多个查询（多条 <code>SELECT</code> 语句），并将结果作为一个查询结果集返回。这些组合查询通常称为并（<code>union</code>）或复合查询（compound query）。</p>
</blockquote>
</li>
</ol>
<p>主要有两种情况需要使用组合查询：</p>
<ol>
<li>在一个查询中从不同的表返回结构数据；</li>
<li><p>对一个表执行多个查询，按一个查询返回数据。</p>
<p>任何具有多个<code>WHERE</code> 子句的 <code>SELECT</code> 语句都可以作为一个组合查询。</p>
</li>
</ol>
<h3 id="使用UNION"><a href="#使用UNION" class="headerlink" title="使用UNION"></a>使用UNION</h3><p>可用 <code>UNION</code> 操作符来组合数条 SQL 查询。利用 <code>UNION</code> ，可给出多条<code>SELECT</code> 语句，将它们的结果组合成一个结果集。使用 <code>UNION</code> 很简单，所要做的只是给出每条 <code>SELECT</code> 语句，在各条语句之间放上关键字 <code>UNION</code> 。</p>
<p>创建 <code>UNION</code> 涉及编写多条 <code>SELECT</code> 语句。首先来看单条语句：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_state IN (&apos;IL&apos;,&apos;IN&apos;,&apos;MI&apos;);</div></pre></td></tr></table></figure></p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_name = &apos;Fun4All&apos;;</div></pre></td></tr></table></figure>
<p>第一条 <code>SELECT</code> 把 <code>Illinois</code>、<code>Indiana</code>、<code>Michigan</code>等州的缩写传递给 <code>IN</code> 子句，检索出这些州的所有行。第二条 <code>SELECT</code> 利用简单的相等测试找出所有<code>Fun4All</code> 。<br>组合这两条语句，可以如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_state IN (&apos;IL&apos;,&apos;IN&apos;,&apos;MI&apos;)</div><div class="line">UNION</div><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_name = &apos;Fun4All&apos;;</div></pre></td></tr></table></figure></p>
<p>这条语句由前面的两条 <code>SELECT</code> 语句组成，之间用 <code>UNION</code> 关键字分隔。<code>UNION</code> 指示 DBMS执行这两条 <code>SELECT</code> 语句，并把输出组合成一个查询结果并集。</p>
<p>为了便于参考，这里给出使用多条 <code>WHERE</code> 子句而不是 <code>UNION</code> 的相同查询：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_state IN (&apos;IL&apos;,&apos;IN&apos;,&apos;MI&apos;)</div><div class="line">OR cust_name = &apos;Fun4All&apos;;</div></pre></td></tr></table></figure></p>
<p>#### </p>
<ol>
<li><code>UNION</code> 必须由两条或两条以上的 <code>SELECT</code> 语句组成，语句之间用关键字 <code>UNION</code> 分隔（因此，如果组合四条 <code>SELECT</code> 语句，将要使用三个 <code>UNION</code>关键字）。</li>
<li><code>UNION</code> 中的每个查询必须包含相同的列、表达式或聚集函数（不过，各个列不需要以相同的次序列出）。</li>
<li>列数据类型必须兼容：类型不必完全相同，但必须是 DBMS可以隐含转换的类型（例如，不同的数值类型或不同的日期类型）。<h4 id="包含或取消重复的行"><a href="#包含或取消重复的行" class="headerlink" title="包含或取消重复的行"></a>包含或取消重复的行</h4><code>UNION</code> 从查询结果集中自动去除了重复的行，这是 <code>UNION</code> 的默认行为；换句话说，它的行为与一条 <code>SELECT</code> 语句中使用多个 <code>WHERE</code> 子句条件一样。如果想返回所有的匹配行，可使用 <code>UNION ALL</code> 而不是 <code>UNION</code> 。<br><code>UNION</code> 几乎总是完成与多个 <code>WHERE</code> 条件相同的工作。 <code>UNION ALL</code> 为 <code>UNION</code> 的一种形式，它完成 <code>WHERE</code> 子句完成不了的工作。如果确实需要每个条件的匹配行全部出现（包括重复行），就必须使用 <code>UNION ALL</code> ，而不是 <code>WHERE</code> 。<h4 id="对组合查询结果排序"><a href="#对组合查询结果排序" class="headerlink" title="对组合查询结果排序"></a>对组合查询结果排序</h4><blockquote>
<p><code>SELECT</code> 语句的输出用 <code>ORDER BY</code> 子句排序。在用 <code>UNION</code> 组合查询时，只能使用一条 <code>ORDER BY</code> 子句，它必须位于最后一条 <code>SELECT</code> 语句之后，不允许使用多条 <code>ORDER BY</code> 子句。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_state IN (&apos;IL&apos;,&apos;IN&apos;,&apos;MI&apos;)</div><div class="line">UNION</div><div class="line">SELECT cust_name, cust_contact, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_name = &apos;Fun4All&apos;</div><div class="line">ORDER BY cust_name, cust_contact;</div></pre></td></tr></table></figure>
</blockquote>
</li>
</ol>
<h2 id="插入数据"><a href="#插入数据" class="headerlink" title="插入数据"></a>插入数据</h2><h3 id="数据插入"><a href="#数据插入" class="headerlink" title="数据插入"></a>数据插入</h3><p>INSERT 用来将行插入（或添加）到数据库表。插入有几种方式：</p>
<ol>
<li>插入完整的行；</li>
<li>插入行的一部分；</li>
<li>插入某些查询的结果。<h4 id="插入完整的行"><a href="#插入完整的行" class="headerlink" title="插入完整的行"></a>插入完整的行</h4>要求指定表名和插入到新行中的值。</li>
</ol>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> Customers</div><div class="line"><span class="keyword">VALUES</span>(<span class="string">'1000000006'</span>,</div><div class="line">	<span class="string">'Toy Land'</span>,</div><div class="line">	<span class="string">'123 Any Street'</span>,</div><div class="line">	<span class="string">'New York'</span>,</div><div class="line">	<span class="string">'NY'</span>,</div><div class="line">	<span class="string">'11111'</span>,</div><div class="line">	<span class="string">'USA'</span>,</div><div class="line">	<span class="literal">NULL</span>,</div><div class="line">	<span class="literal">NULL</span>);</div></pre></td></tr></table></figure>
<p>这个例子将一个新顾客插入到 <code>Customers</code> 表中。存储到表中每一列的数据在 <code>VALUES</code> 子句中给出，必须给每一列提供一个值。如果某列没有值，如上面的 <code>cust_contact</code> 和 <code>cust_email</code> 列，则应该使用 <code>NULL</code> 值（假定表允许对该列指定空值）。各列必须以它们在表定义中出现的次序填充。</p>
<p>上面的 SQL语句高度依赖于表中列的定义次序，还依赖于其容易获得的次序信息。即使可以得到这种次序信息，也不能保证各列在下一次表结构变动后保持完全相同的次序。<br>编写 INSERT 语句的更安全（不过更烦琐）的方法如下：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line">INSERT INTO Customers(cust_id,</div><div class="line">	cust_name,</div><div class="line">	cust_address,</div><div class="line">	cust_city,</div><div class="line">	cust_state,</div><div class="line">	cust_zip,</div><div class="line">	cust_country,</div><div class="line">	cust_contact,</div><div class="line">	cust_email)</div><div class="line">VALUES(&apos;1000000006&apos;,</div><div class="line">	   &apos;Toy Land&apos;,</div><div class="line">	   &apos;123 Any Street&apos;,</div><div class="line">	   &apos;New York&apos;,</div><div class="line">	   &apos;NY&apos;,</div><div class="line">	   &apos;11111&apos;,</div><div class="line">	   &apos;USA&apos;,</div><div class="line">	   NULL,</div><div class="line">	   NULL);</div></pre></td></tr></table></figure>
<p>在表名后的括号里明确给出了列名。在插入行时，DBMS 将用 <code>VALUES</code> 列表中的相应值填入列表中的对应项。 <code>VALUES</code> 中的第一个值对应于第一个指定列名，第二个值对应于第二个列名，如此等等。<br>因为提供了列名， <code>VALUES</code> 必须以其指定的次序匹配指定的列名，不一定按各列出现在表中的实际次序。其优点是，即使表的结构改变，这条<code>INSERT</code> 语句仍然能正确工作。</p>
<h5 id="注意事项-6"><a href="#注意事项-6" class="headerlink" title="注意事项"></a>注意事项</h5><p> 推荐使用列的列表，不推荐使用没有明确给出列的 <code>INSERT</code> 语句。给出列能使 SQL代码继续发挥作用，即使表结构发生了变化。<br>不管使用哪种 <code>INSERT</code> 语法， <code>VALUES</code> 的数目都必须正确。如果不提供列名，则必须给每个表列提供一个值；如果提供列名，则必须给列出的每个列一个值。</p>
<h4 id="插入部分行"><a href="#插入部分行" class="headerlink" title="插入部分行"></a>插入部分行</h4><p>使用 <code>INSERT</code> 推荐给出表的列名的方式，还可以省略列，这表示可以只给某些列提供值，给其他列不提供值。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">INSERT INTO Customers(cust_id,</div><div class="line">	cust_name,</div><div class="line">	cust_address,</div><div class="line">	cust_city,</div><div class="line">	cust_state,</div><div class="line">	cust_zip,</div><div class="line">	cust_country)</div><div class="line">VALUES(&apos;1000000006&apos;,</div><div class="line">	   &apos;Toy Land&apos;,</div><div class="line">	   &apos;123 Any Street&apos;,</div><div class="line">	   &apos;New York&apos;,</div><div class="line">	   &apos;NY&apos;,</div><div class="line">	   &apos;11111&apos;,</div><div class="line">	   &apos;USA&apos;);</div></pre></td></tr></table></figure></p>
<p>没有给 <code>cust_contact</code> 和 <code>cust_email</code> 这两列提供值。这表示没必要在 <code>INSERT</code> 语句中包含它们。因此，这里的 <code>INSERT</code>语句省略了这两列及其对应的值。<br> 省略的列必须满足以下某个条件。<br>该列定义为允许 <code>NULL</code> 值（无值或空值）。<br>在表定义中给出默认值。这表示如果不给出值，将使用默认值。<br>如果表中不允许有 <code>NULL</code> 值或者默认值，这时却省略了表中的值，DBMS就会产生错误消息，相应的行不能成功插入。</p>
<h4 id="插入检索出的数据"><a href="#插入检索出的数据" class="headerlink" title="插入检索出的数据"></a>插入检索出的数据</h4><p><code>INSERT</code> 一般用来给表插入具有指定列值的行。 <code>INSERT</code> 还存在另一种形式，可以利用它将 <code>SELECT</code> 语句的结果插入表中，这就是所谓的<code>INSERT SELECT</code> 。</p>
<p>假如想把另一表中的顾客列合并到 <code>Customers</code> 表中。不需要每次读取一行再将它用 <code>INSERT</code> 插入，可以如下进行：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line">INSERT INTO Customers(cust_id,</div><div class="line">	cust_contact,</div><div class="line">	cust_email,</div><div class="line">	cust_name,</div><div class="line">	cust_address,</div><div class="line">	cust_city,</div><div class="line">	cust_state,</div><div class="line">	cust_zip,</div><div class="line">	cust_country)</div><div class="line">SELECT cust_id,</div><div class="line">	cust_contact,</div><div class="line">	cust_email,</div><div class="line">	cust_name,</div><div class="line">	cust_address,</div><div class="line">	cust_city,</div><div class="line">	cust_state,</div><div class="line">	cust_zip,</div><div class="line">	cust_country</div><div class="line">FROM CustNew;</div></pre></td></tr></table></figure>
<p>这个例子使用 <code>INSERT SELECT</code> 从 <code>CustNew</code> 中将所有数据导入<code>Customers</code> 。 <code>SELECT</code> 语句从 <code>CustNew</code> 检索出要插入的值，而不是列出它们。 <code>SELECT</code> 中列出的每一列对应于 <code>Customers</code> 表名后所跟的每一列。<br> <code>INSERT SELECT</code> 中 <code>SELECT</code> 语句可以包含 <code>WHERE</code> 子句，以过滤插入的数据。</p>
<h3 id="从一个表复制到另一个表"><a href="#从一个表复制到另一个表" class="headerlink" title="从一个表复制到另一个表"></a>从一个表复制到另一个表</h3><blockquote>
<p>要将一个表的内容复制到一个全新的表（运行中创建的表），可以使用 <code>SELECT INTO</code> 语句。</p>
</blockquote>
<p>  <code>INSERT SELECT</code> 与 <code>SELECT INTO</code>它们之间的一个重要差别是前者导入数据，而后者导出数据。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">INTO CustCopy</div><div class="line">FROM Customers;</div></pre></td></tr></table></figure></p>
<p>这条 <code>SELECT</code> 语句创建一个名为 <code>CustCopy</code> 的新表，并把 <code>Customers</code> 表的整个内容复制到新表中。因为这里使用的是 <code>SELECT *</code>，所以将在<code>CustCopy</code> 表中创建（并填充）与 <code>Customers</code> 表的每一列相同的列。要想只复制部分的列，可以明确给出列名，而不是使用 <code>*</code> 通配符。</p>
<h5 id="注意事项-7"><a href="#注意事项-7" class="headerlink" title="注意事项"></a>注意事项</h5><p> 任何 <code>SELECT</code> 选项和子句都可以使用，包括 <code>WHERE</code> 和 <code>GROUP BY</code>；<br>可利用联结从多个表插入数据；<br>不管从多少个表中检索数据，数据都只能插入到一个表中。</p>
<p> <code>SELECT INTO</code> 是试验新 SQL语句前进行<strong>表复制</strong>的很好工具。先进行复制，可在复制的数据上测试 SQL代码，而不会影响实际的数据。</p>
<h2 id="更新和删除数据"><a href="#更新和删除数据" class="headerlink" title="更新和删除数据"></a>更新和删除数据</h2><h3 id="更新数据"><a href="#更新数据" class="headerlink" title="更新数据"></a>更新数据</h3><p>更新（修改）表中的数据，可以使用 <code>UPDATE</code> 语句。有两种使用 <code>UPDATE</code>的方式：</p>
<ol>
<li>更新表中的特定行。</li>
<li>更新表中的所有行。</li>
</ol>
<p>基本的 <code>UPDATE</code> 语句由三部分组成，分别是：</p>
<ol>
<li>要更新的表；</li>
<li>列名和它们的新值；</li>
<li>确定要更新哪些行的过滤条件。<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">UPDATE Customers</div><div class="line">SET cust_email = &apos;kim@thetoystore.com&apos;</div><div class="line">WHERE cust_id = &apos;1000000005&apos;;</div></pre></td></tr></table></figure>
</li>
</ol>
<p><code>UPDATE</code> 语句总是以要更新的表名开始。在这个例子中，要更新的表名为<code>Customers</code> 。 <code>SET</code> 命令用来将新值赋给被更新的列。在这里， <code>SET</code> 子句设置 <code>cust_email</code> 列为指定的值：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SET cust_email = &apos;kim@thetoystore.com&apos;</div></pre></td></tr></table></figure>
<p><code>UPDATE</code> 语句以 <code>WHERE</code> 子句结束，它告诉 DBMS更新哪一行。没有 <code>WHERE</code>子句，DBMS将会用这个电子邮件地址更新 <code>Customers</code> 表中的所有行，这不是我们希望的。</p>
<p>更新多个列：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">UPDATE Customers</div><div class="line">SET cust_contact = &apos;Sam Roberts&apos;,</div><div class="line">cust_email = &apos;sam@toyland.com&apos;</div><div class="line">WHERE cust_id = &apos;1000000006&apos;;</div></pre></td></tr></table></figure></p>
<p>在更新多个列时，只需要使用一条 <code>SET</code> 命令，每个“列=值”对之间用逗号分隔（最后一列之后不用逗号）。</p>
<p>要删除某个列的值，可设置它为 NULL （假如表定义允许 NULL 值）。如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">UPDATE Customers</div><div class="line">SET cust_email = NULL</div><div class="line">WHERE cust_id = &apos;1000000005&apos;;</div></pre></td></tr></table></figure></p>
<h3 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h3><p>从一个表中删除（去掉）数据，使用 <code>DELETE</code> 语句。有两种使用 <code>DELETE</code><br>的方式：</p>
<ol>
<li>从表中删除特定的行；</li>
<li>从表中删除所有行。</li>
</ol>
<p>下面的语句从 <code>Customers</code> 表中删除一行：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">DELETE FROM Customers</div><div class="line">WHERE cust_id = &apos;1000000006&apos;;</div></pre></td></tr></table></figure>
<p><code>DELETE FROM</code> 要求指定从中删除数据的表名，<code>WHERE</code> 子句过滤要删除的行。如果省略 <code>WHERE</code> 子句，它将删除表中每个行。</p>
<p>简单联结两个表只需要这两个表中的常用字段。存在外键时，DBMS 使用它们实施引用完整性。<mark>使用外键确保引用完整性的一个好处是，DBMS 通常可以防止删除某个关系需要用到的行。</mark>例如，要从<code>Products</code> 表中删除一个产品，而这个产品用在 <code>OrderItems</code> 的已有订单中，那么 <code>DELETE</code> 语句将抛出错误并中止。这是总要定义外键的另一个理由。</p>
<p> 在某些 SQL实现中，跟在 <code>DELETE</code> 后的关键字 <code>FROM</code> 是可选的。但是即使不需要，也最好提供这个关键字。这样做将保证SQL代码在DBMS之间可移植。</p>
<p> 删除表的内容而不是表，<code>DELETE</code> 语句从表中删除行，甚至是删除表中所有行。但是， <code>DELETE</code>不删除表本身。<br>如果想从表中删除所有行，不要使用 <code>DELETE</code> 。可使用 <code>TRUNCATE TABLE</code>语句，它完成相同的工作，而速度更快（因为不记录数据的变动）。</p>
<h3 id="更新和删除的指导原则"><a href="#更新和删除的指导原则" class="headerlink" title="更新和删除的指导原则"></a>更新和删除的指导原则</h3><p>使用的 <code>UPDATE</code> 和 <code>DELETE</code> 语句都有 <code>WHERE</code> 子句，这样做的理由很充分。如果省略了 <code>WHERE</code> 子句，则 <code>UPDATE</code> 或 <code>DELETE</code> 将被应用到表中所有的行。</p>
<p>下面是许多 SQL程序员使用 <code>UPDATE</code> 或 <code>DELETE</code> 时所遵循的重要原则。</p>
<ol>
<li>除非确实打算更新和删除每一行，否则绝对不要使用不带 <code>WHERE</code> 子句<br>的 <code>UPDATE</code> 或 <code>DELETE</code> 语句。</li>
<li>保证每个表都有主键，尽可能像 <code>WHERE</code> 子句那样使用它（可以指定各主键、多个值或值的范围）。</li>
<li>在 <code>UPDATE</code> 或 <code>DELETE</code> 语句使用 <code>WHERE</code> 子句前，应该先用 <code>SELECT</code> 进行测试，保证它过滤的是正确的记录，以防编写的 <code>WHERE</code> 子句不正确。</li>
<li>使用强制实施引用完整性的数据库（外键联结），这样 DBMS将不允许删除其数据与其他表相关联的行。</li>
<li>有的 DBMS 允许数据库管理员施加约束，防止执行不带 <code>WHERE</code> 子句的 <code>UPDATE</code> 或 <code>DELETE</code> 语句。如果所采用的 DBMS支持这个特性，应该使用它。</li>
</ol>
<h2 id="创建和操纵表"><a href="#创建和操纵表" class="headerlink" title="创建和操纵表"></a>创建和操纵表</h2><p>###创建表<br>一般有两种创建表的方法：</p>
<ol>
<li>多数 DBMS都具有交互式创建和管理数据库表的工具；</li>
<li>表也可以直接用 SQL语句操纵。</li>
</ol>
<p>用程序创建表，可以使用 SQL的 <code>CREATE TABLE</code> 语句。需要注意的是，使用交互式工具时实际上就是使用 SQL语句。这些语句不是用户编写的，界面工具会自动生成并执行相应的 SQL语句。</p>
<h4 id="表创建基础"><a href="#表创建基础" class="headerlink" title="表创建基础"></a>表创建基础</h4><p>利用 <code>CREATE TABLE</code> 创建表，必须给出下列信息：</p>
<ol>
<li>新表的名字，在关键字 <code>CREATE TABLE</code> 之后给出；</li>
<li>表列的名字和定义，用逗号分隔；</li>
<li>有的 DBMS还要求指定表的位置</li>
</ol>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE Products</div><div class="line">(</div><div class="line">prod_id CHAR(10) NOT NULL,</div><div class="line">vend_id CHAR(10) NOT NULL,</div><div class="line">prod_name CHAR(254) NOT NULL,</div><div class="line">prod_price DECIMAL(8,2) NOT NULL,</div><div class="line">prod_desc VARCHAR(1000) NULL</div><div class="line">);</div></pre></td></tr></table></figure>
<p>表名紧跟 <code>CREATE TABLE</code> 关键字。实际的表定义（所有列）括在圆括号之中，各列之间用<strong>逗号</strong>分隔。这个表由 5 列组成，每列的定义以列名（它在表中必须是唯一的）开始，后跟列的数据类型。</p>
<p> 在创建新的表时，指定的表名必须不存在，否则会出错。防止意外覆盖已有的表，SQL 要求首先手工删除该表（请参阅后面的内容），然后再重建它，而不是简单地用创建表语句覆盖它。</p>
<h4 id="使用-NULL-值"><a href="#使用-NULL-值" class="headerlink" title="使用 NULL 值"></a>使用 NULL 值</h4><blockquote>
<p><code>NULL</code> 值就是没有值或缺值。允许 <code>NULL</code> 值的列也允许在插入行时不给出该列的值。不允许 <code>NULL</code> 值的列不接受没有列值的行，换句话说，在插入或更新行时，该列必须有值。</p>
</blockquote>
<p>每个表列要么是 <code>NULL</code> 列，要么是 <code>NOT NULL</code> 列<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"> CREATE TABLE Orders</div><div class="line">(</div><div class="line">order_num INTEGER NOT NULL,</div><div class="line">order_date DATETIME NOT NULL,</div><div class="line">cust_id CHAR(10) NOT NULL</div><div class="line">);</div></pre></td></tr></table></figure></p>
<p>每一列的定义都含有关键字 <code>NOT NULL</code> 。这就会阻止插入没有值的列。如果插入没有值的列，将返回错误，且插入失败。</p>
<p>创建混合了 <code>NULL</code> 和 <code>NOT NULL</code> 列的表：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE Vendors</div><div class="line">(</div><div class="line">vend_id CHAR(10) NOT NULL,</div><div class="line">vend_name CHAR(50) NOT NULL,</div><div class="line">vend_address CHAR(50) ,</div><div class="line">vend_city CHAR(50) ,</div><div class="line">vend_state CHAR(5) ,</div><div class="line">vend_zip CHAR(10) ,</div><div class="line">vend_country CHAR(50)</div><div class="line">);</div></pre></td></tr></table></figure></p>
<p>供应商 ID 和供应商名字列是必需的，因此指定为 <code>NOT NULL</code> 。其余五列全都允许 <code>NULL</code> 值，所以不指定 <code>NOT NULL</code> 。 <code>NULL</code> 为默认设置，如果不指定 <code>NOT NULL</code> ，就认为指定的是 <code>NULL</code> 。</p>
<h5 id="注意事项-8"><a href="#注意事项-8" class="headerlink" title="注意事项"></a>注意事项</h5><p> 主键和 <code>NULL</code> 值，主键是其值唯一标识表中每一行的列。只有不允许 <code>NULL</code>值的列可作为主键，允许 <code>NULL</code> 值的列不能作为唯一标识。<br>在不指定 <code>NOT NULL</code> 时，多数 DBMS认为指定的是 <code>NULL</code> ，但不是所有的 DBMS都这样。某些 DBMS要求指定关键字 <code>NULL</code> ，如果不指定将出错。</p>
<h4 id="指定默认值"><a href="#指定默认值" class="headerlink" title="指定默认值"></a>指定默认值</h4><blockquote>
<p>SQL 允许指定默认值，在插入行时如果不给出值，DBMS 将自动采用默认值。默认值在 <code>CREATE TABLE</code> 语句的列定义中用关键字 <code>DEFAULT</code> 指定。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE OrderItems</div><div class="line">(</div><div class="line">order_num INTEGER NOT NULL,</div><div class="line">order_item INTEGER NOT NULL,</div><div class="line">prod_id CHAR(10) NOT NULL,</div><div class="line">quantity INTEGER NOT NULL DEFAULT 1,</div><div class="line">item_price DECIMAL(8,2) NOT NULL</div><div class="line">);</div></pre></td></tr></table></figure></p>
</blockquote>
<p><code>quantity</code> 列为订单中每个物品的数量，这一列的描述增加了 <code>DEFAULT 1</code> ，指示 DBMS，如果不给出数量则使用数量 1 。</p>
<p> 默认值经常用于日期或时间戳列。MySQL 用户指定 <code>DEFAULT CURRENT_DATE()</code> ，Oracle 用户指定 <code>DEFAULT SYSDATE</code> ，而 SQL Server用户指定 <code>DEFAULT GETDATE()</code> 。</p>
<p>获得系统日期<br>|DBMS| 函数/变量|<br>|:—:|:—:|<br>|Access |<code>NOW()</code><br>|DB2 |<code>CURRENT_DATE</code><br>|MySQL| <code>CURRENT_DATE()</code><br>|Oracle| <code>SYSDATE</code><br>|PostgreSQL| <code>CURRENT_DATE</code><br>|SQL Server |<code>GETDATE()</code><br>|SQLite |<code>date(&#39;now&#39;)</code></p>
<h3 id="更新表"><a href="#更新表" class="headerlink" title="更新表"></a>更新表</h3><blockquote>
<p>更新表定义，可以使用 <code>ALTER TABLE</code> 语句。</p>
</blockquote>
<p>使用 <code>ALTER TABLE</code> 更改表结构，必须给出下面的信息：</p>
<ol>
<li>在 <code>ALTER TABLE</code> 之后给出要更改的表名（该表必须存在，否则将出错）；</li>
<li>列出要做哪些更改。<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ALTER TABLE Vendors</div><div class="line">ADD vend_phone CHAR(20);</div></pre></td></tr></table></figure>
</li>
</ol>
<p>这条语句给 <code>Vendors</code> 表增加一个名为 <code>vend_phone</code> 的列，其数据类型为 <code>CHAR</code> 。</p>
<p>删除列：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ALTER TABLE Vendors</div><div class="line">DROP COLUMN vend_phone;</div></pre></td></tr></table></figure></p>
<p> 使用 <code>ALTER TABLE</code> 要极为小心，应该在进行改动前做完整的备份（表结构和数据的备份）。数据库表的更改不能撤销，如果增加了不需要的列，也许无法删除它们。类似地，如果删除了不应该删除的列，可能会丢失该列中的所有数据。</p>
<h3 id="删除表"><a href="#删除表" class="headerlink" title="删除表"></a>删除表</h3><p>删除表（删除整个表而不是其内容）非常简单，使用 <code>DROP TABLE</code> 语句即可：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">DROP TABLE CustCopy;</div></pre></td></tr></table></figure></p>
<p>这条语句删除 <code>CustCopy</code> 表。删除表没有确认，也不能撤销，执行这条语句将永久删除该表。<br> 使用关系规则防止意外删除<br>许多 DBMS允许强制实施有关规则，防止删除与其他表相关联的表。在实施这些规则时，如果对某个表发布一条 <code>DROP TABLE</code> 语句，且该表是某个关系的组成部分，则 DBMS将阻止这条语句执行，直到该关系被删除为止。如果允许，应该启用这些选项，它能防止意外删除有用的表。</p>
<h3 id="重命名表"><a href="#重命名表" class="headerlink" title="重命名表"></a>重命名表</h3><p>SQL Server用户使用 <code>sp_rename</code> 语句重命名，要求指定旧表名和新表名。</p>
<h2 id="使用视图"><a href="#使用视图" class="headerlink" title="使用视图"></a>使用视图</h2><blockquote>
<p>视图是虚拟的表 ，包含的不是数据而是根据需要检索数据的查询。视图提供了一种封装 <code>SELECT</code> 语句的层次，可用来简化数据处理，重新格式化或保护基础数据。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM Customers, Orders, OrderItems</div><div class="line">WHERE Customers.cust_id = Orders.cust_id</div><div class="line">AND OrderItems.order_num = Orders.order_num</div><div class="line">AND prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure>
<p>假如可以把整个查询包装成一个名为 <code>ProductCustomers</code> 的虚拟表，则可以如下轻松地检索出相同的数据：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM ProductCustomers</div><div class="line">WHERE prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure>
<p>这就是视图的作用。 <code>ProductCustomers</code> 是一个视图，作为视图，它不包<br>含任何列或数据，包含的是一个查询。</p>
<h4 id="为什么使用视图"><a href="#为什么使用视图" class="headerlink" title="为什么使用视图"></a>为什么使用视图</h4><ol>
<li>重用 SQL语句。</li>
<li>简化复杂的 SQL操作。在编写查询后，可以方便地重用它而不必知道其基本查询细节。</li>
<li>使用表的一部分而不是整个表。</li>
<li>保护数据。可以授予用户访问表的特定部分的权限，而不是整个表的访问权限。</li>
<li>更改数据格式和表示。视图可返回与底层表的表示和格式不同的数据。</li>
</ol>
<p>创建视图之后，可以用与表基本相同的方式使用它们。可以对视图执行<code>SELECT</code> 操作，过滤和排序数据，将视图联结到其他视图或表，甚至添加和更新数据。</p>
<p> 视图仅仅是用来查看存储在别处数据，视图本身不包含数据，每次使用视图时，都必须处理查询执行时需要的所有检索，因此返回的数据是从其他表中检索出来的。在添加或更改这些表中的数据时，视图将返回改变过的数据。</p>
<h4 id="视图的规则和限制"><a href="#视图的规则和限制" class="headerlink" title="视图的规则和限制"></a>视图的规则和限制</h4><ol>
<li>与表一样，视图必须唯一命名。</li>
<li>对于可以创建的视图数目没有限制。</li>
<li>创建视图，必须具有足够的访问权限。这些权限通常由数据库管理人员授予。</li>
<li>视图可以嵌套，即可以利用从其他视图中检索数据的查询来构造视图。</li>
<li>许多 DBMS禁止在视图查询中使用 <code>ORDER BY</code> 子句。</li>
<li>有些 DBMS要求对返回的所有列进行命名，如果列是计算字段，则需要使用别名。</li>
<li>视图不能索引，也不能有关联的触发器或默认值。</li>
<li>有些 DBMS把视图作为只读的查询，这表示可以从视图检索数据，但不能将数据写回底层表。</li>
<li>有些 DBMS 允许创建这样的视图，它不能进行导致行不再属于视图的插入或更新。例如有一个视图，只检索带有电子邮件地址的顾客。如果更新某个顾客，删除他的电子邮件地址，将使该顾客不再属于视图。<h3 id="创建视图"><a href="#创建视图" class="headerlink" title="创建视图"></a>创建视图</h3><blockquote>
<p>各种 DBMS中用来创建视图的语法相当一致。</p>
</blockquote>
</li>
</ol>
<p>视图用 <code>CREATE VIEW</code> 语句来创建。与 <code>CREATE TABLE</code> 一样， <code>CREATE VIEW</code>只能用于创建不存在的视图。<br>删除视图，可以使用 <code>DROP</code> 语句，其语法为 <code>DROP VIEW viewname;</code> 。<br>覆盖（或更新）视图，必须先删除它，然后再重新创建。</p>
<h3 id="利用视图简化复杂的联结"><a href="#利用视图简化复杂的联结" class="headerlink" title="利用视图简化复杂的联结"></a>利用视图简化复杂的联结</h3><p>一个最常见的视图应用是隐藏复杂的 SQL，这通常涉及联结。请看下面的例子：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">CREATE VIEW ProductCustomers AS</div><div class="line">SELECT cust_name, cust_contact, prod_id</div><div class="line">FROM Customers, Orders, OrderItems</div><div class="line">WHERE Customers.cust_id = Orders.cust_id</div><div class="line">AND OrderItems.order_num = Orders.order_num;</div></pre></td></tr></table></figure>
<p>这条语句创建一个名为 <code>ProductCustomers</code> 的视图，它联结三个表，返<br>回已订购了任意产品的所有顾客的列表。如果执行</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"> SELECT * FROM</div><div class="line">ProductCustomers ;</div></pre></td></tr></table></figure>
<p>将列出订购了任意产品的顾客。</p>
<p>检索订购了产品 <code>RGAN01</code> 的顾客，可如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT cust_name, cust_contact</div><div class="line">FROM ProductCustomers</div><div class="line">WHERE prod_id = &apos;RGAN01&apos;;</div></pre></td></tr></table></figure></p>
<p> 视图极大地简化了复杂 SQL语句的使用。利用视图，可一次性编写基础的 SQL，然后根据需要多次使用。</p>
<h4 id="用视图重新格式化检索出的数据"><a href="#用视图重新格式化检索出的数据" class="headerlink" title="用视图重新格式化检索出的数据"></a>用视图重新格式化检索出的数据</h4><p>视图的另一常见用途是重新格式化检索出的数据。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure></p>
<p>或</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SELECT RTRIM(vend_name) || &apos; (&apos; || RTRIM(vend_country) || &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors</div><div class="line">ORDER BY vend_name;</div></pre></td></tr></table></figure>
<p>假设经常需要这个格式的结果。我们不必在每次需要时执行这种拼接，而是创建一个视图，使用它即可。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">CREATE VIEW VendorLocations AS</div><div class="line">SELECT RTRIM(vend_name) + &apos; (&apos; + RTRIM(vend_country) + &apos;)&apos;</div><div class="line">AS vend_title</div><div class="line">FROM Vendors;</div></pre></td></tr></table></figure></p>
<p>要检索数据，创建所有的邮件标签，可如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">FROM VendorLocations;</div></pre></td></tr></table></figure></p>
<h4 id="用视图过滤不想要的数据"><a href="#用视图过滤不想要的数据" class="headerlink" title="用视图过滤不想要的数据"></a>用视图过滤不想要的数据</h4><p>视图对于应用普通的 <code>WHERE</code> 子句也很有用。例如，可以义<code>CustomerEMailList</code> 视图，过滤没有电子邮件地址的顾客。为此，可使用下面的语句：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">CREATE VIEW CustomerEMailList AS</div><div class="line">SELECT cust_id, cust_name, cust_email</div><div class="line">FROM Customers</div><div class="line">WHERE cust_email IS NOT NULL;</div></pre></td></tr></table></figure></p>
<p>这里的 <code>WHERE</code> 子句过滤了 <code>cust_email</code> 列中具有 <code>NULL</code> 值的那些行，使它们不被检索出来。<br>现在，可以像使用其他表一样使用视图 <code>CustomerEMailList</code> 。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">FROM CustomerEMailList;</div></pre></td></tr></table></figure></p>
<h4 id="使用视图与计算字段"><a href="#使用视图与计算字段" class="headerlink" title="使用视图与计算字段"></a>使用视图与计算字段</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">CREATE VIEW OrderItemsExpanded AS</div><div class="line">SELECT order_num,</div><div class="line">prod_id,</div><div class="line">quantity,</div><div class="line">item_price,</div><div class="line">quantity*item_price AS expanded_price</div><div class="line">FROM OrderItems;</div></pre></td></tr></table></figure>
<p>检索订单 20008 的详细内容（上面的输出），如下进行：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">SELECT *</div><div class="line">FROM OrderItemsExpanded</div><div class="line">WHERE order_num = 20008;</div></pre></td></tr></table></figure>
<p>正确使用视图，可极大地简化复杂数据的处理。</p>
<p>##使用存储过程</p>
<h3 id="存储过程"><a href="#存储过程" class="headerlink" title="存储过程"></a>存储过程</h3><blockquote>
<p>存储过程就是为以后使用而保存的一条或多条 SQL语句。可将其视为批文件，虽然它们的作用不仅限于批处理。 </p>
<h3 id="为什么要使用存储过程"><a href="#为什么要使用存储过程" class="headerlink" title="为什么要使用存储过程"></a>为什么要使用存储过程</h3><p>存储过程有三个主要的好处，即简单、安全、高性能。</p>
<ol>
<li>通过把处理封装在一个易用的单元中，可以简化复杂的操作。</li>
<li>由于不要求反复建立一系列处理步骤，因而保证了数据的一致性。如果所有开发人员和应用程序都使用同一存储过程，则所使用的代码都是相同的。</li>
<li>简化对变动的管理。如果表名、列名或业务逻辑（或别的内容）有变化，那么只需要更改存储过程的代码。这一点的延伸就是安全性。通过存储过程限制对基础数据的访问，减少了数据讹误的机会。</li>
<li>因为存储过程通常以编译过的形式存储，所以 DBMS处理命令所需的工作量少，提高了性能。</li>
</ol>
</blockquote>
<p>缺陷：</p>
<ol>
<li>不同 DBMS中的存储过程语法有所不同。事实上，编写真正的可移植存储过程几乎是不可能的。</li>
<li>一般来说，编写存储过程比编写基本 SQL语句复杂，需要更高的技能，更丰富的经验。<h3 id="执行存储过程"><a href="#执行存储过程" class="headerlink" title="执行存储过程"></a>执行存储过程</h3>存储过程的执行远比编写要使用得更多，执行存储过程的 SQL语句很简单，即 <code>EXECUTE</code> 。 <code>EXECUTE</code> 接受存储过程名和需要传递给它的任何参数。</li>
</ol>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">EXECUTE</span> AddNewProduct( <span class="string">'JTS01'</span>,</div><div class="line"><span class="string">'Stuffed Eiffel Tower'</span>,</div><div class="line"><span class="number">6.49</span>,</div><div class="line"><span class="string">'Plush stuffed toy with the text La</span></div><div class="line">Tour Eiffel in red white and blue' );</div></pre></td></tr></table></figure>
<p>这里执行一个名为 <code>AddNewProduct</code> 的存储过程，将一个新产品添加到<code>Products</code> 表中。 <code>AddNewProduct</code> 有四个参数，分别是：供应商 <code>ID</code>（ <code>Vendors</code> 表的主键）、产品名、价格和描述。这 4 个参数匹配存储过程中 4个预期变量（定义为存储过程自身的组成部分）。此存储过程将新行添加到 <code>Products</code> 表，并将传入的属性赋给相应的列。</p>
<h3 id="创建存储过程"><a href="#创建存储过程" class="headerlink" title="创建存储过程"></a>创建存储过程</h3><p>下面是该过程的 Oracle版本：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">CREATE PROCEDURE MailingListCount (</div><div class="line">ListCount OUT INTEGER</div><div class="line">)</div><div class="line">IS</div><div class="line">v_rows INTEGER;</div><div class="line">BEGIN</div><div class="line">SELECT COUNT(*) INTO v_rows</div><div class="line">FROM Customers</div><div class="line">WHERE NOT cust_email IS NULL;</div><div class="line">ListCount := v_rows;</div><div class="line">END;</div></pre></td></tr></table></figure></p>
<p>这个存储过程有一个名为 <code>ListCount</code> 的参数。此参数从存储过程返回一个值，关键字 <code>OUT</code> 用来指示这种行为。<code>Oracle</code>支持 <code>IN</code> （传递值给存储过程）、 <code>OUT</code> （从存储过程返回值，如这里）、<code>INOUT</code> （既传递值给存储过程也从存储过程传回值）类型的参数。存储过程的代码括在 <code>BEGIN</code> 和 <code>END</code> 语句中，这里执行一条简单的 <code>SELECT</code> 语句，它检索具有邮件地址的顾客。然后检索出行数<code>ListCount</code>（要传递的输出参数）。<br>调用 Oracle例子可以像下面这样：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">var ReturnValue NUMBER</div><div class="line">EXEC MailingListCount(:ReturnValue);</div><div class="line">SELECT ReturnValue;</div></pre></td></tr></table></figure>
<p>这段代码声明了一个变量来保存存储过程返回的任何值，然后执行存储过程，再使用 <code>SELECT</code> 语句显示返回的值。<br>下面是该过程的 SQL Server 版本<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> MailingListCount</div><div class="line"><span class="keyword">AS</span></div><div class="line"><span class="keyword">DECLARE</span> @cnt <span class="built_in">INTEGER</span></div><div class="line"><span class="keyword">SELECT</span> @cnt = <span class="keyword">COUNT</span>(*)</div><div class="line"><span class="keyword">FROM</span> Customers</div><div class="line"><span class="keyword">WHERE</span> <span class="keyword">NOT</span> cust_email <span class="keyword">IS</span> <span class="literal">NULL</span>;</div><div class="line">RETURN @cnt;</div></pre></td></tr></table></figure></p>
<p>此存储过程没有参数。调用程序检索 <code>SQL Server</code>的返回代码提供的值。其中用 <code>DECLARE</code> 语句声明了一个名为 <code>@cnt</code> 的局部变量（<code>SQL Server</code>中所有局部变量名都以 <code>@</code> 起头）；然后在 <code>SELECT</code> 语句中使用这个变量，让它包含 <code>COUNT()</code> 函数返回的值；最后，用 <code>RETURN @cnt</code> 语句将计数返回给调用程序。<br>调用 SQL Server例子可以像下面这样：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">DECLARE</span> @ReturnValue <span class="built_in">INT</span></div><div class="line"><span class="keyword">EXECUTE</span> @ReturnValue=MailingListCount;</div><div class="line"><span class="keyword">SELECT</span>  @ReturnValue;</div></pre></td></tr></table></figure>
<p>这段代码声明了一个变量来保存存储过程返回的任何值，然后执行存储过程，再使用 <code>SELECT</code> 语句显示返回的值。</p>
<p>下面是SQL存储过程的另一个例子，这次在 Orders 表中插入一个新订单:</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">CREATE PROCEDURE NewOrder @cust_id CHAR(10)</div><div class="line">AS</div><div class="line">-- Declare variable for order number</div><div class="line">DECLARE @order_num INTEGER</div><div class="line">-- Get current highest order number</div><div class="line">SELECT @order_num=MAX(order_num)</div><div class="line">FROM Orders</div><div class="line">-- Determine next order number</div><div class="line">SELECT @order_num=@order_num+1</div><div class="line">-- Insert new order</div><div class="line">INSERT INTO Orders(order_num, order_date, cust_id)</div><div class="line">VALUES(@order_num, GETDATE(), @cust_id)</div><div class="line">-- Return order number</div><div class="line">RETURN @order_num;</div></pre></td></tr></table></figure>
<p>此存储过程在 <code>Orders</code> 表中创建一个新订单。它只有一个参数，即下订单顾客的 <code>ID</code>。订单号和订单日期这两列在存储过程中自动生成。代码首先<strong>用<code>@</code>声明一个局部变量</strong>来存储订单号。接着，检索当前最大订单号（使用<code>MAX()</code> 函数）并增加 1（使用 <code>SELECT</code> 语句）。然后用 <code>INSERT</code> 语句插入由新生成的订单号、当前系统日期（用 <code>GETDATE()</code> 函数检索）和传递的顾客 <code>ID</code>组成的订单。最后，<code>RETURN @order_num</code> 返回订单号（处理订单物品需要它）。<br> 应该注释所有代码，存储过程也不例外。增加注释不影响性能，因此不存在缺陷（除了增加编写时间外）。<br>注释代码的好处很多，包括使别人（以及你自己）更容易地理解和更安全地修改代码。<br>对代码进行注释的标准方式是在之前放置 <code>--</code> （两个连字符）。<br>有的DBMS还支持其他的注释语法，不过所有 DBMS都支持 <code>--</code> ，因此在注释代码时最好都使用这种语法。</p>
<p>下面是相同 SQL Server代码的一个很不同的版本：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> NewOrder @cust_id <span class="built_in">CHAR</span>(<span class="number">10</span>)</div><div class="line"><span class="keyword">AS</span></div><div class="line"><span class="comment">-- Insert new order</span></div><div class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> Orders(cust_id)</div><div class="line"><span class="keyword">VALUES</span>(@cust_id)</div><div class="line"><span class="comment">-- Return order number</span></div><div class="line"><span class="keyword">SELECT</span> order_num = @@<span class="keyword">IDENTITY</span>;</div></pre></td></tr></table></figure>
<p>此存储过程也在 <code>Orders</code> 表中创建一个新订单。这次由 DBMS生成订单号，大多数 DBMS都支持这种功能；SQL Server中称这些自动增量的列为标识字段（<code>identity field</code>），而其他 DBMS称之为自动编号（<code>auto number</code>）或序列（<code>sequence</code>）。传递给此过程的参数也是一个，即下订单的顾客 <code>ID</code>。订单号和订单日期没有给出，DBMS对日期使用默认值（ <code>GETDATE()</code> 函数），订单号自动生成。怎样才能得到这个自动生成的 <code>ID</code>？在 SQL Server上可在全局变量 <code>@@IDENTITY</code> 中得到，它返回到调用程序（这里使用 <code>SELECT</code> 语句）。</p>
<h2 id="管理事务处理"><a href="#管理事务处理" class="headerlink" title="管理事务处理"></a>管理事务处理</h2><blockquote>
<p>事务是必须<strong>完整执行</strong>的 SQL语句块，利用事务处理，可以保证一组操作不会中途停止，它们要么完全执行，要么完全不执行（除非明确指示）。如果没有错误发生，整组语句提交给（写到）数据库表；如果发生错误，则进行回退（撤销），将数据库恢复到某个已知且安全的状态。</p>
<h3 id="事务处理"><a href="#事务处理" class="headerlink" title="事务处理"></a>事务处理</h3><p>使用事务处理（``），通过确保成批的 SQL 操作要么完全执行，要么完全不执行，来维护数据库的完整性。</p>
</blockquote>
<p>在某种程度上说，设计良好的数据库模式都是关联的。订单存储在 <code>Orders</code> 和<code>OrderItems</code> 两个表中： <code>Orders</code> 存储实际的订单， <code>OrderItems</code> 存储订购的各项物品。这两个表使用称为主键的唯一 <code>ID</code>互相关联，又与包含客户和产品信息的其他表相关联。</p>
<p>给系统添加订单的过程如下：<br>(1) 检查数据库中是否存在相应的顾客，如果不存在，添加他；<br>(2) 提交顾客信息；<br>(3) 检索顾客的 <code>ID</code>；<br>(4) 在 <code>Orders</code> 表添加一行，与顾客 <code>ID</code>相关联；<br>(5) 如果向 <code>Orders</code> 表添加行时出现故障，回退；<br>(6) 检索 <code>Orders</code> 表中赋予的新订单 <code>ID</code>；<br>(7) 对于订购的每项物品，添加新行到 <code>OrderItems</code> 表；通过检索出来的 <code>ID</code>把它与 <code>Orders</code> 表关联（并且通过产品 <code>ID</code>与 <code>Products</code> 表关联）。<br>(8) 如果向 <code>OrderItems</code> 添加行时出现故障，回退所有添加的 <code>OrderItems</code><br>行和 <code>Orders</code> 行。</p>
<p>在使用事务处理时，有几个反复出现的关键词。下面是关于事务处理需要知道的几个术语：</p>
<ol>
<li>事务（<code>transaction</code>）指一组 SQL语句；</li>
<li>回退（<code>rollback</code>）指撤销指定 SQL语句的过程；</li>
<li>提交（<code>commit</code>）指将未存储的 SQL语句结果写入数据库表；</li>
<li><p>保留点（<code>savepoint</code>）指事务处理中设置的临时占位符（<code>placeholder</code>），可以对它发布回退（与回退整个事务处理不同）。</p>
<p>可以回退哪些语句？<br>事务处理用来管理 <code>INSERT</code> 、 <code>UPDATE</code> 和 <code>DELETE</code> 语句。不能回退 <code>SELECT</code>语句（回退 <code>SELECT</code> 语句也没有必要），也<strong>不能回退 <code>CREATE</code> 或 <code>DROP</code> 操作</strong>。事务处理中可以使用这些语句块，但进行的回退操作不能撤销。</p>
<h3 id="控制事务处理"><a href="#控制事务处理" class="headerlink" title="控制事务处理"></a>控制事务处理</h3><blockquote>
<p>管理事务的关键在于将 SQL语句组分解为逻辑块，并明确规定数据何时应该回退，何时不应该回退。</p>
</blockquote>
</li>
</ol>
<p>有的DBMS要求明确标识事务处理块的开始和结束。如在SQL Server中，标识如下：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">BEGIN</span> <span class="keyword">TRANSACTION</span></div><div class="line">...</div><div class="line"><span class="keyword">COMMIT</span> <span class="keyword">TRANSACTION</span></div></pre></td></tr></table></figure>
<p>MariaDB和 MySQL中等同的代码为：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">START TRANSACTION</div></pre></td></tr></table></figure>
<p>Oracle使用的语法：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SET TRANSACTION</div></pre></td></tr></table></figure>
<p>多数实现没有明确标识事务处理在何处结束。事务一直存在，直到被中断。通常， <code>COMMITT</code> 用于保存更改， <code>ROLLBACK</code> 用于撤销 。</p>
<h4 id="使用-ROLLBACK"><a href="#使用-ROLLBACK" class="headerlink" title="使用 ROLLBACK"></a>使用 ROLLBACK</h4><p>SQL的 <code>ROLLBACK</code> 命令用来回退（撤销）SQL语句，请看下面的语句：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">DELETE FROM Orders;</div><div class="line">ROLLBACK;</div></pre></td></tr></table></figure></p>
<p>在此例子中，执行 <code>DELETE</code> 操作，然后用 <code>ROLLBACK</code> 语句撤销。</p>
<h4 id="使用-COMMIT"><a href="#使用-COMMIT" class="headerlink" title="使用 COMMIT"></a>使用 COMMIT</h4><p>一般的 SQL语句都是针对数据库表直接执行和编写的。这就是所谓的隐式提交（<code>implicit commit</code>），即提交（写或保存）操作是自动进行的。<br><strong>在事务处理块中，提交不会隐式进行。</strong>进行明确的提交，使用 <code>COMMIT</code> 语句。下面是一个 SQL Server的例子：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">BEGIN TRANSACTION</div><div class="line">DELETE OrderItems WHERE order_num = 12345</div><div class="line">DELETE Orders WHERE order_num = 12345</div><div class="line">COMMIT TRANSACTION</div></pre></td></tr></table></figure></p>
<p>在这个 SQL Server例子中，从系统中完全删除订单 12345 。因为涉及更新两个数据库表 <code>Orders</code> 和 <code>OrderItems</code> ，所以使用事务处理块来保证订单不被部分删除。最后的 <code>COMMIT</code> 语句仅在不出错时写出更改。如果第一条 <code>DELETE</code> 起作用，但第二条失败，则 <code>DELETE</code> 不会提交。</p>
<p>Oracle中完成相同的工作，可如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">SET TRANSACTION</div><div class="line">DELETE OrderItems WHERE order_num = 12345;</div><div class="line">DELETE Orders WHERE order_num = 12345;</div><div class="line">COMMIT;</div></pre></td></tr></table></figure></p>
<h4 id="使用保留点"><a href="#使用保留点" class="headerlink" title="使用保留点"></a>使用保留点</h4><p>使用简单的 <code>ROLLBACK</code> 和 <code>COMMIT</code> 语句，就可以写入或撤销整个事务。但是，只对简单的事务才能这样做，复杂的事务可能需要部分提交或回退。要支持回退部分事务，必须在事务处理块中的合适位置放置占位符。这样，如果需要回退，可以回退到某个占位符。在 SQL 中，这些占位符称为保留点。<br>在 SQL Server 中，如下进行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SAVE TRANSACTION delete1;</div></pre></td></tr></table></figure></p>
<p>每个保留点都要取能够标识它的唯一名字，以便在回退时，DBMS 知道回退到何处。<br>要回退到本例给出的保留点，在 SQL Server 中可如下进行。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ROLLBACK TRANSACTION delete1;</div></pre></td></tr></table></figure></p>
<p>在 MariaDB、MySQL 和 Oracle 中创建占位符，可使用 <code>SAVEPOINT</code> 语句。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SAVEPOINT delete1;</div></pre></td></tr></table></figure></p>
<p>回退到保留点<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ROLLBACK TO delete1;</div></pre></td></tr></table></figure></p>
<p>下面是一个完整的 SQL Server例子：<br><figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line">BEGIN TRANSACTION</div><div class="line">INSERT INTO Customers(cust_id, cust_name)</div><div class="line">VALUES('1000000010', 'Toys Emporium');</div><div class="line">SAVE TRANSACTION StartOrder;</div><div class="line">INSERT INTO Orders(order_num, order_date, cust_id)</div><div class="line">VALUES(20100,'2001/12/1','1000000010');</div><div class="line">IF @@ERROR &lt;&gt; 0 </div><div class="line">ROLLBACK TRANSACTION StartOrder;</div><div class="line">INSERT INTO OrderItems(order_num, order_item, prod_id, quantity,item_price)</div><div class="line">VALUES(20100, 1, 'BR01', 100, 5.49);</div><div class="line">IF @@ERROR &lt;&gt; 0 </div><div class="line">ROLLBACK TRANSACTION StartOrder;</div><div class="line">INSERT INTO OrderItems(order_num, order_item, prod_id, quantity,item_price)</div><div class="line">VALUES(20100, 2, 'BR03', 100, 10.99);</div><div class="line">IF @@ERROR &lt;&gt; 0 </div><div class="line">ROLLBACK TRANSACTION StartOrder;</div><div class="line">COMMIT TRANSACTION</div></pre></td></tr></table></figure></p>
<p>这里的事务处理块中包含了 4条 <code>INSERT</code> 语句。在第一条 <code>INSERT</code> 语句之后定义了一个保留点，因此，如果后面的任何一个 <code>INSERT</code> 操作失败，事务处理能够回退到这里。在 SQL Server 中，可检查一个名为 <code>@@ERROR</code>的变量，看操作是否成功。如果 <code>@@ERROR</code> 返回一个非 0 的值，表示有错误发生，事务处理回退到保留点。如果整个事务处理成功，发布 <code>COMMIT</code> 以保留数据。</p>
<p> 保留点越多越好<br>可以在 SQL代码中设置任意多的保留点，越多越好。因为保留点越多，越能灵活地进行回退。</p>
<h2 id="使用游标"><a href="#使用游标" class="headerlink" title="使用游标"></a>使用游标</h2><h3 id="游标"><a href="#游标" class="headerlink" title="游标"></a>游标</h3><p>SQL检索操作返回一组称为结果集的行，这组返回的行都是与 SQL语句相匹配的行（零行或多行）。简单地使用 <code>SELECT</code> 语句，没有办法得到第一行、下一行或前 10行。<br>有时，需要在检索出来的行中前进或后退一行或多行，这就是游标的用途所在。游标（<code>cursor</code>）是一个存储在 DBMS 服务器上的数据库查询，它不是一条 <code>SELECT</code> 语句，而<strong>是被该语句检索出来的结果集</strong>。<br> 结果集（result set）是SQL查询所检索出的结果。</p>
<p>不同的 DBMS 支持不同的游标选项和特性。常见的一些选项和特性如下。</p>
<ol>
<li>能够标记游标为只读，使数据能读取，但不能更新和删除。</li>
<li>能控制可以执行的定向操作（向前、向后、第一、最后、绝对位置、相对位置等）。</li>
<li>能标记某些列为可编辑的，某些列为不可编辑的。</li>
<li>规定范围，使游标对创建它的特定请求（如存储过程）或对所有请求可访问。</li>
<li>指示 DBMS对检索出的数据（而不是指出表中活动数据）进行复制，使数据在游标打开和访问期间不变化。</li>
</ol>
<p>游标主要用于交互式应用，其中用户需要滚动屏幕上的数据，并对数据进行浏览或做出更改。</p>
<p>###使用游标<br>使用游标涉及几个明确的步骤。</p>
<ol>
<li>在使用游标前，必须声明（定义）它。这个过程实际上没有检索数据，它只是定义要使用的 <code>SELECT</code> 语句和游标选项。</li>
<li>一旦声明，就必须打开游标以供使用。这个过程用前面定义的 <code>SELECT</code>语句把数据实际检索出来。</li>
<li>对于填有数据的游标，根据需要取出（检索）各行。</li>
<li>在结束游标使用时，必须关闭游标，可能的话，释放游标（有赖于具体的 DBMS）。<h4 id="创建游标"><a href="#创建游标" class="headerlink" title="创建游标"></a>创建游标</h4>使用 <code>DECLARE</code> 语句创建游标，<code>DECLARE</code> 命名游标，并定义相应的 <code>SELECT</code> 语句，根据需要带 <code>WHERE</code> 和其他子句。<br>创建此游标的 DB2、MariaDB、MySQL和 SQL Server版本:<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">DECLARE CustCursor CURSOR</div><div class="line">FOR</div><div class="line">SELECT * FROM Customers</div><div class="line">WHERE cust_email IS NULL</div></pre></td></tr></table></figure>
</li>
</ol>
<p>Oracle和 PostgreSQL版本：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">DECLARE CURSOR CustCursor</div><div class="line">IS</div><div class="line">SELECT * FROM Customers</div><div class="line">WHERE cust_email IS NULL</div></pre></td></tr></table></figure>
<p>在上面两个版本中， <code>DECLARE</code> 语句用来定义和命名游标，这里为<code>CustCursor</code> 。 <code>SELECT</code> 语句定义一个包含没有电子邮件地址（ <code>NULL</code> 值）的所有顾客的游标。定义游标之后，就可以打开它了。</p>
<h4 id="使用游标-1"><a href="#使用游标-1" class="headerlink" title="使用游标"></a>使用游标</h4><p>使用 <code>OPEN CURSOR</code> 语句打开游标，这条语句很简单，在大多数 DBMS<br>中的语法相同：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">OPEN CURSOR CustCursor</div></pre></td></tr></table></figure></p>
<p>在处理 <code>OPEN CURSOR</code> 语句时，执行查询，存储检索出的数据以供浏览和滚动。<br>现在可以用 <code>FETCH</code> 语句访问游标数据了。 <code>FETCH</code> 指出要检索哪些行，从何处检索它们以及将它们放于何处（如变量名）。</p>
<p>使用 Oracle语法：<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">DECLARE</span> <span class="keyword">TYPE</span> CustCursor <span class="keyword">IS</span> <span class="keyword">REF</span> <span class="keyword">CURSOR</span> <span class="comment">--声明变量</span></div><div class="line"><span class="keyword">RETURN</span> Customers%ROWTYPE;</div><div class="line"><span class="keyword">DECLARE</span> CustRecord Customers%ROWTYPE</div><div class="line"><span class="keyword">BEGIN</span></div><div class="line"><span class="keyword">OPEN</span> CustCursor;</div><div class="line">FETCH CustCursor INTO CustRecord;</div><div class="line">CLOSE CustCursor;</div><div class="line"><span class="keyword">END</span>;</div></pre></td></tr></table></figure></p>
<p> <code>FETCH</code> 用来检索当前行（自动从第一行开始），放到声明的变量<code>CustRecord</code> 中。对于检索出来的数据不做任何处理。</p>
<p>从第一行到最后一行，对检索出来的数据进行循环：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">DECLARE</span> <span class="keyword">TYPE</span> CustCursor <span class="keyword">IS</span> <span class="keyword">REF</span> <span class="keyword">CURSOR</span></div><div class="line"><span class="keyword">RETURN</span> Customers%ROWTYPE;</div><div class="line"><span class="keyword">DECLARE</span> CustRecord Customers%ROWTYPE</div><div class="line"><span class="keyword">BEGIN</span></div><div class="line"><span class="keyword">OPEN</span> CustCursor;</div><div class="line">LOOP</div><div class="line">FETCH CustCursor INTO CustRecord;</div><div class="line">EXIT WHEN CustCursor%NOTFOUND;</div><div class="line">...</div><div class="line"><span class="keyword">END</span> <span class="keyword">LOOP</span>;</div><div class="line">CLOSE CustCursor;</div><div class="line"><span class="keyword">END</span>;</div></pre></td></tr></table></figure>
<p>使用 <code>FETCH</code> 检索当前行，放到一个名为<code>CustRecord</code> 的变量中。但不一样的是，这里的 <code>FETCH</code> 位于 <code>LOOP</code> 内，因此它反复执行。代码 <code>EXIT WHEN CustCursor%NOTFOUND</code> 在取不出更多的行时终止处理（退出循环）。</p>
<p>使用 Microsoft SQL Server语法：</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">DECLARE</span> @cust_id <span class="built_in">CHAR</span>(<span class="number">10</span>),</div><div class="line">@cust_name <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_address <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_city <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_state <span class="built_in">CHAR</span>(<span class="number">5</span>),</div><div class="line">@cust_zip <span class="built_in">CHAR</span>(<span class="number">10</span>),</div><div class="line">@cust_country <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_contact <span class="built_in">CHAR</span>(<span class="number">50</span>),</div><div class="line">@cust_email <span class="built_in">CHAR</span>(<span class="number">255</span>)</div><div class="line"><span class="keyword">OPEN</span> CustCursor</div><div class="line"><span class="keyword">FETCH</span> <span class="keyword">NEXT</span> <span class="keyword">FROM</span> CustCursor</div><div class="line"><span class="keyword">INTO</span> @cust_id, @cust_name, @cust_address,</div><div class="line">@cust_city, @cust_state, @cust_zip,</div><div class="line">@cust_country, @cust_contact, @cust_email</div><div class="line"><span class="keyword">WHILE</span> @@FETCH_STATUS = <span class="number">0</span></div><div class="line"><span class="keyword">BEGIN</span></div><div class="line"><span class="keyword">FETCH</span> <span class="keyword">NEXT</span> <span class="keyword">FROM</span> CustCursor</div><div class="line"><span class="keyword">INTO</span> @cust_id, @cust_name, @cust_address,</div><div class="line">@cust_city, @cust_state, @cust_zip,</div><div class="line">@cust_country, @cust_contact, @cust_email</div><div class="line"><span class="keyword">END</span></div><div class="line"><span class="keyword">CLOSE</span> CustCursor</div></pre></td></tr></table></figure>
<p>为每个检索出的列声明一个变量， <code>FETCH</code> 语句检索一行并保存值到这些变量中。使用 <code>WHILE</code> 循环处理每一行，条件 <code>WHILE @@FETCH_STATUS = 0</code> 在取不出更多的行时终止处理（退出循环）。</p>
<h4 id="关闭游标"><a href="#关闭游标" class="headerlink" title="关闭游标"></a>关闭游标</h4><p>游标在使用完毕时需要关闭，释放游标所占用的资源。<br><figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">CLOSE CustCursor <span class="comment">--游标名</span></div><div class="line"><span class="keyword">DEALLOCATE</span> <span class="keyword">CURSOR</span> CustCursor</div></pre></td></tr></table></figure></p>
<p>一旦游标关闭，如果不再次打开，将不能使用。第二次使用它时不需要再声明，只需用 <code>OPEN</code> 打开它即可。</p>
<h2 id="高级-SQL-特性"><a href="#高级-SQL-特性" class="headerlink" title="高级 SQL 特性"></a>高级 SQL 特性</h2><blockquote>
<p>约束是实施引用完整性的重要部分，索引可改善数据检索的性能，触发器可以用来执行运行前后的处理，安全选项可用来管理数据访问。</p>
<h3 id="约束"><a href="#约束" class="headerlink" title="约束"></a>约束</h3><p>关系数据库存储分解为多个表的数据，每个表存储相应的数据。利用键来建立从一个表到另一个表的引用（由此产生了术语引用完整性<code>referential integrity</code>）。<br>正确地进行关系数据库设计，需要一种方法保证只在表中插入合法数据。如果 <code>Orders</code> 表存储订单信息， <code>OrderItems</code> 表存储订单详细内容，应该保证 <code>OrderItems</code> 中引用的任何订单 <code>ID</code>都存在于 <code>Orders</code> 中。类似地，在 <code>Orders</code> 表中引用的任意顾客必须存在于 <code>Customers</code> 表中。</p>
</blockquote>
<p> 约束（<code>constraint</code>）<br>管理如何插入或处理数据库数据的规则。DBMS 通过在数据库表上施加约束来实施引用完整性。大多数约束是在表定义时定义的</p>
<p>####主键</p>
<blockquote>
<p>主键是一种特殊的约束，用来保证一列（或多列）中的值是唯一的，而且永不改动。换句话说，表中的一列（或多个列）的值唯一标识表中的每一行。这方便了直接或交互地处理表中的行。没有主键，要安全地 <code>UPDATE</code> 或 <code>DELETE</code> 特定行而不影响其他行会非常困难。</p>
</blockquote>
<p>表中任意列只要满足以下条件，都可以用于主键。</p>
<ol>
<li>任意两行的主键值都不相同。</li>
<li>每行都具有一个主键值（不允许 <code>NULL</code> 值）。</li>
<li>包含主键值的列从不修改或更新。</li>
<li>主键值不能重用。如果从表中删除某一行，其主键值不分配给新行。</li>
</ol>
<p>定义主键：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE Vendors</div><div class="line">(</div><div class="line">vend_id CHAR(10) NOT NULL PRIMARY KEY,</div><div class="line">vend_name CHAR(50) NOT NULL,</div><div class="line">vend_address CHAR(50) NULL,</div><div class="line">vend_city CHAR(50) NULL,</div><div class="line">vend_state CHAR(5) NULL,</div><div class="line">vend_zip CHAR(10) NULL,</div><div class="line">vend_country CHAR(50) NULL</div><div class="line">);</div></pre></td></tr></table></figure></p>
<p>给表的 <code>vend_id</code> 列定义添加关键字 <code>PRIMARY KEY</code> ，使其成为主键。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ALTER TABLE Vendors</div><div class="line">ADD CONSTRAINT PRIMARY KEY (vend_id);</div></pre></td></tr></table></figure>
<p>这里定义相同的列为主键，但使用的是 <code>CONSTRAINT</code> 语法。此语法也可以用于 <code>CREATE TABLE</code> 和 <code>ALTER TABLE</code> 语句。</p>
<h4 id="外键"><a href="#外键" class="headerlink" title="外键"></a>外键</h4><blockquote>
<p>外键是表中的一列，其值必须列在另一表的主键中。外键是保证引用完整性的极其重要部分。</p>
</blockquote>
<p>我们举个例子来理解外键。<br><code>Orders</code> 表将录入到系统的每个订单作为一行包含其中。顾客信息存储在<br><code>Customers</code> 表中。 <code>Orders</code> 表中的订单通过顾客 <code>ID</code>与 <code>Customers</code> 表中的特定行相关联。顾客 <code>ID</code> 为 <code>Customers</code> 表的主键，每个顾客都有唯一的<code>ID</code>。订单号为 <code>Orders</code> 表的主键，每个订单都有唯一的订单号。<code>Orders</code> 表中顾客 <code>ID</code>列的值不一定是唯一的。如果某个顾客有多个订单，则有多个行具有相同的顾客 <code>ID</code>（虽然每个订单都有不同的订单号）。同时， <code>Orders</code> 表中顾客 <code>ID</code>列的合法值为 <code>Customers</code> 表中顾客的 <code>ID</code>。<br>这就是外键的作用。在这个例子中，在 <code>Orders</code> 的顾客 <code>ID</code>列上定义了一<br>个外键，因此该列只能接受 <code>Customers</code> 表的主键值。</p>
<p>外键定义方法：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE Orders</div><div class="line">(</div><div class="line">order_num INTEGER NOT NULL PRIMARY KEY,</div><div class="line">order_date DATETIME NOT NULL,</div><div class="line">cust_id CHAR(10) NOT NULL REFERENCES Customers(cust_id)</div><div class="line">);</div></pre></td></tr></table></figure></p>
<p>其中的表定义使用了 <code>REFERENCES</code> 关键字，它表示 <code>cust_id</code> 中的任何值都必须是 <code>Customers</code> 表的 <code>cust_id</code> 中的值。<br>也可以在 <code>ALTER TABLE</code> 语句中用 <code>CONSTRAINT</code> 语法来完成：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">ALTER TABLE Orders</div><div class="line">ADD CONSTRAINT</div><div class="line">FOREIGN KEY (cust_id) REFERENCES Customers (cust_id)</div></pre></td></tr></table></figure></p>
<p> 外键有助防止意外删除<br>除帮助保证引用完整性外，外键还有另一个重要作用。在定义外后，<code>DBMS</code> 不允许删除在另一个表中具有关联行的行。例如，不能删除关联订单的顾客。删除该顾客的唯一方法是首先删除相关的订单（这表示还要删除相关的订单项），有的 DBMS支持称为级联删除（<code>cascading delete</code>）的特性。例如，如果启用级联删除并且从 <code>Customers</code> 表中删除某个顾客，则任何关联的订单行也会被自动删除。</p>
<h4 id="唯一约束"><a href="#唯一约束" class="headerlink" title="唯一约束"></a>唯一约束</h4><p>唯一约束用来保证一列（或一组列）中的数据是唯一的。它们类似于主键，但存在以下重要区别。</p>
<ol>
<li>表可包含多个唯一约束，但每个表只允许一个主键。</li>
<li>唯一约束列可包含 <code>NULL</code> 值。</li>
<li>唯一约束列可修改或更新。</li>
<li>唯一约束列的值可重复使用。</li>
<li><p>与主键不一样，唯一约束不能用来定义外键。</p>
<p>唯一约束既可以用 <code>UNIQUE</code> 关键字在表定义中定义，也可以用单独的 <code>CONSTRAINT</code> 定义。</p>
</li>
</ol>
<h4 id="检查约束"><a href="#检查约束" class="headerlink" title="检查约束"></a>检查约束</h4><blockquote>
<p>检查约束用来保证一列（或一组列）中的数据满足一组指定的条件。</p>
</blockquote>
<p>检查约束的常见用途有以下几点。</p>
<ol>
<li>检查最小或最大值。例如，防止 0个物品的订单（即使 0是合法的数）。</li>
<li>指定范围。例如，保证发货日期大于等于今天的日期，但不超过今天<br>起一年后的日期。</li>
<li>只允许特定的值。例如，在性别字段中只允许 M 或 F。</li>
</ol>
<p>检查约束在数据类型内又做了进一步的限制，这些限制极其重要，可以确保插入数据库的数据正是你想要的数据。不需要依赖于客户端应用程序或用户来保证正确获取它，DBMS本身将会拒绝任何无效的数据。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">CREATE TABLE OrderItems</div><div class="line">(</div><div class="line">order_num INTEGER NOT NULL,</div><div class="line">order_item INTEGER NOT NULL,</div><div class="line">prod_id CHAR(10) NOT NULL,</div><div class="line">quantity INTEGER NOT NULL CHECK (quantity &gt; 0),</div><div class="line">item_price MONEY NOT NULL</div><div class="line">);</div></pre></td></tr></table></figure>
<p>利用这个约束，任何插入（或更新）的行都会被检查，保证 <code>quantity</code>大于 0。<br>检查名为 <code>gender</code> 的列只包含 M 或 F ，可编写如下的 <code>ALTER TABLE</code> 语句：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">ADD CONSTRAINT CHECK (gender LIKE &apos;[MF]&apos;)</div></pre></td></tr></table></figure></p>
<h3 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h3><blockquote>
<p>索引用来排序数据以加快搜索和排序操作的速度。</p>
</blockquote>
<p>主键数据总是排序的，这是 DBMS的工作。因此，按主键检索特定行总是一种快速有效的操作，但是，搜索其他列中的值通常效率不高。</p>
<p>使用索引，可以在一个或多个列上定义索引，使 DBMS保存其内容的一个排过序的列表。DBMS 搜索排过序的索引，找出匹配的位置，然后<br>检索这些行。</p>
<p>在开始创建索引前，应该记住以下内容:</p>
<ol>
<li>索引改善检索操作的性能，但降低了数据插入、修改和删除的性能。<br>在执行这些操作时，DBMS必须动态地更新索引。</li>
<li>索引数据可能要占用大量的存储空间。</li>
<li>并非所有数据都适合做索引。</li>
<li>索引用于数据过滤和数据排序。</li>
<li>可以在索引中定义多个列（例如，州加上城市）。</li>
</ol>
<p>索引用 <code>CREATE INDEX</code> 语句创建。</p>
<figure class="highlight sql"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">CREATE</span> <span class="keyword">INDEX</span> prod_name_ind</div><div class="line"><span class="keyword">ON</span> Products (prod_name);</div></pre></td></tr></table></figure>
<p>索引必须唯一命名。这里的索引名 <code>prod_name_ind</code> 在关键字 <code>CREATE INDEX</code> 之后定义。 <code>ON</code> 用来指定被索引的表，而索引中包含的列（此例中仅有一列）在表名后的圆括号中给出。<br> 检查索引<br>索引的效率随表数据的增加或改变而变化。最好定期检查索引，并根据需要对索引进行调整。</p>
<h3 id="触发器"><a href="#触发器" class="headerlink" title="触发器"></a>触发器</h3><blockquote>
<p>触发器是特殊的存储过程，它在特定的数据库活动发生时自动执行。触发器可以与特定表上的 <code>INSERT</code> 、 <code>UPDATE</code> 和 <code>DELETE</code> 操作（或组合）相关联。</p>
</blockquote>
<p>与存储过程不一样，触发器与单个的表相关联。例如， <code>Orders</code> 表上的 <code>INSERT</code> 操作相关联的触发器只在<code>Orders</code> 表中插入行时执行。</p>
<p>触发器内的代码具有以下数据的访问权：</p>
<ol>
<li><code>INSERT</code> 操作中的所有新数据；</li>
<li><code>UPDATE</code> 操作中的所有新数据和旧数据；</li>
<li><code>DELETE</code> 操作中删除的数据。</li>
</ol>
<p>触发器的一些常见用途：</p>
<ol>
<li>保证数据一致。例如，在 <code>INSERT</code> 或 <code>UPDATE</code> 操作中将所有州名转换<br>为大写。</li>
<li>基于某个表的变动在其他表上执行活动。例如，每当更新或删除一行<br>时将审计跟踪记录写入某个日志表。</li>
<li>进行额外的验证并根据需要回退数据。例如，保证某个顾客的可用资<br>金不超限定，如果已经超出，则阻塞插入。</li>
<li>计算计算列的值或更新时间戳。</li>
</ol>
<p>下面的例子创建一个触发器，它对所有 <code>INSERT</code> 和 <code>UPDATE</code> 操作，将<code>Customers</code> 表中的 <code>cust_state</code> 列转换为大写。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">CREATE TRIGGER customer_state</div><div class="line">ON Customers</div><div class="line">FOR INSERT, UPDATE</div><div class="line">AS</div><div class="line">UPDATE Customers</div><div class="line">SET cust_state = Upper(cust_state)</div><div class="line">WHERE Customers.cust_id = inserted.cust_id;</div></pre></td></tr></table></figure>
<p> 约束的处理比触发器快，因此在可能的时候，应该尽量使<br>用约束。</p>
<h3 id="数据库安全"><a href="#数据库安全" class="headerlink" title="数据库安全"></a>数据库安全</h3><p>任何安全系统的基础都是用户授权和身份确认。这是一种处理，通过这种处理对用户进行确认，保证他是有权用户，允许执行他要执行的操作。</p>
<p>需要保护的操作有：</p>
<ol>
<li>对数据库管理功能（创建表、更改或删除已存在的表等）的访问；</li>
<li>对特定数据库或表的访问；</li>
<li>访问的类型（只读、对特定列的访问等）；</li>
<li>仅通过视图或存储过程对表进行访问；</li>
<li>创建多层次的安全措施，从而允许多种基于登录的访问和控制；</li>
<li>限制管理用户账号的能力。</li>
</ol>
<p>安全性使用 SQL的 <code>GRANT</code> 和 <code>REVOKE</code> 语句来管理，不过，大多数 DBMS提供了交互式的管理实用程序，这些实用程序在内部使用 <code>GRANT</code> 和<code>REVOKE</code> 语句。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;SQL学习笔记&quot;&gt;&lt;a href=&quot;#SQL学习笔记&quot; class=&quot;headerlink&quot; title=&quot;SQL学习笔记&quot;&gt;&lt;/a&gt;SQL学习笔记&lt;/h1&gt;&lt;p&gt;@(数据库)&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./azure_sql_featured_image
    
    </summary>
    
    
      <category term="tas1" scheme="http://yoursite.com/tags/tas1/"/>
    
      <category term="tag2" scheme="http://yoursite.com/tags/tag2/"/>
    
  </entry>
  
  <entry>
    <title>github入门</title>
    <link href="http://yoursite.com/2016/10/31/github%E5%85%A5%E9%97%A8/"/>
    <id>http://yoursite.com/2016/10/31/github入门/</id>
    <published>2016-10-31T11:34:54.000Z</published>
    <updated>2016-10-31T11:36:38.669Z</updated>
    
    <content type="html"><![CDATA[<p><img src="http://i.imgur.com/xwgYY5O.jpg" alt="github"></p>
<h3 id="Welcome-to-Github-World"><a href="#Welcome-to-Github-World" class="headerlink" title="Welcome to Github World"></a><em>Welcome to Github World</em></h3><blockquote>
<p>GitHub 是为开发者提供 Git 仓库的托管服务。这是一个让开发者与 朋友、同事、同学及陌生人共享代码的完美场所。</p>
<h4 id="GitHub-与-Git-的区别"><a href="#GitHub-与-Git-的区别" class="headerlink" title="GitHub 与 Git 的区别"></a>GitHub 与 Git 的区别</h4><p>GitHub 与 Git 是完全不同的两个东西。<br>在 Git 中，开发者将源代码存入名叫“Git 仓库”的资料库中并加以使用。而 GitHub 则是在网络上提供 Git 仓库的一项服务。也就是说，GitHub 上公开的软件源代码全都由 Git 进行管理。</p>
</blockquote>
<p>理解 Git，是熟练运用 GitHub 的关键所在。</p>
<p><strong>Pull Request</strong> 是指开发者在本地对源代码进行更改后，向 GitHub 中托管的 Git 仓库请求合并的功能。开发者可以在 Pull Request 上通过评论交流，来更改源代码，并公开更改的细节，然后向仓库提交合并请求。而且，如果请求的更改与项目的初衷相违，也可以选择拒绝合并。GitHub 的 Pull Request 不但能轻松查看源代码的前后差别，还可以对指定的一行代码进行评论，使代码审查的工作变得非常便捷。</p>
<h4 id="社会化编程"><a href="#社会化编程" class="headerlink" title="社会化编程"></a>社会化编程</h4><p>革命领导者 GitHub 的口号便是“社会化编程”。<br>Github让世界上任何人都可以比从前更加容易地获得源代码，将其自由更改并加以公开。</p>
<ul>
<li><strong>不要闭目塞听，要接触不同的文化</strong><br>在工作中接触非公开代码的职业程序员们，更应该接触世界上的不同文化，拓展见闻。如果只在公司这一封闭的小世界中敲代码，往往在不知不觉间，手中的技术就变得陈腐不堪了。</li>
<li><strong>GitHub 最大的特征是“面向人”</strong><br>GitHub 除项目之外，还可以把注意力集中到人身上。我们不但能阅览一个人公开的所有源代码，只要查看其控制面板中的 News Feed，还能知道他对哪些仓库感兴趣，什么时候做过提交等。一个人在 GitHub进行的开发是一目了然的。<h4 id="GitHub-提供的主要功能"><a href="#GitHub-提供的主要功能" class="headerlink" title="GitHub 提供的主要功能"></a>GitHub 提供的主要功能</h4></li>
</ul>
<ol>
<li>Git 仓库，我们可以免费建立任意个 GitHub 提供的 Git 仓库。</li>
<li>Organization，个人使用时只要使用个人账户就足够了，但如果是公司，建议使用 Organization 账户。它的优点在于可以统一管理账户和权限，还能统一支付一些费用。</li>
<li><p>Issue，Issue 功能，是将一个任务或问题分配给一个 Issue 进行追踪和管理的功能。可以像 BUG 管理系统或 TiDD（Ticket-driven Development）的Ticket 一样使用。在 GitHub 上，每当进行Pull Request，都会同时创建一个 Issue。</p>
<blockquote>
<p> 每一个功能更改或修正都对应一个 Issue，讨论或修正都以这个Issue 为中心进行。只要查看 Issue，就能知道和这个更改相关的一切信息，并以此进行管理。</p>
</blockquote>
</li>
<li><p>Wiki，通过 Wiki 功能，任何人都能随时对一篇文章进行更改并保存，因此可以多人共同完成一篇文章。该功能常用在开发文档或手册的编写<br>中。</p>
<blockquote>
<p>Wiki 页也是作为 Git 仓库进行管理的，改版的历史记录会被切实保存下来，使用者可以放心改写。由于其支持克隆至本地进行编辑，所以程序员使用时可以不必开启浏览器。</p>
</blockquote>
</li>
<li><p>Pull Request，开发者向 GitHub 的仓库推送更改或功能添加后，可以通过 PullRequest 功能向别人的仓库提出申请，请求对方合并。</p>
<blockquote>
<p>Pull Request 送出后，目标仓库的管理者等人将能够查看 PullRequest 的内容及其中包含的代码更改。<br>同时，GitHub 还提供了对 Pull Request 和源代码前后差别进行讨论的功能。通过此功能，可以以<strong>行</strong>为单位对源代码添加评论，让程序员之间高效地交流。</p>
</blockquote>
</li>
</ol>
<h3 id="Git-的导入"><a href="#Git-的导入" class="headerlink" title="Git 的导入"></a>Git 的导入</h3><blockquote>
<p>GitHub 的核心功能是 Git 仓库管理 。因此，使用 GitHub 之前必须先掌握 Git 的相关知识，同时本地的设备还要安装 Git 的环境。</p>
<h4 id="版本管理"><a href="#版本管理" class="headerlink" title="版本管理"></a>版本管理</h4><p>版本管理就是管理更新的历史记录。它为我们提供了一些在软件开发过程中必不可少的功能，例如记录一款软件添加或更改源代码的过程，回滚到特定阶段，恢复误删除的文件等。</p>
<h5 id="集中型与分散型"><a href="#集中型与分散型" class="headerlink" title="集中型与分散型"></a>集中型与分散型</h5><p>以 Subversion 为代表的集中型将所有数据集中存放在服务器当中，有便于管理的优点。但是一旦开发者所处的环境不能连接服务器，就无法获取最新的源代码，开发也就几乎无法进行。服务器宕机时也是同样的道理，而且万一服务器故障导致数据消失，恐怕开发者就再也见不到最新的源代码了。</p>
</blockquote>
<h2 id="社区功能"><a href="#社区功能" class="headerlink" title="社区功能"></a>社区功能</h2><p>Follow（关注）别人</p>
<blockquote>
<p> Follow 的用户的活动就会显示在您的控制面板页面<br> 中。您可以通过这种方法知道那个人在 GitHub 上都做了些什么。</p>
</blockquote>
<p>也可以使用 Watch 功能获取最新的开发信息。如果您经<br>常使用的某个软件正在 GitHub 上进行开发，不妨去 Watch 一下。</p>
<h2 id="新建仓库"><a href="#新建仓库" class="headerlink" title="新建仓库"></a>新建仓库</h2><h3 id="Initialize-this-repository-with-a-README"><a href="#Initialize-this-repository-with-a-README" class="headerlink" title="Initialize this repository with a README"></a>Initialize this repository with a README</h3><blockquote>
<p>在 Initialize this repository with a README 选项上打钩，随后<br>GitHub 会自动初始化仓库并设置 README 文件，让用户可以立刻clone 这个仓库。如果想向 GitHub 添加手中已有的 Git 仓库，建议不要勾选，直接手动 push。<small>README.md 在初始化时已经生成好了。README.md 文件的内容<br>会自动显示在仓库的首页当中。因此，人们一般会在这个文件中标明本<br>仓库所包含的软件的概要、使用流程、许可协议等信息。如果使用<br>Markdown 语法进行描述，还可以添加标记，提高可读性。</small></p>
<h2 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a><strong>基本操作</strong></h2><h2 id="git-init——初始化仓库"><a href="#git-init——初始化仓库" class="headerlink" title="git init——初始化仓库"></a>git init——初始化仓库</h2><p>要使用 Git 进行版本管理，必须先初始化仓库。Git 是使用 git<br>init命令进行初始化的。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">$ mkdir git-tutorial</div><div class="line">$ cd git-tutorial</div><div class="line">$ git init</div><div class="line">Initialized empty Git repository in /Users/hirocaster/github/github-book</div><div class="line">/git-tutorial/.git/</div></pre></td></tr></table></figure>
<p><hr></p>
<blockquote>
<p>如果初始化成功，执行了 git init命令的目录下就会生成 .git 目录。这个 .git 目录里存储着管理当前目录内容所需的仓库数据。</p>
</blockquote>
<h3 id="git-status——查看仓库的状态"><a href="#git-status——查看仓库的状态" class="headerlink" title="git status——查看仓库的状态"></a>git status——查看仓库的状态</h3><blockquote>
<p>git status命令用于显示 Git 仓库的状态。这是一个十分常用的命令，请务必牢记。</p>
<h3 id="git-add——向暂存区中添加文件"><a href="#git-add——向暂存区中添加文件" class="headerlink" title="git add——向暂存区中添加文件"></a>git add——向暂存区中添加文件</h3><p>如果只是用 Git 仓库的工作树创建了文件，那么该文件并不会被记入 Git 仓库的版本管理对象当中。因此我们用 git status命令查看README.md 文件时，它会显示在 Untracked files 里。要想让文件成为 Git 仓库的管理对象，就需要用 git add命令将其加入暂存区（Stage 或者 Index）中。暂存区是提交之前的一个临时区域。</p>
<h3 id="git-commit——保存仓库的历史记录"><a href="#git-commit——保存仓库的历史记录" class="headerlink" title="git commit——保存仓库的历史记录"></a>git commit——保存仓库的历史记录</h3><p>git commit命令可以将当前暂存区中的文件实际保存到仓库的历史记录中。通过这些记录，我们就可以在工作树中复原文件。</p>
</blockquote>
<ul>
<li>记述一行提交信息</li>
</ul>
<p>我们来实际运行一下 git commit命令。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">$ git commit -m &quot;First commit&quot;</div><div class="line">[master (root-commit) 9f129ba] First commit</div><div class="line">1 file changed, 0 insertions(+), 0 deletions(-)</div><div class="line">create mode 100644 README.md</div></pre></td></tr></table></figure>
<p>-m 参数后的 “First commit”称作提交信息，是对这个提交的概述。</p>
<ul>
<li>记述详细提交信息</li>
</ul>
<p>如果想要记述得更加详<br>细，请不加 -m，直接执行 git commit命令。<br>执行后编辑器就会启<br>动，并显示如下结果。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"># Please enter the commit message for your changes. Lines starting</div><div class="line"># with &apos;#&apos; will be ignored, and an empty message aborts the commit.</div><div class="line"># On branch master</div><div class="line">#</div><div class="line"># Initial commit</div><div class="line">#</div><div class="line"># Changes to be committed:</div><div class="line"># (use &quot;git rm --cached &lt;file&gt;...&quot; to unstage)</div><div class="line">#</div><div class="line"># new file: README.md</div><div class="line">#</div></pre></td></tr></table></figure>
<p>在编辑器中记述提交信息的格式如下。<br>第一行：用一行文字简述提交的更改内容<br>第二行：空行<br>第三行以后：记述更改的原因和详细内容</p>
<p>在以 #（井号）标为注释的 Changes to be committed（要提交的更改）栏中，可以查看本次提交中包含的文件。</p>
<ul>
<li>中止提交<br>如果在编辑器启动后想中止提交，请将提交信息留空并直接关闭编<br>辑器，随后提交就会被中止。<h3 id="git-log——查看提交日志"><a href="#git-log——查看提交日志" class="headerlink" title="git log——查看提交日志"></a>git log——查看提交日志</h3><blockquote>
<p>git log命令可以查看以往仓库中提交的日志。</p>
</blockquote>
</li>
</ul>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">$ git log</div><div class="line">commit 9f129bae19b2c82fb4e98cde5890e52a6c546922</div><div class="line">Author: hirocaster &lt;hohtsuka@gmail.com&gt;</div><div class="line">Date: Sun May 5 16:06:49 2013 +0900</div><div class="line">First commit</div></pre></td></tr></table></figure>
<p>如上图所示，屏幕显示了刚刚的提交操作。commit 栏旁边显示的“9f129b……”是指向这个提交的哈希值。Git 的其他命令中，在指向提交时会用到这个哈希值。<br>Author 栏中显示我们给 Git 设置的用户名和邮箱地址。Date 栏中显示提交执行的日期和时间。再往下就是该提交的提交信息。</p>
<ul>
<li>只显示提交信息的第一行<br>如果只想让程序显示第一行简述信息，可以在 git log命令后加上 –pretty=short。这样一来开发人员就能够更轻松地把握多个提交。</li>
</ul>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">$ git log --pretty=short</div><div class="line">commit 9f129bae19b2c82fb4e98cde5890e52a6c546922</div><div class="line">Author: hirocaster &lt;hohtsuka@gmail.com&gt;</div><div class="line">First commit</div></pre></td></tr></table></figure>
<ul>
<li><p>只显示指定目录、文件的日志<br>只要在 git log命令后加上目录名，便会只显示该目录下的日志。<br>如果加的是文件名，就会只显示与该文件相关的日志。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ git log README.md</div></pre></td></tr></table></figure>
</li>
<li><p>显示文件的改动<br>如果想查看<strong>提交</strong>所带来的改动，可以加上 -p参数，文件的前后差别就会显示在提交信息之后。</p>
</li>
</ul>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ git log -p</div></pre></td></tr></table></figure>
<p>比如，执行下面的命令，就可以只查看 README.md 文件的提交日志以及提交前后的差别。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ git log -p README.md</div></pre></td></tr></table></figure>
<h3 id="git-diff——查看更改前后的差别"><a href="#git-diff——查看更改前后的差别" class="headerlink" title="git diff——查看更改前后的差别"></a>git diff——查看更改前后的差别</h3><blockquote>
<p>git diff命令可以查看工作树、暂存区、最新提交之间的差别。</p>
</blockquote>
<p>我们在刚刚提交的 README.md 中写点东西。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"># Git教程</div></pre></td></tr></table></figure></p>
<ul>
<li>查看工作树和暂存区的差别<br><strong>执行 git diff命令，查看当前工作树与暂存区的差别。</strong></li>
</ul>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">$ git diff</div><div class="line">diff --git a/README.md b/README.md</div><div class="line">index e69de29..cb5dc9f 100644</div><div class="line">--- a/README.md</div><div class="line">+++ b/README.md</div><div class="line">@@ -0,0 +1 @@</div><div class="line">+# Git教程</div></pre></td></tr></table></figure>
<p>由于我们尚未用 git add命令向暂存区添加任何东西，所以程序只会显示工作树与最新提交状态之间的差别。</p>
<blockquote>
<p>“+”号标出的是新添加的行，被删除的行则用“-”号标出。</p>
</blockquote>
<ul>
<li>查看工作树和最新提交的差别<br>如果现在执行 git diff命令，由于工作树和暂存区的状态并无差别，结果什么都不会显示。要查看与最新提交的差别，请执行以下命令。</li>
</ul>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">$ git diff HEAD</div><div class="line">diff --git a/README.md b/README.md</div><div class="line">index e69de29..cb5dc9f 100644</div><div class="line">--- a/README.md</div><div class="line">+++ b/README.md</div><div class="line">@@ -0,0 +1 @@</div><div class="line">+# Git教程</div></pre></td></tr></table></figure>
<p>不妨养成这样一个好习惯：在执行 <code>git commit</code>命令<strong>之前</strong>先执行<br><code>git diff HEAD</code>命令，查看本次提交与上次提交之间有什么差别，等确认完毕后再进行提交。<strong>这里的 <code>HEAD</code> 是指向当前分支中最新一次提交的指针。</strong></p>
]]></content>
    
    <summary type="html">
    
      github入门
    
    </summary>
    
      <category term="代码托管" scheme="http://yoursite.com/categories/%E4%BB%A3%E7%A0%81%E6%89%98%E7%AE%A1/"/>
    
    
      <category term="github" scheme="http://yoursite.com/tags/github/"/>
    
  </entry>
  
  <entry>
    <title>JavaScript之数组</title>
    <link href="http://yoursite.com/2016/10/21/JavaScript%E4%B9%8B%E6%95%B0%E7%BB%84/"/>
    <id>http://yoursite.com/2016/10/21/JavaScript之数组/</id>
    <published>2016-10-21T13:28:23.000Z</published>
    <updated>2016-10-21T13:30:12.750Z</updated>
    
    <content type="html"><![CDATA[<h2 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h2><p>@(JavaScript)</p>
<hr>
<p>[TOC]</p>
<h3 id="数组元素的增删"><a href="#数组元素的增删" class="headerlink" title="数组元素的增删"></a>数组元素的增删</h3><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> array = [];</div><div class="line">array[<span class="number">0</span>] = <span class="number">22</span>;</div><div class="line">array.push(<span class="string">"sstd"</span>);  <span class="comment">// 尾部追加</span></div><div class="line">array[array.length]=<span class="number">521</span>; <span class="comment">// 尾部追加</span></div><div class="line">array.unshift(<span class="number">5</span>);  <span class="comment">// 头部添加</span></div><div class="line"><span class="built_in">console</span>.log(array);</div><div class="line">result：</div><div class="line">[ <span class="number">5</span>, <span class="number">22</span>, <span class="string">'sstd'</span>, <span class="number">521</span> ]</div></pre></td></tr></table></figure>
<h3 id="数组元素的删除"><a href="#数组元素的删除" class="headerlink" title="数组元素的删除"></a>数组元素的删除</h3><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">delete</span> array[<span class="number">1</span>];  <span class="comment">// 删除其中一个元素,该值变为undefined,数组长度不变</span></div><div class="line"><span class="built_in">console</span>.log(array);  <span class="comment">// [ 5, , 'sstd', 521 ]</span></div><div class="line"><span class="built_in">console</span>.log(array.length); <span class="comment">// 4</span></div><div class="line">array.length -= <span class="number">1</span>;  <span class="comment">// 删除最后一个元素 </span></div><div class="line"><span class="built_in">console</span>.log(array);  <span class="comment">// [ 5, , 'sstd' ]</span></div><div class="line"><span class="built_in">console</span>.log(array.length); <span class="comment">// 3</span></div><div class="line">array.pop();  <span class="comment">// 尾部删除元素</span></div><div class="line"><span class="built_in">console</span>.log(array);  <span class="comment">// [ 5,  ]</span></div><div class="line"><span class="built_in">console</span>.log(array.length); <span class="comment">// 2</span></div><div class="line">array.shift();  <span class="comment">// 头部删除元素 </span></div><div class="line"><span class="built_in">console</span>.log(array); <span class="comment">// [  ]</span></div><div class="line"><span class="built_in">console</span>.log(array.length); <span class="comment">// 1</span></div></pre></td></tr></table></figure>
<h3 id="数组遍历"><a href="#数组遍历" class="headerlink" title="数组遍历"></a>数组遍历</h3><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> array = [[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>], [<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]];</div><div class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; array.length; i++)</div><div class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> j = <span class="number">0</span>; j &lt; array[i].length; j++)</div><div class="line">        <span class="built_in">console</span>.log(array[i][j]);</div><div class="line">        </div><div class="line"><span class="keyword">var</span> arr = [<span class="number">15</span>, <span class="number">2</span>, <span class="number">110</span>, <span class="number">78</span>, <span class="number">65</span>, <span class="number">100</span>];</div><div class="line">arr.forEach(<span class="function"><span class="keyword">function</span> (<span class="params">x</span>)</span></div><div class="line">&#123;</div><div class="line">    <span class="built_in">console</span>.log(x);</div><div class="line">&#125;)</div></pre></td></tr></table></figure>
<h3 id="数组方法"><a href="#数组方法" class="headerlink" title="数组方法"></a>数组方法</h3><h4 id="数组转字符串"><a href="#数组转字符串" class="headerlink" title="数组转字符串"></a>数组转字符串</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</div><div class="line"></div><div class="line"><span class="built_in">console</span>.log(arr.join());</div><div class="line"><span class="built_in">console</span>.log(arr.join(<span class="string">"-"</span>));</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">repeatString</span>(<span class="params">str, n</span>)</span></div><div class="line">    &#123;</div><div class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Array</span>(n+<span class="number">1</span>).join(str);</div><div class="line">    &#125;</div><div class="line"><span class="built_in">console</span>.log(repeatString(<span class="string">"Hi"</span>,<span class="number">3</span>));</div><div class="line">result:</div><div class="line"><span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span></div><div class="line"><span class="number">1</span><span class="number">-2</span><span class="number">-3</span></div><div class="line">HiHiHi</div></pre></td></tr></table></figure>
<h4 id="数组逆置"><a href="#数组逆置" class="headerlink" title="数组逆置"></a>数组逆置</h4><blockquote>
<p>数组逆置会改变原数组顺序。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</div><div class="line">arr.reverse();</div><div class="line"><span class="built_in">console</span>.log(arr);   <span class="comment">//  [ 3, 2, 1 ]</span></div></pre></td></tr></table></figure>
<h4 id="数组排序"><a href="#数组排序" class="headerlink" title="数组排序"></a>数组排序</h4><blockquote>
<p>数组排序会改变原数组顺序。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [ <span class="number">15</span>, <span class="number">110</span>, <span class="number">2</span>, <span class="string">'d'</span>, <span class="string">'a'</span>, <span class="string">'c'</span> ]</div><div class="line">arr.sort();</div><div class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// [ 110, 15, 2, 'a', 'c', 'd' ]</span></div><div class="line">arr.sort(<span class="function"><span class="keyword">function</span> (<span class="params">a, b</span>)</span></div><div class="line">&#123;</div><div class="line">    <span class="keyword">return</span> a-b;</div><div class="line">&#125;);</div><div class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// [ 2, 15, 110, 'a', 'c', 'd' ]</span></div></pre></td></tr></table></figure>
<h4 id="数组截取"><a href="#数组截取" class="headerlink" title="数组截取"></a>数组截取</h4><blockquote>
<p>数组截取不会改变原数组。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">15</span>,<span class="number">2</span>,<span class="number">110</span>,<span class="number">78</span>,<span class="number">65</span>,<span class="number">100</span>];</div><div class="line"><span class="built_in">console</span>.log(arr.slice(<span class="number">1</span>));  <span class="comment">// [ 2, 110, 78, 65, 100 ]</span></div><div class="line"><span class="built_in">console</span>.log(arr.slice(<span class="number">1</span>,<span class="number">3</span>));  <span class="comment">//前闭后开 [ 2, 110 ]</span></div><div class="line"><span class="built_in">console</span>.log(arr.slice(<span class="number">1</span>,<span class="number">-1</span>)); <span class="comment">// 去掉头尾 [ 2, 110, 78, 65 ]</span></div><div class="line"><span class="built_in">console</span>.log(arr); <span class="comment">//原数组不变 [ 15, 2, 110, 78, 65, 100 ]</span></div></pre></td></tr></table></figure>
<h4 id="数组拼接"><a href="#数组拼接" class="headerlink" title="数组拼接"></a>数组拼接</h4><blockquote>
<p>数组拼接会改变原数组。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">15</span>,<span class="number">2</span>,<span class="number">110</span>,<span class="number">78</span>,<span class="number">65</span>,<span class="number">100</span>];</div><div class="line"><span class="built_in">console</span>.log(arr.splice(<span class="number">2</span>)); <span class="comment">// [ 2, 110, 78, 65, 100 ]</span></div><div class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// [ 15, 2 ]</span></div><div class="line"></div><div class="line"><span class="keyword">var</span> arr = [<span class="number">15</span>,<span class="number">2</span>,<span class="number">110</span>,<span class="number">78</span>,<span class="number">65</span>,<span class="number">100</span>]; </div><div class="line"><span class="built_in">console</span>.log(arr.splice(<span class="number">1</span>,<span class="number">3</span>)); <span class="comment">// 一个参数为位置，第二个为删除个数 结果为 [ 2, 110, 78 ]</span></div><div class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// [ 15, 65, 100 ]</span></div><div class="line"><span class="keyword">var</span> arr = [<span class="number">15</span>,<span class="number">2</span>,<span class="number">110</span>,<span class="number">78</span>,<span class="number">65</span>,<span class="number">100</span>]; </div><div class="line"><span class="built_in">console</span>.log(arr.splice(<span class="number">2</span>,<span class="number">1</span>,<span class="string">"a"</span>,<span class="number">55</span>,<span class="string">"ss"</span>));  <span class="comment">// [ 110 ] 从删除的位置添加元素</span></div><div class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// [ 15, 2, 'a', 55, 'ss', 78, 65, 100 ]</span></div></pre></td></tr></table></figure>
<h4 id="数组映射"><a href="#数组映射" class="headerlink" title="数组映射"></a>数组映射</h4><blockquote>
<p>数组映射不会改变原数组。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">15</span>, <span class="number">2</span>, <span class="number">110</span>, <span class="number">78</span>, <span class="number">65</span>, <span class="number">100</span>];</div><div class="line"><span class="built_in">console</span>.log(arr.map(<span class="function"><span class="keyword">function</span> (<span class="params">x</span>)</span></div><div class="line">&#123;</div><div class="line">    <span class="keyword">return</span> x+<span class="number">100</span>;</div><div class="line">&#125;));  <span class="comment">// [ 115, 102, 210, 178, 165, 200 ]</span></div><div class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// [ 15, 2, 110, 78, 65, 100 ]</span></div></pre></td></tr></table></figure>
<h4 id="数组过滤"><a href="#数组过滤" class="headerlink" title="数组过滤"></a>数组过滤</h4><blockquote>
<p>数组过滤不会改变原数组。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">15</span>, <span class="number">2</span>, <span class="number">110</span>, <span class="number">78</span>, <span class="number">65</span>, <span class="number">100</span>];</div><div class="line"><span class="built_in">console</span>.log(arr.filter(<span class="function"><span class="keyword">function</span> (<span class="params">value,index</span>)</span></div><div class="line">&#123;  <span class="comment">// 筛选出索引为偶数或者值大于100的元素</span></div><div class="line">    <span class="keyword">return</span> index%<span class="number">2</span>===<span class="number">1</span>||value&gt;<span class="number">100</span>;</div><div class="line">&#125;));  <span class="comment">// [ 2, 110, 78, 100 ]</span></div><div class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// [ 15, 2, 110, 78, 65, 100 ]</span></div></pre></td></tr></table></figure>
<h4 id="数组判断"><a href="#数组判断" class="headerlink" title="数组判断"></a>数组判断</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">5</span>, <span class="number">7</span>, <span class="number">6</span>, <span class="number">3</span>, <span class="number">54</span>, <span class="number">55</span>];</div><div class="line"><span class="built_in">console</span>.log(arr.every(<span class="function"><span class="keyword">function</span> (<span class="params">value</span>)</span></div><div class="line">&#123;  <span class="comment">// 判断是否所有元素都大于10</span></div><div class="line">    <span class="keyword">return</span> value &gt; <span class="number">10</span>;</div><div class="line">&#125;)); <span class="comment">// false</span></div><div class="line"></div><div class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">5</span>, <span class="number">7</span>, <span class="number">6</span>, <span class="number">3</span>, <span class="number">54</span>, <span class="number">55</span>];</div><div class="line"><span class="built_in">console</span>.log(arr.some(<span class="function"><span class="keyword">function</span> (<span class="params">value</span>)</span></div><div class="line">&#123;  <span class="comment">// 判断是否有元素大于0</span></div><div class="line">    <span class="keyword">return</span> value &gt; <span class="number">10</span>;</div><div class="line">&#125;));  <span class="comment">// true</span></div></pre></td></tr></table></figure>
<h4 id="reduce"><a href="#reduce" class="headerlink" title="reduce"></a>reduce</h4><blockquote>
<p>每次的结果作为第一个参数。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">5</span>, <span class="number">7</span>, <span class="number">6</span>, <span class="number">3</span>, <span class="number">54</span>, <span class="number">55</span>,<span class="number">4</span>];</div><div class="line"><span class="built_in">console</span>.log(arr.reduce(<span class="function"><span class="keyword">function</span> (<span class="params">value1,value2</span>)</span></div><div class="line">&#123;  <span class="comment">// 求最大值</span></div><div class="line">    <span class="keyword">return</span> value1&gt;value2?value1:value2;</div><div class="line">&#125;));</div><div class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// 55</span></div></pre></td></tr></table></figure>
<h4 id="数组检索"><a href="#数组检索" class="headerlink" title="数组检索"></a>数组检索</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">5</span>, <span class="number">7</span>, <span class="number">6</span>, <span class="number">3</span>, <span class="number">7</span>,<span class="number">54</span>, <span class="number">55</span>,<span class="number">4</span>,<span class="number">7</span>];</div><div class="line"><span class="built_in">console</span>.log(arr.indexOf(<span class="number">7</span>))  <span class="comment">// 从左向右查找   2</span></div><div class="line"><span class="built_in">console</span>.log(arr.indexOf(<span class="number">7</span>,<span class="number">3</span>));  <span class="comment">// 第二个参数表示查找位置  5</span></div><div class="line"><span class="built_in">console</span>.log(arr.indexOf(<span class="number">7</span>,<span class="number">-2</span>)); <span class="comment">// 从倒数第二个元素开始查找 9</span></div><div class="line"><span class="built_in">console</span>.log(arr.lastIndexOf(<span class="number">7</span>)); <span class="comment">//从右向左查找  9</span></div><div class="line"><span class="built_in">console</span>.log(arr.lastIndexOf(<span class="number">7</span>,<span class="number">-2</span>)); <span class="comment">//从右向左查找  5</span></div></pre></td></tr></table></figure>
<h4 id="数组判断-1"><a href="#数组判断-1" class="headerlink" title="数组判断"></a>数组判断</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">5</span>, <span class="number">7</span>, <span class="number">6</span>, <span class="number">3</span>, <span class="number">7</span>,<span class="number">54</span>, <span class="number">55</span>,<span class="number">4</span>,<span class="number">7</span>];</div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">Array</span>.isArray(arr));  <span class="comment">// true</span></div><div class="line"><span class="built_in">console</span>.log(arr <span class="keyword">instanceof</span>  <span class="built_in">Array</span>);  <span class="comment">// true</span></div><div class="line"><span class="built_in">console</span>.log(arr.constructor === <span class="built_in">Array</span>);  <span class="comment">// true</span></div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      JavaScript数组的基本方法简介。
    
    </summary>
    
      <category term="前端" scheme="http://yoursite.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="JavaScript" scheme="http://yoursite.com/tags/JavaScript/"/>
    
      <category term="数组" scheme="http://yoursite.com/tags/%E6%95%B0%E7%BB%84/"/>
    
  </entry>
  
  <entry>
    <title>你不知道的JavaScript(上卷)学习笔记</title>
    <link href="http://yoursite.com/2016/10/13/%E4%BD%A0%E4%B8%8D%E7%9F%A5%E9%81%93%E7%9A%84JavaScript-%E4%B8%8A%E5%8D%B7-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    <id>http://yoursite.com/2016/10/13/你不知道的JavaScript-上卷-学习笔记/</id>
    <published>2016-10-13T06:35:54.000Z</published>
    <updated>2016-10-13T06:39:33.919Z</updated>
    
    <content type="html"><![CDATA[<h3 id="函数作用域"><a href="#函数作用域" class="headerlink" title="函数作用域"></a>函数作用域</h3><h4 id="包装函数"><a href="#包装函数" class="headerlink" title="包装函数"></a>包装函数</h4><p> 包装函数的声明以 <code>(function...</code> 而不仅是以 <code>function...</code> 开始。函数会被当作函数表达式而不是一个标准的函数声明来处理。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</div><div class="line">(<span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>)</span></div><div class="line">        &#123;</div><div class="line">            <span class="keyword">var</span> a = <span class="number">3</span>;</div><div class="line">            <span class="built_in">console</span>.log(a);  <span class="comment">// 3</span></div><div class="line">        &#125;)();</div><div class="line"><span class="built_in">console</span>.log(a);  <span class="comment">// 2</span></div></pre></td></tr></table></figure></p>
<blockquote>
<p>区分函数声明和表达式最简单的方法是看 <code>function</code> 关键字出现在声明中的位置（不仅仅是一行代码，而是整个声明中的位置）。如果 <code>function</code> 是声明中的第一个词，那么就是一个函数声明，否则就是一个函数表达式。</p>
<p> <code>(function foo(){ .. })</code> 作为函数表达式意味着 <code>foo</code> 只能在 <code>..</code> 所代表的位置中被访问，外部作用域则不行。 <code>foo</code> 变量名被隐藏在自身中意味着不会非必要地污染外部作用域。</p>
<h4 id="立即执行函数表达式-IIFE"><a href="#立即执行函数表达式-IIFE" class="headerlink" title="立即执行函数表达式(IIFE)"></a>立即执行函数表达式(IIFE)</h4><p> 由于函数被包含在一对 <code>( )</code> 括号内部，因此成为了一个表达式，通过在末尾加上另外一个<br><code>( )</code> 可以立即执行这个函数，比如 <code>(function foo(){ .. })()</code> 。第一个 <code>( )</code> 将函数变成表达式，第二个 <code>( )</code> 执行了这个函数。<br>函数名对 IIFE 不是必须的，IIFE 最常见的用法是使用一个匿名函数表达式。</p>
</blockquote>
<p>很多人都更喜欢另一个改进的形式: <code>(function(){ .. }())</code> 。<br>IIFE 的另一个非常普遍的进阶用法是把它们当作函数调用并传递参数进去。<br>例如：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</div><div class="line">(<span class="function"><span class="keyword">function</span> <span class="title">IIFE</span>(<span class="params"> global </span>) </span>&#123;  <span class="comment">// 形参</span></div><div class="line"><span class="keyword">var</span> a = <span class="number">3</span>;</div><div class="line"><span class="built_in">console</span>.log( a );  <span class="comment">// 3</span></div><div class="line"><span class="built_in">console</span>.log( global.a );  <span class="comment">// 2</span></div><div class="line">&#125;)( <span class="built_in">window</span> );  <span class="comment">// 实参</span></div><div class="line"><span class="built_in">console</span>.log( a );  <span class="comment">// 2</span></div></pre></td></tr></table></figure></p>
<p>将 <code>window</code> 对象的引用传递进去，但将参数命名为 <code>global</code> ，因此在代码风格上对全局对象的引用变得比引用一个没有“全局”字样的变量更加清晰。<br>将一个参数命名为 <code>undefined</code> ，但是在对应的位置不传入任何值，这样就可以保证在代码块中 <code>undefined</code> 标识符的值真的是 <code>undefined</code> ：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="literal">undefined</span> = <span class="literal">true</span>; <span class="comment">// 给其他代码挖了一个大坑！绝对不要这样做！</span></div><div class="line">(<span class="function"><span class="keyword">function</span> <span class="title">IIFE</span>(<span class="params"> undefined </span>) </span>&#123;</div><div class="line"><span class="keyword">var</span> a;</div><div class="line"><span class="keyword">if</span> (a === <span class="literal">undefined</span>) &#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="string">"Undefined is safe here!"</span> );</div><div class="line">&#125;</div><div class="line">&#125;)();</div></pre></td></tr></table></figure></p>
<blockquote>
<p>IIFE 还有一种变化的用途是倒置代码的运行顺序，将需要运行的函数放在第二位，在 IIFE执行之后当作参数传递进去。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">(<span class="function"><span class="keyword">function</span> <span class="title">IIFE</span>(<span class="params">def</span>)</span></div><div class="line">    &#123;</div><div class="line">        def(<span class="built_in">window</span>);</div><div class="line">    &#125;)(<span class="function"><span class="keyword">function</span> <span class="title">def</span>(<span class="params">global</span>)</span></div><div class="line">&#123;</div><div class="line">    <span class="keyword">var</span> a = <span class="number">3</span>;</div><div class="line">    <span class="built_in">console</span>.log(a); <span class="comment">// 3</span></div><div class="line">    <span class="built_in">console</span>.log(global.a); <span class="comment">// 2</span></div><div class="line">&#125;);</div></pre></td></tr></table></figure></p>
</blockquote>
<p>  <em>函数表达式 <code>def</code> 定义在片段的第二部分，然后当作参数（这个参数也叫作 <code>def</code> ）被传递进IIFE 函数定义的第一部分中。最后，参数 <code>def</code> （也就是传递进去的函数）被调用，并将<code>window</code> 传入当作 <code>global</code> 参数的值 。</em></p>
<h4 id="块作用域"><a href="#块作用域" class="headerlink" title="块作用域"></a>块作用域</h4><blockquote>
<p>使用 <code>var</code> 声明变量时，它写在哪里都是一样的，因为它们最终都会属于外部作用域。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> foo = <span class="literal">true</span>;</div><div class="line"><span class="keyword">if</span> (foo)</div><div class="line">    &#123;</div><div class="line">        <span class="keyword">var</span> bar = foo * <span class="number">2</span>;</div><div class="line">    &#125;</div><div class="line"><span class="built_in">console</span>.log(bar);  <span class="comment">// 2</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="let"><a href="#let" class="headerlink" title="let"></a>let</h4><blockquote>
<p> <code>let</code> 关键字可以将变量绑定到所在的任意作用域中（通常是 <code>{ .. }</code> 内部）。换句话说， <code>let</code>把声明的变量隐式在了所在的块作用域。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> foo = <span class="literal">true</span>;</div><div class="line"><span class="keyword">if</span> (foo)</div><div class="line">    &#123;</div><div class="line">        <span class="keyword">let</span> bar = foo * <span class="number">2</span>;</div><div class="line">        <span class="built_in">console</span>.log(bar);</div><div class="line">    &#125;</div><div class="line"><span class="built_in">console</span>.log(bar); <span class="comment">// ReferenceErrorar;</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h3 id="提升"><a href="#提升" class="headerlink" title="提升"></a>提升</h3><blockquote>
<p>所有的声明（变量和函数）都会被“移动”到各自作用域的最顶端，这个过程被称为提升。变量和函数声明会在任何代码被执行前首先被处理，而赋值或其他运行逻辑会留在原地 。</p>
<h4 id="声明提升"><a href="#声明提升" class="headerlink" title="声明提升"></a>声明提升</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">foo();</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( a ); <span class="comment">// undefined</span></div><div class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
</blockquote>
<p>  <code>foo</code> 函数的声明（这个例子还包括实际函数的隐含值）被提升了，因此第一行中的调用可<br>以正常执行。<br>这段代码实际上会被理解为下面的形式：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="keyword">var</span> a;</div><div class="line"><span class="built_in">console</span>.log( a ); <span class="comment">// undefined</span></div><div class="line">a = <span class="number">2</span>;</div><div class="line">&#125;</div><div class="line">foo();</div></pre></td></tr></table></figure></p>
<p><em>可以看到，函数声明会被提升，但是函数表达式却不会被提升。</em></p>
<blockquote>
<p>即使是具名的函数表达式，名称标识符在赋值之前也无法在所在作用域中使用：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">foo(); <span class="comment">// TypeError</span></div><div class="line">bar(); <span class="comment">// ReferenceError</span></div><div class="line"><span class="keyword">var</span> foo = <span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="comment">// ...</span></div><div class="line">&#125;;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>这个代码片段经过提升后，实际上会被理解为以下形式：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> foo;</div><div class="line">foo(); <span class="comment">// TypeError</span></div><div class="line">bar(); <span class="comment">// ReferenceError</span></div><div class="line">foo = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="keyword">var</span> bar = ...self...</div><div class="line"><span class="comment">// ...</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p><em><code>foo()</code> 由于对 <code>undefined</code> 值进行函数调用而导致非法操作，因此抛出 <code>TypeError</code> 异常。</em></p>
<h4 id="函数优先"><a href="#函数优先" class="headerlink" title="函数优先"></a>函数优先</h4><blockquote>
<p>函数声明和变量声明都会被提升，但函数会首先被提升，然后才是变量。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">foo(); <span class="comment">// 1</span></div><div class="line"><span class="keyword">var</span> foo;</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="number">1</span> );</div><div class="line">&#125;</div><div class="line">foo = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="number">2</span> );</div><div class="line">&#125;;</div></pre></td></tr></table></figure></p>
</blockquote>
<p>会输出 1 而不是 2 ！这个代码片段会被引擎理解为如下形式：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="number">1</span> );</div><div class="line">&#125;</div><div class="line">foo(); <span class="comment">// 1</span></div><div class="line">foo = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="number">2</span> );</div><div class="line">&#125;;</div></pre></td></tr></table></figure></p>
<p> <em><code>var foo</code> 尽管出现在 <code>function foo()...</code> 的声明之前，但它是重复的声明（因此被忽略了），因为函数声明会被提升到普通变量之前。</em><br> <strong>如果有重复的声明，后面的会覆盖前面的声明。</strong></p>
<h3 id="作用域闭包"><a href="#作用域闭包" class="headerlink" title="作用域闭包"></a>作用域闭包</h3><blockquote>
<p>闭包：当函数可以记住并访问所在的词法作用域，即函数是在当前词法作用域之外执行，这时就产生了闭包。</p>
<p>模块有两个主要特征：（1）为创建内部作用域而调用了一个包装函数；（2）包装函数的返回值必须至少包括一个对内部函数的引用，这样就会创建涵盖整个包装函数内部作用域的闭包。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> fn;</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">baz</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( a );</div><div class="line">&#125;</div><div class="line">fn = baz; <span class="comment">// 将 baz 分配给全局变量</span></div><div class="line">&#125;</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</div><div class="line">fn(); <span class="comment">// 这就是闭包！</span></div><div class="line">&#125;</div><div class="line">foo();</div><div class="line">bar(); <span class="comment">// 2</span></div></pre></td></tr></table></figure></p>
</blockquote>
<p><em>将内部函数传递到所在的词法作用域以外，它都会持有对原始定义作用域的引用，无论在何处执行这个函数都会使用闭包。</em></p>
<blockquote>
<p>词法作用域是一套关于引擎如何寻找变量以及会在何处找到变量的规则。词法作用域最重要的特征是它的定义过程发生在代码的书写阶段（假设你没有使用<code>eval()</code> 或 <code>with</code> ）。</p>
<p>动态作用域并不关心函数和作用域是如何声明以及在何处声明的，只关心它们从何处调用。换句话说，作用域链是基于调用栈的，而不是代码中的作用域嵌套。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( a ); <span class="comment">// 2</span></div><div class="line">&#125;</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="keyword">var</span> a = <span class="number">3</span>;</div><div class="line">foo();</div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</div><div class="line">bar();</div></pre></td></tr></table></figure></p>
<p>需要明确的是，事实上 JavaScript 并不具有动态作用域。它只有词法作用域，简单明了。但是 this 机制某种程度上很像动态作用域。</p>
<p>主要区别：词法作用域是在写代码或者说定义时确定的，而动态作用域是在运行时确定的。（ this 也是！）词法作用域关注函数在何处声明，而动态作用域关注函数从何处调用。</p>
<h4 id="this-词法"><a href="#this-词法" class="headerlink" title="this 词法"></a>this 词法</h4><p>箭头函数在涉及 this 绑定时的行为和普通函数的行为完全不一致。它放弃了所有普通 this 绑定的规则，取而代之的是用当前的词法作用域覆盖了 this 本来的值。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">num</span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="string">"foo: "</span> + num );</div><div class="line"><span class="comment">// 记录 foo 被调用的次数</span></div><div class="line"><span class="comment">// 注意，在当前的调用方式下（参见下方代码），this 确实指向 foo</span></div><div class="line"><span class="keyword">this</span>.count++;</div><div class="line">&#125;</div><div class="line">foo.count = <span class="number">0</span>;</div><div class="line"><span class="keyword">var</span> i;</div><div class="line"><span class="keyword">for</span> (i=<span class="number">0</span>; i&lt;<span class="number">10</span>; i++) &#123;</div><div class="line"><span class="keyword">if</span> (i &gt; <span class="number">5</span>) &#123;</div><div class="line"><span class="comment">// 使用 call(..) 可以确保 this 指向函数对象 foo 本身</span></div><div class="line">foo.call( foo, i );</div><div class="line">&#125;</div><div class="line">&#125;</div><div class="line"><span class="comment">// foo: 6</span></div><div class="line"><span class="comment">// foo: 7</span></div><div class="line"><span class="comment">// foo: 8</span></div><div class="line"><span class="comment">// foo: 9</span></div><div class="line"><span class="comment">// foo 被调用了多少次？</span></div><div class="line"><span class="built_in">console</span>.log( foo.count ); <span class="comment">// 4</span></div></pre></td></tr></table></figure></p>
<p> this 是在运行时进行绑定的，并不是在编写时绑定，它的上下文取决于函数调用时的各种条件。 this 的绑定和函数声明的位置没有任何关系，只取决于函数的调用方式。</p>
</blockquote>
<h4 id="this-全面解析"><a href="#this-全面解析" class="headerlink" title="this 全面解析"></a>this 全面解析</h4><p>在理解 this 的绑定过程之前，首先要理解调用位置：调用位置就是函数在代码中被调用的<br>位置（而不是声明的位置）。</p>
<h4 id="默认绑定"><a href="#默认绑定" class="headerlink" title="默认绑定"></a>默认绑定</h4><ul>
<li>this 的默认绑定， 指向全局对象。</li>
<li>严格模式（ strict mode ），那么全局对象将无法使用默认绑定，因此 this 会绑定<br>到 undefined 。<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="meta">"use strict"</span>;</div><div class="line"><span class="built_in">console</span>.log( <span class="keyword">this</span>.a );</div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</div><div class="line">foo(); <span class="comment">// TypeError: this is undefined</span></div></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="隐式绑定"><a href="#隐式绑定" class="headerlink" title="隐式绑定"></a>隐式绑定</h4><ul>
<li>当函数引用有上下文对象时，隐式绑定规则会把函数调用中的 this 绑定到这个上下文对象。</li>
<li>对象属性引用链中只有最顶层或者说最后一层会影响调用位置。</li>
<li>在分析隐式绑定时，我们必须在一个对象内部包含一个指向函数的属性，并通过这个属性间接引用函数，从而把 this 间接（隐式）绑定到这个对象上。<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="keyword">this</span>.a );</div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> obj2 = &#123;</div><div class="line"><span class="attr">a</span>: <span class="number">42</span>,</div><div class="line"><span class="attr">foo</span>: foo</div><div class="line">&#125;;</div><div class="line"><span class="keyword">var</span> obj1 = &#123;</div><div class="line"><span class="attr">a</span>: <span class="number">2</span>,</div><div class="line"><span class="attr">obj2</span>: obj2</div><div class="line">&#125;;</div><div class="line">obj1.obj2.foo(); <span class="comment">// 42</span></div></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="显式绑定"><a href="#显式绑定" class="headerlink" title="显式绑定"></a>显式绑定</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">JavaScript 环境中内置的 setTimeout() 函数实现和下面的伪代码类似：</div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">setTimeout</span>(<span class="params">fn,delay</span>) </span>&#123;</div><div class="line"><span class="comment">// 等待 delay 毫秒</span></div><div class="line">fn(); <span class="comment">// &lt;-- 调用位置！</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>可以使用函数的 call(..) 和apply(..) 方法。</p>
<blockquote>
<p>它们的第一个参数是一个对象，它们会把这个对象绑定到this ，接着在调用函数时指定这个 this 。因为你可以直接指定 this 的绑定对象，因此我们称之为显式绑定。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="keyword">this</span>.a );</div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> obj = &#123;</div><div class="line"><span class="attr">a</span>:<span class="number">2</span></div><div class="line">&#125;;</div><div class="line">foo.call( obj ); <span class="comment">// 2</span></div></pre></td></tr></table></figure>
</blockquote>
<h5 id="硬绑定"><a href="#硬绑定" class="headerlink" title="硬绑定"></a>硬绑定</h5><p>我们创建了函数 bar() ，并在它的内部手动调用了 foo.apply( obj, arguments ) ，因此强制把 foo 的 this 绑定到了 obj 。无论之后如何调用函数 bar ，它总会手动在 obj 上调用 foo 。这种绑定是一种显式的强制绑定，因此我们称之为硬绑定。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">something</span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="keyword">this</span>.a, something );</div><div class="line"><span class="keyword">return</span> <span class="keyword">this</span>.a + something;</div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> obj = &#123;</div><div class="line"><span class="attr">a</span>:<span class="number">2</span></div><div class="line">&#125;;</div><div class="line"><span class="keyword">var</span> bar = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="keyword">return</span> foo.apply( obj, <span class="built_in">arguments</span> );</div><div class="line">&#125;;</div><div class="line"><span class="keyword">var</span> b = bar( <span class="number">3</span> ); <span class="comment">// 2 3</span></div><div class="line"><span class="built_in">console</span>.log( b ); <span class="comment">// 5</span></div></pre></td></tr></table></figure></p>
<p>tips:</p>
<blockquote>
<p>如果你把 null 或者 undefined 作为 this 的绑定对象传入 call 、 apply 或者 bind ，这些值在调用时会被忽略，实际应用的是默认绑定规则。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="keyword">this</span>.a );</div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</div><div class="line">foo.call( <span class="literal">null</span> ); <span class="comment">// 2</span></div></pre></td></tr></table></figure></p>
</blockquote>
<p>使用 <code>null</code> 来忽略 <code>this</code> 绑定可能产生一些副作用。建议使用空对象，在 JavaScript 中创建一个空对象最简单的方法都是<code>Object.create(null)</code>，<code>Object.create(null)</code> 和 <code>{}</code> 很像，但是并不会创建 <code>Object.prototype</code> 这个委托，所以它比 <code>{}</code> “更空”：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">a,b</span>) </span>&#123;</div><div class="line"><span class="built_in">console</span>.log( <span class="string">"a:"</span> + a + <span class="string">", b:"</span> + b );</div><div class="line">&#125;</div><div class="line"><span class="comment">// 我们的 DMZ 空对象</span></div><div class="line"><span class="keyword">var</span> ø = <span class="built_in">Object</span>.create( <span class="literal">null</span> );</div><div class="line"><span class="comment">// 把数组展开成参数</span></div><div class="line">foo.apply( ø, [<span class="number">2</span>, <span class="number">3</span>] ); <span class="comment">// a:2, b:3</span></div><div class="line"><span class="comment">// 使用 bind(..) 进行柯里化</span></div><div class="line"><span class="keyword">var</span> bar = foo.bind( ø, <span class="number">2</span> );</div><div class="line">bar( <span class="number">3</span> ); <span class="comment">// a:2, b:3</span></div></pre></td></tr></table></figure></p>
<blockquote>
<p>使用变量名 <code>ø</code> 不仅让函数变得更加“安全”，而且可以提高代码的可读性，因为 <code>ø</code> 表示“我希望 <code>this</code> 是空”，这比 <code>null</code> 的含义更清楚。</p>
<h4 id="new-绑定"><a href="#new-绑定" class="headerlink" title="new 绑定"></a>new 绑定</h4><p>在 JavaScript 中，构造函数只是一些<br>使用 <code>new</code> 操作符时被调用的函数。它们并不会属于某个类，也不会实例化一个类。实际上，它们甚至都不能说是一种特殊的函数类型，它们只是被 <code>new</code> 操作符调用的普通函数而已。</p>
<p>包括内置对象函数（比如 <code>Number(..)</code> ）在内的所有函数都可以用 <code>new</code> 来调用，这种函数调用被称为构造函数调用。这里有一个重要但是非常细微的区别：实际上并不存在所谓的“构造函数”，只有对于函数的“构造调用”。</p>
</blockquote>
<p>使用 new 来调用函数，或者说发生构造函数调用时，会自动执行下面的操作。</p>
<ol>
<li>创建（或者说构造）一个全新的对象。</li>
<li>这个新对象会被执行 [[ 原型 ]] 连接。</li>
<li>这个新对象会绑定到函数调用的 this 。</li>
<li>如果函数没有返回其他对象，那么 new 表达式中的函数调用会自动返回这个新对象。<h4 id="四条规则判断-this-的绑定对象："><a href="#四条规则判断-this-的绑定对象：" class="headerlink" title="四条规则判断 this 的绑定对象："></a>四条规则判断 this 的绑定对象：</h4>如果要判断一个运行中函数的 this 绑定，就需要找到这个函数的直接调用位置。找到之后就可以顺序应用下面这四条规则来判断 this 的绑定对象。</li>
<li>由 new 调用？绑定到新创建的对象。</li>
<li>由 call 或者 apply （或者 bind ）调用？绑定到指定的对象。</li>
<li>由上下文对象调用？绑定到那个上下文对象。</li>
<li>默认：在严格模式下绑定到 undefined ，否则绑定到全局对象。<h3 id="对象"><a href="#对象" class="headerlink" title="对象"></a>对象</h3><h4 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h4>对象可以通过两种形式定义：声明（文字）形式和构造形式。<br>对象的文字语法大概是这样：<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObj = &#123;</div><div class="line"><span class="attr">key</span>: value</div><div class="line"><span class="comment">// ...</span></div><div class="line">&#125;;</div></pre></td></tr></table></figure>
</li>
</ol>
<p>构造形式大概是这样：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObj = <span class="keyword">new</span> <span class="built_in">Object</span>();</div><div class="line">myObj.key = value;</div></pre></td></tr></table></figure></p>
<blockquote>
<p>构造形式和文字形式生成的对象是一样的。唯一的区别是，在文字声明中你可以添加多个键 / 值对，但是在构造形式中你必须逐个添加属性。</p>
<h4 id="类型"><a href="#类型" class="headerlink" title="类型"></a>类型</h4><p>内置对象：<br>String<br>Number<br>Boolean<br>Object<br>Function<br>Array<br>Date<br>RegExp<br>Error<br>null 和 undefined 没有对应的构造形式，它们只有文字形式。相反， Date 只有构造，没有文字形式。</p>
<p>对于 Object 、 Array 、 Function 和 RegExp （正则表达式）来说，无论使用文字形式还是构造形式，它们都是对象，不是字面量。由于这两种形式都可以创建对象，所以我们首选更简单的文字形式。建议只在需要那些额外选项时使用构造形式。</p>
<p>一般是在抛出异常时被自动创建。也可以使用 new Error(..) 这种构造形式来创建，不过一般来说用不着。</p>
<h4 id="内容"><a href="#内容" class="headerlink" title="内容"></a>内容</h4><p>对象的内容是由一些存储在特定命名位置的（任意类型的）值组成的，我们称之为属性。</p>
</blockquote>
<p>存储在对象容器内部的是这些属性的名称，它们就像指针一样，指向这些值真正的存储位置。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123;</div><div class="line"><span class="attr">a</span>: <span class="number">2</span></div><div class="line">&#125;;</div><div class="line">myObject.a; <span class="comment">// 2</span></div><div class="line">myObject[<span class="string">"a"</span>]; <span class="comment">// 2</span></div></pre></td></tr></table></figure></p>
<blockquote>
<p>如果要访问 myObject 中 a 位置上的值，我们需要使用 . 操作符或者 [] 操作符。 .a 语法通常被称为“属性访问”， [“a”] 语法通常被称为“键访问”。实际上它们访问的是同一个位置，并且会返回相同的值 2，所以这两个术语是可以互换的。</p>
<p>这两种语法的主要区别在于 . 操作符要求属性名满足标识符的命名规范，而 [“..”] 语法可以接受任意 UTF-8/Unicode 字符串作为属性名。</p>
<p>在对象中，属性名永远都是字符串。如果你使用 string （字面量）以外的其他值作为属性名，那它首先会被转换为一个字符串。即使是数字也不例外。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123; &#125;;</div><div class="line">myObject[<span class="literal">true</span>] = <span class="string">"foo"</span>;</div><div class="line">myObject[<span class="number">3</span>] = <span class="string">"bar"</span>;</div><div class="line">myObject[myObject] = <span class="string">"baz"</span>;</div><div class="line">myObject[<span class="string">"true"</span>]; <span class="comment">// "foo"</span></div><div class="line">myObject[<span class="string">"3"</span>]; <span class="comment">// "bar"</span></div><div class="line">myObject[<span class="string">"[object Object]"</span>]; <span class="comment">// "baz"</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h5 id="可计算属性名"><a href="#可计算属性名" class="headerlink" title="可计算属性名"></a>可计算属性名</h5><p>ES6 增加了可计算属性名，可以在文字形式中使用 [] 包裹一个表达式来当作属性名：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> prefix = <span class="string">"foo"</span>;</div><div class="line"><span class="keyword">var</span> myObject = &#123;</div><div class="line">[prefix + <span class="string">"bar"</span>]:<span class="string">"hello"</span>,</div><div class="line">[prefix + <span class="string">"baz"</span>]: <span class="string">"world"</span></div><div class="line">&#125;;</div><div class="line">myObject[<span class="string">"foobar"</span>]; <span class="comment">// hello</span></div><div class="line">myObject[<span class="string">"foobaz"</span>]; <span class="comment">// world</span></div></pre></td></tr></table></figure></p>
<h4 id="复制对象"><a href="#复制对象" class="headerlink" title="复制对象"></a>复制对象</h4><p> ES6 定义了 Object.assign(..) 方<br>法来实现浅复制。 Object.assign(..) 方法的第一个参数是目标对象，之后还可以跟一个<br>或多个源对象。它会遍历一个或多个源对象的所有可枚举（enumerable）的自有键（owned key，很快会介绍）并把它们复制（使用 = 操作符赋值）到目标对象，最后返回目标对象，就像这样：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> newObj = <span class="built_in">Object</span>.assign( &#123;&#125;, myObject );</div><div class="line">newObj.a; <span class="comment">// 2</span></div><div class="line">newObj.b === anotherObject; <span class="comment">// true</span></div><div class="line">newObj.c === anotherArray; <span class="comment">// true</span></div><div class="line">newObj.d === anotherFunction; <span class="comment">// true</span></div></pre></td></tr></table></figure></p>
<h4 id="属性描述符"><a href="#属性描述符" class="headerlink" title="属性描述符"></a>属性描述符</h4><blockquote>
<p>普通的对象属性对应的属性描述符（也被称为“数据描述符”，因为它只保存一个数据值）不仅仅 包含值，还包含另外三个特性： writable （可写）、enumerable （可枚举）和 configurable （可配置）。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123;&#125;;</div><div class="line"><span class="built_in">Object</span>.defineProperty( myObject, <span class="string">"a"</span>, &#123;</div><div class="line"><span class="attr">value</span>: <span class="number">2</span>,</div><div class="line"><span class="attr">writable</span>: <span class="literal">true</span>,</div><div class="line"><span class="attr">configurable</span>: <span class="literal">true</span>,</div><div class="line"><span class="attr">enumerable</span>: <span class="literal">true</span></div><div class="line">&#125; );</div><div class="line">myObject.a; <span class="comment">// 2</span></div></pre></td></tr></table></figure></p>
</blockquote>
<p>writable决定是否可以更改属性的值。<br>configurable只要属性是可配置的，就可以使用difineProperty(..)方法修改属性描述符。</p>
<blockquote>
<p>即便属性是 configurable:false ， 我们还是可以把 writable 的状态由 true 改为 false ，但是无法由 false 改为 true 。<br>除了无法修改， configurable:false 还会禁止删除这个属性。</p>
<h4 id="不变性"><a href="#不变性" class="headerlink" title="不变性"></a>不变性</h4><ul>
<li>对象常量<br>结合 writable:false 和 configurable:false 就可以创建一个真正的常量属性（不可修改、重定义或者删除）：<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123;&#125;;</div><div class="line"><span class="built_in">Object</span>.defineProperty( myObject, <span class="string">"FAVORITE_NUMBER"</span>, &#123;</div><div class="line"><span class="attr">value</span>: <span class="number">42</span>,</div><div class="line"><span class="attr">writable</span>: <span class="literal">false</span>,</div><div class="line"><span class="attr">configurable</span>: <span class="literal">false</span></div><div class="line">&#125; );</div></pre></td></tr></table></figure>
</li>
</ul>
</blockquote>
<ul>
<li>禁止扩展<br>如果想禁止一个对象添加新属性并且保留已有属性，可以使用 Object.preventExtensions(..) ：<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123;</div><div class="line"><span class="attr">a</span>:<span class="number">2</span></div><div class="line">&#125;;</div><div class="line"><span class="built_in">Object</span>.preventExtensions( myObject );</div><div class="line">myObject.b = <span class="number">3</span>;</div><div class="line">myObject.b; <span class="comment">// undefined</span></div></pre></td></tr></table></figure>
</li>
</ul>
<p>在非严格模式下，创建属性 b 会静默失败。在严格模式下，将会抛出 TypeError 错误。</p>
<ul>
<li>密封<blockquote>
<p>Object.seal(..) 会创建一个“密封”的对象，这个方法实际上会在一个现有对象上调用<br>Object.preventExtensions(..) 并把所有现有属性标记为 configurable:false 。<br>所以，密封之后不仅不能添加新属性，也不能重新配置或者删除任何现有属性（虽然可以<br>修改属性的值）。</p>
</blockquote>
</li>
<li>冻结<br>Object.freeze(..) 会创建一个冻结对象，这个方法实际上会在一个现有对象上调用<br>Object.seal(..) 并把所有“数据访问”属性标记为 writable:false ，这样就无法修改它们<br>的值。<br>这个方法是你可以应用在对象上的级别最高的不可变性，它会禁止对于对象本身及其任意<br>直接属性的修改。<h4 id="getter和setter"><a href="#getter和setter" class="headerlink" title="getter和setter"></a>getter和setter</h4>当给一个属性定义 getter、setter 或者两者都有时，这个属性会被定义为“访问描述符”（和“数据描述符”相对）。对于访问描述符来说，JavaScript 会忽略它们的 value 和writable 特性，取而代之的是关心 set 和 get （还有 configurable 和 enumerable ）特性。<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123;</div><div class="line"><span class="comment">// 给 a 定义一个 getter</span></div><div class="line">get a() &#123;</div><div class="line"><span class="keyword">return</span> <span class="number">2</span>;</div><div class="line">&#125;</div><div class="line">&#125;;</div><div class="line"><span class="built_in">Object</span>.defineProperty(</div><div class="line">myObject, <span class="comment">// 目标对象</span></div><div class="line"><span class="string">"b"</span>, <span class="comment">// 属性名</span></div><div class="line">&#123; <span class="comment">// 描述符</span></div><div class="line"><span class="comment">// 给 b 设置一个 getter</span></div><div class="line">get: <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="keyword">return</span> <span class="keyword">this</span>.a * <span class="number">2</span> &#125;,</div><div class="line"><span class="comment">// 确保 b 会出现在对象的属性列表中</span></div><div class="line">enumerable: <span class="literal">true</span></div><div class="line">&#125;</div><div class="line">);</div><div class="line">myObject.a; <span class="comment">// 2</span></div><div class="line">myObject.b; <span class="comment">// 4</span></div></pre></td></tr></table></figure>
</li>
</ul>
<p>不管是对象文字语法中的 get a() { .. } ，还是 defineProperty(..) 中的显式定义，二者<br>都会在对象中<strong>创建</strong>一个不包含值的属性，对于这个属性的访问会自动调用一个隐藏函数，<br>它的返回值会被当作属性访问的返回值：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123;</div><div class="line"><span class="comment">// 给 a 定义一个 getter</span></div><div class="line">get a() &#123;</div><div class="line"><span class="keyword">return</span> <span class="number">2</span>;</div><div class="line">&#125;</div><div class="line">&#125;;</div><div class="line">myObject.a = <span class="number">3</span>;</div><div class="line">myObject.a; <span class="comment">// 2</span></div></pre></td></tr></table></figure></p>
<p>由于我们只定义了 a 的 getter，所以对 a 的值进行设置时 set 操作会忽略赋值操作，不会抛<br>出错误。而且即便有合法的 setter，由于我们自定义的 getter 只会返回 2，所以 set 操作是<br>没有意义的。<br>为了让属性更合理，还应当定义 setter，和你期望的一样，setter 会覆盖单个属性默认的<br>[[Put]] （也被称为赋值）操作。通常来说 getter 和 setter 是成对出现的（只定义一个的话<br>通常会产生意料之外的行为）：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123;</div><div class="line"><span class="comment">// 给 a 定义一个 getter</span></div><div class="line">get a() &#123;</div><div class="line"><span class="keyword">return</span> <span class="keyword">this</span>._a_;</div><div class="line">&#125;,</div><div class="line"><span class="comment">// 给 a 定义一个 setter</span></div><div class="line">set a(val) &#123;</div><div class="line"><span class="keyword">this</span>._a_ = val * <span class="number">2</span>;</div><div class="line">&#125;</div><div class="line">&#125;;</div><div class="line">myObject.a = <span class="number">2</span>;</div><div class="line">myObject.a; <span class="comment">// 4</span></div></pre></td></tr></table></figure></p>
<h4 id="存在性"><a href="#存在性" class="headerlink" title="存在性"></a>存在性</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123;</div><div class="line"><span class="attr">a</span>:<span class="number">2</span></div><div class="line">&#125;;</div><div class="line">(<span class="string">"a"</span> <span class="keyword">in</span> myObject); <span class="comment">// true</span></div><div class="line">(<span class="string">"b"</span> <span class="keyword">in</span> myObject); <span class="comment">// false</span></div><div class="line">myObject.hasOwnProperty( <span class="string">"a"</span> ); <span class="comment">// true</span></div><div class="line">myObject.hasOwnProperty( <span class="string">"b"</span> ); <span class="comment">// false</span></div></pre></td></tr></table></figure>
<blockquote>
<p><code>in</code> 操作符会检查属性是否在对象及其 <code>[[Prototype]]</code> 原型链中。<br><code>hasOwnProperty(..)</code> 只会检查属性是否在 <code>myObject</code> 对象中，不会检查 <code>[[Prototype]]</code> 链。<br>更加强硬的判断方法， <code>Object.prototype.hasOwnProperty.call(myObject,&quot;a&quot;)</code> ，它借用基础的 <code>hasOwnProperty(..)</code> 方法并把它显式绑定到 <code>myObject</code> 上。</p>
</blockquote>
<p><em>看起来 <code>in</code> 操作符可以检查容器内是否有某个值，但是它实际上检查的是某个<code>属性名</code>是否存在。对于数组来说这个区别非常重要， 4 in [2, 4, 6] 的结果并不是你期待的 True ，因为 [2, 4, 6] 这个数组中包含的属性名是 0、1、2，没有 4。</em></p>
<blockquote>
<p>在数组上应用 for..in 循环有时会产生出人意料的结果，因为这种枚举不仅会包含所有数值索引，还会包含所有可枚举属性。最好只在<code>对象</code>上应用for..in 循环，如果要遍历数组就使用传统的 for 循环来遍历数值索引。</p>
</blockquote>
<p>也可以通过另一种方式来区分属性是否可枚举：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myObject = &#123; &#125;;</div><div class="line"><span class="built_in">Object</span>.defineProperty(</div><div class="line">myObject,</div><div class="line"><span class="string">"a"</span>,</div><div class="line"><span class="comment">// 让 a 像普通属性一样可以枚举</span></div><div class="line">&#123; <span class="attr">enumerable</span>: <span class="literal">true</span>, <span class="attr">value</span>: <span class="number">2</span> &#125;</div><div class="line">);</div><div class="line"><span class="built_in">Object</span>.defineProperty(</div><div class="line">myObject,</div><div class="line"><span class="string">"b"</span>,</div><div class="line"><span class="comment">// 让 b 不可枚举</span></div><div class="line">&#123; <span class="attr">enumerable</span>: <span class="literal">false</span>, <span class="attr">value</span>: <span class="number">3</span> &#125;</div><div class="line">);</div><div class="line">myObject.propertyIsEnumerable( <span class="string">"a"</span> ); <span class="comment">// true</span></div><div class="line">myObject.propertyIsEnumerable( <span class="string">"b"</span> ); <span class="comment">// false</span></div><div class="line"><span class="built_in">Object</span>.keys( myObject ); <span class="comment">// ["a"]</span></div><div class="line"><span class="built_in">Object</span>.getOwnPropertyNames( myObject ); <span class="comment">// ["a", "b"]</span></div></pre></td></tr></table></figure></p>
<blockquote>
<p><code>propertyIsEnumerable(..)</code> 会检查给定的属性名是否直接存在于对象中（而不是在原型链上）并且满足 <code>enumerable:true</code> 。</p>
<p><code>Object.keys(..)</code> 会返回一个数组，包含所有<code>可枚举属性</code>， <code>Object.getOwnPropertyNames(..)</code>会返回一个数组，包含所有属性，无论它们是否可枚举。</p>
<p><code>in</code> 和 <code>hasOwnProperty(..)</code> 的区别在于是否查找 <code>[[Prototype]]</code> 链，然而， <code>Object.keys(..)</code>和 <code>Object.getOwnPropertyNames(..)</code> 都只会查找对象直接包含的属性。</p>
<h4 id="遍历"><a href="#遍历" class="headerlink" title="遍历"></a>遍历</h4><p>使用 for..in 遍历对象是无法直接获取属性值的，因为它实际上遍历的是对象中的所有可枚举属性，需要手动获取属性值。</p>
<p>使用 for..in 遍历对象时原理和查找 [[Prototype]] 链类似，任何可以通过原型链访问到（并且是 enumerable ）的属性都会被枚举。使用 in 操作符来检查属性在对象中是否存在时，同样会查找对象的整条原型链（无论属性是否可枚举）</p>
<p>ES6 增加了一种用来遍历数组的 for..of 循环语法，直接遍历值，而不是数组下标。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> myArray = [ <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span> ];</div><div class="line"><span class="keyword">for</span> (<span class="keyword">var</span> v <span class="keyword">of</span> myArray) &#123;</div><div class="line"><span class="built_in">console</span>.log( v );</div><div class="line">&#125;</div><div class="line"><span class="comment">// 1</span></div><div class="line"><span class="comment">// 2</span></div><div class="line"><span class="comment">// 3</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h3 id="原型"><a href="#原型" class="headerlink" title="原型"></a>原型</h3><p>Object.create(..)会创建一个对象并把这个对象的 [[Prototype]] 关联到指定的对象。<br>所有普通的 [[Prototype]] 链最终都会指向内置的 Object.prototype 。</p>
<h4 id="属性设置与屏蔽"><a href="#属性设置与屏蔽" class="headerlink" title="属性设置与屏蔽"></a>属性设置与屏蔽</h4><p>给一个对象设置属性并不仅仅是添加一个新属性或者修改已有的属性值。</p>
<pre><code>myObject.foo = &quot;bar&quot;;
</code></pre><p>如果 myObject 对象中包含名为 foo 的普通数据访问属性，这条赋值语句只会修改已有的属<br>性值。<br>如果 foo 不是直接存在于 myObject 中， [[Prototype]] 链就会被遍历，如果原型链上找不到 foo ， foo 就会被直接添加到 myObject 上。<br>如果 foo 存在于原型链上层，在于原型链上层时 myObject.foo = “bar” 会出现的三种情况。</p>
<ol>
<li>如果在 [[Prototype]] 链上层存在名为 foo 的普通数据访问属性并且没有被标记为只读（ writable:false ），那就会直接在 myObject 中添加一个名为 foo 的新属性，它是屏蔽属性。</li>
<li>如果在 [[Prototype]] 链上层存在 foo ，但是它被标记为只读（ writable:false ），那么无法修改已有属性或者在 myObject 上创建屏蔽属性。如果运行在严格模式下，代码会抛出一个错误。否则，这条赋值语句会被忽略。总之，不会发生屏蔽。</li>
<li>如果在 [[Prototype]] 链上层存在 foo 并且它是一个 setter（参见第 3 章），那就一定会调用这个 setter。 foo 不会被添加到（或者说屏蔽于） myObject ，也不会重新定义 foo 这个 setter。</li>
</ol>
<p><em>如果你希望在第二种和第三种情况下也屏蔽 foo ，那就不能使用 = 操作符来赋值，而是使用 Object.defineProperty(..) 来向 myObject 添加 foo 。</em></p>
<h4 id="“类”函数"><a href="#“类”函数" class="headerlink" title="“类”函数"></a>“类”函数</h4><p>JavaScript 中只有对象，对象直接定义自己的行为。<br>函数特性：所有的函数默认都会拥有一个名为 prototype 的公有并且不可枚举的属性，它会指向另一个对象：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="comment">// ...</span></div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> Foo();</div><div class="line"><span class="built_in">Object</span>.getPrototypeOf( a ) === Foo.prototype; <span class="comment">// true</span></div></pre></td></tr></table></figure></p>
<p>new Foo() 会生成一个新对象（我们称之为 a ），这个新对象的内部链接 [[Prototype]] 关联<br>的是 Foo.prototype 对象。<br> new Foo() 只是间接创建了一个关联到其他对象的新对象。<br> JavaScript 会在两个对象之间创建一个关联，这样一个对象就可以通过委托访问另一个对象的属性和函数。<br> 在普通的函数调用前面加上 new 关键字之后，就会把这个函数调用变成一个“构造函数调用”。实际上， new 会劫持所有普通函数并用构造对象的形式来调用它。<br> 函数不是构造函数，但是当且仅当使用 new 时，函数调用会变成“构造函数调用”。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Foo</span>(<span class="params">name</span>) </span>&#123;</div><div class="line"><span class="keyword">this</span>.name = name;</div><div class="line">&#125;</div><div class="line">Foo.prototype.myName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="keyword">return</span> <span class="keyword">this</span>.name;</div><div class="line">&#125;;</div><div class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> Foo( <span class="string">"a"</span> );</div><div class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> Foo( <span class="string">"b"</span> );</div><div class="line">a.myName(); <span class="comment">// "a"</span></div><div class="line">b.myName(); <span class="comment">// "b"</span></div></pre></td></tr></table></figure></p>
<p>Foo.prototype.myName = … 可能个更有趣的技巧，它会给 Foo.prototype 对象添加一<br>个属性（函数）。现在， a.myName() 可以正常工作。</p>
<h4 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h4><p>要创建一个合适的关联对象，我们必须使用 Object.create(..)<br>对比一下两种把 Bar.prototype 关联到 Foo.prototype 的方法：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// ES6 之前需要抛弃默认的 Bar.prototype</span></div><div class="line">Bar.ptototype = <span class="built_in">Object</span>.create( Foo.prototype );</div><div class="line"><span class="comment">// ES6 开始可以直接修改现有的 Bar.prototype</span></div><div class="line"><span class="built_in">Object</span>.setPrototypeOf( Bar.prototype, Foo.prototype );</div></pre></td></tr></table></figure></p>
<p>如果忽略掉 Object.create(..) 方法带来的轻微性能损失（抛弃的对象需要进行垃圾回<br>收），它实际上比 ES6 及其之后的方法更短而且可读性更高。</p>
<h4 id="检查“类”关系"><a href="#检查“类”关系" class="headerlink" title="检查“类”关系"></a>检查“类”关系</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">Foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line"><span class="comment">// ...</span></div><div class="line">&#125;</div><div class="line">Foo.prototype.blah = ...;</div><div class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> Foo();</div><div class="line">a <span class="keyword">instanceof</span> Foo; <span class="comment">// true</span></div></pre></td></tr></table></figure>
<p>instanceof 操作符的左操作数是一个普通的对象，右操作数是一个函数。 instanceof 回答的问题是：在 a 的整条[[Prototype]] 链中是否有指向 Foo.prototype 的对象，这个方法<strong>只能处理对象（ a ）和函数</strong>（带 .prototype 引用的 Foo ）之间的关系。</p>
<p>判断 [[Prototype]] 反射的方法：</p>
<pre><code>Foo.prototype.isPrototypeOf( a ); // true
</code></pre><p>我们只需要一个可以用来判断的对象（本例中Foo.prototype ）就行。 isPrototypeOf(..) 回答的问题是：在 a 的整条 [[Prototype]] 链中是否出现过 Foo.prototype ？<br>语言内置的 isPrototypeOf(..) 函数就是我们的 isRelatedTo(..) 函数。<br>我们也可以直接获取一个对象的 [[Prototype]] 链。在 ES5中，标准的方法是：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">Object</span>.getPrototypeOf( a );</div></pre></td></tr></table></figure>
<p>可以验证一下，这个对象引用是否和我们想的一样：</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">Object</span>.getPrototypeOf( a ) === Foo.prototype; <span class="comment">// true</span></div></pre></td></tr></table></figure>
<h4 id="对象关联"><a href="#对象关联" class="headerlink" title="对象关联"></a>对象关联</h4><p>Object.create(..) 会创建一个新对象（ bar ）并把它关联到我们指定的对象（ foo ），这样我们就可以充分发挥 [[Prototype]] 机制的威力（委托）并且避免不必要的麻烦（比如使用 new 的构造函数调用会生成 .prototype 和 .constructor 引用）。</p>
<blockquote>
<p>Object.create(null) 会 创 建 一 个 拥 有 空（ 或 者 说 null ） [[Prototype]]链接的对象，这个对象无法进行委托。</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      你不知道的JavaScript(上卷)学习笔记精华
    
    </summary>
    
      <category term="前端" scheme="http://yoursite.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="JavaScript" scheme="http://yoursite.com/tags/JavaScript/"/>
    
  </entry>
  
  <entry>
    <title>different image host performance</title>
    <link href="http://yoursite.com/2016/10/06/different-image-host-performance/"/>
    <id>http://yoursite.com/2016/10/06/different-image-host-performance/</id>
    <published>2016-10-06T01:34:09.000Z</published>
    <updated>2016-10-06T03:36:50.484Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Image-host-compare"><a href="#Image-host-compare" class="headerlink" title="Image host compare:"></a>Image host compare:</h1><h2 id="七牛"><a href="#七牛" class="headerlink" title="七牛"></a>七牛</h2><p><img src="http://oekg9a0ty.bkt.clouddn.com/1.jpg" alt=""></p>
<p><img src="http://oekg9a0ty.bkt.clouddn.com/2.jpg" alt=""></p>
<p><img src="http://oekg9a0ty.bkt.clouddn.com/3.png" alt=""></p>
<h2 id="Imgur"><a href="#Imgur" class="headerlink" title="Imgur"></a>Imgur</h2><p><img src="http://imgur.com/DXEE0Dq.jpg" alt=""></p>
<p><img src="http://imgur.com/M1WVg9h.jpg" alt=""></p>
<p><img src="http://imgur.com/v74BVIV.jpg" alt=""></p>
<h2 id="PhotoBucket"><a href="#PhotoBucket" class="headerlink" title="PhotoBucket"></a>PhotoBucket</h2><p><img src="http://i1076.photobucket.com/albums/w448/sstd521/Mobile%20Uploads/ea36af03tw1ejhruh9ttbj20go0bnjs2_zps45m2bvxg.jpg" alt=""></p>
<p><img src="http://i1076.photobucket.com/albums/w448/sstd521/Mobile%20Uploads/1516291831_zpsy66hnces.jpg" alt=""></p>
<p><img src="http://i1076.photobucket.com/albums/w448/sstd521/Mobile%20Uploads/mmexport1470608951897_zpsc8mm8utm.png" alt=""></p>
<h2 id="Flicker"><a href="#Flicker" class="headerlink" title="Flicker"></a>Flicker</h2><p><img src="https://c5.staticflickr.com/6/5076/30028702572_3ddc05203e_z.jpg" alt=""></p>
<p><a data-flickr-embed="true" href="https://www.flickr.com/photos/147777450@N03/29514303483/in/dateposted-public/" title="2" target="_blank" rel="external"><img src="https://c4.staticflickr.com/9/8537/29514303483_f10f056342_z.jpg" width="640" height="480" alt="2"></a><script async src="//embedr.flickr.com/assets/client-code.js" charset="utf-8"></script></p>
<p><img src="https://c4.staticflickr.com/9/8537/29514303483_f10f056342_z.jpg" alt=""></p>
<p><img src="https://c6.staticflickr.com/6/5731/30058683541_e426c6e70c_z.jpg" alt=""></p>
<h2 id="Tinypic"><a href="#Tinypic" class="headerlink" title="Tinypic"></a>Tinypic</h2><p><img src="http://i67.tinypic.com/zjypzk.jpg" alt=""></p>
<p><img src="http://i66.tinypic.com/294s2oo.jpg" alt=""></p>
<p><img src="http://i68.tinypic.com/30280mo.jpg" alt=""></p>
<h2 id="ImageSafe"><a href="#ImageSafe" class="headerlink" title="ImageSafe"></a>ImageSafe</h2><p><img src="http://i.imgsafe.org/5bdd2c5ec4.jpg" alt=""></p>
<p><img src="http://i.imgsafe.org/5bdd6c7cfb.jpg" alt=""></p>
<p><img src="http://i.imgsafe.org/5bde37f8bd.png" alt=""></p>
<h2 id="ImageStack"><a href="#ImageStack" class="headerlink" title="ImageStack"></a>ImageStack</h2><p><img src="http://imageshack.com/a/img921/9382/B4RM1R.jpg" alt=""></p>
<p><img src="http://imageshack.com/a/img923/5526/5THOE1.jpg" alt=""></p>
<p><img src="http://imageshack.com/a/img921/636/qTEgFM.png" alt=""></p>
]]></content>
    
    <summary type="html">
    
      Image host compare
    
    </summary>
    
      <category term="工具" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/"/>
    
    
      <category term="Image Host" scheme="http://yoursite.com/tags/Image-Host/"/>
    
  </entry>
  
  <entry>
    <title>在hexo-new之后立马打开Markdown文稿</title>
    <link href="http://yoursite.com/2016/10/05/%E5%9C%A8hexo-new%E4%B9%8B%E5%90%8E%E7%AB%8B%E9%A9%AC%E6%89%93%E5%BC%80Markdown%E6%96%87%E7%A8%BF/"/>
    <id>http://yoursite.com/2016/10/05/在hexo-new之后立马打开Markdown文稿/</id>
    <published>2016-10-05T05:36:51.000Z</published>
    <updated>2016-10-05T06:49:49.543Z</updated>
    
    <content type="html"><![CDATA[<h2 id="在hexo-new之后立即用马克飞象打开Markdown文稿"><a href="#在hexo-new之后立即用马克飞象打开Markdown文稿" class="headerlink" title="在hexo new之后立即用马克飞象打开Markdown文稿"></a>在hexo new之后立即用马克飞象打开Markdown文稿</h2><h2 id="第一步"><a href="#第一步" class="headerlink" title="第一步"></a>第一步</h2><p>　　　在根目录新建一个文件夹scripts(因为本来没有，只能新建)，然后新建txt文本文件，重命名为 “open_editor_after_hexo_new_immediately.js”，这个在Windows下干活的人都知道吧，名字可以随便，不过我觉得这个名字比较好认，你以后可能要改也方便。<br><img src="/images/js.png" alt="Alt text"></p>
<h2 id="第二步"><a href="#第二步" class="headerlink" title="第二步:"></a>第二步:</h2><p>　　　随便用一个文本编辑器打开，比如用Sublime Text。读者可以选择自己偏好的markdown编辑器(马克飞象不能用，因为无法保存)，我个人修改内容如下:<br>　　　<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> exec = <span class="built_in">require</span>(<span class="string">'child_process'</span>).exec;</div><div class="line"><span class="comment">// Hexo 2.x 传进来的参数即是路径</span></div><div class="line"><span class="comment">/*</span></div><div class="line">hexo.on('new', function(path)&#123;</div><div class="line">    exec('"S:\\Hexo\\MarkdownPad2\\MarkdownPad2.exe" ' + "\"" + [path] + "\"");</div><div class="line">&#125;);</div><div class="line">*/</div><div class="line"><span class="comment">// Hexo 3 传进来的参数是一个&#123;&#125;定义的多属性对象，</span></div><div class="line"><span class="comment">// 参见"hexo根目录/node_modules/hexo/lib/plugins/console/new.js"</span></div><div class="line">hexo.on(<span class="string">'new'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">data</span>)</span>&#123;</div><div class="line">    exec(<span class="string">'"D:/Program Files/Typora/Typora.exe" '</span> + <span class="string">"\""</span> + [data.path] + <span class="string">"\""</span>);</div><div class="line">&#125;);</div></pre></td></tr></table></figure></p>
<p>进入hexo根目录，git bash here 输入hexo new title ,会自动打开markdown编辑软件(我用的是免费的Typora,功能很强大，十分推荐)，然后就可以愉快的编辑le,编辑后，hexo server 本地预览后就可以生成更新部署啦。</p>
<p><img src="/images/Typora.png" alt="Typora"></p>
<p>参考：<a href="http://durant35.github.io/2016/02/02/hexo_%E5%9C%A8hexo%20new%E4%B9%8B%E5%90%8E%E7%AB%8B%E5%8D%B3%E6%89%93%E5%BC%80%E6%96%B0%E5%BB%BA%E7%9A%84Markdown%E6%96%87%E7%A8%BF/" target="_blank" rel="external">在hexo new之后立即打开Markdown文稿</a></p>
]]></content>
    
    <summary type="html">
    
      在hexo-new之后立马打开Markdown文稿，好处就不啰嗦了，直接操作吧。
    
    </summary>
    
      <category term="博客" scheme="http://yoursite.com/categories/%E5%8D%9A%E5%AE%A2/"/>
    
    
      <category term="MarkDown" scheme="http://yoursite.com/tags/MarkDown/"/>
    
      <category term="Hexo" scheme="http://yoursite.com/tags/Hexo/"/>
    
  </entry>
  
  <entry>
    <title>Cocos学习笔记00</title>
    <link href="http://yoursite.com/2016/10/05/Cocos%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B000/"/>
    <id>http://yoursite.com/2016/10/05/Cocos学习笔记00/</id>
    <published>2016-10-05T05:05:13.000Z</published>
    <updated>2016-10-05T05:25:03.125Z</updated>
    
    <content type="html"><![CDATA[<h1 id="cocos-creator"><a href="#cocos-creator" class="headerlink" title="cocos creator"></a>cocos creator</h1><p>@(cocos学习笔记)</p>
<blockquote>
<p>Cocos Creator 是一款以内容创作为导向的新型游戏开发工具，它完整集成了组件化的 Cocos2d-x WEB 版本，可发布游戏到 Web，iOS，Android，Mac，Windows等平台，更支持直接发布 Cocos Play 平台，把握手机页游渠道的快速发展带来的新机遇。<br><img src="/images/structure.png" alt="Alt text"></p>
<p>Cocos2d-x 中，开发方式是以代码来驱动,在 Cocos Creator 框架中，所有场景都会被序列化为纯数据，在运行时使用这些纯数据来重新构建场景，界面，动画甚至组件等元素。 Cocos Creator 提供的是更加彻底的数据驱动方式，在编辑器中编辑的所有信息都会被序列化到数据文件中，在运行时，引擎会通过反序列化的方式将数据直接转化为对象。引擎中类的属性是可直接被序列化和反序列化的，不需要通过任何映射关系来转化。</p>
<h2 id="序列化和反序列化"><a href="#序列化和反序列化" class="headerlink" title="序列化和反序列化"></a>序列化和反序列化</h2><p>序列化和反序列化支持 Cocos Creator 中类的绝大多数公有属性，这些属性通过属性检查器面板暴露给开发者。开发者可以在编辑器中随意修改并保存，保存的过程就是将资源和场景数据序列化到资源数据库（Asset Database）中。反之，在加载场景的过程中，反序列化机制会根据场景数据实例化相应的对象，并还原编辑器中设置的所有属性。<br>数据驱动的强大之处在于，用户自己编辑的组件也可以进行属性声明。这些属性可以在编辑器中被编辑，也会被保存到场景数据中，最后在运行时被反序列化到游戏场景中。资源数据库在编辑器中以资源管理器的形式呈现。</p>
<p>Cocos Creator 的工作流是内容创作为导向的。数据驱动使得场景可以被自由得进行编辑，不仅可以可视化得搭建整个场景，还可以对游戏逻辑进行编辑（编辑组件暴露出的属性）。这使得一切的入口点变成了编辑器，而不是代码。<br><img src="/images/edit_e.png" alt="Alt text"></p>
</blockquote>
<h2 id="逻辑树和渲染树"><a href="#逻辑树和渲染树" class="headerlink" title="逻辑树和渲染树"></a>逻辑树和渲染树</h2><blockquote>
<p>开发者在编辑器中搭建的节点树和挂载的组件共同组成了逻辑树，其中节点构成实体单位，组件负责逻辑。逻辑树会生成场景的渲染树，决定渲染顺序，不过开发者并不需要关心这些，只要在编辑器中保障显示效果正确即可。在编辑器的 Node Tree 层级管理器中，开发者可以调整逻辑树的顺序和父子关系。</p>
<h2 id="场景管理"><a href="#场景管理" class="headerlink" title="场景管理"></a>场景管理</h2><p>在 Cocos Creator 中，开发者在编辑器中搭建完的场景，所有数据会保存为一个 scene-name.fire 文件，存在资源数据库（Asset Database）中。开发者可以通过 cc.director.loadScene 来加载一个场景资源，参见具体范例：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> sceneName = <span class="string">'scene-name'</span>;</div><div class="line"><span class="keyword">var</span> onLaunched = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">'Scene '</span> + sceneName + <span class="string">' launched'</span>);</div><div class="line">&#125;;</div><div class="line"><span class="comment">// 第一个参数为场景的名字，第二个可选参数为场景加载后的回调函数</span></div><div class="line">cc.director.loadScene(sceneName, onLaunched);</div></pre></td></tr></table></figure></p>
</blockquote>
<p>此外，我们提供了访问场景节点的接口：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// 获取逻辑树的场景节点</span></div><div class="line"><span class="keyword">var</span> logicScene = cc.director.getScene();</div></pre></td></tr></table></figure></p>
<blockquote>
<p>在 Cocos Creator 中，游戏场景（Scene）是开发时组织游戏内容的中心，也是呈现给玩家所有游戏内容的载体。</p>
<h3 id="Canvas"><a href="#Canvas" class="headerlink" title="Canvas"></a>Canvas</h3><p>打开场景后，层级管理器中会显示当前场景中的所有节点和他们的层级关系，Canvas可以被称为画布节点或渲染根节点。</p>
<p>组件系统变成了整个引擎的核心，组件可以以各种各样的方式扩展逻辑节点的功能。甚至可以说，逻辑节点本身不应该包含任何实际游戏逻辑，它应该由各种逻辑组件组合出完整的逻辑。</p>
<p>Cocos Creator 中，子节点坐标系的坐标原点就是父节点的坐标位置，也就是其锚点的位置。这样的修改对于编辑器更友好。<br><img src="/images/cocos_p.png" alt="Alt text"></p>
</blockquote>
<h3 id="Texture-和-SpriteFrame-资源类型"><a href="#Texture-和-SpriteFrame-资源类型" class="headerlink" title="Texture 和 SpriteFrame 资源类型"></a>Texture 和 SpriteFrame 资源类型</h3><blockquote>
<p>SpriteFrame 是核心渲染组件 Sprite 所使用的资源，设置或替换 Sprite 组件中的 spriteFrame 属性，就可以切换显示的图像。</p>
<h3 id="图集资源（Atlas）"><a href="#图集资源（Atlas）" class="headerlink" title="图集资源（Atlas）"></a>图集资源（Atlas）</h3><p>图集（Atlas）也称作 Sprite Sheet，是游戏开发中常见的一种美术资源。图集是通过专门的工具如TexturePacker等将多张图片合并成一张大图，并通过 plist 等格式的文件索引的资源。可供 Cocos Creator 使用的图集资源由 plist 和 png 文件组成。</p>
<p><strong>在游戏中使用多张图片合成的图集作为美术资源，有以下优势：</strong><br>合成图集时会去除每张图片周围的空白区域，加上可以在整体上实施各种优化算法，合成图集后可以大大减少游戏包体和内存占用<br>多个 Sprite 如果渲染的是来自同一张图集的图片时，这些 Sprite 可以使用同一个渲染批次来处理，大大减少 CPU 的运算时间，提高运行效率。</p>
</blockquote>
<h3 id="字体资源"><a href="#字体资源" class="headerlink" title="字体资源"></a>字体资源</h3><blockquote>
<p>Cocos Creator 制作的游戏中可以使用三类字体资源：系统字体，动态字体和位图字体。</p>
</blockquote>
<h3 id="使用-cc-Class-声明类型"><a href="#使用-cc-Class-声明类型" class="headerlink" title="使用 cc.Class 声明类型"></a>使用 cc.Class 声明类型</h3><blockquote>
<p>cc.Class 是一个很常用的 API，用于声明 Cocos Creator 中的类，为了方便区分，我们把使用 cc.Class 声明的类叫做 CCClass。</p>
<h4 id="定义-CCClass"><a href="#定义-CCClass" class="headerlink" title="定义 CCClass"></a>定义 CCClass</h4><p>调用 cc.Class，传入一个原型对象，在原型对象中以<code>键值对</code>的形式设定所需的类型参数，就能创建出所需要的类。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">var Sprite = cc.Class(&#123;</div><div class="line">    name: &quot;sprite&quot;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<blockquote>
<p>以上代码用 cc.Class 创建了一个类型，并且赋给了 Sprite 变量。同时还将类名设为 “sprite”，类名用于序列化，一般可以省略。</p>
<h4 id="实例化"><a href="#实例化" class="headerlink" title="实例化:"></a>实例化:</h4><p>Sprite 变量保存的是一个 JavaScript 构造函数，可以直接 new 出一个对象：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">var obj = new Sprite();</div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      Cocos Creator 是一款以内容创作为导向的新型游戏开发工具，它完整集成了组件化的 Cocos2d-x WEB 版本，可发布游戏到 Web，iOS，Android，Mac，Windows等平台，更支持直接发布 Cocos Play 平台，把握手机页游渠道的快速发展带来的新机遇。
    
    </summary>
    
      <category term="游戏" scheme="http://yoursite.com/categories/%E6%B8%B8%E6%88%8F/"/>
    
    
      <category term="Cocos creator" scheme="http://yoursite.com/tags/Cocos-creator/"/>
    
  </entry>
  
  <entry>
    <title>Windows下如何关闭某个端口</title>
    <link href="http://yoursite.com/2016/10/05/Windows%E4%B8%8B%E5%A6%82%E4%BD%95%E5%85%B3%E9%97%AD%E6%9F%90%E4%B8%AA%E7%AB%AF%E5%8F%A3/"/>
    <id>http://yoursite.com/2016/10/05/Windows下如何关闭某个端口/</id>
    <published>2016-10-05T04:57:48.000Z</published>
    <updated>2016-10-05T05:30:11.139Z</updated>
    
    <content type="html"><![CDATA[<p>hexo - 使用默认localhost:4000端口访问本地blog一直无响应，因为该端口被占用了。<br>解决办法：</p>
<blockquote>
<p>Windows下关闭某个端口，这里我们关闭4000端口。<br>按下Win+R调出命令行窗口，输入netstat -aon|findstr “4000”，找到指定行最后一列的数字（PID），我们这里是2644.<br>输入tasklist|findstr “2644”，发现是foxitprotect.exe占用了4000端口。<br>再次输入taskkill /f /t /im foxitprotect.exe结束该进程。<br>原来是pdf阅读器占用了该端口，找到该软件，卸载掉即可。<br><img src="/images/port.png" alt="Alt text"></p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      解决hexo - 使用localhost:4000访问本地blog一直无响应的问题。
    
    </summary>
    
      <category term="Window" scheme="http://yoursite.com/categories/Window/"/>
    
    
      <category term="skill" scheme="http://yoursite.com/tags/skill/"/>
    
  </entry>
  
  <entry>
    <title>你不知道的JavaScript01--原生函数</title>
    <link href="http://yoursite.com/2016/10/05/%E4%BD%A0%E4%B8%8D%E7%9F%A5%E9%81%93%E7%9A%84JavaScript01-%E5%8E%9F%E7%94%9F%E5%87%BD%E6%95%B0/"/>
    <id>http://yoursite.com/2016/10/05/你不知道的JavaScript01-原生函数/</id>
    <published>2016-10-05T04:37:22.000Z</published>
    <updated>2016-10-05T04:42:33.840Z</updated>
    
    <content type="html"><![CDATA[<h2 id="JavaScript学习笔记01"><a href="#JavaScript学习笔记01" class="headerlink" title="JavaScript学习笔记01"></a>JavaScript学习笔记01</h2><p>@(JavaScript)</p>
<h3 id="原生函数"><a href="#原生函数" class="headerlink" title="原生函数"></a>原生函数</h3><blockquote>
<p>原生函数可以当做构造函数来使用，通过构造函数（如 <code>new String(&quot;abc&quot;)</code>）创建出来的是封装了基本类型值（如 <code>&quot;abc&quot;</code>）的封装对象。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span>  string = <span class="keyword">new</span> <span class="built_in">String</span>(<span class="string">"abc"</span>);</div><div class="line"><span class="built_in">console</span>.log(string);  <span class="comment">// [String: 'abc']</span></div><div class="line"><span class="built_in">console</span>.log(string <span class="keyword">instanceof</span> <span class="built_in">String</span>);  <span class="comment">// true</span></div><div class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> string);  <span class="comment">// object</span></div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.prototype.toString.call(string));  <span class="comment">// [object String]</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="内部属性"><a href="#内部属性" class="headerlink" title="内部属性"></a>内部属性</h4><blockquote>
<p>所有typeof返回值为<code>&quot;object&quot;</code>的对象都包含了一个内部属性，可以通过<code>Object.prototype.toString(..)</code>查看。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.prototype.toString.call([<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]))  <span class="comment">// [object Array]</span></div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.prototype.toString.call(<span class="literal">true</span>))  <span class="comment">// [object Boolean]</span></div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.prototype.toString.call(<span class="number">42</span>))  <span class="comment">// [object Number]</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="封装对象包装"><a href="#封装对象包装" class="headerlink" title="封装对象包装"></a>封装对象包装</h4><blockquote>
<p>基本类型没有 <code>.length</code>和<code>.toString()</code>等这样的属性和方法，需要封装对象才可使用，此时JavaScript会为基本类型值自动包装成一个封装对象，浏览器对此也进行了优化，因此应优先考虑用基本类型值，而不是<code>new  String(&quot;abc&quot;)</code>新对象。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span>  a = <span class="string">"abc"</span>;</div><div class="line"><span class="built_in">console</span>.log(a.length); <span class="comment">// 3</span></div><div class="line"><span class="built_in">console</span>.log(a.toUpperCase()); <span class="comment">//ABC</span></div></pre></td></tr></table></figure></p>
<p>一般不推荐直接使用封装对象，如果想要自行封装基本类型值，可以使用<code>Object(..)</code>函数（不带<code>new</code>关键字）。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="string">"abc"</span>;</div><div class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> <span class="built_in">String</span>(a);</div><div class="line"><span class="keyword">var</span> c = <span class="built_in">Object</span>(a);</div><div class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> a);  <span class="comment">// string</span></div><div class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> b);  <span class="comment">// object</span></div><div class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> c);  <span class="comment">// object</span></div><div class="line"><span class="built_in">console</span>.log(b <span class="keyword">instanceof</span> <span class="built_in">String</span>);  <span class="comment">// true</span></div><div class="line"><span class="built_in">console</span>.log(c <span class="keyword">instanceof</span> <span class="built_in">String</span>);  <span class="comment">// true</span></div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.prototype.toString.call(b));  <span class="comment">// [object String]</span></div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.prototype.toString.call(c)); <span class="comment">// [object String]</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="拆封"><a href="#拆封" class="headerlink" title="拆封"></a>拆封</h4><blockquote>
<p>如果想要得到封装对象的基本类型值，可以使用valueof()函数:<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> <span class="built_in">String</span>(<span class="string">"abc"</span>);</div><div class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> <span class="built_in">Boolean</span>(<span class="literal">true</span>);</div><div class="line">a.valueof();  <span class="comment">// "abc"</span></div><div class="line">b.valueof();  <span class="comment">// true</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="原生函数作为构造函数"><a href="#原生函数作为构造函数" class="headerlink" title="原生函数作为构造函数"></a>原生函数作为构造函数</h4><blockquote>
<p>构造函数<code>Array(..)</code>不要求必须带<code>new</code>关键字，不带时，系统会自动补上。<code>Array</code>构造函数只带一个数字参数的时候，该参数会被当做数组的预设长度，而非一个元素。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="built_in">Array</span>(<span class="number">3</span>);</div><div class="line"><span class="built_in">console</span>.log(a.length);  <span class="comment">// 3</span></div></pre></td></tr></table></figure></p>
<p>创建包含<code>undefined</code>的数组：<code>apply(..)</code>是一个工具，适用于所有对象，它会以一种特殊的方式来调用传递给它的函数。第一个参数是<code>this</code>对象，第二个必须是一个数组（或类数组对象，array-like object），数组中的值被用作函数的参数。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="built_in">Array</span>.apply(<span class="literal">null</span>,&#123; <span class="attr">length</span>:<span class="number">3</span>&#125;);</div><div class="line"><span class="built_in">console</span>.log(a);  <span class="comment">// [ undefined, undefined, undefined ]</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="Object-、Function-、RegExp"><a href="#Object-、Function-、RegExp" class="headerlink" title="Object(..)、Function(..)、RegExp(..)"></a>Object(..)、Function(..)、RegExp(..)</h4><blockquote>
<p>尽量不要使用<code>Object(..)、Function(..)、Regexp(..)</code>，在实际情况中没有必要使用<code>new Object()</code>来创建对象，因为这样无法像常量形式那样一次设定多个属性，而必须逐一设定。强烈建议使用常量形式定义正则表达式，语法简单、效率高。</p>
<h4 id="Data-、Error"><a href="#Data-、Error" class="headerlink" title="Data(..)、Error(..)"></a>Data(..)、Error(..)</h4><p>创建日起对象必须使用<code>new Date()</code>。<code>Date(..)</code>可以带参数，用来指定日期时间，若不带参数则使用当前的如期和时间。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log((<span class="keyword">new</span> <span class="built_in">Date</span>()).getDate());  <span class="comment">// 28</span></div></pre></td></tr></table></figure></p>
<p>构造函数<code>Error(..)</code>,带不带<code>new</code>关键字都可以。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">x</span>) </span>&#123;</div><div class="line">  <span class="keyword">if</span> (!x)</div><div class="line">  <span class="keyword">throw</span>  <span class="built_in">Error</span>(<span class="string">"x not found"</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="Symbol"><a href="#Symbol" class="headerlink" title="Symbol(..)"></a>Symbol(..)</h4><blockquote>
<p>基本数据类型，符号(<code>Symbol</code>)是具有唯一性的特殊值，用它来命名对象属性不容易导致重名，符号还可以自行定义，<code>Symbol</code>以静态属性形式出现，如<code>Symbol.create</code>等、不能用<code>new</code>关键字创建。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> mysym  = <span class="built_in">Symbol</span>(<span class="string">"abcd"</span>);</div><div class="line"><span class="built_in">console</span>.log(mysym);  <span class="comment">// Symbol(abcd)</span></div><div class="line"><span class="keyword">var</span> a = &#123;&#125;;</div><div class="line">a[mysym] = <span class="string">"apple"</span>;</div><div class="line"><span class="built_in">console</span>.log(a[mysym]);  <span class="comment">// apple</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="原生原型"><a href="#原生原型" class="headerlink" title="原生原型"></a>原生原型</h4><blockquote>
<p>原生构造函数都有自己的<code>.prototype</code>对象，如<code>Array.prototype</code>等，这些对象包含其对应子类型所有的行为特征。比如将字符串封装成字符串对象，就可以访问<code>String.prototype</code>中定义的方法。</p>
<p>根据文档约定，可将<code>.prototype.</code>替换为<code>#</code>，如<code>String.prototype.trim()</code>简写为<code>String#trim()</code>来去掉字符串的前后空格，<code>Array#concat(..)</code>合并数组，<code>Number#tofixed(..)</code>将数字转换为指定长度的整数字符串，其他对象同样如此。</p>
<p><code>Function.prototype</code>是一个空函数，<code>Array.prototype</code>是一个空数组，<code>RegExp.prototype</code>是一个空正则表达式，对未赋值的变量来说，它们是很好的默认值，在ES6中可通过函数声明的内置语法来设置(取而代之)。</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      原生函数可以当做构造函数来使用，通过构造函数（如 new String(&quot;abc&quot;)）创建出来的是封装了基本类型值（如 &quot;abc&quot;）的封装对象。
    
    </summary>
    
      <category term="前端" scheme="http://yoursite.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="JavaScript" scheme="http://yoursite.com/tags/JavaScript/"/>
    
  </entry>
  
  <entry>
    <title>你不知道的JavaScript00——原型</title>
    <link href="http://yoursite.com/2016/10/05/%E4%BD%A0%E4%B8%8D%E7%9F%A5%E9%81%93%E7%9A%84JavaScript00%E2%80%94%E2%80%94%E5%8E%9F%E5%9E%8B/"/>
    <id>http://yoursite.com/2016/10/05/你不知道的JavaScript00——原型/</id>
    <published>2016-10-05T04:21:43.000Z</published>
    <updated>2016-10-05T04:40:22.522Z</updated>
    
    <content type="html"><![CDATA[<h3 id="JavaScript学习笔记00"><a href="#JavaScript学习笔记00" class="headerlink" title="JavaScript学习笔记00"></a>JavaScript学习笔记00</h3><p>@(JavaScript)</p>
<hr>
<blockquote>
<p>JavaScript 是一种弱类型的语言。</p>
</blockquote>
<h3 id="类型"><a href="#类型" class="headerlink" title="类型"></a>类型</h3><p> <img src="/images/数据类型.png" alt="Alt text"></p>
<blockquote>
<ul>
<li>JavaScript 中的变量是没有类型，值有才类型，变量持有值的类型，类型定义了值的行为特征。</li>
<li>对变量执行<code>typeof</code>操作时，得到的是该变量持有的值的类型。</li>
<li><code>typeof</code>运算符总会返回一个字符串。<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">typeof</span> <span class="keyword">typeof</span> <span class="number">42</span>; <span class="comment">// "string"</span></div></pre></td></tr></table></figure>
</li>
</ul>
</blockquote>
<p><em><code>typeof 42</code> 首先返回一个字符串 <code>&quot;number&quot;</code>，然后<code>typeof &quot;number&quot;</code>,返回 <code>&quot;string&quot;</code></em>。</p>
<blockquote>
<p>在作用域中声明但还没有赋值的变量为<code>undefined</code>，是值的一种，没有在作用域中声明过的变量为<code>undeclared</code>，两个不可混为一谈。</p>
</blockquote>
<h3 id="值"><a href="#值" class="headerlink" title="值"></a>值</h3><h4 id="数组与字符串"><a href="#数组与字符串" class="headerlink" title="数组与字符串"></a>数组与字符串</h4><blockquote>
<p>对数组声明后即可向其加入值，无需预先指定数组大小。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [ ];</div><div class="line">arr.length; <span class="comment">// 0</span></div><div class="line">a[<span class="number">0</span>] = <span class="number">1</span>;</div><div class="line">a[<span class="number">1</span>] = <span class="string">"2"</span>;</div><div class="line">a[<span class="number">2</span>] = [<span class="number">3</span>];</div><div class="line">a.length; <span class="comment">// 3</span></div></pre></td></tr></table></figure></p>
<p>数组不仅可以通过数字下标作为索引，也可以用包含字符串键值和属性，但这些不计入数组长度内。</p>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [ ];</div><div class="line"></div><div class="line">a[<span class="number">0</span>] = <span class="string">"2"</span>;</div><div class="line">a[<span class="number">1</span>] = <span class="number">20</span>;</div><div class="line">arr.length; <span class="comment">// </span></div><div class="line">a[<span class="string">"apple"</span>] = <span class="number">1</span>;</div><div class="line">a.length; <span class="comment">// 2</span></div><div class="line">a[<span class="string">"apple"</span>] = <span class="number">1</span>;</div><div class="line">a.apple = <span class="number">1</span>;</div></pre></td></tr></table></figure>
<p>如果字符串键值可以被强制类型转换为十进制数字的话，依旧会被当做数字索引来处理。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> arr = [ ];</div><div class="line">a[<span class="string">"13"</span>] = <span class="number">22</span>;</div><div class="line">a.lenth; <span class="comment">// 14</span></div></pre></td></tr></table></figure></p>
<p>不建议在数组中加入字符串作为下标，建议使用数字下标。<br>JavaScript 中的字符串是不可变的，而数组可变。字符串不可变是指字符串的成员函数不会改变其原始值，而是创建并返回一个新的字符串。</p>
</blockquote>
<p>字符串反转：<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> chars = <span class="string">"ufo"</span>;</div><div class="line"><span class="keyword">var</span> reverseChars = chars.split(<span class="string">""</span>).reverse().join(<span class="string">""</span>)<span class="comment">//ofu</span></div></pre></td></tr></table></figure></p>
<p><em><code>chars.split(&quot;&quot;)</code>将字符串转换为字符数组，<code>.reverse()</code>将数组中的字符逆序排序，<code>.join(&quot;&quot;)</code>将数组中的字符拼接回字符串</em>。</p>
<h4 id="数字"><a href="#数字" class="headerlink" title="数字"></a>数字</h4><blockquote>
<p>JavaScript 中只有一种数值类型：<code>number</code>(数字)，包括十进制的小数和没有小数的整数。</p>
<p>#####数字语法<br>数字前面的0和小数点后最后面的0可以省略,但不建议这样写。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> num = <span class="number">.42</span>;</div><div class="line"><span class="keyword">var</span> num = <span class="number">42.</span>;</div></pre></td></tr></table></figure></p>
<p>tofixed(..),可以指定小数部分显示的位数，输出结果为字符串形式。<br> toprecision(..)，用来指定有效位数。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> num = <span class="number">13.1341</span>;</div><div class="line">num.tofixed(<span class="number">3</span>); <span class="comment">// "13.134"</span></div><div class="line">num.toprecision(<span class="number">4</span>); <span class="comment">// "13.13"</span></div><div class="line">(<span class="number">42</span>).tofixed(<span class="number">3</span>); <span class="comment">// "42.000"</span></div><div class="line"><span class="number">42.</span>.tofixed(<span class="number">3</span>); <span class="comment">// "42.000"  罕见</span></div><div class="line"><span class="number">0.42</span>.tofixed(<span class="number">3</span>); <span class="comment">// "0.420" 罕见</span></div><div class="line"><span class="number">42.</span>tofixed(<span class="number">3</span>); <span class="comment">// 语法错误，相当于(42)tofixed(3);</span></div><div class="line"><span class="number">42</span> .tofixed(<span class="number">3</span>); <span class="comment">// "42.000"  不推荐</span></div></pre></td></tr></table></figure></p>
<p>进制，从ES6开始采用严格模式(strict mode)，为了代码的易读性，建议使用小写的0b、0o、0x代表二进制、八进制和十六进制。</p>
<h5 id="整数检测"><a href="#整数检测" class="headerlink" title="整数检测"></a>整数检测</h5><p> 检测一个数是否为整数<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">Number</span>.isInteger(<span class="number">42.0</span>); <span class="comment">// true</span></div></pre></td></tr></table></figure></p>
</blockquote>
<p> 检测一个数是否为安全的整数(最大值为 Number.MAX_SAFE_NTEGER == Math.pow(2,53)-1 )<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">Number</span>.isSafeInteger(<span class="built_in">Math</span>.pow(<span class="number">2</span>,<span class="number">53</span>)<span class="number">-1</span>); <span class="comment">// true</span></div></pre></td></tr></table></figure></p>
<h4 id="特殊数值"><a href="#特殊数值" class="headerlink" title="特殊数值"></a>特殊数值</h4><h5 id="不是值的值"><a href="#不是值的值" class="headerlink" title="不是值的值"></a>不是值的值</h5><blockquote>
<ul>
<li><p>undefined 数据类型只有一个的值，即 undefined，表示没有值、从未赋值，通过void 运算符即可得到该值。</p>
</li>
<li><p>null 类型也只有一个值，即 null，表示空值，目前没有值。</p>
</li>
</ul>
<p>两者常用来表示”空值”或”不是值”的值。</p>
<h5 id="特殊的数字"><a href="#特殊的数字" class="headerlink" title="特殊的数字"></a>特殊的数字</h5><ul>
<li>不是数字的数字<code>NaN(not a number)</code>，也可理解为”无效数值”、”坏数值”。<code>NaN</code> 是一个特殊值，它和自身不相等，是唯一一个非自反值(即<code>x===x</code>不成立)，而<code>NaN != NaN</code>为 <code>true</code>。“不是数字的数字”仍然是数字类型。</li>
</ul>
<p>如果需要判断一个值是否是<code>NaN</code>，建议使用<code>Number.isNaN(..)</code>。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">if</span>(!<span class="built_in">Number</span>.isNaN)&#123;</div><div class="line"><span class="built_in">Number</span>.isNaN = <span class="function"><span class="keyword">function</span>(<span class="params">n</span>) </span>&#123;</div><div class="line"><span class="keyword">return</span> n !== n;</div><div class="line">&#125;;</div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> a = <span class="number">2</span> / <span class="string">"foo"</span>;</div><div class="line"><span class="keyword">var</span> b = <span class="string">"foo"</span>;</div><div class="line"><span class="built_in">Number</span>.isNaN( a ); <span class="comment">// true</span></div></pre></td></tr></table></figure></p>
<ul>
<li>无穷数,相对于 <code>Infinity</code>， <code>Number.MAX_ VALUE + Math.pow(2, 969)</code> 与 <code>Number.MAX_VALUE</code> 更为接近，因此它被“向下取整”（ round down）；而 <code>Number.MAX_VALUE + Math.pow(2, 970)</code> 与 <code>Infinity</code> 更为接近，所以它被“向上取整”（ round up）。</li>
</ul>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="number">1</span> / <span class="number">0</span>; <span class="comment">// Infinity</span></div><div class="line"><span class="keyword">var</span> b = <span class="number">-1</span> / <span class="number">0</span>; <span class="comment">// -Infinity</span></div></pre></td></tr></table></figure>
<blockquote>
<ul>
<li>零值，加法和减法运算不会得到负零（ negative zero）。根据规范，对负零进行字符串化会返回 “0”，将其从字符串转换为数字，得到的结果是准确的。<code>-0==0</code>，<code>-0===0</code>，都为<code>true</code>，ES6 中新加入了一个工具方法 Object.is(..) 来判断两个值是否绝对相等。<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="number">0</span> / <span class="number">-3</span>; <span class="comment">// -0</span></div><div class="line"><span class="keyword">var</span> b = <span class="number">2</span>/<span class="string">"foo"</span> ; <span class="comment">// NaN</span></div><div class="line"><span class="comment">// 但是规范定义的返回结果是这样！</span></div><div class="line">a.toString(); <span class="comment">// "0"</span></div><div class="line">a + <span class="string">""</span>; <span class="comment">// "0"</span></div><div class="line"><span class="built_in">String</span>( a ); <span class="comment">// "0"</span></div><div class="line"><span class="built_in">JSON</span>.stringify( a ); <span class="comment">// "0"</span></div><div class="line"></div><div class="line">+<span class="string">"-0"</span>; <span class="comment">// -0</span></div><div class="line"><span class="built_in">Number</span>( <span class="string">"-0"</span> ); <span class="comment">// -0</span></div><div class="line"><span class="built_in">JSON</span>.parse( <span class="string">"-0"</span> ); <span class="comment">// -0</span></div><div class="line">	</div><div class="line"><span class="built_in">Object</span>.is( b, <span class="literal">NaN</span> ); <span class="comment">// true</span></div><div class="line"><span class="built_in">Object</span>.is( a, <span class="number">-0</span> ); <span class="comment">// true</span></div><div class="line"><span class="built_in">Object</span>.is( a, <span class="number">0</span> ); <span class="comment">// false</span></div></pre></td></tr></table></figure>
</li>
</ul>
</blockquote>
<h4 id="值和引用"><a href="#值和引用" class="headerlink" title="值和引用"></a>值和引用</h4><blockquote>
<p>简单值（即标量基本类型值， scalar primitive） 总是通过值复制的方式来赋值 / 传递，包括 <code>null</code>、 <code>undefined</code>、字符串、数字、布尔和 ES6 中的 <code>symbol</code>。</p>
<p>复合值（ compound value）——对象（包括数组和封装对象）和函数，则总是通过引用复制的方式来赋值 / 传递。</p>
<p>引用指向的是值本身而非变量，所以一个引用无法更改另一个引用的指向。我们无法自行决定用值复制还是引用复制，一切都由值的类型来决定。</p>
<p><code>Arr.slice()</code> 不带参数会返回当前数组的一个浅复本（ shallow copy）。由于传递给函数的是指向该复本的引用，所以 <code>foo(Arr.slice(..))</code> 中的操作不会影响 <code>Arr</code> 指向的数组。</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</div><div class="line"><span class="keyword">var</span> b = a;</div><div class="line">a; <span class="comment">// [1,2,3]</span></div><div class="line">b; <span class="comment">// [1,2,3]</span></div><div class="line"><span class="comment">// 然后</span></div><div class="line">b = [<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>];</div><div class="line">a; <span class="comment">// [1,2,3]</span></div><div class="line">b; <span class="comment">// [4,5,6]</span></div></pre></td></tr></table></figure>
<blockquote>
<p>标量基本类型值是不可更改的（字符串和布尔也是如此）。如果一个数字对象的标 量基本类型值是 2，那么该值就不能更改，除非创建一个包含新值的数字对象。<br><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">x</span>) </span>&#123;</div><div class="line">x = x + <span class="number">1</span>;</div><div class="line">x; <span class="comment">// 3</span></div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</div><div class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> <span class="built_in">Number</span>( a ); <span class="comment">// Object(a)也一样</span></div><div class="line">foo( b );</div><div class="line"><span class="built_in">console</span>.log( b ); <span class="comment">// 是2，不是3</span></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">wrapper</span>) </span>&#123;</div><div class="line">wrapper.a = <span class="number">42</span>;</div><div class="line">&#125;</div><div class="line"><span class="keyword">var</span> obj = &#123;</div><div class="line"><span class="attr">a</span>: <span class="number">2</span></div><div class="line">&#125;;</div><div class="line">foo( obj );</div><div class="line">obj.a; <span class="comment">// 42</span></div></pre></td></tr></table></figure></p>
</blockquote>
<h4 id="隐式转换"><a href="#隐式转换" class="headerlink" title="隐式转换"></a>隐式转换</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> num = <span class="string">"32"</span>+<span class="number">32</span>;<span class="comment">// "3232"</span></div><div class="line"><span class="keyword">var</span> num = <span class="string">"32"</span><span class="number">-32</span>;<span class="comment">// 0</span></div><div class="line"></div><div class="line">	    </div><div class="line">“<span class="number">1.23</span>” == <span class="number">1.23</span></div><div class="line"><span class="number">0</span> == <span class="literal">false</span></div><div class="line"><span class="literal">null</span> == <span class="literal">undefined</span></div><div class="line"><span class="keyword">new</span> <span class="built_in">Object</span>() == <span class="keyword">new</span> <span class="built_in">Object</span>()</div><div class="line">[<span class="number">1</span>, <span class="number">2</span>] == [<span class="number">1</span>, <span class="number">2</span>]</div><div class="line"><span class="comment">/*类型不同，返回false</span></div><div class="line">类型相同*/</div><div class="line"><span class="literal">NaN</span> ≠ <span class="literal">NaN</span></div><div class="line"><span class="keyword">new</span> <span class="built_in">Object</span> ≠ <span class="keyword">new</span> <span class="built_in">Object</span></div><div class="line"><span class="literal">null</span> === <span class="literal">null</span></div><div class="line"><span class="literal">undefined</span> === <span class="literal">undefined</span></div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      JavaScript 是一种弱类型的语言。
    
    </summary>
    
      <category term="前端" scheme="http://yoursite.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="JavaScript" scheme="http://yoursite.com/tags/JavaScript/"/>
    
  </entry>
  
  <entry>
    <title>Hello World</title>
    <link href="http://yoursite.com/2016/10/04/hello-world/"/>
    <id>http://yoursite.com/2016/10/04/hello-world/</id>
    <published>2016-10-03T23:51:56.317Z</published>
    <updated>2016-10-05T04:52:11.417Z</updated>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="external">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="external">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="external">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="external">GitHub</a>.</p>
<p>&lt;&gt;</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ hexo new <span class="string">"My New Post"</span></div></pre></td></tr></table></figure>
<h3 id="Inser-a-image"><a href="#Inser-a-image" class="headerlink" title="Inser a image"></a>Inser a image</h3><blockquote>
<p>要加入图片，应该在source下建立一个与博文_posts同级别的文件夹 images,将图片放入。路片路径形如：<br>/hexo/source/images/myImg.png</p>
</blockquote>
<p>在博客中插入，要使用相对路径，应写为 /images/myImg.png</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">![Alt text](/images/数据类型.png)</div></pre></td></tr></table></figure>
<p>或者：<br><img src="/images/数据类型.png" alt="Alt text"><br><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">&lt;img src=<span class="string">"/images/mylmg.png"</span> width=50% height=50% align=center/&gt;</div></pre></td></tr></table></figure></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ hexo server</div></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="external">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ hexo generate</div></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="external">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ hexo deploy</div></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/deployment.html" target="_blank" rel="external">Deployment</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Welcome to &lt;a href=&quot;https://hexo.io/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Hexo&lt;/a&gt;! This is your very first post. Check &lt;a href=&quot;https://hexo.
    
    </summary>
    
    
  </entry>
  
</feed>
